var __index = {"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"index.html","title":"AYON Katana Addon API Reference","text":"<p>Katana integration for AYON.</p>"},{"location":"known-limitations.html","title":"Known limitations and feature gaps","text":"<p>AYON Katana focuses on Katana-native production workflows and exposes only features that map to supported Katana workflows.</p>"},{"location":"known-limitations.html#platform-and-host-coverage","title":"Platform and host coverage","text":"<ul> <li>Windows remains the primary artist-validated production platform.</li> <li>Rocky Linux 9 with Katana 9.0v1 is verified for headless AYON launch and the   core workfile, creator, loader, publishing, Scene Inventory, Workfile Builder,   USD, and Deadline-metadata workflows.</li> <li>Interactive Katana menu/UI behavior, Viewer capture, and external-renderer   execution have not yet been artist-accepted on Linux.</li> <li>Katana 8.0v1 has not yet been verified on Linux.</li> <li>macOS is not supported.</li> <li>Katana versions other than 8.0v1 and 9.0v1 are not currently supported.</li> </ul>"},{"location":"known-limitations.html#workfiles-and-context","title":"Workfiles and context","text":"<ul> <li>Workfiles use filesystem <code>.katana</code> paths; asset-ID workfiles are not   supported.</li> <li>Cross-context Save As and version-up depend on AYON Core's workfile services.</li> <li>Native save, new, open, reopen, and creator metadata persistence are covered by   live acceptance tests. Interactive Workfiles UI and cross-context UX still   require artist acceptance.</li> </ul>"},{"location":"known-limitations.html#loading","title":"Loading","text":"<ul> <li>USD, Alembic, image, node graph, and supported semantic USD workflows are   covered.</li> <li>FBX, HDA, BGEO, VDB, Arnold ASS, renderer proxy formats, KLF Look Files, and   LiveGroup source assets are not exposed without a proven Katana-native   consumer.</li> <li>The native <code>UsdSubLayerAdd</code> path can show stale animated camera locators in   Katana 9.0v1; <code>UsdIn</code> is the supported animated-camera playback route.</li> <li>AYON entity URIs require the optional AYON USD Resolver and are disabled by   default.</li> </ul>"},{"location":"known-limitations.html#publishing","title":"Publishing","text":"<ul> <li>Local rendering is synchronous and blocks Publisher until Katana batch exits.</li> <li>Render paths are validated but not repaired automatically.</li> <li>Node graph publishing is limited to one selected <code>Group</code> with an output.</li> <li>USD Look publishing collects supported local external resources into the   published resource directory and remaps authored asset paths, including UDIM   texture sets. Remote URIs, missing or ambiguously anchored resources, and   transfer destination collisions are rejected rather than silently rewritten.</li> <li>Semantic USD Camera publishing verifies that the exported stage contains at   least one composed <code>Camera</code> prim. Layout and assembly creators still describe   intent without enforcing a universal stage-content predicate.</li> <li>USD farm export is not implemented; local native USD export is the supported   path.</li> <li>Scene Review can capture one unambiguous visible Katana Viewer to a PNG image   sequence for AYON review processing. Linux interactive Viewer capture remains   unverified. ImageWrite review output and still workfile thumbnails remain   supported as separate paths.</li> </ul>"},{"location":"known-limitations.html#renderer-and-farm-scope","title":"Renderer and farm scope","text":"<ul> <li>RenderMan local rendering is live-tested on Windows with Katana 9.0v1,   including a real frame through the production local-render extractor.</li> <li>The validated Rocky Linux Katana image intentionally contains no external   renderer plugin, so Linux renderer execution remains a coverage gap.</li> <li>3Delight support remains provisional and should be verified against a valid   studio renderer/license setup before production use.</li> <li>Arnold/KtoA is not part of the supported deployment matrix.</li> <li>Deadline support supplies Katana submission metadata and depends on the   studio's AYON Deadline addon, Deadline repository, renderer, and host   configuration. Automated acceptance validates the metadata path but does not   submit a real farm job.</li> </ul>"},{"location":"known-limitations.html#deliberately-out-of-scope","title":"Deliberately out of scope","text":"<p>SOP/BGEO/APEX authoring, HUSD output processors, value clips, legacy shelf tooling, and host-specific conversion utilities are not implemented because Katana does not expose those workflows.</p>"},{"location":"license.html","title":"License","text":"<pre><code>                             Apache License\n                       Version 2.0, January 2004\n                    http://www.apache.org/licenses/\n</code></pre> <p>TERMS AND CONDITIONS FOR USE, REPRODUCTION, AND DISTRIBUTION</p> <ol> <li> <p>Definitions.</p> <p>\"License\" shall mean the terms and conditions for use, reproduction,   and distribution as defined by Sections 1 through 9 of this document.</p> <p>\"Licensor\" shall mean the copyright owner or entity authorized by   the copyright owner that is granting the License.</p> <p>\"Legal Entity\" shall mean the union of the acting entity and all   other entities that control, are controlled by, or are under common   control with that entity. For the purposes of this definition,   \"control\" means (i) the power, direct or indirect, to cause the   direction or management of such entity, whether by contract or   otherwise, or (ii) ownership of fifty percent (50%) or more of the   outstanding shares, or (iii) beneficial ownership of such entity.</p> <p>\"You\" (or \"Your\") shall mean an individual or Legal Entity   exercising permissions granted by this License.</p> <p>\"Source\" form shall mean the preferred form for making modifications,   including but not limited to software source code, documentation   source, and configuration files.</p> <p>\"Object\" form shall mean any form resulting from mechanical   transformation or translation of a Source form, including but   not limited to compiled object code, generated documentation,   and conversions to other media types.</p> <p>\"Work\" shall mean the work of authorship, whether in Source or   Object form, made available under the License, as indicated by a   copyright notice that is included in or attached to the work   (an example is provided in the Appendix below).</p> <p>\"Derivative Works\" shall mean any work, whether in Source or Object   form, that is based on (or derived from) the Work and for which the   editorial revisions, annotations, elaborations, or other modifications   represent, as a whole, an original work of authorship. For the purposes   of this License, Derivative Works shall not include works that remain   separable from, or merely link (or bind by name) to the interfaces of,   the Work and Derivative Works thereof.</p> <p>\"Contribution\" shall mean any work of authorship, including   the original version of the Work and any modifications or additions   to that Work or Derivative Works thereof, that is intentionally   submitted to Licensor for inclusion in the Work by the copyright owner   or by an individual or Legal Entity authorized to submit on behalf of   the copyright owner. For the purposes of this definition, \"submitted\"   means any form of electronic, verbal, or written communication sent   to the Licensor or its representatives, including but not limited to   communication on electronic mailing lists, source code control systems,   and issue tracking systems that are managed by, or on behalf of, the   Licensor for the purpose of discussing and improving the Work, but   excluding communication that is conspicuously marked or otherwise   designated in writing by the copyright owner as \"Not a Contribution.\"</p> <p>\"Contributor\" shall mean Licensor and any individual or Legal Entity   on behalf of whom a Contribution has been received by Licensor and   subsequently incorporated within the Work.</p> </li> <li> <p>Grant of Copyright License. Subject to the terms and conditions of       this License, each Contributor hereby grants to You a perpetual,       worldwide, non-exclusive, no-charge, royalty-free, irrevocable       copyright license to reproduce, prepare Derivative Works of,       publicly display, publicly perform, sublicense, and distribute the       Work and such Derivative Works in Source or Object form.</p> </li> <li> <p>Grant of Patent License. Subject to the terms and conditions of       this License, each Contributor hereby grants to You a perpetual,       worldwide, non-exclusive, no-charge, royalty-free, irrevocable       (except as stated in this section) patent license to make, have made,       use, offer to sell, sell, import, and otherwise transfer the Work,       where such license applies only to those patent claims licensable       by such Contributor that are necessarily infringed by their       Contribution(s) alone or by combination of their Contribution(s)       with the Work to which such Contribution(s) was submitted. If You       institute patent litigation against any entity (including a       cross-claim or counterclaim in a lawsuit) alleging that the Work       or a Contribution incorporated within the Work constitutes direct       or contributory patent infringement, then any patent licenses       granted to You under this License for that Work shall terminate       as of the date such litigation is filed.</p> </li> <li> <p>Redistribution. You may reproduce and distribute copies of the       Work or Derivative Works thereof in any medium, with or without       modifications, and in Source or Object form, provided that You       meet the following conditions:</p> <p>(a) You must give any other recipients of the Work or       Derivative Works a copy of this License; and</p> <p>(b) You must cause any modified files to carry prominent notices       stating that You changed the files; and</p> <p>(c) You must retain, in the Source form of any Derivative Works       that You distribute, all copyright, patent, trademark, and       attribution notices from the Source form of the Work,       excluding those notices that do not pertain to any part of       the Derivative Works; and</p> <p>(d) If the Work includes a \"NOTICE\" text file as part of its       distribution, then any Derivative Works that You distribute must       include a readable copy of the attribution notices contained       within such NOTICE file, excluding those notices that do not       pertain to any part of the Derivative Works, in at least one       of the following places: within a NOTICE text file distributed       as part of the Derivative Works; within the Source form or       documentation, if provided along with the Derivative Works; or,       within a display generated by the Derivative Works, if and       wherever such third-party notices normally appear. The contents       of the NOTICE file are for informational purposes only and       do not modify the License. You may add Your own attribution       notices within Derivative Works that You distribute, alongside       or as an addendum to the NOTICE text from the Work, provided       that such additional attribution notices cannot be construed       as modifying the License.</p> <p>You may add Your own copyright statement to Your modifications and   may provide additional or different license terms and conditions   for use, reproduction, or distribution of Your modifications, or   for any such Derivative Works as a whole, provided Your use,   reproduction, and distribution of the Work otherwise complies with   the conditions stated in this License.</p> </li> <li> <p>Submission of Contributions. Unless You explicitly state otherwise,       any Contribution intentionally submitted for inclusion in the Work       by You to the Licensor shall be under the terms and conditions of       this License, without any additional terms or conditions.       Notwithstanding the above, nothing herein shall supersede or modify       the terms of any separate license agreement you may have executed       with Licensor regarding such Contributions.</p> </li> <li> <p>Trademarks. This License does not grant permission to use the trade       names, trademarks, service marks, or product names of the Licensor,       except as required for reasonable and customary use in describing the       origin of the Work and reproducing the content of the NOTICE file.</p> </li> <li> <p>Disclaimer of Warranty. Unless required by applicable law or       agreed to in writing, Licensor provides the Work (and each       Contributor provides its Contributions) on an \"AS IS\" BASIS,       WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or       implied, including, without limitation, any warranties or conditions       of TITLE, NON-INFRINGEMENT, MERCHANTABILITY, or FITNESS FOR A       PARTICULAR PURPOSE. You are solely responsible for determining the       appropriateness of using or redistributing the Work and assume any       risks associated with Your exercise of permissions under this License.</p> </li> <li> <p>Limitation of Liability. In no event and under no legal theory,       whether in tort (including negligence), contract, or otherwise,       unless required by applicable law (such as deliberate and grossly       negligent acts) or agreed to in writing, shall any Contributor be       liable to You for damages, including any direct, indirect, special,       incidental, or consequential damages of any character arising as a       result of this License or out of the use or inability to use the       Work (including but not limited to damages for loss of goodwill,       work stoppage, computer failure or malfunction, or any and all       other commercial damages or losses), even if such Contributor       has been advised of the possibility of such damages.</p> </li> <li> <p>Accepting Warranty or Additional Liability. While redistributing       the Work or Derivative Works thereof, You may choose to offer,       and charge a fee for, acceptance of support, warranty, indemnity,       or other liability obligations and/or rights consistent with this       License. However, in accepting such obligations, You may act only       on Your own behalf and on Your sole responsibility, not on behalf       of any other Contributor, and only if You agree to indemnify,       defend, and hold each Contributor harmless for any liability       incurred by, or claims asserted against, such Contributor by reason       of your accepting any such warranty or additional liability.</p> </li> </ol> <p>END OF TERMS AND CONDITIONS</p> <p>APPENDIX: How to apply the Apache License to your work.</p> <pre><code>  To apply the Apache License to your work, attach the following\n  boilerplate notice, with the fields enclosed by brackets \"[]\"\n  replaced with your own identifying information. (Don't include\n  the brackets!)  The text should be enclosed in the appropriate\n  comment syntax for the file format. We also recommend that a\n  file or class name and description of purpose be included on the\n  same \"printed page\" as the copyright notice for easier\n  identification within third-party archives.\n</code></pre> <p>Copyright 2026 Ahmed Hindy</p> <p>Licensed under the Apache License, Version 2.0 (the \"License\");    you may not use this file except in compliance with the License.    You may obtain a copy of the License at</p> <pre><code>   http://www.apache.org/licenses/LICENSE-2.0\n</code></pre> <p>Unless required by applicable law or agreed to in writing, software    distributed under the License is distributed on an \"AS IS\" BASIS,    WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.    See the License for the specific language governing permissions and    limitations under the License.</p>"},{"location":"autoapi/summary.html","title":"Summary","text":"<ul> <li>client<ul> <li>ayon_katana<ul> <li>addon</li> <li>api<ul> <li>callbacks</li> <li>colorspace</li> <li>compat</li> <li>containers</li> <li>context</li> <li>dependencies</li> <li>image</li> <li>instances</li> <li>lib</li> <li>lifecycle</li> <li>menu</li> <li>pipeline</li> <li>plugin</li> <li>render</li> <li>review</li> <li>startup</li> <li>thumbnail</li> <li>usd</li> <li>usd_look</li> <li>usd_publish</li> <li>usd_resources</li> <li>workfile_template_builder</li> <li>workio</li> </ul> </li> <li>hooks<ul> <li>pre_add_workfile_arg</li> <li>set_default_display_and_view</li> <li>set_paths</li> </ul> </li> <li>plugins<ul> <li>create<ul> <li>create_image</li> <li>create_nodegraph</li> <li>create_render</li> <li>create_review</li> <li>create_usd_layer</li> <li>create_workfile</li> </ul> </li> <li>deadline<ul> <li>submit_katana_deadline</li> </ul> </li> <li>inventory<ul> <li>select_containers</li> <li>select_managed_sources</li> </ul> </li> <li>load<ul> <li>clear_usd_resolver_cache</li> <li>load_alembic</li> <li>load_image</li> <li>load_katana</li> <li>load_usd</li> </ul> </li> <li>publish<ul> <li>actions</li> <li>collect_current_file</li> <li>collect_image</li> <li>collect_local_render_instances</li> <li>collect_nodegraph</li> <li>collect_render</li> <li>collect_render_frame_range</li> <li>collect_render_node</li> <li>collect_render_settings</li> <li>collect_render_task_handles</li> <li>collect_review</li> <li>collect_usd_layer</li> <li>collect_workfile</li> <li>collect_workfile_dependencies</li> <li>extract_local_render</li> <li>extract_nodegraph</li> <li>extract_review_capture</li> <li>extract_usd_layer</li> <li>extract_workfile</li> <li>extract_workfile_thumbnail</li> <li>finalize_usd_publish</li> <li>increment_current_file</li> <li>save_current_scene</li> <li>validate_image</li> <li>validate_instance_in_context</li> <li>validate_nodegraph</li> <li>validate_render</li> <li>validate_render_camera</li> <li>validate_render_colorspace</li> <li>validate_render_frame_range</li> <li>validate_render_output_extensions</li> <li>validate_render_output_names</li> <li>validate_render_output_paths</li> <li>validate_render_output_tokens</li> <li>validate_render_product_paths_unique</li> <li>validate_render_resolution</li> <li>validate_review</li> <li>validate_usd_asset_contribution_default_prim</li> <li>validate_usd_camera_content</li> <li>validate_usd_layer</li> <li>validate_usd_look_assignments</li> <li>validate_usd_look_disallowed_types</li> <li>validate_usd_look_material_definitions</li> <li>validate_workfile_context</li> <li>validate_workfile_paths</li> <li>validate_workfile_saved</li> </ul> </li> <li>workfile_build<ul> <li>create_placeholder</li> <li>load_placeholder</li> <li>script_placeholder</li> </ul> </li> </ul> </li> <li>usd_bindings</li> <li>version</li> </ul> </li> </ul> </li> <li>server<ul> <li>addon</li> <li>settings<ul> <li>common</li> <li>create</li> <li>imageio</li> <li>load</li> <li>main</li> <li>publish</li> <li>templated_workfile_build</li> </ul> </li> </ul> </li> </ul>"},{"location":"autoapi/client/ayon_katana/index.html","title":"ayon_katana","text":"<p>AYON integration for Foundry Katana.</p>"},{"location":"autoapi/client/ayon_katana/index.html#client.ayon_katana.KatanaAddon","title":"<code>KatanaAddon</code>","text":"<p>               Bases: <code>AYONAddon</code>, <code>IHostAddon</code></p> <p>Configure AYON launches for the Katana host integration.</p> Source code in <code>client/ayon_katana/addon.py</code> <pre><code>class KatanaAddon(AYONAddon, IHostAddon):\n    \"\"\"Configure AYON launches for the Katana host integration.\"\"\"\n\n    name = \"katana\"\n    version = __version__\n    host_name = \"katana\"\n\n    def add_implementation_envs(self, env, _app):\n        \"\"\"Add the Katana integration to the launch environment.\"\"\"\n        # Add the integration resources to KATANA_RESOURCES.\n        resources_path = os.path.normpath(os.path.join(KATANA_HOST_DIR, \"resources\"))\n        new_katana_resources = [resources_path]\n\n        old_katana_resources = env.get(\"KATANA_RESOURCES\") or \"\"\n        for path in old_katana_resources.split(os.pathsep):\n            if not path:\n                continue\n\n            norm_path = os.path.normpath(path)\n            if os.path.normcase(norm_path) not in {\n                os.path.normcase(item) for item in new_katana_resources\n            }:\n                new_katana_resources.append(norm_path)\n\n        env[\"KATANA_RESOURCES\"] = os.pathsep.join(new_katana_resources)\n\n        # Make the addon package importable from Katana's Python environment\n        package_parent = os.path.normpath(os.path.dirname(KATANA_HOST_DIR))\n        new_python_path = [package_parent]\n\n        old_python_path = env.get(\"PYTHONPATH\") or \"\"\n        for path in old_python_path.split(os.pathsep):\n            if not path:\n                continue\n\n            norm_path = os.path.normpath(path)\n            if os.path.normcase(norm_path) not in {\n                os.path.normcase(item) for item in new_python_path\n            }:\n                new_python_path.append(norm_path)\n\n        env[\"PYTHONPATH\"] = os.pathsep.join(new_python_path)\n\n    def get_launch_hook_paths(self, app):\n        \"\"\"Return Katana prelaunch hook paths for a matching application.\"\"\"\n        if app.host_name != self.host_name:\n            return []\n        return [os.path.join(KATANA_HOST_DIR, \"hooks\")]\n\n    def get_workfile_extensions(self):\n        \"\"\"Return workfile extensions supported by Katana.\"\"\"\n        return [\".katana\"]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/index.html#client.ayon_katana.KatanaAddon.add_implementation_envs","title":"<code>add_implementation_envs(env, _app)</code>","text":"<p>Add the Katana integration to the launch environment.</p> Source code in <code>client/ayon_katana/addon.py</code> <pre><code>def add_implementation_envs(self, env, _app):\n    \"\"\"Add the Katana integration to the launch environment.\"\"\"\n    # Add the integration resources to KATANA_RESOURCES.\n    resources_path = os.path.normpath(os.path.join(KATANA_HOST_DIR, \"resources\"))\n    new_katana_resources = [resources_path]\n\n    old_katana_resources = env.get(\"KATANA_RESOURCES\") or \"\"\n    for path in old_katana_resources.split(os.pathsep):\n        if not path:\n            continue\n\n        norm_path = os.path.normpath(path)\n        if os.path.normcase(norm_path) not in {\n            os.path.normcase(item) for item in new_katana_resources\n        }:\n            new_katana_resources.append(norm_path)\n\n    env[\"KATANA_RESOURCES\"] = os.pathsep.join(new_katana_resources)\n\n    # Make the addon package importable from Katana's Python environment\n    package_parent = os.path.normpath(os.path.dirname(KATANA_HOST_DIR))\n    new_python_path = [package_parent]\n\n    old_python_path = env.get(\"PYTHONPATH\") or \"\"\n    for path in old_python_path.split(os.pathsep):\n        if not path:\n            continue\n\n        norm_path = os.path.normpath(path)\n        if os.path.normcase(norm_path) not in {\n            os.path.normcase(item) for item in new_python_path\n        }:\n            new_python_path.append(norm_path)\n\n    env[\"PYTHONPATH\"] = os.pathsep.join(new_python_path)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/index.html#client.ayon_katana.KatanaAddon.get_launch_hook_paths","title":"<code>get_launch_hook_paths(app)</code>","text":"<p>Return Katana prelaunch hook paths for a matching application.</p> Source code in <code>client/ayon_katana/addon.py</code> <pre><code>def get_launch_hook_paths(self, app):\n    \"\"\"Return Katana prelaunch hook paths for a matching application.\"\"\"\n    if app.host_name != self.host_name:\n        return []\n    return [os.path.join(KATANA_HOST_DIR, \"hooks\")]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/index.html#client.ayon_katana.KatanaAddon.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Return workfile extensions supported by Katana.</p> Source code in <code>client/ayon_katana/addon.py</code> <pre><code>def get_workfile_extensions(self):\n    \"\"\"Return workfile extensions supported by Katana.\"\"\"\n    return [\".katana\"]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/addon.html","title":"addon","text":"<p>AYON client addon definition for Katana.</p>"},{"location":"autoapi/client/ayon_katana/addon.html#client.ayon_katana.addon.KatanaAddon","title":"<code>KatanaAddon</code>","text":"<p>               Bases: <code>AYONAddon</code>, <code>IHostAddon</code></p> <p>Configure AYON launches for the Katana host integration.</p> Source code in <code>client/ayon_katana/addon.py</code> <pre><code>class KatanaAddon(AYONAddon, IHostAddon):\n    \"\"\"Configure AYON launches for the Katana host integration.\"\"\"\n\n    name = \"katana\"\n    version = __version__\n    host_name = \"katana\"\n\n    def add_implementation_envs(self, env, _app):\n        \"\"\"Add the Katana integration to the launch environment.\"\"\"\n        # Add the integration resources to KATANA_RESOURCES.\n        resources_path = os.path.normpath(os.path.join(KATANA_HOST_DIR, \"resources\"))\n        new_katana_resources = [resources_path]\n\n        old_katana_resources = env.get(\"KATANA_RESOURCES\") or \"\"\n        for path in old_katana_resources.split(os.pathsep):\n            if not path:\n                continue\n\n            norm_path = os.path.normpath(path)\n            if os.path.normcase(norm_path) not in {\n                os.path.normcase(item) for item in new_katana_resources\n            }:\n                new_katana_resources.append(norm_path)\n\n        env[\"KATANA_RESOURCES\"] = os.pathsep.join(new_katana_resources)\n\n        # Make the addon package importable from Katana's Python environment\n        package_parent = os.path.normpath(os.path.dirname(KATANA_HOST_DIR))\n        new_python_path = [package_parent]\n\n        old_python_path = env.get(\"PYTHONPATH\") or \"\"\n        for path in old_python_path.split(os.pathsep):\n            if not path:\n                continue\n\n            norm_path = os.path.normpath(path)\n            if os.path.normcase(norm_path) not in {\n                os.path.normcase(item) for item in new_python_path\n            }:\n                new_python_path.append(norm_path)\n\n        env[\"PYTHONPATH\"] = os.pathsep.join(new_python_path)\n\n    def get_launch_hook_paths(self, app):\n        \"\"\"Return Katana prelaunch hook paths for a matching application.\"\"\"\n        if app.host_name != self.host_name:\n            return []\n        return [os.path.join(KATANA_HOST_DIR, \"hooks\")]\n\n    def get_workfile_extensions(self):\n        \"\"\"Return workfile extensions supported by Katana.\"\"\"\n        return [\".katana\"]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/addon.html#client.ayon_katana.addon.KatanaAddon.add_implementation_envs","title":"<code>add_implementation_envs(env, _app)</code>","text":"<p>Add the Katana integration to the launch environment.</p> Source code in <code>client/ayon_katana/addon.py</code> <pre><code>def add_implementation_envs(self, env, _app):\n    \"\"\"Add the Katana integration to the launch environment.\"\"\"\n    # Add the integration resources to KATANA_RESOURCES.\n    resources_path = os.path.normpath(os.path.join(KATANA_HOST_DIR, \"resources\"))\n    new_katana_resources = [resources_path]\n\n    old_katana_resources = env.get(\"KATANA_RESOURCES\") or \"\"\n    for path in old_katana_resources.split(os.pathsep):\n        if not path:\n            continue\n\n        norm_path = os.path.normpath(path)\n        if os.path.normcase(norm_path) not in {\n            os.path.normcase(item) for item in new_katana_resources\n        }:\n            new_katana_resources.append(norm_path)\n\n    env[\"KATANA_RESOURCES\"] = os.pathsep.join(new_katana_resources)\n\n    # Make the addon package importable from Katana's Python environment\n    package_parent = os.path.normpath(os.path.dirname(KATANA_HOST_DIR))\n    new_python_path = [package_parent]\n\n    old_python_path = env.get(\"PYTHONPATH\") or \"\"\n    for path in old_python_path.split(os.pathsep):\n        if not path:\n            continue\n\n        norm_path = os.path.normpath(path)\n        if os.path.normcase(norm_path) not in {\n            os.path.normcase(item) for item in new_python_path\n        }:\n            new_python_path.append(norm_path)\n\n    env[\"PYTHONPATH\"] = os.pathsep.join(new_python_path)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/addon.html#client.ayon_katana.addon.KatanaAddon.get_launch_hook_paths","title":"<code>get_launch_hook_paths(app)</code>","text":"<p>Return Katana prelaunch hook paths for a matching application.</p> Source code in <code>client/ayon_katana/addon.py</code> <pre><code>def get_launch_hook_paths(self, app):\n    \"\"\"Return Katana prelaunch hook paths for a matching application.\"\"\"\n    if app.host_name != self.host_name:\n        return []\n    return [os.path.join(KATANA_HOST_DIR, \"hooks\")]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/addon.html#client.ayon_katana.addon.KatanaAddon.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Return workfile extensions supported by Katana.</p> Source code in <code>client/ayon_katana/addon.py</code> <pre><code>def get_workfile_extensions(self):\n    \"\"\"Return workfile extensions supported by Katana.\"\"\"\n    return [\".katana\"]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/usd_bindings.html","title":"usd_bindings","text":"<p>Expose Katana's USD bindings through the standard <code>pxr</code> namespace.</p>"},{"location":"autoapi/client/ayon_katana/usd_bindings.html#client.ayon_katana.usd_bindings.UsdBindingConflictError","title":"<code>UsdBindingConflictError</code>","text":"<p>               Bases: <code>RuntimeError</code></p> <p>Raised when another USD build already owns the <code>pxr</code> namespace.</p> Source code in <code>client/ayon_katana/usd_bindings.py</code> <pre><code>class UsdBindingConflictError(RuntimeError):\n    \"\"\"Raised when another USD build already owns the ``pxr`` namespace.\"\"\"\n</code></pre>"},{"location":"autoapi/client/ayon_katana/usd_bindings.html#client.ayon_katana.usd_bindings.install_usd_bindings","title":"<code>install_usd_bindings()</code>","text":"<p>Expose Katana's bundled <code>fnpxr</code> binding as <code>pxr</code>.</p> <p>Katana ships Foundry's namespaced <code>fnpxr</code> package, while AYON's shared USD publishing code follows the standard Pixar <code>pxr</code> import contract. This function imports Katana's required modules first and then publishes an atomic set of aliases in :mod:<code>sys.modules</code>. It never selects an external USD build.</p> <p>Binding import failures are intentionally non-fatal so Katana can still start and use non-USD AYON features. USD tools will report their native import error when invoked. A different binding already loaded as <code>pxr</code> is a fatal configuration conflict because mixing USD builds in one Katana process is unsafe.</p> <p>Returns:</p> Type Description <code>str | None</code> <p><code>\"fnpxr\"</code> when the aliases were installed, or <code>None</code> when</p> <code>str | None</code> <p>Katana's binding could not be initialized.</p> <p>Raises:</p> Type Description <code>UsdBindingConflictError</code> <p>A different USD binding already owns one of the required <code>pxr</code> module names.</p> Source code in <code>client/ayon_katana/usd_bindings.py</code> <pre><code>def install_usd_bindings() -&gt; str | None:\n    \"\"\"Expose Katana's bundled ``fnpxr`` binding as ``pxr``.\n\n    Katana ships Foundry's namespaced ``fnpxr`` package, while AYON's shared\n    USD publishing code follows the standard Pixar ``pxr`` import contract.\n    This function imports Katana's required modules first and then publishes an\n    atomic set of aliases in :mod:`sys.modules`. It never selects an external\n    USD build.\n\n    Binding import failures are intentionally non-fatal so Katana can still\n    start and use non-USD AYON features. USD tools will report their native\n    import error when invoked. A different binding already loaded as ``pxr``\n    is a fatal configuration conflict because mixing USD builds in one Katana\n    process is unsafe.\n\n    Returns:\n        ``\"fnpxr\"`` when the aliases were installed, or ``None`` when\n        Katana's binding could not be initialized.\n\n    Raises:\n        UsdBindingConflictError: A different USD binding already owns one of\n            the required ``pxr`` module names.\n    \"\"\"\n    try:\n        root_module = import_module(\"fnpxr\")\n        submodules = {name: import_module(f\"fnpxr.{name}\") for name in USD_SUBMODULES}\n    except (ImportError, OSError) as exc:\n        log.warning(\"Katana's fnpxr package could not be initialized: %s\", exc)\n        return None\n\n    expected_modules = {\"pxr\": root_module}\n    expected_modules.update(\n        {f\"pxr.{name}\": module for name, module in submodules.items()}\n    )\n    conflicting_modules = [\n        name\n        for name, expected_module in expected_modules.items()\n        if (loaded_module := sys.modules.get(name)) is not None\n        and loaded_module is not expected_module\n    ]\n    if conflicting_modules:\n        names = \", \".join(sorted(conflicting_modules))\n        raise UsdBindingConflictError(\n            \"Katana cannot initialize its bundled fnpxr binding because a \"\n            f\"different USD build already owns: {names}. Remove the external \"\n            \"pxr package from Katana's environment.\"\n        )\n\n    sys.modules[\"pxr\"] = root_module\n    for name, module in submodules.items():\n        setattr(root_module, name, module)\n        sys.modules[f\"pxr.{name}\"] = module\n\n    log.debug(\"Exposed Katana's fnpxr package as pxr.\")\n    return \"fnpxr\"\n</code></pre>"},{"location":"autoapi/client/ayon_katana/version.html","title":"version","text":"<p>Version metadata for the AYON Katana client package.</p>"},{"location":"autoapi/client/ayon_katana/api/index.html","title":"api","text":"<p>Public API for the AYON Katana host integration.</p>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost","title":"<code>KatanaHost</code>","text":"<p>               Bases: <code>HostBase</code>, <code>IWorkfileHost</code>, <code>ILoadHost</code>, <code>IPublishHost</code></p> <p>Implement AYON host interfaces for Katana.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>class KatanaHost(HostBase, IWorkfileHost, ILoadHost, IPublishHost):\n    \"\"\"Implement AYON host interfaces for Katana.\"\"\"\n\n    name = \"katana\"\n\n    def __init__(self):\n        super().__init__()\n        self._lifecycle = LifecycleController(self)\n        self._callback_manager = CallbackManager(self._lifecycle)\n        self._event_callbacks: list[object] = []\n        self._deadline_plugins_registered = False\n        self._has_been_setup = False\n\n    @property\n    def is_installed(self) -&gt; bool:\n        \"\"\"Return whether the Katana host services are currently installed.\"\"\"\n        return self._has_been_setup\n\n    def get_app_information(self):\n        \"\"\"Return the running Katana application's name and version.\"\"\"\n        import Katana\n        from ayon_core.host import ApplicationInformation\n\n        katana_version = getattr(Katana, \"__version__\", None)\n        if not katana_version:\n            katana_root = os.environ.get(\"KATANA_ROOT\", \"\").rstrip(\"/\\\\\")\n            katana_root_name = katana_root.replace(\"\\\\\", \"/\").rsplit(\"/\", 1)[-1]\n            if katana_root_name.startswith(\"Katana\"):\n                katana_version = katana_root_name.removeprefix(\"Katana\")\n\n        return ApplicationInformation(\n            app_name=\"Katana\",\n            app_version=katana_version,\n        )\n\n    def maintained_selection(self):\n        \"\"\"Return a context manager that restores the Katana selection.\"\"\"\n        return lib.maintained_selection()\n\n    def install(self):\n        \"\"\"Register Katana plugins, callbacks, and host services once.\"\"\"\n        if self._has_been_setup:\n            return\n\n        pyblish.api.register_host(self.name)\n        pyblish.api.register_plugin_path(PUBLISH_PATH)\n        if importlib.util.find_spec(\"ayon_deadline\") is not None:\n            pyblish.api.register_plugin_path(DEADLINE_PUBLISH_PATH)\n            self._deadline_plugins_registered = True\n        register_loader_plugin_path(LOAD_PATH)\n        register_creator_plugin_path(CREATE_PATH)\n        register_inventory_action_path(INVENTORY_PATH)\n        register_workfile_build_plugin_path(WORKFILE_BUILD_PATH)\n        self._lifecycle.install()\n        self._callback_manager.install()\n        self._event_callbacks.append(\n            register_event_callback(\"taskChanged\", self._lifecycle.on_task_changed)\n        )\n        self._event_callbacks.append(\n            register_event_callback(\n                \"workfile.save.before\",\n                self._lifecycle.on_workfile_save_before,\n            )\n        )\n        self._has_been_setup = True\n        log.info(\"Installed AYON Katana host.\")\n\n    def uninstall(self):\n        \"\"\"Remove plugins, callbacks, and host services registered by AYON.\"\"\"\n        if not self._has_been_setup:\n            return\n\n        for event_callback in self._event_callbacks:\n            event_callback.deregister()\n        self._event_callbacks.clear()\n        self._callback_manager.uninstall()\n        self._lifecycle.uninstall()\n        deregister_workfile_build_plugin_path(WORKFILE_BUILD_PATH)\n        deregister_inventory_action_path(INVENTORY_PATH)\n        deregister_creator_plugin_path(CREATE_PATH)\n        deregister_loader_plugin_path(LOAD_PATH)\n        if self._deadline_plugins_registered:\n            pyblish.api.deregister_plugin_path(DEADLINE_PUBLISH_PATH)\n            self._deadline_plugins_registered = False\n        pyblish.api.deregister_plugin_path(PUBLISH_PATH)\n        pyblish.api.deregister_host(self.name)\n        self._has_been_setup = False\n        log.info(\"Uninstalled AYON Katana host.\")\n\n    def workfile_has_unsaved_changes(self):\n        \"\"\"Return whether the current Katana project has unsaved changes.\"\"\"\n        return workio.workfile_has_unsaved_changes()\n\n    def get_workfile_extensions(self):\n        \"\"\"Return workfile extensions supported by Katana.\"\"\"\n        return workio.get_workfile_extensions()\n\n    def save_workfile(self, dst_path=None):\n        \"\"\"Save the current Katana project, optionally to a new path.\"\"\"\n        return workio.save_workfile(dst_path)\n\n    def open_workfile(self, filepath):\n        \"\"\"Open a Katana project from a filesystem path.\"\"\"\n        return workio.open_workfile(filepath)\n\n    def get_current_workfile(self):\n        \"\"\"Return the current Katana project path.\"\"\"\n        return workio.get_current_workfile()\n\n    def get_containers(self):\n        \"\"\"Return containers found in the current Katana project.\"\"\"\n        return ls()\n\n    def get_context_data(self):\n        \"\"\"Return AYON context data embedded in the current project.\"\"\"\n        return context_lib.get_context_data()\n\n    def update_context_data(self, data, changes=None):\n        \"\"\"Persist AYON context data in the current Katana project.\"\"\"\n        context_lib.update_context_data(data, changes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.is_installed","title":"<code>is_installed</code>  <code>property</code>","text":"<p>Return whether the Katana host services are currently installed.</p>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.get_app_information","title":"<code>get_app_information()</code>","text":"<p>Return the running Katana application's name and version.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def get_app_information(self):\n    \"\"\"Return the running Katana application's name and version.\"\"\"\n    import Katana\n    from ayon_core.host import ApplicationInformation\n\n    katana_version = getattr(Katana, \"__version__\", None)\n    if not katana_version:\n        katana_root = os.environ.get(\"KATANA_ROOT\", \"\").rstrip(\"/\\\\\")\n        katana_root_name = katana_root.replace(\"\\\\\", \"/\").rsplit(\"/\", 1)[-1]\n        if katana_root_name.startswith(\"Katana\"):\n            katana_version = katana_root_name.removeprefix(\"Katana\")\n\n    return ApplicationInformation(\n        app_name=\"Katana\",\n        app_version=katana_version,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.get_containers","title":"<code>get_containers()</code>","text":"<p>Return containers found in the current Katana project.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def get_containers(self):\n    \"\"\"Return containers found in the current Katana project.\"\"\"\n    return ls()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.get_context_data","title":"<code>get_context_data()</code>","text":"<p>Return AYON context data embedded in the current project.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def get_context_data(self):\n    \"\"\"Return AYON context data embedded in the current project.\"\"\"\n    return context_lib.get_context_data()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.get_current_workfile","title":"<code>get_current_workfile()</code>","text":"<p>Return the current Katana project path.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def get_current_workfile(self):\n    \"\"\"Return the current Katana project path.\"\"\"\n    return workio.get_current_workfile()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Return workfile extensions supported by Katana.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def get_workfile_extensions(self):\n    \"\"\"Return workfile extensions supported by Katana.\"\"\"\n    return workio.get_workfile_extensions()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.install","title":"<code>install()</code>","text":"<p>Register Katana plugins, callbacks, and host services once.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def install(self):\n    \"\"\"Register Katana plugins, callbacks, and host services once.\"\"\"\n    if self._has_been_setup:\n        return\n\n    pyblish.api.register_host(self.name)\n    pyblish.api.register_plugin_path(PUBLISH_PATH)\n    if importlib.util.find_spec(\"ayon_deadline\") is not None:\n        pyblish.api.register_plugin_path(DEADLINE_PUBLISH_PATH)\n        self._deadline_plugins_registered = True\n    register_loader_plugin_path(LOAD_PATH)\n    register_creator_plugin_path(CREATE_PATH)\n    register_inventory_action_path(INVENTORY_PATH)\n    register_workfile_build_plugin_path(WORKFILE_BUILD_PATH)\n    self._lifecycle.install()\n    self._callback_manager.install()\n    self._event_callbacks.append(\n        register_event_callback(\"taskChanged\", self._lifecycle.on_task_changed)\n    )\n    self._event_callbacks.append(\n        register_event_callback(\n            \"workfile.save.before\",\n            self._lifecycle.on_workfile_save_before,\n        )\n    )\n    self._has_been_setup = True\n    log.info(\"Installed AYON Katana host.\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.maintained_selection","title":"<code>maintained_selection()</code>","text":"<p>Return a context manager that restores the Katana selection.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def maintained_selection(self):\n    \"\"\"Return a context manager that restores the Katana selection.\"\"\"\n    return lib.maintained_selection()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.open_workfile","title":"<code>open_workfile(filepath)</code>","text":"<p>Open a Katana project from a filesystem path.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def open_workfile(self, filepath):\n    \"\"\"Open a Katana project from a filesystem path.\"\"\"\n    return workio.open_workfile(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.save_workfile","title":"<code>save_workfile(dst_path=None)</code>","text":"<p>Save the current Katana project, optionally to a new path.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def save_workfile(self, dst_path=None):\n    \"\"\"Save the current Katana project, optionally to a new path.\"\"\"\n    return workio.save_workfile(dst_path)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.uninstall","title":"<code>uninstall()</code>","text":"<p>Remove plugins, callbacks, and host services registered by AYON.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def uninstall(self):\n    \"\"\"Remove plugins, callbacks, and host services registered by AYON.\"\"\"\n    if not self._has_been_setup:\n        return\n\n    for event_callback in self._event_callbacks:\n        event_callback.deregister()\n    self._event_callbacks.clear()\n    self._callback_manager.uninstall()\n    self._lifecycle.uninstall()\n    deregister_workfile_build_plugin_path(WORKFILE_BUILD_PATH)\n    deregister_inventory_action_path(INVENTORY_PATH)\n    deregister_creator_plugin_path(CREATE_PATH)\n    deregister_loader_plugin_path(LOAD_PATH)\n    if self._deadline_plugins_registered:\n        pyblish.api.deregister_plugin_path(DEADLINE_PUBLISH_PATH)\n        self._deadline_plugins_registered = False\n    pyblish.api.deregister_plugin_path(PUBLISH_PATH)\n    pyblish.api.deregister_host(self.name)\n    self._has_been_setup = False\n    log.info(\"Uninstalled AYON Katana host.\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.update_context_data","title":"<code>update_context_data(data, changes=None)</code>","text":"<p>Persist AYON context data in the current Katana project.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def update_context_data(self, data, changes=None):\n    \"\"\"Persist AYON context data in the current Katana project.\"\"\"\n    context_lib.update_context_data(data, changes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.KatanaHost.workfile_has_unsaved_changes","title":"<code>workfile_has_unsaved_changes()</code>","text":"<p>Return whether the current Katana project has unsaved changes.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def workfile_has_unsaved_changes(self):\n    \"\"\"Return whether the current Katana project has unsaved changes.\"\"\"\n    return workio.workfile_has_unsaved_changes()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.containerise","title":"<code>containerise(name, namespace, context, loader=None, parent_node=None)</code>","text":"<p>Bundle a Katana graph and imprint it with metadata.</p> <p>Containerisation enables tracking of version, author and origin for loaded products.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <p>Name of the loaded product.</p> required <code>namespace</code> <p>Namespace under which to host the container.</p> required <code>context</code> <p>Loaded product context information.</p> required <code>loader</code> <p>Name of the loader used to produce the container.</p> <code>None</code> <code>parent_node</code> <p>Optional Katana parent node.</p> <code>None</code> <p>Returns:</p> Type Description <p>Created Katana container Group.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def containerise(name, namespace, context, loader=None, parent_node=None):\n    \"\"\"Bundle a Katana graph and imprint it with metadata.\n\n    Containerisation enables tracking of version, author and origin for loaded\n    products.\n\n    Args:\n        name: Name of the loaded product.\n        namespace: Namespace under which to host the container.\n        context: Loaded product context information.\n        loader: Name of the loader used to produce the container.\n        parent_node: Optional Katana parent node.\n\n    Returns:\n        Created Katana container Group.\n    \"\"\"\n    return containers.containerise(\n        name,\n        namespace,\n        context,\n        loader=loader,\n        parent_node=parent_node,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.ls","title":"<code>ls()</code>","text":"<p>Yield AYON containers in the current Katana project.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def ls():\n    \"\"\"Yield AYON containers in the current Katana project.\"\"\"\n    return containers.ls()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/index.html#client.ayon_katana.api.maintained_selection","title":"<code>maintained_selection()</code>","text":"<p>Maintain selection during context.</p> <p>This restores the selected nodes after the context exits and ignores nodes that no longer exist.</p> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>@contextmanager\ndef maintained_selection():\n    \"\"\"Maintain selection during context.\n\n    This restores the selected nodes after the context exits and ignores nodes\n    that no longer exist.\n    \"\"\"\n    from . import compat\n\n    selected_nodes = compat.get_selected_nodes()\n    try:\n        yield\n    finally:\n        live_nodes = []\n        for node in selected_nodes:\n            try:\n                if compat.get_node(node.getName()) is node:\n                    live_nodes.append(node)\n            except Exception:\n                continue\n        compat.set_selected_nodes(live_nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/callbacks.html","title":"callbacks","text":"<p>Katana callback lifecycle for the AYON host.</p>"},{"location":"autoapi/client/ayon_katana/api/callbacks.html#client.ayon_katana.api.callbacks.CallbackManager","title":"<code>CallbackManager</code>","text":"<p>Install and remove Katana callbacks without duplicates.</p> Source code in <code>client/ayon_katana/api/callbacks.py</code> <pre><code>class CallbackManager:\n    \"\"\"Install and remove Katana callbacks without duplicates.\"\"\"\n\n    def __init__(self, lifecycle):\n        self._lifecycle = lifecycle\n        self._registered_callbacks = []\n        self._new_scene_pending = False\n\n    def install(self):\n        \"\"\"Register each supported Katana callback once.\"\"\"\n        if self._registered_callbacks:\n            return\n        self._register_optional(\"onStartupComplete\", self._on_startup_complete)\n        self._register_optional(\"onSceneAboutToLoad\", self._on_scene_about_to_load)\n        self._register_optional(\"onSceneLoad\", self._on_scene_load)\n        self._register_optional(\"onSceneAboutToSave\", self._on_scene_about_to_save)\n        self._register_optional(\"onSceneSave\", self._on_scene_save)\n        self._register_optional(\"onNewScene\", self._on_new_scene)\n\n    def uninstall(self):\n        \"\"\"Remove all callbacks registered by this manager.\"\"\"\n        for callback_type, callback in reversed(self._registered_callbacks):\n            try:\n                Callbacks.delCallback(callback_type, callback)\n            except (RuntimeError, ValueError):\n                log.exception(\"Failed to remove Katana callback %r.\", callback_type)\n        self._registered_callbacks.clear()\n        self._new_scene_pending = False\n\n    def _register_optional(self, callback_type_name, callback):\n        callback_type = getattr(Callbacks.Type, callback_type_name, None)\n        if callback_type is None:\n            log.debug(\"Katana callback type %s is unavailable.\", callback_type_name)\n            return\n        Callbacks.addCallback(callback_type, callback)\n        self._registered_callbacks.append((callback_type, callback))\n\n    def _on_startup_complete(self, *_args, **_kwargs):\n        self._lifecycle.on_startup_complete()\n\n    def _on_scene_about_to_load(self, *_args, **_kwargs):\n        self._new_scene_pending = False\n\n    def _on_scene_load(self, *_args, **_kwargs):\n        \"\"\"Dispatch an open, except for the paired callback after ``New()``.\n\n        Katana 8.0v1 and 9.0v1 both report the following sequences:\n\n        - ``New()``: ``onSceneAboutToLoad``, ``onNewScene``, ``onSceneLoad``;\n        - ``Load()``: ``onSceneAboutToLoad``, ``onSceneLoad``.\n\n        The trailing load event after ``New()`` must not treat the new scene\n        as an artist opening an existing workfile.\n        \"\"\"\n        if self._new_scene_pending:\n            self._new_scene_pending = False\n            self._lifecycle.on_new_scene_loaded()\n            return\n        self._lifecycle.on_open()\n\n    def _on_scene_about_to_save(self, *_args, **_kwargs):\n        self._lifecycle.before_save()\n\n    def _on_scene_save(self, *_args, **_kwargs):\n        self._lifecycle.on_save()\n\n    def _on_new_scene(self, *_args, **_kwargs):\n        self._new_scene_pending = True\n        self._lifecycle.on_new()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/callbacks.html#client.ayon_katana.api.callbacks.CallbackManager.install","title":"<code>install()</code>","text":"<p>Register each supported Katana callback once.</p> Source code in <code>client/ayon_katana/api/callbacks.py</code> <pre><code>def install(self):\n    \"\"\"Register each supported Katana callback once.\"\"\"\n    if self._registered_callbacks:\n        return\n    self._register_optional(\"onStartupComplete\", self._on_startup_complete)\n    self._register_optional(\"onSceneAboutToLoad\", self._on_scene_about_to_load)\n    self._register_optional(\"onSceneLoad\", self._on_scene_load)\n    self._register_optional(\"onSceneAboutToSave\", self._on_scene_about_to_save)\n    self._register_optional(\"onSceneSave\", self._on_scene_save)\n    self._register_optional(\"onNewScene\", self._on_new_scene)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/callbacks.html#client.ayon_katana.api.callbacks.CallbackManager.uninstall","title":"<code>uninstall()</code>","text":"<p>Remove all callbacks registered by this manager.</p> Source code in <code>client/ayon_katana/api/callbacks.py</code> <pre><code>def uninstall(self):\n    \"\"\"Remove all callbacks registered by this manager.\"\"\"\n    for callback_type, callback in reversed(self._registered_callbacks):\n        try:\n            Callbacks.delCallback(callback_type, callback)\n        except (RuntimeError, ValueError):\n            log.exception(\"Failed to remove Katana callback %r.\", callback_type)\n    self._registered_callbacks.clear()\n    self._new_scene_pending = False\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/colorspace.html","title":"colorspace","text":"<p>Colorspace helpers for Katana render products.</p>"},{"location":"autoapi/client/ayon_katana/api/colorspace.html#client.ayon_katana.api.colorspace.ARenderProduct","title":"<code>ARenderProduct</code>","text":"<p>This is the minimal data structure required to get <code>ayon_core.pipeline.farm.pyblish_functions.create_instances_for_aov</code> to work with the Deadline addon's job submissions.</p> Source code in <code>client/ayon_katana/api/colorspace.py</code> <pre><code>class ARenderProduct:\n    \"\"\"This is the minimal data structure required to get\n    `ayon_core.pipeline.farm.pyblish_functions.create_instances_for_aov` to\n    work with the Deadline addon's job submissions.\n    \"\"\"\n\n    def __init__(self, aov_names: List[str], colorspace: str = \"\"):\n        colorspace = colorspace or get_scene_linear_colorspace()\n        products = [\n            RenderProduct(colorspace=colorspace, productName=aov_name)\n            for aov_name in aov_names\n        ]\n        self.layer_data = LayerMetadata(products=products)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/colorspace.html#client.ayon_katana.api.colorspace.LayerMetadata","title":"<code>LayerMetadata</code>","text":"<p>Data class for Render Layer metadata.</p> Source code in <code>client/ayon_katana/api/colorspace.py</code> <pre><code>@attr.s\nclass LayerMetadata:\n    \"\"\"Data class for Render Layer metadata.\"\"\"\n\n    products: List[RenderProduct] = attr.ib()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/colorspace.html#client.ayon_katana.api.colorspace.RenderProduct","title":"<code>RenderProduct</code>","text":"<p>Specific Render Product Parameter for submitting.</p> Source code in <code>client/ayon_katana/api/colorspace.py</code> <pre><code>@attr.s\nclass RenderProduct:\n    \"\"\"Specific Render Product Parameter for submitting.\"\"\"\n\n    colorspace: str = attr.ib()\n    productName: Optional[str] = attr.ib(default=None)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/colorspace.html#client.ayon_katana.api.colorspace.get_imageio_file_rule_colorspace","title":"<code>get_imageio_file_rule_colorspace(filepath, context, project_settings=None)</code>","text":"<p>Return an AYON ImageIO file-rule colorspace for an image path.</p> <p>Callers should check representation metadata before evaluating host or global file rules. Configuration and Core API errors propagate.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Resolved image path used for file-rule matching.</p> required <code>context</code> <code>dict</code> <p>AYON load context containing project/folder/task entities.</p> required <code>project_settings</code> <code>Optional[dict]</code> <p>Optional already-resolved project settings.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>Matched colorspace name, or an empty string when host management is</p> <code>str</code> <p>disabled, context is incomplete, or no rule matches.</p> Source code in <code>client/ayon_katana/api/colorspace.py</code> <pre><code>def get_imageio_file_rule_colorspace(\n    filepath: str,\n    context: dict,\n    project_settings: Optional[dict] = None,\n) -&gt; str:\n    \"\"\"Return an AYON ImageIO file-rule colorspace for an image path.\n\n    Callers should check representation metadata before evaluating host or\n    global file rules. Configuration and Core API errors propagate.\n\n    Args:\n        filepath: Resolved image path used for file-rule matching.\n        context: AYON load context containing project/folder/task entities.\n        project_settings: Optional already-resolved project settings.\n\n    Returns:\n        Matched colorspace name, or an empty string when host management is\n        disabled, context is incomplete, or no rule matches.\n    \"\"\"\n    project = context.get(\"project\") or {}\n    project_name = project.get(\"name\")\n    if not project_name:\n        return \"\"\n\n    if project_settings is None:\n        project_settings = context.get(\"project_settings\")\n    if project_settings is None:\n        from ayon_core.settings import get_project_settings\n\n        project_settings = get_project_settings(project_name)\n\n    host_imageio = (project_settings.get(\"katana\") or {}).get(\"imageio\") or {}\n    if not host_imageio.get(\"activate_host_color_management\", True):\n        return \"\"\n\n    folder = context.get(\"folder\") or {}\n    task = context.get(\"task\") or {}\n    from ayon_core.pipeline.colorspace import (\n        get_imageio_config_preset,\n        get_imageio_file_rules,\n        get_imageio_file_rules_colorspace_from_filepath,\n    )\n\n    config_data = get_imageio_config_preset(\n        project_name,\n        folder.get(\"path\") or \"\",\n        task.get(\"name\") or \"\",\n        \"katana\",\n        os.environ.get(\"AYON_APP_NAME\"),\n        project_settings=project_settings,\n    )\n    if not config_data:\n        return \"\"\n    file_rules = get_imageio_file_rules(\n        project_name,\n        \"katana\",\n        project_settings,\n    )\n    return str(\n        get_imageio_file_rules_colorspace_from_filepath(\n            filepath,\n            \"katana\",\n            project_name,\n            config_data,\n            file_rules=file_rules,\n            project_settings=project_settings,\n        )\n        or \"\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/colorspace.html#client.ayon_katana.api.colorspace.get_ocio_config_path","title":"<code>get_ocio_config_path()</code>","text":"<p>Return the active OCIO config path, validating explicit paths.</p> Source code in <code>client/ayon_katana/api/colorspace.py</code> <pre><code>def get_ocio_config_path() -&gt; str:\n    \"\"\"Return the active OCIO config path, validating explicit paths.\"\"\"\n    config_path = os.environ.get(\"OCIO\", \"\")\n    if config_path and not Path(config_path).is_file():\n        raise FileNotFoundError(f\"OCIO config does not exist: {config_path!r}\")\n    return config_path\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/colorspace.html#client.ayon_katana.api.colorspace.get_scene_linear_colorspace","title":"<code>get_scene_linear_colorspace()</code>","text":"<p>Return colorspace name for Katana's OCIO scene linear role.</p> <p>By default, renderers in Katana render output images in the scene linear role colorspace. Config parsing errors propagate from Core.</p> <p>Returns:</p> Type Description <code>str</code> <p>The colorspace name for the <code>scene_linear</code> role in the active OCIO</p> <code>str</code> <p>config, or an empty string when OCIO or the role is not configured.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>The configured OCIO file does not exist.</p> Source code in <code>client/ayon_katana/api/colorspace.py</code> <pre><code>def get_scene_linear_colorspace() -&gt; str:\n    \"\"\"Return colorspace name for Katana's OCIO scene linear role.\n\n    By default, renderers in Katana render output images in the scene linear\n    role colorspace. Config parsing errors propagate from Core.\n\n    Returns:\n        The colorspace name for the ``scene_linear`` role in the active OCIO\n        config, or an empty string when OCIO or the role is not configured.\n\n    Raises:\n        FileNotFoundError: The configured OCIO file does not exist.\n    \"\"\"\n    ocio_config_path = get_ocio_config_path()\n    if not ocio_config_path:\n        return \"\"\n\n    colorspaces = get_ocio_config_colorspaces(ocio_config_path)\n    return colorspaces[\"roles\"].get(\"scene_linear\", {}).get(\"colorspace\", \"\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/compat.html","title":"compat","text":"<p>Shared Katana host API helpers.</p>"},{"location":"autoapi/client/ayon_katana/api/compat.html#client.ayon_katana.api.compat.get_node","title":"<code>get_node(node_name)</code>","text":"<p>Return a Katana node by its globally unique name.</p> Source code in <code>client/ayon_katana/api/compat.py</code> <pre><code>def get_node(node_name: str):\n    \"\"\"Return a Katana node by its globally unique name.\"\"\"\n    return NodegraphAPI.GetNode(node_name)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/compat.html#client.ayon_katana.api.compat.get_output_ports","title":"<code>get_output_ports(node)</code>","text":"<p>Return all native output ports exposed by a Katana node.</p> Source code in <code>client/ayon_katana/api/compat.py</code> <pre><code>def get_output_ports(node):\n    \"\"\"Return all native output ports exposed by a Katana node.\"\"\"\n    return list(node.getOutputPorts() or [])\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/compat.html#client.ayon_katana.api.compat.get_selected_nodes","title":"<code>get_selected_nodes()</code>","text":"<p>Return selected Katana nodes.</p> Source code in <code>client/ayon_katana/api/compat.py</code> <pre><code>def get_selected_nodes() -&gt; list:\n    \"\"\"Return selected Katana nodes.\"\"\"\n    return list(NodegraphAPI.GetAllSelectedNodes())\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/compat.html#client.ayon_katana.api.compat.import_katana_file","title":"<code>import_katana_file(filepath, parent_node=None, float_nodes=False)</code>","text":"<p>Import a Katana file and return all imported nodes.</p> Source code in <code>client/ayon_katana/api/compat.py</code> <pre><code>def import_katana_file(\n    filepath: str,\n    parent_node=None,\n    float_nodes: bool = False,\n) -&gt; list:\n    \"\"\"Import a Katana file and return all imported nodes.\"\"\"\n    existing_nodes = set(iter_nodes())\n    result = KatanaFile.Import(\n        filepath,\n        floatNodes=float_nodes,\n        parentNode=parent_node,\n    )\n    imported_nodes = list(result or [])\n    if imported_nodes:\n        return imported_nodes\n    return list(set(iter_nodes()) - existing_nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/compat.html#client.ayon_katana.api.compat.is_descendant","title":"<code>is_descendant(node, parent_node)</code>","text":"<p>Return whether a node is nested below another Katana node.</p> Source code in <code>client/ayon_katana/api/compat.py</code> <pre><code>def is_descendant(node, parent_node):\n    \"\"\"Return whether a node is nested below another Katana node.\"\"\"\n    current_node = node.getParent()\n    while current_node is not None:\n        if current_node is parent_node:\n            return True\n        current_node = current_node.getParent()\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/compat.html#client.ayon_katana.api.compat.iter_nodes","title":"<code>iter_nodes(parent_node=None)</code>","text":"<p>Yield all Katana nodes recursively.</p> Source code in <code>client/ayon_katana/api/compat.py</code> <pre><code>def iter_nodes(parent_node=None):\n    \"\"\"Yield all Katana nodes recursively.\"\"\"\n    parent_node = parent_node or NodegraphAPI.GetRootNode()\n    get_children = getattr(parent_node, \"getChildren\", None)\n    if get_children is None:\n        return\n\n    for child in get_children() or []:\n        yield child\n        yield from iter_nodes(child)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/compat.html#client.ayon_katana.api.compat.set_parent","title":"<code>set_parent(node, parent_node)</code>","text":"<p>Move a node below a Group.</p> Source code in <code>client/ayon_katana/api/compat.py</code> <pre><code>def set_parent(node, parent_node) -&gt; None:\n    \"\"\"Move a node below a Group.\"\"\"\n    node.setParent(parent_node)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/compat.html#client.ayon_katana.api.compat.set_selected_nodes","title":"<code>set_selected_nodes(nodes)</code>","text":"<p>Replace the Katana node selection.</p> Source code in <code>client/ayon_katana/api/compat.py</code> <pre><code>def set_selected_nodes(nodes: list) -&gt; None:\n    \"\"\"Replace the Katana node selection.\"\"\"\n    NodegraphAPI.SetAllSelectedNodes(nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html","title":"containers","text":"<p>AYON container metadata and managed graph structure for Katana.</p>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.connect_managed_group_to_user","title":"<code>connect_managed_group_to_user(managed_group, user_group)</code>","text":"<p>Connect a managed Group's output to the artist-editable Group.</p> <p>Parameters:</p> Name Type Description Default <code>managed_group</code> <p>Loader-owned Group to connect.</p> required <code>user_group</code> <p>Artist-editable Group within the same container.</p> required <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Required graph ports are missing.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def connect_managed_group_to_user(managed_group, user_group) -&gt; None:\n    \"\"\"Connect a managed Group's output to the artist-editable Group.\n\n    Args:\n        managed_group: Loader-owned Group to connect.\n        user_group: Artist-editable Group within the same container.\n\n    Raises:\n        RuntimeError: Required graph ports are missing.\n    \"\"\"\n    managed_output = managed_group.getOutputPort(\"out\")\n    user_input = user_group.getInputPort(\"in\")\n    if managed_output is None or user_input is None:\n        raise RuntimeError(\"The AYON container has incomplete managed/user ports.\")\n    managed_output.connect(user_input)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.containerise","title":"<code>containerise(name, namespace, context, loader=None, parent_node=None)</code>","text":"<p>Bundle a Katana graph into an AYON container.</p> <p>Containerisation enables tracking of version, author and origin for loaded products. The container output flows through an artist-editable <code>USER</code> group while loader updates remain inside <code>AYON_MANAGED</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the loaded product.</p> required <code>namespace</code> <code>str</code> <p>Namespace under which to host the container.</p> required <code>context</code> <code>dict[str, Any]</code> <p>Loaded product context information.</p> required <code>loader</code> <p>Name of the loader used to produce the container.</p> <code>None</code> <code>parent_node</code> <p>Optional Katana parent node. Defaults to the root node.</p> <code>None</code> <p>Returns:</p> Type Description <p>Created Katana container Group.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def containerise(\n    name: str,\n    namespace: str,\n    context: dict[str, Any],\n    loader=None,\n    parent_node=None,\n):\n    \"\"\"Bundle a Katana graph into an AYON container.\n\n    Containerisation enables tracking of version, author and origin for loaded\n    products. The container output flows through an artist-editable ``USER``\n    group while loader updates remain inside ``AYON_MANAGED``.\n\n    Args:\n        name: Name of the loaded product.\n        namespace: Namespace under which to host the container.\n        context: Loaded product context information.\n        loader: Name of the loader used to produce the container.\n        parent_node: Optional Katana parent node. Defaults to the root node.\n\n    Returns:\n        Created Katana container Group.\n    \"\"\"\n    parent_node = parent_node or NodegraphAPI.GetRootNode()\n    container_node = None\n    try:\n        container_node = NodegraphAPI.CreateNode(\"Group\", parent_node)\n        container_node.setName(\n            lib.sanitize_node_name(f\"{namespace}_{name}_CON\", \"AYON_Container\")\n        )\n        container_node.addOutputPort(\"out\")\n\n        managed_group = create_managed_group(container_node)\n\n        user_group = NodegraphAPI.CreateNode(\"Group\", container_node)\n        user_group.setName(USER_GROUP_NAME)\n        user_group.addInputPort(\"in\")\n        user_group.addOutputPort(\"out\")\n        set_user_group_role(user_group)\n\n        user_group.getSendPort(\"in\").connect(user_group.getReturnPort(\"out\"))\n        connect_managed_group_to_user(managed_group, user_group)\n        user_group.getOutputPort(\"out\").connect(container_node.getReturnPort(\"out\"))\n\n        representation = context[\"representation\"]\n        project = context.get(\"project\") or {}\n        imprint(\n            container_node,\n            {\n                \"schema\": _CONTAINER_SCHEMA,\n                \"id\": AYON_CONTAINER_ID,\n                \"name\": name,\n                \"namespace\": namespace,\n                \"loader\": loader,\n                \"representation\": representation[\"id\"],\n                \"project_name\": project.get(\"name\"),\n            },\n        )\n        return container_node\n    except Exception:\n        if container_node is not None:\n            with suppress(Exception):\n                container_node.delete()\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.create_managed_group","title":"<code>create_managed_group(container_node, name=MANAGED_GROUP_NAME, role=_MANAGED_ROLE)</code>","text":"<p>Create a loader-owned Group below an AYON container.</p> <p>Parameters:</p> Name Type Description Default <code>container_node</code> <p>Outer AYON container Group node.</p> required <code>name</code> <code>str</code> <p>Name for the managed Group.</p> <code>MANAGED_GROUP_NAME</code> <code>role</code> <code>Optional[str]</code> <p>Optional AYON managed-graph role. Use <code>None</code> for a temporary update candidate until it has replaced the current managed group.</p> <code>_MANAGED_ROLE</code> <p>Returns:</p> Type Description <p>Created managed Group node.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def create_managed_group(\n    container_node,\n    name: str = MANAGED_GROUP_NAME,\n    role: Optional[str] = _MANAGED_ROLE,\n):\n    \"\"\"Create a loader-owned Group below an AYON container.\n\n    Args:\n        container_node: Outer AYON container Group node.\n        name: Name for the managed Group.\n        role: Optional AYON managed-graph role. Use ``None`` for a temporary\n            update candidate until it has replaced the current managed group.\n\n    Returns:\n        Created managed Group node.\n    \"\"\"\n    managed_group = NodegraphAPI.CreateNode(\"Group\", container_node)\n    managed_group.setName(name)\n    managed_group.addOutputPort(\"out\")\n    if role is not None:\n        _set_node_role(managed_group, role)\n    return managed_group\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.disconnect_managed_group_from_user","title":"<code>disconnect_managed_group_from_user(managed_group, user_group)</code>","text":"<p>Disconnect a managed Group from the artist-editable Group.</p> <p>Parameters:</p> Name Type Description Default <code>managed_group</code> <p>Loader-owned Group currently feeding the user Group.</p> required <code>user_group</code> <p>Artist-editable Group within the same container.</p> required <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Required graph ports are missing.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def disconnect_managed_group_from_user(managed_group, user_group) -&gt; None:\n    \"\"\"Disconnect a managed Group from the artist-editable Group.\n\n    Args:\n        managed_group: Loader-owned Group currently feeding the user Group.\n        user_group: Artist-editable Group within the same container.\n\n    Raises:\n        RuntimeError: Required graph ports are missing.\n    \"\"\"\n    managed_output = managed_group.getOutputPort(\"out\")\n    user_input = user_group.getInputPort(\"in\")\n    if managed_output is None or user_input is None:\n        raise RuntimeError(\"The AYON container has incomplete managed/user ports.\")\n    user_input.disconnect(managed_output)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.find_managed_node","title":"<code>find_managed_node(container_node, role)</code>","text":"<p>Find a role-tagged node inside the managed group.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def find_managed_node(container_node, role: str):\n    \"\"\"Find a role-tagged node inside the managed group.\"\"\"\n    managed_group = get_managed_group(container_node)\n    if managed_group is None:\n        return None\n    return find_node_by_role(managed_group, role)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.find_node_by_role","title":"<code>find_node_by_role(parent_node, role)</code>","text":"<p>Find a direct child by its AYON managed-graph role.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def find_node_by_role(parent_node, role: str):\n    \"\"\"Find a direct child by its AYON managed-graph role.\"\"\"\n    for child in parent_node.getChildren():\n        if lib.get_string_parameter(child, _NODE_ROLE_PARAMETER) == role:\n            return child\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.get_managed_group","title":"<code>get_managed_group(container_node)</code>","text":"<p>Return the loader-owned Group in a container.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def get_managed_group(container_node):\n    \"\"\"Return the loader-owned Group in a container.\"\"\"\n    return find_node_by_role(container_node, _MANAGED_ROLE)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.get_user_group","title":"<code>get_user_group(container_node)</code>","text":"<p>Return the artist-owned Group in a container.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def get_user_group(container_node):\n    \"\"\"Return the artist-owned Group in a container.\"\"\"\n    return find_node_by_role(container_node, _USER_ROLE)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.imprint","title":"<code>imprint(node, data)</code>","text":"<p>Write AYON container metadata onto a Katana node.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <p>Katana container node.</p> required <code>data</code> <code>dict[str, Any]</code> <p>Container metadata to store.</p> required Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def imprint(node, data: dict[str, Any]) -&gt; None:\n    \"\"\"Write AYON container metadata onto a Katana node.\n\n    Args:\n        node: Katana container node.\n        data: Container metadata to store.\n    \"\"\"\n    container_data = dict(data)\n    container_data.setdefault(\"schema\", _CONTAINER_SCHEMA)\n    container_data.setdefault(\"id\", AYON_CONTAINER_ID)\n    lib.write_json_parameter(node, _CONTAINER_PARAMETER, container_data)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.ls","title":"<code>ls()</code>","text":"<p>Yield AYON containers in the current project.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def ls():\n    \"\"\"Yield AYON containers in the current project.\"\"\"\n    for node in NodegraphAPI.GetAllNodesByType(\"Group\"):\n        container = parse_container(node)\n        if container is not None:\n            yield container\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.parse_container","title":"<code>parse_container(node)</code>","text":"<p>Parse AYON container metadata from a Katana node.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <p>Katana node to inspect.</p> required <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Parsed container data including transient node information, or <code>None</code>.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def parse_container(node) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Parse AYON container metadata from a Katana node.\n\n    Args:\n        node: Katana node to inspect.\n\n    Returns:\n        Parsed container data including transient node information, or ``None``.\n    \"\"\"\n    data = lib.read_json_parameter(node, _CONTAINER_PARAMETER)\n    if data is None or data.get(\"id\") != AYON_CONTAINER_ID:\n        return None\n\n    data.setdefault(\"schema\", _CONTAINER_SCHEMA)\n    data[\"objectName\"] = node.getName()\n    data[\"node\"] = node\n    return data\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.replace_managed_group","title":"<code>replace_managed_group(container_node, replacement_group, data)</code>","text":"<p>Replace the managed graph, restoring the previous state on failure.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def replace_managed_group(\n    container_node,\n    replacement_group,\n    data: dict[str, Any],\n) -&gt; None:\n    \"\"\"Replace the managed graph, restoring the previous state on failure.\"\"\"\n    previous_group = get_managed_group(container_node)\n    user_group = get_user_group(container_node)\n    if previous_group is None or user_group is None:\n        with suppress(Exception):\n            replacement_group.delete()\n        raise RuntimeError(\"The AYON container is missing its managed/user groups.\")\n\n    previous_data = parse_container(container_node)\n    if previous_data is None:\n        with suppress(Exception):\n            replacement_group.delete()\n        raise RuntimeError(\"The AYON Katana container has invalid metadata.\")\n    previous_data.pop(\"node\", None)\n    previous_data.pop(\"objectName\", None)\n\n    previous_name = previous_group.getName()\n    replacement_name = replacement_group.getName()\n\n    def rollback() -&gt; None:\n        \"\"\"Best-effort restore without masking the original failure.\"\"\"\n        with suppress(Exception):\n            disconnect_managed_group_from_user(replacement_group, user_group)\n        with suppress(Exception):\n            _set_node_role(replacement_group, _INACTIVE_ROLE)\n        with suppress(Exception):\n            replacement_group.setName(replacement_name)\n        with suppress(Exception):\n            previous_group.setName(previous_name)\n        with suppress(Exception):\n            set_managed_group_role(previous_group)\n        with suppress(Exception):\n            connect_managed_group_to_user(previous_group, user_group)\n        with suppress(Exception):\n            imprint(container_node, previous_data)\n        with suppress(Exception):\n            replacement_group.delete()\n\n    try:\n        disconnect_managed_group_from_user(previous_group, user_group)\n        connect_managed_group_to_user(replacement_group, user_group)\n        previous_group.setName(_OLD_MANAGED_GROUP_NAME)\n        _set_node_role(previous_group, _INACTIVE_ROLE)\n        replacement_group.setName(MANAGED_GROUP_NAME)\n        set_managed_group_role(replacement_group)\n        update_container(container_node, data)\n    except Exception:\n        rollback()\n        raise\n\n    try:\n        previous_group.delete()\n    except Exception:\n        log.warning(\n            \"Could not delete the previous managed group after replacement.\",\n            exc_info=True,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.set_managed_group_role","title":"<code>set_managed_group_role(managed_group)</code>","text":"<p>Mark a container child as the active loader-owned Group.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def set_managed_group_role(managed_group) -&gt; None:\n    \"\"\"Mark a container child as the active loader-owned Group.\"\"\"\n    _set_node_role(managed_group, _MANAGED_ROLE)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.set_managed_node_role","title":"<code>set_managed_node_role(node, role)</code>","text":"<p>Assign a loader-specific role to a managed child node.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def set_managed_node_role(node, role: str) -&gt; None:\n    \"\"\"Assign a loader-specific role to a managed child node.\"\"\"\n    _set_node_role(node, role)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.set_user_group_role","title":"<code>set_user_group_role(user_group)</code>","text":"<p>Mark a container child as the artist-editable Group.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def set_user_group_role(user_group) -&gt; None:\n    \"\"\"Mark a container child as the artist-editable Group.\"\"\"\n    _set_node_role(user_group, _USER_ROLE)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/containers.html#client.ayon_katana.api.containers.update_container","title":"<code>update_container(container_node, data)</code>","text":"<p>Update persistent metadata on an existing container.</p> Source code in <code>client/ayon_katana/api/containers.py</code> <pre><code>def update_container(container_node, data: dict[str, Any]) -&gt; None:\n    \"\"\"Update persistent metadata on an existing container.\"\"\"\n    current_data = parse_container(container_node) or {}\n    current_data.pop(\"node\", None)\n    current_data.pop(\"objectName\", None)\n    current_data.update(data)\n    imprint(container_node, current_data)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/context.html","title":"context","text":"<p>Persist AYON context on Katana's root node.</p>"},{"location":"autoapi/client/ayon_katana/api/context.html#client.ayon_katana.api.context.apply_context_settings","title":"<code>apply_context_settings(host, data=None)</code>","text":"<p>Apply the safe Katana subset of AYON context project settings.</p> <p>Embedded AYON context and task frame range/current frame are updated in one place for new scenes and task changes. Katana 8/9 cannot apply AYON FPS without a non-authoritative workaround, and its <code>resolution</code> project parameter is a renderer-independent preset name rather than a width/height project setting. Neither setting is mutated here.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <p>Installed AYON Katana host.</p> required <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Optional context fields supplied by an AYON lifecycle event.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict</code> <p>Context data persisted on the Katana root node.</p> Source code in <code>client/ayon_katana/api/context.py</code> <pre><code>def apply_context_settings(\n    host,\n    data: Optional[dict[str, Any]] = None,\n) -&gt; dict:\n    \"\"\"Apply the safe Katana subset of AYON context project settings.\n\n    Embedded AYON context and task frame range/current frame are updated in\n    one place for new scenes and task changes. Katana 8/9 cannot apply AYON\n    FPS without a non-authoritative workaround, and its ``resolution``\n    project parameter is a renderer-independent preset name rather than a\n    width/height project setting. Neither setting is mutated here.\n\n    Args:\n        host: Installed AYON Katana host.\n        data: Optional context fields supplied by an AYON lifecycle event.\n\n    Returns:\n        Context data persisted on the Katana root node.\n    \"\"\"\n    context_data = update_embedded_context(host, data)\n    if not all(\n        context_data.get(key) for key in (\"project_name\", \"folder_path\", \"task_name\")\n    ):\n        log.debug(\"Skipping Katana timing without a complete AYON context.\")\n        return context_data\n    try:\n        task_entity = _get_current_task_entity()\n    except Exception:\n        log.exception(\"Could not resolve the active AYON task for Katana timing.\")\n        return context_data\n\n    if task_entity is None:\n        log.debug(\"Skipping Katana timing without an active AYON task.\")\n        return context_data\n\n    apply_frame_range(task_entity)\n    log_fps_limitation(task_entity)\n    return context_data\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/context.html#client.ayon_katana.api.context.apply_current_frame_range","title":"<code>apply_current_frame_range()</code>","text":"<p>Apply the active AYON task range without changing Katana FPS.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when the active task supplied a valid frame range.</p> Source code in <code>client/ayon_katana/api/context.py</code> <pre><code>def apply_current_frame_range() -&gt; bool:\n    \"\"\"Apply the active AYON task range without changing Katana FPS.\n\n    Returns:\n        ``True`` when the active task supplied a valid frame range.\n    \"\"\"\n    try:\n        task_entity = _get_current_task_entity()\n    except Exception:\n        log.exception(\"Could not resolve the active AYON task frame range.\")\n        return False\n    if task_entity is None:\n        log.warning(\"Cannot set Katana frame range without an active AYON task.\")\n        return False\n    return apply_frame_range(task_entity)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/context.html#client.ayon_katana.api.context.apply_frame_range","title":"<code>apply_frame_range(task_entity)</code>","text":"<p>Apply a task frame range and place Katana on its first frame.</p> <p>The order handles a target range that does not overlap the existing Katana range. Katana requires <code>inTime &lt; outTime</code> for every individual setter call.</p> <p>Parameters:</p> Name Type Description Default <code>task_entity</code> <code>dict[str, Any]</code> <p>AYON task entity carrying frame and handle attributes.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when a valid frame range was applied.</p> Source code in <code>client/ayon_katana/api/context.py</code> <pre><code>def apply_frame_range(task_entity: dict[str, Any]) -&gt; bool:\n    \"\"\"Apply a task frame range and place Katana on its first frame.\n\n    The order handles a target range that does not overlap the existing\n    Katana range. Katana requires ``inTime &lt; outTime`` for every individual\n    setter call.\n\n    Args:\n        task_entity: AYON task entity carrying frame and handle attributes.\n\n    Returns:\n        ``True`` when a valid frame range was applied.\n    \"\"\"\n    frame_range = _frame_range_from_task(task_entity)\n    if frame_range is None:\n        return False\n    frame_start, frame_end = frame_range\n\n    current_in_time = NodegraphAPI.GetInTime()\n    if frame_end &gt; current_in_time:\n        NodegraphAPI.SetOutTime(frame_end)\n    NodegraphAPI.SetInTime(frame_start)\n    NodegraphAPI.SetOutTime(frame_end)\n    NodegraphAPI.SetCurrentTime(frame_start)\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/context.html#client.ayon_katana.api.context.get_context_data","title":"<code>get_context_data()</code>","text":"<p>Read AYON context data from the Katana root node.</p> Source code in <code>client/ayon_katana/api/context.py</code> <pre><code>def get_context_data() -&gt; dict[str, Any]:\n    \"\"\"Read AYON context data from the Katana root node.\"\"\"\n    data = lib.read_json_parameter(\n        NodegraphAPI.GetRootNode(),\n        _CONTEXT_PARAMETER,\n    )\n    return data or {}\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/context.html#client.ayon_katana.api.context.get_current_context_data","title":"<code>get_current_context_data(host, data=None)</code>","text":"<p>Return the current AYON context, optionally updated by event data.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <p>Installed AYON Katana host.</p> required <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Optional context fields supplied by an AYON lifecycle event.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict</code> <p>Canonical project, folder, and task context data.</p> Source code in <code>client/ayon_katana/api/context.py</code> <pre><code>def get_current_context_data(host, data: Optional[dict[str, Any]] = None) -&gt; dict:\n    \"\"\"Return the current AYON context, optionally updated by event data.\n\n    Args:\n        host: Installed AYON Katana host.\n        data: Optional context fields supplied by an AYON lifecycle event.\n\n    Returns:\n        Canonical project, folder, and task context data.\n    \"\"\"\n    try:\n        host_context = host.get_current_context()\n    except KeyError:\n        log.debug(\"AYON context is unavailable in the current Katana session.\")\n        host_context = {}\n    context_data = {\n        key: value for key, value in host_context.items() if key in _CONTEXT_KEYS\n    }\n    if data:\n        context_data.update(\n            {\n                key: value\n                for key, value in data.items()\n                if key in _CONTEXT_KEYS and value is not None\n            }\n        )\n    return context_data\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/context.html#client.ayon_katana.api.context.get_current_task_type","title":"<code>get_current_task_type()</code>","text":"<p>Return the active AYON task type when it can be resolved.</p> Source code in <code>client/ayon_katana/api/context.py</code> <pre><code>def get_current_task_type() -&gt; Optional[str]:\n    \"\"\"Return the active AYON task type when it can be resolved.\"\"\"\n    task_entity = get_current_task_entity(fields={\"taskType\"})\n    if task_entity is None:\n        return None\n    return task_entity.get(\"taskType\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/context.html#client.ayon_katana.api.context.get_workfile_instance_data","title":"<code>get_workfile_instance_data()</code>","text":"<p>Return persisted workfile creator data.</p> Source code in <code>client/ayon_katana/api/context.py</code> <pre><code>def get_workfile_instance_data() -&gt; dict[str, Any]:\n    \"\"\"Return persisted workfile creator data.\"\"\"\n    workfile_data = get_context_data().get(\"workfile\")\n    return dict(workfile_data) if isinstance(workfile_data, dict) else {}\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/context.html#client.ayon_katana.api.context.log_fps_limitation","title":"<code>log_fps_limitation(task_entity=None)</code>","text":"<p>Report that Katana has no authoritative project FPS setter.</p> <p>Real Katana 8.0v1 and 9.0v1 probes expose frame-range and current-time APIs but no FPS getter or setter. <code>timeIncrement</code> is documented as the timeline navigation increment, not a frame-rate setting, so it must not be repurposed as an FPS compatibility shim.</p> <p>Parameters:</p> Name Type Description Default <code>task_entity</code> <code>Optional[dict[str, Any]]</code> <p>Optional task entity used only to include requested FPS in the log message.</p> <code>None</code> Source code in <code>client/ayon_katana/api/context.py</code> <pre><code>def log_fps_limitation(task_entity: Optional[dict[str, Any]] = None) -&gt; None:\n    \"\"\"Report that Katana has no authoritative project FPS setter.\n\n    Real Katana 8.0v1 and 9.0v1 probes expose frame-range and current-time\n    APIs but no FPS getter or setter. ``timeIncrement`` is documented as the\n    timeline navigation increment, not a frame-rate setting, so it must not\n    be repurposed as an FPS compatibility shim.\n\n    Args:\n        task_entity: Optional task entity used only to include requested FPS\n            in the log message.\n    \"\"\"\n    fps = None\n    if task_entity is not None:\n        fps = (task_entity.get(\"attrib\") or {}).get(\"fps\")\n    if fps is None:\n        log.warning(\n            \"Katana 8/9 expose no authoritative project FPS setter; \"\n            \"leaving scene FPS unchanged.\"\n        )\n        return\n    log.warning(\n        \"Katana 8/9 expose no authoritative project FPS setter; \"\n        \"leaving scene FPS unchanged (AYON task requests %s FPS).\",\n        fps,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/context.html#client.ayon_katana.api.context.set_workfile_instance_data","title":"<code>set_workfile_instance_data(data)</code>","text":"<p>Persist workfile creator data without replacing project context.</p> Source code in <code>client/ayon_katana/api/context.py</code> <pre><code>def set_workfile_instance_data(data: dict[str, Any]) -&gt; None:\n    \"\"\"Persist workfile creator data without replacing project context.\"\"\"\n    context_data = get_context_data()\n    context_data[\"workfile\"] = dict(data)\n    _write_context_data(context_data)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/context.html#client.ayon_katana.api.context.update_context_data","title":"<code>update_context_data(data, _changes=None)</code>","text":"<p>Write AYON context data to the Katana root node.</p> <p>Workfile creator data is preserved when AYON updates the project context.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Complete context data.</p> required <code>_changes</code> <code>Optional[Any]</code> <p>Optional AYON change-tracking object. <code>data</code> already contains the complete new context, so the change details are informational.</p> <code>None</code> Source code in <code>client/ayon_katana/api/context.py</code> <pre><code>def update_context_data(data: dict[str, Any], _changes: Optional[Any] = None) -&gt; None:\n    \"\"\"Write AYON context data to the Katana root node.\n\n    Workfile creator data is preserved when AYON updates the project context.\n\n    Args:\n        data: Complete context data.\n        _changes: Optional AYON change-tracking object. ``data`` already contains\n            the complete new context, so the change details are informational.\n    \"\"\"\n    context_data = get_context_data()\n    context_data.update(data)\n    _write_context_data(context_data)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/context.html#client.ayon_katana.api.context.update_embedded_context","title":"<code>update_embedded_context(host, data=None)</code>","text":"<p>Persist the active AYON context in the current Katana project.</p> <p>Parameters:</p> Name Type Description Default <code>host</code> <p>Installed AYON Katana host.</p> required <code>data</code> <code>Optional[dict[str, Any]]</code> <p>Optional context fields supplied by an AYON lifecycle event.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict</code> <p>Context data written to the Katana root node.</p> Source code in <code>client/ayon_katana/api/context.py</code> <pre><code>def update_embedded_context(\n    host,\n    data: Optional[dict[str, Any]] = None,\n) -&gt; dict:\n    \"\"\"Persist the active AYON context in the current Katana project.\n\n    Args:\n        host: Installed AYON Katana host.\n        data: Optional context fields supplied by an AYON lifecycle event.\n\n    Returns:\n        Context data written to the Katana root node.\n    \"\"\"\n    context_data = get_current_context_data(host, data)\n    if context_data:\n        host.update_context_data(context_data)\n    return context_data\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/context.html#client.ayon_katana.api.context.update_instance_context_metadata","title":"<code>update_instance_context_metadata(folder_path, task_name)</code>","text":"<p>Persist a new folder and task on existing publish instances.</p> <p>Parameters:</p> Name Type Description Default <code>folder_path</code> <code>str</code> <p>Target AYON folder path.</p> required <code>task_name</code> <code>str</code> <p>Target AYON task name.</p> required <p>Returns:</p> Type Description <code>int</code> <p>Number of node-backed or workfile instances updated.</p> Source code in <code>client/ayon_katana/api/context.py</code> <pre><code>def update_instance_context_metadata(folder_path: str, task_name: str) -&gt; int:\n    \"\"\"Persist a new folder and task on existing publish instances.\n\n    Args:\n        folder_path: Target AYON folder path.\n        task_name: Target AYON task name.\n\n    Returns:\n        Number of node-backed or workfile instances updated.\n    \"\"\"\n    updated_count = 0\n    for node, instance_data in instances.iter_instances():\n        output = dict(instance_data)\n        changed = False\n        if output.get(\"folderPath\") and output[\"folderPath\"] != folder_path:\n            output[\"folderPath\"] = folder_path\n            changed = True\n        if output.get(\"task\") and output[\"task\"] != task_name:\n            output[\"task\"] = task_name\n            changed = True\n        if changed:\n            instances.imprint(node, output)\n            updated_count += 1\n\n    workfile_data = get_workfile_instance_data()\n    if workfile_data:\n        changed = False\n        if workfile_data.get(\"folderPath\") != folder_path:\n            workfile_data[\"folderPath\"] = folder_path\n            changed = True\n        if workfile_data.get(\"task\") != task_name:\n            workfile_data[\"task\"] = task_name\n            changed = True\n        if changed:\n            set_workfile_instance_data(workfile_data)\n            updated_count += 1\n    return updated_count\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html","title":"dependencies","text":"<p>Inspect filesystem references authored in a Katana workfile.</p>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html#client.ayon_katana.api.dependencies.WorkfileReference","title":"<code>WorkfileReference</code>  <code>dataclass</code>","text":"<p>One registered file parameter discovered in the Katana node graph.</p> Source code in <code>client/ayon_katana/api/dependencies.py</code> <pre><code>@dataclass(frozen=True)\nclass WorkfileReference:\n    \"\"\"One registered file parameter discovered in the Katana node graph.\"\"\"\n\n    node: Any\n    parameter: Any\n    node_type: str\n    parameter_name: str\n    authored_value: str\n    resolved_value: Optional[str]\n    is_expression: bool\n    workfile_dir: Optional[str] = None\n\n    @property\n    def node_name(self) -&gt; str:\n        \"\"\"Return the node name.\"\"\"\n        return str(self.node.getName())\n\n    @property\n    def label(self) -&gt; str:\n        \"\"\"Return the node and parameter label.\"\"\"\n        return f\"{self.node_name}.{self.parameter_name}\"\n\n    @property\n    def is_entity_uri(self) -&gt; bool:\n        \"\"\"Return whether the value is a URI.\"\"\"\n        return bool(\n            self.authored_value and _URI_SCHEME_PATTERN.match(self.authored_value)\n        )\n\n    @property\n    def has_environment_tokens(self) -&gt; bool:\n        \"\"\"Return whether the value contains environment tokens.\"\"\"\n        return has_environment_tokens(self.authored_value)\n\n    @property\n    def is_relative_literal(self) -&gt; bool:\n        \"\"\"Return whether the value is a repairable relative path.\"\"\"\n        if (\n            self.is_expression\n            or self.is_entity_uri\n            or self.has_environment_tokens\n            or not self.authored_value\n        ):\n            return False\n        return not os.path.isabs(self.authored_value)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html#client.ayon_katana.api.dependencies.WorkfileReference.has_environment_tokens","title":"<code>has_environment_tokens</code>  <code>property</code>","text":"<p>Return whether the value contains environment tokens.</p>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html#client.ayon_katana.api.dependencies.WorkfileReference.is_entity_uri","title":"<code>is_entity_uri</code>  <code>property</code>","text":"<p>Return whether the value is a URI.</p>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html#client.ayon_katana.api.dependencies.WorkfileReference.is_relative_literal","title":"<code>is_relative_literal</code>  <code>property</code>","text":"<p>Return whether the value is a repairable relative path.</p>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html#client.ayon_katana.api.dependencies.WorkfileReference.label","title":"<code>label</code>  <code>property</code>","text":"<p>Return the node and parameter label.</p>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html#client.ayon_katana.api.dependencies.WorkfileReference.node_name","title":"<code>node_name</code>  <code>property</code>","text":"<p>Return the node name.</p>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html#client.ayon_katana.api.dependencies.collect_workfile_references","title":"<code>collect_workfile_references()</code>","text":"<p>Collect registered filesystem references from the current Katana graph.</p> Source code in <code>client/ayon_katana/api/dependencies.py</code> <pre><code>def collect_workfile_references() -&gt; list[WorkfileReference]:\n    \"\"\"Collect registered filesystem references from the current Katana graph.\"\"\"\n    workfile_path = workio.get_current_workfile()\n    workfile_dir = Path(workfile_path).parent if workfile_path else None\n    references = []\n    for node in compat.iter_nodes():\n        node_type = str(node.getType())\n        parameter_names = REFERENCE_PARAMETERS.get(node_type)\n        if not parameter_names:\n            continue\n        for parameter_name in parameter_names:\n            parameter = node.getParameter(parameter_name)\n            if parameter is None:\n                continue\n            references.append(\n                _reference_from_parameter(\n                    node,\n                    node_type,\n                    parameter,\n                    parameter_name,\n                    workfile_dir,\n                )\n            )\n    return references\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html#client.ayon_katana.api.dependencies.expand_sequence_path","title":"<code>expand_sequence_path(value, frame)</code>","text":"<p>Replace the first supported sequence token with a concrete frame.</p> Source code in <code>client/ayon_katana/api/dependencies.py</code> <pre><code>def expand_sequence_path(value: str, frame: int) -&gt; str:\n    \"\"\"Replace the first supported sequence token with a concrete frame.\"\"\"\n    hash_match = _HASH_SEQUENCE_PATTERN.search(value)\n    if hash_match:\n        padding = len(hash_match.group(1))\n        return (\n            value[: hash_match.start()]\n            + f\"{int(frame):0{padding}d}\"\n            + value[hash_match.end() :]\n        )\n\n    printf_match = _PRINTF_SEQUENCE_PATTERN.search(value)\n    if printf_match:\n        padding = int(printf_match.group(1) or 1)\n        return (\n            value[: printf_match.start()]\n            + f\"{int(frame):0{padding}d}\"\n            + value[printf_match.end() :]\n        )\n\n    dollar_f_match = _DOLLAR_F_SEQUENCE_PATTERN.search(value)\n    if dollar_f_match:\n        padding = int(dollar_f_match.group(1) or 1)\n        return (\n            value[: dollar_f_match.start()]\n            + f\"{int(frame):0{padding}d}\"\n            + value[dollar_f_match.end() :]\n        )\n    return value\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html#client.ayon_katana.api.dependencies.has_environment_tokens","title":"<code>has_environment_tokens(value)</code>","text":"<p>Return whether shell-style environment expansion appears in a value.</p> Source code in <code>client/ayon_katana/api/dependencies.py</code> <pre><code>def has_environment_tokens(value: str) -&gt; bool:\n    \"\"\"Return whether shell-style environment expansion appears in a value.\"\"\"\n    return bool(value and (\"$\" in value or (\"%\" in value and value.count(\"%\") &gt;= 2)))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html#client.ayon_katana.api.dependencies.has_sequence_token","title":"<code>has_sequence_token(value)</code>","text":"<p>Return whether a path contains a supported frame-sequence token.</p> Source code in <code>client/ayon_katana/api/dependencies.py</code> <pre><code>def has_sequence_token(value: str) -&gt; bool:\n    \"\"\"Return whether a path contains a supported frame-sequence token.\"\"\"\n    if not value:\n        return False\n    return bool(\n        _HASH_SEQUENCE_PATTERN.search(value)\n        or _PRINTF_SEQUENCE_PATTERN.search(value)\n        or _DOLLAR_F_SEQUENCE_PATTERN.search(value)\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html#client.ayon_katana.api.dependencies.required_reference_paths","title":"<code>required_reference_paths(reference, *, frame_start=None, frame_end=None, frame_step=1)</code>","text":"<p>Return concrete filesystem paths required by one reference.</p> <p>When a publish frame range is available, evaluate the Katana parameter at every required frame. This is necessary for expression-driven or animated string parameters whose value at frame 0 is unrelated to the published sequence.</p> Source code in <code>client/ayon_katana/api/dependencies.py</code> <pre><code>def required_reference_paths(\n    reference: WorkfileReference,\n    *,\n    frame_start: Optional[int] = None,\n    frame_end: Optional[int] = None,\n    frame_step: int = 1,\n) -&gt; list[str]:\n    \"\"\"Return concrete filesystem paths required by one reference.\n\n    When a publish frame range is available, evaluate the Katana parameter at\n    every required frame. This is necessary for expression-driven or animated\n    string parameters whose value at frame 0 is unrelated to the published\n    sequence.\n    \"\"\"\n    if reference.is_entity_uri:\n        return []\n\n    if frame_start is None or frame_end is None:\n        value = reference.resolved_value\n        if not value or has_sequence_token(value):\n            return []\n        return [value]\n\n    if int(frame_step) &lt;= 0:\n        raise ValueError(\"Reference frame step must be greater than zero.\")\n    if int(frame_end) &lt; int(frame_start):\n        raise ValueError(f\"Invalid reference frame range: {frame_start}-{frame_end}.\")\n\n    workfile_dir = Path(reference.workfile_dir) if reference.workfile_dir else None\n    output = []\n    seen = set()\n    for frame in range(int(frame_start), int(frame_end) + 1, int(frame_step)):\n        evaluated = _parameter_value(reference.parameter, float(frame))\n        if not evaluated:\n            raise ValueError(f\"path is empty at frame {frame}\")\n        if _URI_SCHEME_PATTERN.match(evaluated):\n            raise ValueError(\n                f\"entity URI at frame {frame} cannot be filesystem-validated\"\n            )\n        resolved = _resolve_literal_value(evaluated, workfile_dir)\n        if not resolved:\n            raise ValueError(\n                f\"path could not be resolved at frame {frame}: {evaluated}\"\n            )\n        concrete = (\n            expand_sequence_path(resolved, frame)\n            if has_sequence_token(resolved)\n            else resolved\n        )\n        key = os.path.normcase(os.path.normpath(concrete))\n        if key in seen:\n            continue\n        seen.add(key)\n        output.append(concrete)\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/dependencies.html#client.ayon_katana.api.dependencies.unique_reference_nodes","title":"<code>unique_reference_nodes(references)</code>","text":"<p>Return referenced Katana nodes once, preserving discovery order.</p> Source code in <code>client/ayon_katana/api/dependencies.py</code> <pre><code>def unique_reference_nodes(references: list[WorkfileReference]) -&gt; list[Any]:\n    \"\"\"Return referenced Katana nodes once, preserving discovery order.\"\"\"\n    nodes = []\n    seen = set()\n    for reference in references:\n        key = reference.node_name\n        if key in seen:\n            continue\n        seen.add(key)\n        nodes.append(reference.node)\n    return nodes\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/image.html","title":"image","text":"<p>Native Katana ImageWrite configuration and output helpers.</p>"},{"location":"autoapi/client/ayon_katana/api/image.html#client.ayon_katana.api.image.configure_image_write","title":"<code>configure_image_write(node, *, output_path, file_format, colorspace='', single_frame=False, frame=1)</code>","text":"<p>Configure a native Katana <code>ImageWrite</code> node.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>Any</code> <p>Native Katana <code>ImageWrite</code> node.</p> required <code>output_path</code> <code>str | Path</code> <p>Still or hash-tokenized sequence path.</p> required <code>file_format</code> <code>str</code> <p>Native output format.</p> required <code>colorspace</code> <code>str</code> <p>Optional output colorspace.</p> <code>''</code> <code>single_frame</code> <code>bool</code> <p>Whether the node writes only one frame.</p> <code>False</code> <code>frame</code> <code>int</code> <p>Frame written when <code>single_frame</code> is enabled.</p> <code>1</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>The format, path, or frame is invalid.</p> <code>RuntimeError</code> <p>A required Katana parameter is unavailable.</p> Source code in <code>client/ayon_katana/api/image.py</code> <pre><code>def configure_image_write(\n    node: Any,\n    *,\n    output_path: str | Path,\n    file_format: str,\n    colorspace: str = \"\",\n    single_frame: bool = False,\n    frame: int = 1,\n) -&gt; None:\n    \"\"\"Configure a native Katana ``ImageWrite`` node.\n\n    Args:\n        node: Native Katana ``ImageWrite`` node.\n        output_path: Still or hash-tokenized sequence path.\n        file_format: Native output format.\n        colorspace: Optional output colorspace.\n        single_frame: Whether the node writes only one frame.\n        frame: Frame written when ``single_frame`` is enabled.\n\n    Raises:\n        ValueError: The format, path, or frame is invalid.\n        RuntimeError: A required Katana parameter is unavailable.\n    \"\"\"\n    normalized_format = str(file_format).lower().lstrip(\".\")\n    if normalized_format not in IMAGE_FILE_FORMATS:\n        raise ValueError(f\"Unsupported image file format: {file_format!r}.\")\n    normalized_path = Path(output_path).as_posix()\n    if not normalized_path:\n        raise ValueError(\"ImageWrite output path cannot be empty.\")\n    path_extension = Path(normalized_path).suffix.lower().lstrip(\".\")\n    if path_extension != normalized_format:\n        raise ValueError(\n            f\"ImageWrite path extension {path_extension!r} does not match \"\n            f\"format {normalized_format!r}.\"\n        )\n\n    _get_parameter(node, \"inputs.in.file\").setValue(normalized_path, 0.0)\n    _get_parameter(node, \"inputs.in.image.fileFormat\").setValue(normalized_format, 0.0)\n    _get_parameter(node, \"inputs.in.image.colorspace\").setValue(\n        str(colorspace or \"\"), 0.0\n    )\n    _get_parameter(node, \"singleFrame\").setValue(int(bool(single_frame)), 0.0)\n    _get_parameter(node, \"frame\").setValue(int(frame), 0.0)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/image.html#client.ayon_katana.api.image.expand_image_output_pattern","title":"<code>expand_image_output_pattern(output_pattern, frame_start, frame_end, frame_step)</code>","text":"<p>Expand one ImageWrite hash pattern into concrete normalized paths.</p> <p>Parameters:</p> Name Type Description Default <code>output_pattern</code> <code>str</code> <p>Still path or path containing one hash group.</p> required <code>frame_start</code> <code>int</code> <p>First output frame.</p> required <code>frame_end</code> <code>int</code> <p>Last output frame.</p> required <code>frame_step</code> <code>int</code> <p>Positive inclusive frame increment.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>Concrete normalized image paths.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Range is invalid or a sequence has no unambiguous token.</p> Source code in <code>client/ayon_katana/api/image.py</code> <pre><code>def expand_image_output_pattern(\n    output_pattern: str,\n    frame_start: int,\n    frame_end: int,\n    frame_step: int,\n) -&gt; list[str]:\n    \"\"\"Expand one ImageWrite hash pattern into concrete normalized paths.\n\n    Args:\n        output_pattern: Still path or path containing one hash group.\n        frame_start: First output frame.\n        frame_end: Last output frame.\n        frame_step: Positive inclusive frame increment.\n\n    Returns:\n        Concrete normalized image paths.\n\n    Raises:\n        ValueError: Range is invalid or a sequence has no unambiguous token.\n    \"\"\"\n    if frame_end &lt; frame_start:\n        raise ValueError(f\"Invalid image frame range: {frame_start}-{frame_end}.\")\n    if frame_step &lt; 1:\n        raise ValueError(f\"Invalid image frame step: {frame_step}.\")\n    matches = list(_FRAME_TOKEN.finditer(output_pattern))\n    frame_count = len(range(frame_start, frame_end + 1, frame_step))\n    if frame_count &gt; 1 and len(matches) != 1:\n        raise ValueError(\"ImageWrite sequences require exactly one hash frame token.\")\n    if len(matches) &gt; 1:\n        raise ValueError(\"ImageWrite output has multiple hash frame tokens.\")\n    if not matches:\n        return [os.path.normpath(output_pattern)]\n\n    padding = len(matches[0].group(1))\n    return [\n        os.path.normpath(\n            _FRAME_TOKEN.sub(str(frame).zfill(padding), output_pattern, count=1)\n        )\n        for frame in range(frame_start, frame_end + 1, frame_step)\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/image.html#client.ayon_katana.api.image.read_image_write_settings","title":"<code>read_image_write_settings(node)</code>","text":"<p>Read publish-relevant values from a native <code>ImageWrite</code> node.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>Any</code> <p>Native Katana <code>ImageWrite</code> node.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Output path, format, colorspace, and single-frame settings.</p> Source code in <code>client/ayon_katana/api/image.py</code> <pre><code>def read_image_write_settings(node: Any) -&gt; dict[str, Any]:\n    \"\"\"Read publish-relevant values from a native ``ImageWrite`` node.\n\n    Args:\n        node: Native Katana ``ImageWrite`` node.\n\n    Returns:\n        Output path, format, colorspace, and single-frame settings.\n    \"\"\"\n    return {\n        \"output_path\": str(_get_parameter(node, \"inputs.in.file\").getValue(0.0)),\n        \"file_format\": str(\n            _get_parameter(node, \"inputs.in.image.fileFormat\").getValue(0.0)\n        )\n        .lower()\n        .lstrip(\".\"),\n        \"colorspace\": str(\n            _get_parameter(node, \"inputs.in.image.colorspace\").getValue(0.0) or \"\"\n        ),\n        \"single_frame\": bool(_get_parameter(node, \"singleFrame\").getValue(0.0)),\n        \"frame\": int(_get_parameter(node, \"frame\").getValue(0.0)),\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/instances.html","title":"instances","text":"<p>Node-backed AYON publish instance persistence for Katana.</p>"},{"location":"autoapi/client/ayon_katana/api/instances.html#client.ayon_katana.api.instances.imprint","title":"<code>imprint(node, data)</code>","text":"<p>Store AYON creator instance data on a Katana node.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <p>Katana node representing a publish instance.</p> required <code>data</code> <code>dict[str, Any]</code> <p>Serialized <code>CreatedInstance</code> data.</p> required Source code in <code>client/ayon_katana/api/instances.py</code> <pre><code>def imprint(node, data: dict[str, Any]) -&gt; None:\n    \"\"\"Store AYON creator instance data on a Katana node.\n\n    Args:\n        node: Katana node representing a publish instance.\n        data: Serialized ``CreatedInstance`` data.\n    \"\"\"\n    instance_data = dict(data)\n    instance_data.setdefault(\"id\", AYON_INSTANCE_ID)\n    lib.write_json_parameter(node, _INSTANCE_PARAMETER, instance_data)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/instances.html#client.ayon_katana.api.instances.iter_instances","title":"<code>iter_instances()</code>","text":"<p>Yield Katana nodes carrying AYON creator instance data.</p> Source code in <code>client/ayon_katana/api/instances.py</code> <pre><code>def iter_instances() -&gt; Iterator[tuple[Any, dict[str, Any]]]:\n    \"\"\"Yield Katana nodes carrying AYON creator instance data.\"\"\"\n    for node in iter_nodes():\n        data = read(node)\n        if data is not None:\n            yield node, data\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/instances.html#client.ayon_katana.api.instances.iter_nodes","title":"<code>iter_nodes(parent_node=None)</code>","text":"<p>Yield every Katana node recursively below a parent.</p> <p>Parameters:</p> Name Type Description Default <code>parent_node</code> <p>Parent node. Defaults to Katana's root node.</p> <code>None</code> <p>Yields:</p> Type Description <code>Iterator</code> <p>Katana nodes in depth-first order.</p> Source code in <code>client/ayon_katana/api/instances.py</code> <pre><code>def iter_nodes(parent_node=None) -&gt; Iterator:\n    \"\"\"Yield every Katana node recursively below a parent.\n\n    Args:\n        parent_node: Parent node. Defaults to Katana's root node.\n\n    Yields:\n        Katana nodes in depth-first order.\n    \"\"\"\n    yield from compat.iter_nodes(parent_node)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/instances.html#client.ayon_katana.api.instances.read","title":"<code>read(node)</code>","text":"<p>Read AYON creator instance data from a Katana node.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <p>Katana node to inspect.</p> required <p>Returns:</p> Type Description <code>Optional[dict[str, Any]]</code> <p>Parsed instance data, or <code>None</code> when the node is not an instance.</p> Source code in <code>client/ayon_katana/api/instances.py</code> <pre><code>def read(node) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Read AYON creator instance data from a Katana node.\n\n    Args:\n        node: Katana node to inspect.\n\n    Returns:\n        Parsed instance data, or ``None`` when the node is not an instance.\n    \"\"\"\n    data = lib.read_json_parameter(node, _INSTANCE_PARAMETER)\n    if not isinstance(data, dict) or data.get(\"id\") != AYON_INSTANCE_ID:\n        return None\n    return data\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lib.html","title":"lib","text":"<p>Shared Katana node, parameter, and metadata helpers.</p>"},{"location":"autoapi/client/ayon_katana/api/lib.html#client.ayon_katana.api.lib.ensure_group_parameter","title":"<code>ensure_group_parameter(node, parameter_path)</code>","text":"<p>Create and return a nested Katana group parameter.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <p>Katana node that owns the parameter.</p> required <code>parameter_path</code> <code>str</code> <p>Dot-separated group parameter path.</p> required <p>Returns:</p> Type Description <p>Katana group parameter.</p> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>def ensure_group_parameter(node, parameter_path: str):\n    \"\"\"Create and return a nested Katana group parameter.\n\n    Args:\n        node: Katana node that owns the parameter.\n        parameter_path: Dot-separated group parameter path.\n\n    Returns:\n        Katana group parameter.\n    \"\"\"\n    parameter = node.getParameters()\n    if not parameter_path:\n        return parameter\n    for part in parameter_path.split(\".\"):\n        child = parameter.getChild(part)\n        if child is None:\n            child = parameter.createChildGroup(part)\n        parameter = child\n    return parameter\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lib.html#client.ayon_katana.api.lib.ensure_number_parameter","title":"<code>ensure_number_parameter(node, parameter_path, default_value=0.0)</code>","text":"<p>Create and return a nested Katana number parameter.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <p>Katana node that owns the parameter.</p> required <code>parameter_path</code> <code>str</code> <p>Dot-separated parameter path.</p> required <code>default_value</code> <code>float</code> <p>Value used when creating the parameter.</p> <code>0.0</code> <p>Returns:</p> Type Description <p>Katana number parameter.</p> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>def ensure_number_parameter(node, parameter_path: str, default_value: float = 0.0):\n    \"\"\"Create and return a nested Katana number parameter.\n\n    Args:\n        node: Katana node that owns the parameter.\n        parameter_path: Dot-separated parameter path.\n        default_value: Value used when creating the parameter.\n\n    Returns:\n        Katana number parameter.\n    \"\"\"\n    parent_path, _, leaf_name = parameter_path.rpartition(\".\")\n    parent = ensure_group_parameter(node, parent_path)\n    parameter = parent.getChild(leaf_name)\n    if parameter is None:\n        parameter = parent.createChildNumber(leaf_name, default_value)\n    return parameter\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lib.html#client.ayon_katana.api.lib.ensure_string_parameter","title":"<code>ensure_string_parameter(node, parameter_path, default_value='')</code>","text":"<p>Create and return a nested Katana string parameter.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <p>Katana node that owns the parameter.</p> required <code>parameter_path</code> <code>str</code> <p>Dot-separated parameter path.</p> required <code>default_value</code> <code>str</code> <p>Value used when creating the parameter.</p> <code>''</code> <p>Returns:</p> Type Description <p>Katana string parameter.</p> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>def ensure_string_parameter(node, parameter_path: str, default_value: str = \"\"):\n    \"\"\"Create and return a nested Katana string parameter.\n\n    Args:\n        node: Katana node that owns the parameter.\n        parameter_path: Dot-separated parameter path.\n        default_value: Value used when creating the parameter.\n\n    Returns:\n        Katana string parameter.\n    \"\"\"\n    parent_path, _, leaf_name = parameter_path.rpartition(\".\")\n    parent = ensure_group_parameter(node, parent_path)\n    parameter = parent.getChild(leaf_name)\n    if parameter is None:\n        parameter = parent.createChildString(leaf_name, default_value)\n    return parameter\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lib.html#client.ayon_katana.api.lib.get_color_management_preferences","title":"<code>get_color_management_preferences()</code>","text":"<p>Return Katana color management preferences.</p> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>def get_color_management_preferences() -&gt; dict[str, str]:\n    \"\"\"Return Katana color management preferences.\"\"\"\n    from . import colorspace\n\n    config_path = colorspace.get_ocio_config_path()\n    scene_linear = colorspace.get_scene_linear_colorspace()\n    if not config_path or not scene_linear:\n        return {}\n    return {\"config\": config_path, \"colorspace\": scene_linear}\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lib.html#client.ayon_katana.api.lib.get_parameter_value","title":"<code>get_parameter_value(node, parameter_path, default=None)</code>","text":"<p>Return a nested parameter value or a default.</p> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>def get_parameter_value(node, parameter_path: str, default=None):\n    \"\"\"Return a nested parameter value or a default.\"\"\"\n    parameter = node.getParameter(parameter_path)\n    if parameter is None:\n        return default\n    try:\n        return parameter.getValue(_TIME)\n    except TypeError:\n        return default\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lib.html#client.ayon_katana.api.lib.get_string_parameter","title":"<code>get_string_parameter(node, parameter_path, default=None)</code>","text":"<p>Return a non-empty string parameter value or a default.</p> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>def get_string_parameter(\n    node,\n    parameter_path: str,\n    default: Optional[str] = None,\n) -&gt; Optional[str]:\n    \"\"\"Return a non-empty string parameter value or a default.\"\"\"\n    value = get_parameter_value(node, parameter_path, default)\n    return value if isinstance(value, str) and value else default\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lib.html#client.ayon_katana.api.lib.maintained_selection","title":"<code>maintained_selection()</code>","text":"<p>Maintain selection during context.</p> <p>This restores the selected nodes after the context exits and ignores nodes that no longer exist.</p> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>@contextmanager\ndef maintained_selection():\n    \"\"\"Maintain selection during context.\n\n    This restores the selected nodes after the context exits and ignores nodes\n    that no longer exist.\n    \"\"\"\n    from . import compat\n\n    selected_nodes = compat.get_selected_nodes()\n    try:\n        yield\n    finally:\n        live_nodes = []\n        for node in selected_nodes:\n            try:\n                if compat.get_node(node.getName()) is node:\n                    live_nodes.append(node)\n            except Exception:\n                continue\n        compat.set_selected_nodes(live_nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lib.html#client.ayon_katana.api.lib.read_json_parameter","title":"<code>read_json_parameter(node, parameter_path)</code>","text":"<p>Read dictionary metadata from a Katana string parameter.</p> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>def read_json_parameter(\n    node,\n    parameter_path: str,\n) -&gt; Optional[dict[str, Any]]:\n    \"\"\"Read dictionary metadata from a Katana string parameter.\"\"\"\n    raw_value = get_parameter_value(node, parameter_path)\n    if not raw_value:\n        return None\n    try:\n        data = json.loads(raw_value)\n    except (TypeError, json.JSONDecodeError):\n        return None\n    return data if isinstance(data, dict) else None\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lib.html#client.ayon_katana.api.lib.sanitize_node_name","title":"<code>sanitize_node_name(value, default_name='AYON_Node')</code>","text":"<p>Return a Katana-safe non-empty node name.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>Desired node name.</p> required <code>default_name</code> <code>str</code> <p>Name returned when sanitization removes all characters.</p> <code>'AYON_Node'</code> <p>Returns:</p> Type Description <code>str</code> <p>Sanitized node name.</p> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>def sanitize_node_name(value: str, default_name: str = \"AYON_Node\") -&gt; str:\n    \"\"\"Return a Katana-safe non-empty node name.\n\n    Args:\n        value: Desired node name.\n        default_name: Name returned when sanitization removes all characters.\n\n    Returns:\n        Sanitized node name.\n    \"\"\"\n    sanitized = re.sub(r\"[^A-Za-z0-9_]+\", \"_\", value).strip(\"_\")\n    return sanitized or default_name\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lib.html#client.ayon_katana.api.lib.set_number_parameter","title":"<code>set_number_parameter(node, parameter_path, value)</code>","text":"<p>Set a nested Katana number parameter.</p> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>def set_number_parameter(node, parameter_path: str, value: float) -&gt; None:\n    \"\"\"Set a nested Katana number parameter.\"\"\"\n    ensure_number_parameter(node, parameter_path, value).setValue(value, _TIME)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lib.html#client.ayon_katana.api.lib.set_string_parameter","title":"<code>set_string_parameter(node, parameter_path, value)</code>","text":"<p>Set a nested Katana string parameter.</p> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>def set_string_parameter(node, parameter_path: str, value: str) -&gt; None:\n    \"\"\"Set a nested Katana string parameter.\"\"\"\n    ensure_string_parameter(node, parameter_path, value).setValue(value, _TIME)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lib.html#client.ayon_katana.api.lib.write_json_parameter","title":"<code>write_json_parameter(node, parameter_path, data, skip_unchanged=True)</code>","text":"<p>Serialize dictionary metadata into a Katana string parameter.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <p>Katana node that owns the parameter.</p> required <code>parameter_path</code> <code>str</code> <p>Dot-separated parameter path.</p> required <code>data</code> <code>dict[str, Any]</code> <p>Dictionary to serialize.</p> required <code>skip_unchanged</code> <code>bool</code> <p>Avoid dirtying the scene when content is unchanged.</p> <code>True</code> Source code in <code>client/ayon_katana/api/lib.py</code> <pre><code>def write_json_parameter(\n    node,\n    parameter_path: str,\n    data: dict[str, Any],\n    skip_unchanged: bool = True,\n) -&gt; None:\n    \"\"\"Serialize dictionary metadata into a Katana string parameter.\n\n    Args:\n        node: Katana node that owns the parameter.\n        parameter_path: Dot-separated parameter path.\n        data: Dictionary to serialize.\n        skip_unchanged: Avoid dirtying the scene when content is unchanged.\n    \"\"\"\n    serialized_data = json.dumps(data, sort_keys=True)\n    if skip_unchanged:\n        current_value = get_parameter_value(node, parameter_path)\n        if current_value == serialized_data:\n            return\n    ensure_string_parameter(node, parameter_path).setValue(serialized_data, _TIME)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lifecycle.html","title":"lifecycle","text":"<p>Host lifecycle operations for AYON Katana.</p>"},{"location":"autoapi/client/ayon_katana/api/lifecycle.html#client.ayon_katana.api.lifecycle.LifecycleController","title":"<code>LifecycleController</code>","text":"<p>Apply AYON lifecycle behavior from Katana callbacks.</p> Source code in <code>client/ayon_katana/api/lifecycle.py</code> <pre><code>class LifecycleController:\n    \"\"\"Apply AYON lifecycle behavior from Katana callbacks.\"\"\"\n\n    def __init__(self, host):\n        self._host = host\n        self._outdated_content_pending = False\n        self._pending_workfile_save = None\n\n    def install(self):\n        \"\"\"Install UI integration when Katana already has a main window.\"\"\"\n        if _get_main_window() is not None:\n            self._defer_menu_install()\n\n    def uninstall(self):\n        \"\"\"Remove the AYON menu owned by this host.\"\"\"\n        self._outdated_content_pending = False\n        self._pending_workfile_save = None\n        _uninstall_menu()\n\n    def on_startup_complete(self):\n        \"\"\"Open the requested scene, then emit init after native startup.\"\"\"\n        had_requested_workfile = startup.has_requested_workfile()\n        opened_workfile = startup.open_requested_workfile()\n        emit_event(\"init\")\n        self._defer_menu_install()\n        if not had_requested_workfile and opened_workfile is None:\n            self._trigger_workfile_builder_on_app_launch()\n        self._open_workfiles_on_startup()\n        if self._outdated_content_pending:\n            self._outdated_content_pending = False\n            self._show_outdated_content_popup()\n\n    def on_new(self):\n        \"\"\"Initialize embedded context and project timing for a new scene.\"\"\"\n        emit_event(\"new\")\n        context.apply_context_settings(self._host)\n\n    def on_new_scene_loaded(self):\n        \"\"\"Evaluate the Core new-file trigger after Katana creates a scene.\"\"\"\n        self._trigger_workfile_builder_on_new_file()\n\n    def _has_complete_context(self) -&gt; bool:\n        \"\"\"Return whether Workfile Builder profile matching has enough context.\"\"\"\n        current_context = context.get_current_context_data(self._host)\n        return all(\n            current_context.get(key)\n            for key in (\"project_name\", \"folder_path\", \"task_name\")\n        )\n\n    def _trigger_workfile_builder_on_app_launch(self) -&gt; None:\n        \"\"\"Build a launch template only for an unselected blank workfile.\"\"\"\n        if not self._has_complete_context():\n            log.debug(\n                \"Skipping Workfile Builder launch trigger without a complete \"\n                \"AYON context.\"\n            )\n            return\n        workfile_template_builder.trigger_on_app_launch()\n\n    def _trigger_workfile_builder_on_new_file(self) -&gt; None:\n        \"\"\"Build a new-scene template only when its separate setting allows it.\"\"\"\n        if not self._has_complete_context():\n            log.debug(\n                \"Skipping Workfile Builder new-file trigger without a complete \"\n                \"AYON context.\"\n            )\n            return\n        workfile_template_builder.trigger_on_new_file()\n\n    def on_open(self):\n        \"\"\"Handle an existing Katana project after it opens.\"\"\"\n        emit_event(\"open\")\n        current_context = context.get_current_context_data(self._host)\n        if not all(\n            current_context.get(key)\n            for key in (\"project_name\", \"folder_path\", \"task_name\")\n        ):\n            log.debug(\"Skipping opened-scene checks without an AYON context.\")\n            return\n        context.log_fps_limitation()\n        self._report_outdated_containers()\n\n    def before_save(self):\n        \"\"\"Embed the current AYON context before Katana saves.\"\"\"\n        self._pending_workfile_save = None\n        context.update_embedded_context(self._host)\n        emit_event(\"before.save\")\n\n    def on_save(self):\n        \"\"\"Emit the AYON save event after Katana saves.\"\"\"\n        self._pending_workfile_save = None\n        emit_event(\"save\")\n\n    def on_workfile_save_before(self, event):\n        \"\"\"Remember AYON Workfiles Save As context until ``taskChanged``.\n\n        Args:\n            event: AYON event containing the target workfile context.\n        \"\"\"\n        event_data = event.data\n        self._pending_workfile_save = {\n            \"source\": context.get_current_context_data(self._host),\n            \"target\": dict(event_data),\n        }\n\n    def on_task_changed(self, event):\n        \"\"\"Handle AYON task changes and cross-context Save As updates.\"\"\"\n        event_data = event.data\n\n        pending_save = self._pending_workfile_save\n        self._pending_workfile_save = None\n        if pending_save is not None:\n            target_context = context.get_current_context_data(\n                self._host,\n                event_data or pending_save[\"target\"],\n            )\n            source_context = pending_save[\"source\"]\n            context.update_embedded_context(self._host, target_context)\n            changed_keys = (\"project_name\", \"folder_path\", \"task_name\")\n            changed = any(\n                source_context.get(key) != target_context.get(key)\n                for key in changed_keys\n            )\n            if not changed:\n                return\n\n            main_window = _get_main_window()\n            if main_window is None:\n                log.info(\"Skipping cross-context Save As prompt in headless Katana.\")\n                return\n\n            options = _prompt_context_change(\n                main_window,\n                source_context,\n                target_context,\n            )\n            if options is None:\n                log.info(\"Cross-context Save As scene updates were cancelled.\")\n                return\n            if options.get(\"frame_range\"):\n                context.apply_current_frame_range()\n            if options.get(\"instances\"):\n                folder_path = target_context.get(\"folder_path\")\n                task_name = target_context.get(\"task_name\")\n                if folder_path and task_name:\n                    updated = context.update_instance_context_metadata(\n                        folder_path,\n                        task_name,\n                    )\n                    log.info(\n                        \"Updated %s Katana publish instances for Save As.\",\n                        updated,\n                    )\n            return\n\n        context.apply_context_settings(self._host, event_data)\n\n    def _defer_menu_install(self):\n        if _get_main_window() is None:\n            return\n\n        def _install_if_active():\n            if not self._host.is_installed:\n                return\n            try:\n                _install_menu()\n            except Exception:\n                log.exception(\"Could not install the AYON Katana menu.\")\n\n        _defer_call(_install_if_active, delay=2000)\n\n    def _open_workfiles_on_startup(self):\n        \"\"\"Open Workfiles only when the maintained Core profile enables it.\"\"\"\n        current_context = context.get_current_context_data(self._host)\n        project_name = current_context.get(\"project_name\")\n        task_name = current_context.get(\"task_name\")\n        if not project_name or not task_name:\n            log.debug(\"Skipping Workfiles startup without a complete AYON context.\")\n            return\n\n        try:\n            task_type = context.get_current_task_type()\n            should_open = should_open_workfiles_tool_on_launch(\n                project_name,\n                self._host.name,\n                task_name,\n                task_type,\n            )\n        except Exception:\n            log.exception(\"Could not evaluate the AYON Workfiles startup profile.\")\n            return\n\n        if should_open:\n            self._run_in_ui(self._show_workfiles)\n\n    def _report_outdated_containers(self):\n        \"\"\"Warn about outdated content and offer Scene Inventory in the UI.\"\"\"\n        try:\n            has_outdated_containers = any_outdated_containers()\n        except Exception:\n            log.exception(\"Could not check Katana containers for outdated content.\")\n            return\n\n        if not has_outdated_containers:\n            return\n\n        log.warning(\"Katana scene has outdated content.\")\n        if not self._show_outdated_content_popup():\n            self._outdated_content_pending = True\n\n    def _show_outdated_content_popup(self):\n        \"\"\"Defer the outdated-content popup until Katana has a main window.\"\"\"\n        return self._run_in_ui(self._show_outdated_popup)\n\n    def _run_in_ui(self, callback):\n        \"\"\"Defer a UI action, or leave headless sessions untouched.\"\"\"\n        main_window = _get_main_window()\n        if main_window is None:\n            return False\n\n        def _invoke():\n            if self._host.is_installed:\n                callback(main_window)\n\n        _defer_call(_invoke)\n        return True\n\n    @staticmethod\n    def _show_workfiles(main_window):\n        from ayon_core.tools.utils import host_tools\n\n        host_tools.show_workfiles(parent=main_window)\n\n    @staticmethod\n    def _show_outdated_popup(main_window):\n        from ayon_core.tools.utils import SimplePopup, host_tools\n\n        popup = SimplePopup(parent=main_window)\n        popup.setWindowTitle(\"Katana scene has outdated content\")\n        popup.set_message(\"There are outdated containers in your Katana scene.\")\n        popup.set_button_text(\"Show Scene Inventory\")\n        popup.on_clicked.connect(\n            lambda: host_tools.show_scene_inventory(parent=main_window)\n        )\n        popup.show()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lifecycle.html#client.ayon_katana.api.lifecycle.LifecycleController.before_save","title":"<code>before_save()</code>","text":"<p>Embed the current AYON context before Katana saves.</p> Source code in <code>client/ayon_katana/api/lifecycle.py</code> <pre><code>def before_save(self):\n    \"\"\"Embed the current AYON context before Katana saves.\"\"\"\n    self._pending_workfile_save = None\n    context.update_embedded_context(self._host)\n    emit_event(\"before.save\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lifecycle.html#client.ayon_katana.api.lifecycle.LifecycleController.install","title":"<code>install()</code>","text":"<p>Install UI integration when Katana already has a main window.</p> Source code in <code>client/ayon_katana/api/lifecycle.py</code> <pre><code>def install(self):\n    \"\"\"Install UI integration when Katana already has a main window.\"\"\"\n    if _get_main_window() is not None:\n        self._defer_menu_install()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lifecycle.html#client.ayon_katana.api.lifecycle.LifecycleController.on_new","title":"<code>on_new()</code>","text":"<p>Initialize embedded context and project timing for a new scene.</p> Source code in <code>client/ayon_katana/api/lifecycle.py</code> <pre><code>def on_new(self):\n    \"\"\"Initialize embedded context and project timing for a new scene.\"\"\"\n    emit_event(\"new\")\n    context.apply_context_settings(self._host)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lifecycle.html#client.ayon_katana.api.lifecycle.LifecycleController.on_new_scene_loaded","title":"<code>on_new_scene_loaded()</code>","text":"<p>Evaluate the Core new-file trigger after Katana creates a scene.</p> Source code in <code>client/ayon_katana/api/lifecycle.py</code> <pre><code>def on_new_scene_loaded(self):\n    \"\"\"Evaluate the Core new-file trigger after Katana creates a scene.\"\"\"\n    self._trigger_workfile_builder_on_new_file()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lifecycle.html#client.ayon_katana.api.lifecycle.LifecycleController.on_open","title":"<code>on_open()</code>","text":"<p>Handle an existing Katana project after it opens.</p> Source code in <code>client/ayon_katana/api/lifecycle.py</code> <pre><code>def on_open(self):\n    \"\"\"Handle an existing Katana project after it opens.\"\"\"\n    emit_event(\"open\")\n    current_context = context.get_current_context_data(self._host)\n    if not all(\n        current_context.get(key)\n        for key in (\"project_name\", \"folder_path\", \"task_name\")\n    ):\n        log.debug(\"Skipping opened-scene checks without an AYON context.\")\n        return\n    context.log_fps_limitation()\n    self._report_outdated_containers()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lifecycle.html#client.ayon_katana.api.lifecycle.LifecycleController.on_save","title":"<code>on_save()</code>","text":"<p>Emit the AYON save event after Katana saves.</p> Source code in <code>client/ayon_katana/api/lifecycle.py</code> <pre><code>def on_save(self):\n    \"\"\"Emit the AYON save event after Katana saves.\"\"\"\n    self._pending_workfile_save = None\n    emit_event(\"save\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lifecycle.html#client.ayon_katana.api.lifecycle.LifecycleController.on_startup_complete","title":"<code>on_startup_complete()</code>","text":"<p>Open the requested scene, then emit init after native startup.</p> Source code in <code>client/ayon_katana/api/lifecycle.py</code> <pre><code>def on_startup_complete(self):\n    \"\"\"Open the requested scene, then emit init after native startup.\"\"\"\n    had_requested_workfile = startup.has_requested_workfile()\n    opened_workfile = startup.open_requested_workfile()\n    emit_event(\"init\")\n    self._defer_menu_install()\n    if not had_requested_workfile and opened_workfile is None:\n        self._trigger_workfile_builder_on_app_launch()\n    self._open_workfiles_on_startup()\n    if self._outdated_content_pending:\n        self._outdated_content_pending = False\n        self._show_outdated_content_popup()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lifecycle.html#client.ayon_katana.api.lifecycle.LifecycleController.on_task_changed","title":"<code>on_task_changed(event)</code>","text":"<p>Handle AYON task changes and cross-context Save As updates.</p> Source code in <code>client/ayon_katana/api/lifecycle.py</code> <pre><code>def on_task_changed(self, event):\n    \"\"\"Handle AYON task changes and cross-context Save As updates.\"\"\"\n    event_data = event.data\n\n    pending_save = self._pending_workfile_save\n    self._pending_workfile_save = None\n    if pending_save is not None:\n        target_context = context.get_current_context_data(\n            self._host,\n            event_data or pending_save[\"target\"],\n        )\n        source_context = pending_save[\"source\"]\n        context.update_embedded_context(self._host, target_context)\n        changed_keys = (\"project_name\", \"folder_path\", \"task_name\")\n        changed = any(\n            source_context.get(key) != target_context.get(key)\n            for key in changed_keys\n        )\n        if not changed:\n            return\n\n        main_window = _get_main_window()\n        if main_window is None:\n            log.info(\"Skipping cross-context Save As prompt in headless Katana.\")\n            return\n\n        options = _prompt_context_change(\n            main_window,\n            source_context,\n            target_context,\n        )\n        if options is None:\n            log.info(\"Cross-context Save As scene updates were cancelled.\")\n            return\n        if options.get(\"frame_range\"):\n            context.apply_current_frame_range()\n        if options.get(\"instances\"):\n            folder_path = target_context.get(\"folder_path\")\n            task_name = target_context.get(\"task_name\")\n            if folder_path and task_name:\n                updated = context.update_instance_context_metadata(\n                    folder_path,\n                    task_name,\n                )\n                log.info(\n                    \"Updated %s Katana publish instances for Save As.\",\n                    updated,\n                )\n        return\n\n    context.apply_context_settings(self._host, event_data)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lifecycle.html#client.ayon_katana.api.lifecycle.LifecycleController.on_workfile_save_before","title":"<code>on_workfile_save_before(event)</code>","text":"<p>Remember AYON Workfiles Save As context until <code>taskChanged</code>.</p> <p>Parameters:</p> Name Type Description Default <code>event</code> <p>AYON event containing the target workfile context.</p> required Source code in <code>client/ayon_katana/api/lifecycle.py</code> <pre><code>def on_workfile_save_before(self, event):\n    \"\"\"Remember AYON Workfiles Save As context until ``taskChanged``.\n\n    Args:\n        event: AYON event containing the target workfile context.\n    \"\"\"\n    event_data = event.data\n    self._pending_workfile_save = {\n        \"source\": context.get_current_context_data(self._host),\n        \"target\": dict(event_data),\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/lifecycle.html#client.ayon_katana.api.lifecycle.LifecycleController.uninstall","title":"<code>uninstall()</code>","text":"<p>Remove the AYON menu owned by this host.</p> Source code in <code>client/ayon_katana/api/lifecycle.py</code> <pre><code>def uninstall(self):\n    \"\"\"Remove the AYON menu owned by this host.\"\"\"\n    self._outdated_content_pending = False\n    self._pending_workfile_save = None\n    _uninstall_menu()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/menu.html","title":"menu","text":"<p>AYON menu integration for Katana's graphical interface.</p>"},{"location":"autoapi/client/ayon_katana/api/menu.html#client.ayon_katana.api.menu.get_main_window","title":"<code>get_main_window()</code>","text":"<p>Return Katana's main window when running in UI mode.</p> Source code in <code>client/ayon_katana/api/menu.py</code> <pre><code>def get_main_window():\n    \"\"\"Return Katana's main window when running in UI mode.\"\"\"\n    return UI4.App.MainWindow.GetMainWindow()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/menu.html#client.ayon_katana.api.menu.get_menu_bar","title":"<code>get_menu_bar(main_window=None)</code>","text":"<p>Return Katana's existing native menu bar.</p> <p>Katana does not register its visible menu bar through <code>QMainWindow.setMenuBar</code>. Calling <code>QMainWindow.menuBar</code> therefore creates a second menu bar above Katana's own menu row.</p> Source code in <code>client/ayon_katana/api/menu.py</code> <pre><code>def get_menu_bar(main_window=None):\n    \"\"\"Return Katana's existing native menu bar.\n\n    Katana does not register its visible menu bar through\n    ``QMainWindow.setMenuBar``. Calling ``QMainWindow.menuBar`` therefore\n    creates a second menu bar above Katana's own menu row.\n    \"\"\"\n    main_window = main_window or get_main_window()\n    if main_window is None:\n        return None\n\n    required_actions = {\"File\", \"Edit\", \"Help\"}\n    for menu_bar in main_window.findChildren(QtWidgets.QMenuBar):\n        action_labels = {\n            _normalized_action_text(action) for action in menu_bar.actions()\n        }\n        if required_actions.issubset(action_labels):\n            return menu_bar\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/menu.html#client.ayon_katana.api.menu.install_menu","title":"<code>install_menu()</code>","text":"<p>Install the AYON menu in Katana.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when the menu is available, otherwise <code>False</code>.</p> Source code in <code>client/ayon_katana/api/menu.py</code> <pre><code>def install_menu() -&gt; bool:\n    \"\"\"Install the AYON menu in Katana.\n\n    Returns:\n        ``True`` when the menu is available, otherwise ``False``.\n    \"\"\"\n    global _main_window, _menu_bar, _menu\n\n    main_window = get_main_window()\n    if main_window is None:\n        log.debug(\"Katana main window is unavailable; skipping AYON menu.\")\n        return False\n\n    menu_bar = get_menu_bar(main_window)\n    if menu_bar is None:\n        log.debug(\"Katana native menu bar is unavailable; skipping AYON menu.\")\n        return False\n\n    _main_window = main_window\n    _menu_bar = menu_bar\n    existing_menu = _find_existing_menu(menu_bar)\n    if existing_menu is not None:\n        _menu = existing_menu\n        return True\n\n    menu = QtWidgets.QMenu(_MENU_TITLE, menu_bar)\n    help_action = _find_menu_action(menu_bar, \"Help\")\n    if help_action is not None:\n        menu_bar.insertMenu(help_action, menu)\n    else:\n        menu_bar.addMenu(menu)\n    _add_action(\n        menu,\n        \"Create...\",\n        lambda: host_tools.show_publisher(parent=main_window, tab=\"create\"),\n    )\n    _add_action(\n        menu,\n        \"Load...\",\n        lambda: host_tools.show_loader(parent=main_window, use_context=True),\n    )\n    _add_action(\n        menu,\n        \"Publish...\",\n        lambda: host_tools.show_publisher(parent=main_window, tab=\"publish\"),\n    )\n    _add_action(\n        menu,\n        \"Manage...\",\n        lambda: host_tools.show_scene_inventory(parent=main_window),\n    )\n\n    menu.addSeparator()\n    _add_action(\n        menu,\n        \"Workfiles...\",\n        lambda: host_tools.show_workfiles(parent=main_window),\n    )\n    version_up_action = _add_action(\n        menu,\n        \"Version Up Workfile\",\n        _version_up_workfile,\n    )\n    version_up_action.setEnabled(_version_up_enabled())\n    _add_action(menu, \"Set Frame Range\", _set_frame_range)\n    _add_action(menu, \"Clear USD Resolver Cache\", _clear_usd_resolver_cache)\n\n    menu.addSeparator()\n    _add_action(\n        menu,\n        \"Experimental Tools...\",\n        lambda: host_tools.show_experimental_tools_dialog(parent=main_window),\n    )\n\n    from . import workfile_template_builder\n\n    builder_menu = menu.addMenu(\"Workfile Builder\")\n    _add_action(\n        builder_menu,\n        \"Build Template\",\n        workfile_template_builder.build_workfile_template,\n    )\n    _add_action(\n        builder_menu,\n        \"Update Template\",\n        workfile_template_builder.update_workfile_template,\n    )\n    builder_menu.addSeparator()\n    _add_action(\n        builder_menu,\n        \"Open Template\",\n        workfile_template_builder.open_workfile_template,\n    )\n    _add_action(\n        builder_menu,\n        \"Create Placeholder...\",\n        workfile_template_builder.create_placeholder,\n    )\n    _add_action(\n        builder_menu,\n        \"Update Selected Placeholder...\",\n        workfile_template_builder.update_placeholder,\n    )\n\n    _menu = menu\n    log.info(\"Installed AYON menu in Katana.\")\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/menu.html#client.ayon_katana.api.menu.uninstall_menu","title":"<code>uninstall_menu()</code>","text":"<p>Remove the AYON menu from Katana when present.</p> Source code in <code>client/ayon_katana/api/menu.py</code> <pre><code>def uninstall_menu() -&gt; None:\n    \"\"\"Remove the AYON menu from Katana when present.\"\"\"\n    global _main_window, _menu_bar, _menu\n\n    main_window = _main_window or get_main_window()\n    menu_bar = _menu_bar or get_menu_bar(main_window)\n    menu = _menu or (_find_existing_menu(menu_bar) if menu_bar is not None else None)\n    if menu is None:\n        _main_window = None\n        _menu_bar = None\n        return\n\n    try:\n        if menu_bar is not None:\n            menu_bar.removeAction(menu.menuAction())\n        menu.deleteLater()\n    except RuntimeError:\n        log.debug(\"Katana already deleted the AYON menu during UI teardown.\")\n    _menu = None\n    _menu_bar = None\n    _main_window = None\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html","title":"pipeline","text":"<p>Pipeline tools for AYON Katana integration.</p>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost","title":"<code>KatanaHost</code>","text":"<p>               Bases: <code>HostBase</code>, <code>IWorkfileHost</code>, <code>ILoadHost</code>, <code>IPublishHost</code></p> <p>Implement AYON host interfaces for Katana.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>class KatanaHost(HostBase, IWorkfileHost, ILoadHost, IPublishHost):\n    \"\"\"Implement AYON host interfaces for Katana.\"\"\"\n\n    name = \"katana\"\n\n    def __init__(self):\n        super().__init__()\n        self._lifecycle = LifecycleController(self)\n        self._callback_manager = CallbackManager(self._lifecycle)\n        self._event_callbacks: list[object] = []\n        self._deadline_plugins_registered = False\n        self._has_been_setup = False\n\n    @property\n    def is_installed(self) -&gt; bool:\n        \"\"\"Return whether the Katana host services are currently installed.\"\"\"\n        return self._has_been_setup\n\n    def get_app_information(self):\n        \"\"\"Return the running Katana application's name and version.\"\"\"\n        import Katana\n        from ayon_core.host import ApplicationInformation\n\n        katana_version = getattr(Katana, \"__version__\", None)\n        if not katana_version:\n            katana_root = os.environ.get(\"KATANA_ROOT\", \"\").rstrip(\"/\\\\\")\n            katana_root_name = katana_root.replace(\"\\\\\", \"/\").rsplit(\"/\", 1)[-1]\n            if katana_root_name.startswith(\"Katana\"):\n                katana_version = katana_root_name.removeprefix(\"Katana\")\n\n        return ApplicationInformation(\n            app_name=\"Katana\",\n            app_version=katana_version,\n        )\n\n    def maintained_selection(self):\n        \"\"\"Return a context manager that restores the Katana selection.\"\"\"\n        return lib.maintained_selection()\n\n    def install(self):\n        \"\"\"Register Katana plugins, callbacks, and host services once.\"\"\"\n        if self._has_been_setup:\n            return\n\n        pyblish.api.register_host(self.name)\n        pyblish.api.register_plugin_path(PUBLISH_PATH)\n        if importlib.util.find_spec(\"ayon_deadline\") is not None:\n            pyblish.api.register_plugin_path(DEADLINE_PUBLISH_PATH)\n            self._deadline_plugins_registered = True\n        register_loader_plugin_path(LOAD_PATH)\n        register_creator_plugin_path(CREATE_PATH)\n        register_inventory_action_path(INVENTORY_PATH)\n        register_workfile_build_plugin_path(WORKFILE_BUILD_PATH)\n        self._lifecycle.install()\n        self._callback_manager.install()\n        self._event_callbacks.append(\n            register_event_callback(\"taskChanged\", self._lifecycle.on_task_changed)\n        )\n        self._event_callbacks.append(\n            register_event_callback(\n                \"workfile.save.before\",\n                self._lifecycle.on_workfile_save_before,\n            )\n        )\n        self._has_been_setup = True\n        log.info(\"Installed AYON Katana host.\")\n\n    def uninstall(self):\n        \"\"\"Remove plugins, callbacks, and host services registered by AYON.\"\"\"\n        if not self._has_been_setup:\n            return\n\n        for event_callback in self._event_callbacks:\n            event_callback.deregister()\n        self._event_callbacks.clear()\n        self._callback_manager.uninstall()\n        self._lifecycle.uninstall()\n        deregister_workfile_build_plugin_path(WORKFILE_BUILD_PATH)\n        deregister_inventory_action_path(INVENTORY_PATH)\n        deregister_creator_plugin_path(CREATE_PATH)\n        deregister_loader_plugin_path(LOAD_PATH)\n        if self._deadline_plugins_registered:\n            pyblish.api.deregister_plugin_path(DEADLINE_PUBLISH_PATH)\n            self._deadline_plugins_registered = False\n        pyblish.api.deregister_plugin_path(PUBLISH_PATH)\n        pyblish.api.deregister_host(self.name)\n        self._has_been_setup = False\n        log.info(\"Uninstalled AYON Katana host.\")\n\n    def workfile_has_unsaved_changes(self):\n        \"\"\"Return whether the current Katana project has unsaved changes.\"\"\"\n        return workio.workfile_has_unsaved_changes()\n\n    def get_workfile_extensions(self):\n        \"\"\"Return workfile extensions supported by Katana.\"\"\"\n        return workio.get_workfile_extensions()\n\n    def save_workfile(self, dst_path=None):\n        \"\"\"Save the current Katana project, optionally to a new path.\"\"\"\n        return workio.save_workfile(dst_path)\n\n    def open_workfile(self, filepath):\n        \"\"\"Open a Katana project from a filesystem path.\"\"\"\n        return workio.open_workfile(filepath)\n\n    def get_current_workfile(self):\n        \"\"\"Return the current Katana project path.\"\"\"\n        return workio.get_current_workfile()\n\n    def get_containers(self):\n        \"\"\"Return containers found in the current Katana project.\"\"\"\n        return ls()\n\n    def get_context_data(self):\n        \"\"\"Return AYON context data embedded in the current project.\"\"\"\n        return context_lib.get_context_data()\n\n    def update_context_data(self, data, changes=None):\n        \"\"\"Persist AYON context data in the current Katana project.\"\"\"\n        context_lib.update_context_data(data, changes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.is_installed","title":"<code>is_installed</code>  <code>property</code>","text":"<p>Return whether the Katana host services are currently installed.</p>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.get_app_information","title":"<code>get_app_information()</code>","text":"<p>Return the running Katana application's name and version.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def get_app_information(self):\n    \"\"\"Return the running Katana application's name and version.\"\"\"\n    import Katana\n    from ayon_core.host import ApplicationInformation\n\n    katana_version = getattr(Katana, \"__version__\", None)\n    if not katana_version:\n        katana_root = os.environ.get(\"KATANA_ROOT\", \"\").rstrip(\"/\\\\\")\n        katana_root_name = katana_root.replace(\"\\\\\", \"/\").rsplit(\"/\", 1)[-1]\n        if katana_root_name.startswith(\"Katana\"):\n            katana_version = katana_root_name.removeprefix(\"Katana\")\n\n    return ApplicationInformation(\n        app_name=\"Katana\",\n        app_version=katana_version,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.get_containers","title":"<code>get_containers()</code>","text":"<p>Return containers found in the current Katana project.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def get_containers(self):\n    \"\"\"Return containers found in the current Katana project.\"\"\"\n    return ls()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.get_context_data","title":"<code>get_context_data()</code>","text":"<p>Return AYON context data embedded in the current project.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def get_context_data(self):\n    \"\"\"Return AYON context data embedded in the current project.\"\"\"\n    return context_lib.get_context_data()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.get_current_workfile","title":"<code>get_current_workfile()</code>","text":"<p>Return the current Katana project path.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def get_current_workfile(self):\n    \"\"\"Return the current Katana project path.\"\"\"\n    return workio.get_current_workfile()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Return workfile extensions supported by Katana.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def get_workfile_extensions(self):\n    \"\"\"Return workfile extensions supported by Katana.\"\"\"\n    return workio.get_workfile_extensions()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.install","title":"<code>install()</code>","text":"<p>Register Katana plugins, callbacks, and host services once.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def install(self):\n    \"\"\"Register Katana plugins, callbacks, and host services once.\"\"\"\n    if self._has_been_setup:\n        return\n\n    pyblish.api.register_host(self.name)\n    pyblish.api.register_plugin_path(PUBLISH_PATH)\n    if importlib.util.find_spec(\"ayon_deadline\") is not None:\n        pyblish.api.register_plugin_path(DEADLINE_PUBLISH_PATH)\n        self._deadline_plugins_registered = True\n    register_loader_plugin_path(LOAD_PATH)\n    register_creator_plugin_path(CREATE_PATH)\n    register_inventory_action_path(INVENTORY_PATH)\n    register_workfile_build_plugin_path(WORKFILE_BUILD_PATH)\n    self._lifecycle.install()\n    self._callback_manager.install()\n    self._event_callbacks.append(\n        register_event_callback(\"taskChanged\", self._lifecycle.on_task_changed)\n    )\n    self._event_callbacks.append(\n        register_event_callback(\n            \"workfile.save.before\",\n            self._lifecycle.on_workfile_save_before,\n        )\n    )\n    self._has_been_setup = True\n    log.info(\"Installed AYON Katana host.\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.maintained_selection","title":"<code>maintained_selection()</code>","text":"<p>Return a context manager that restores the Katana selection.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def maintained_selection(self):\n    \"\"\"Return a context manager that restores the Katana selection.\"\"\"\n    return lib.maintained_selection()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.open_workfile","title":"<code>open_workfile(filepath)</code>","text":"<p>Open a Katana project from a filesystem path.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def open_workfile(self, filepath):\n    \"\"\"Open a Katana project from a filesystem path.\"\"\"\n    return workio.open_workfile(filepath)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.save_workfile","title":"<code>save_workfile(dst_path=None)</code>","text":"<p>Save the current Katana project, optionally to a new path.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def save_workfile(self, dst_path=None):\n    \"\"\"Save the current Katana project, optionally to a new path.\"\"\"\n    return workio.save_workfile(dst_path)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.uninstall","title":"<code>uninstall()</code>","text":"<p>Remove plugins, callbacks, and host services registered by AYON.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def uninstall(self):\n    \"\"\"Remove plugins, callbacks, and host services registered by AYON.\"\"\"\n    if not self._has_been_setup:\n        return\n\n    for event_callback in self._event_callbacks:\n        event_callback.deregister()\n    self._event_callbacks.clear()\n    self._callback_manager.uninstall()\n    self._lifecycle.uninstall()\n    deregister_workfile_build_plugin_path(WORKFILE_BUILD_PATH)\n    deregister_inventory_action_path(INVENTORY_PATH)\n    deregister_creator_plugin_path(CREATE_PATH)\n    deregister_loader_plugin_path(LOAD_PATH)\n    if self._deadline_plugins_registered:\n        pyblish.api.deregister_plugin_path(DEADLINE_PUBLISH_PATH)\n        self._deadline_plugins_registered = False\n    pyblish.api.deregister_plugin_path(PUBLISH_PATH)\n    pyblish.api.deregister_host(self.name)\n    self._has_been_setup = False\n    log.info(\"Uninstalled AYON Katana host.\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.update_context_data","title":"<code>update_context_data(data, changes=None)</code>","text":"<p>Persist AYON context data in the current Katana project.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def update_context_data(self, data, changes=None):\n    \"\"\"Persist AYON context data in the current Katana project.\"\"\"\n    context_lib.update_context_data(data, changes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.KatanaHost.workfile_has_unsaved_changes","title":"<code>workfile_has_unsaved_changes()</code>","text":"<p>Return whether the current Katana project has unsaved changes.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def workfile_has_unsaved_changes(self):\n    \"\"\"Return whether the current Katana project has unsaved changes.\"\"\"\n    return workio.workfile_has_unsaved_changes()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.containerise","title":"<code>containerise(name, namespace, context, loader=None, parent_node=None)</code>","text":"<p>Bundle a Katana graph and imprint it with metadata.</p> <p>Containerisation enables tracking of version, author and origin for loaded products.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <p>Name of the loaded product.</p> required <code>namespace</code> <p>Namespace under which to host the container.</p> required <code>context</code> <p>Loaded product context information.</p> required <code>loader</code> <p>Name of the loader used to produce the container.</p> <code>None</code> <code>parent_node</code> <p>Optional Katana parent node.</p> <code>None</code> <p>Returns:</p> Type Description <p>Created Katana container Group.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def containerise(name, namespace, context, loader=None, parent_node=None):\n    \"\"\"Bundle a Katana graph and imprint it with metadata.\n\n    Containerisation enables tracking of version, author and origin for loaded\n    products.\n\n    Args:\n        name: Name of the loaded product.\n        namespace: Namespace under which to host the container.\n        context: Loaded product context information.\n        loader: Name of the loader used to produce the container.\n        parent_node: Optional Katana parent node.\n\n    Returns:\n        Created Katana container Group.\n    \"\"\"\n    return containers.containerise(\n        name,\n        namespace,\n        context,\n        loader=loader,\n        parent_node=parent_node,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/pipeline.html#client.ayon_katana.api.pipeline.ls","title":"<code>ls()</code>","text":"<p>Yield AYON containers in the current Katana project.</p> Source code in <code>client/ayon_katana/api/pipeline.py</code> <pre><code>def ls():\n    \"\"\"Yield AYON containers in the current Katana project.\"\"\"\n    return containers.ls()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html","title":"plugin","text":"<p>Katana specific AYON/Pyblish plugin definitions.</p>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaContextPlugin","title":"<code>KatanaContextPlugin</code>","text":"<p>               Bases: <code>ContextPlugin</code></p> <p>Base class for Katana context publish plugins.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>class KatanaContextPlugin(pyblish.api.ContextPlugin):\n    \"\"\"Base class for Katana context publish plugins.\"\"\"\n\n    hosts = [\"katana\"]\n    settings_category = SETTINGS_CATEGORY\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaCreator","title":"<code>KatanaCreator</code>","text":"<p>               Bases: <code>Creator</code>, <code>KatanaCreatorBase</code></p> <p>Base class for most of the Katana creator plugins.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>class KatanaCreator(Creator, KatanaCreatorBase):\n    \"\"\"Base class for most of the Katana creator plugins.\"\"\"\n\n    settings_category = SETTINGS_CATEGORY\n    node_type = \"Group\"\n\n    def check_existing_product(self, product_name):\n        \"\"\"Validate that the product name is unique in the scene.\n\n        Args:\n            product_name: Product name to validate.\n\n        Raises:\n            CreatorError: A node-backed instance already uses the name.\n        \"\"\"\n        for _node, instance_data in instances.iter_instances():\n            if instance_data.get(\"productName\") == product_name:\n                raise CreatorError(\n                    f\"A Katana publish instance for {product_name!r} already exists.\"\n                )\n\n    def create(self, product_name, instance_data, pre_create_data):\n        \"\"\"Create and persist a node-backed publish instance.\"\"\"\n        self.check_existing_product(product_name)\n\n        product_type = instance_data.get(\"productType\") or self.product_base_type\n        node_type = instance_data.pop(\"node_type\", None)\n        parent_node = instance_data.pop(\"parent_node\", None)\n        instance_node = None\n        created_instance = None\n        try:\n            instance_node = self.create_instance_node(\n                node_type or self.node_type,\n                product_name,\n                parent=parent_node,\n            )\n            created_instance = CreatedInstance(\n                product_base_type=self.product_base_type,\n                product_type=product_type,\n                product_name=product_name,\n                data=instance_data,\n                creator=self,\n            )\n            created_instance[\"instance_node\"] = instance_node.getName()\n            self.apply_staging_dir(created_instance)\n            created_instance.transient_data[\"node\"] = instance_node\n            self._add_instance_to_context(created_instance)\n            instances.imprint(instance_node, created_instance.data_to_store())\n            return created_instance\n        except Exception:\n            if created_instance is not None:\n                with suppress(Exception):\n                    self._remove_instance_from_context(created_instance)\n            if instance_node is not None:\n                with suppress(Exception):\n                    instance_node.delete()\n            raise\n\n    def collect_instances(self):\n        \"\"\"Collect persisted node-backed instances into the create context.\"\"\"\n        self.cache_instance_data(self.collection_shared_data)\n        cached_instances = self.collection_shared_data[\"katana_cached_instances\"]\n        for node, data in cached_instances.get(self.identifier, []):\n            instance_data = dict(self.prepare_collected_instance_data(data))\n            instance_data[\"instance_node\"] = node.getName()\n            created_instance = CreatedInstance.from_existing(instance_data, self)\n            self.apply_staging_dir(created_instance)\n            created_instance.transient_data[\"node\"] = node\n            self._add_instance_to_context(created_instance)\n\n    def prepare_collected_instance_data(self, instance_data):\n        \"\"\"Prepare persisted data before wrapping it in ``CreatedInstance``.\n\n        Args:\n            instance_data: Raw data read from the Katana node.\n\n        Returns:\n            Data used for the collected AYON instance.\n        \"\"\"\n        return instance_data\n\n    def update_instances(self, update_list):\n        \"\"\"Apply creator-instance changes to their Katana nodes.\"\"\"\n        for created_inst, changes in update_list:\n            instance_node = created_inst.transient_data.get(\"node\")\n            if instance_node is None:\n                self._remove_instance_from_context(created_inst)\n                continue\n\n            try:\n                instance_node.getName()\n            except Exception:\n                self._remove_instance_from_context(created_inst)\n                continue\n\n            if \"productName\" in changes.changed_keys:\n                instance_node.setName(changes[\"productName\"].new_value)\n            created_inst[\"instance_node\"] = instance_node.getName()\n            instances.imprint(instance_node, created_inst.data_to_store())\n\n    def remove_instances(self, instances):\n        \"\"\"Delete Katana nodes owned by removed creator instances.\"\"\"\n        for created_inst in instances:\n            instance_node = created_inst.transient_data.get(\"node\")\n            if instance_node is not None:\n                try:\n                    instance_node.delete()\n                except Exception:\n                    self.log.warning(\n                        \"Failed to delete Katana instance node.\",\n                        exc_info=True,\n                    )\n            self._remove_instance_from_context(created_inst)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaCreator.check_existing_product","title":"<code>check_existing_product(product_name)</code>","text":"<p>Validate that the product name is unique in the scene.</p> <p>Parameters:</p> Name Type Description Default <code>product_name</code> <p>Product name to validate.</p> required <p>Raises:</p> Type Description <code>CreatorError</code> <p>A node-backed instance already uses the name.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>def check_existing_product(self, product_name):\n    \"\"\"Validate that the product name is unique in the scene.\n\n    Args:\n        product_name: Product name to validate.\n\n    Raises:\n        CreatorError: A node-backed instance already uses the name.\n    \"\"\"\n    for _node, instance_data in instances.iter_instances():\n        if instance_data.get(\"productName\") == product_name:\n            raise CreatorError(\n                f\"A Katana publish instance for {product_name!r} already exists.\"\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaCreator.collect_instances","title":"<code>collect_instances()</code>","text":"<p>Collect persisted node-backed instances into the create context.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>def collect_instances(self):\n    \"\"\"Collect persisted node-backed instances into the create context.\"\"\"\n    self.cache_instance_data(self.collection_shared_data)\n    cached_instances = self.collection_shared_data[\"katana_cached_instances\"]\n    for node, data in cached_instances.get(self.identifier, []):\n        instance_data = dict(self.prepare_collected_instance_data(data))\n        instance_data[\"instance_node\"] = node.getName()\n        created_instance = CreatedInstance.from_existing(instance_data, self)\n        self.apply_staging_dir(created_instance)\n        created_instance.transient_data[\"node\"] = node\n        self._add_instance_to_context(created_instance)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaCreator.create","title":"<code>create(product_name, instance_data, pre_create_data)</code>","text":"<p>Create and persist a node-backed publish instance.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>def create(self, product_name, instance_data, pre_create_data):\n    \"\"\"Create and persist a node-backed publish instance.\"\"\"\n    self.check_existing_product(product_name)\n\n    product_type = instance_data.get(\"productType\") or self.product_base_type\n    node_type = instance_data.pop(\"node_type\", None)\n    parent_node = instance_data.pop(\"parent_node\", None)\n    instance_node = None\n    created_instance = None\n    try:\n        instance_node = self.create_instance_node(\n            node_type or self.node_type,\n            product_name,\n            parent=parent_node,\n        )\n        created_instance = CreatedInstance(\n            product_base_type=self.product_base_type,\n            product_type=product_type,\n            product_name=product_name,\n            data=instance_data,\n            creator=self,\n        )\n        created_instance[\"instance_node\"] = instance_node.getName()\n        self.apply_staging_dir(created_instance)\n        created_instance.transient_data[\"node\"] = instance_node\n        self._add_instance_to_context(created_instance)\n        instances.imprint(instance_node, created_instance.data_to_store())\n        return created_instance\n    except Exception:\n        if created_instance is not None:\n            with suppress(Exception):\n                self._remove_instance_from_context(created_instance)\n        if instance_node is not None:\n            with suppress(Exception):\n                instance_node.delete()\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaCreator.prepare_collected_instance_data","title":"<code>prepare_collected_instance_data(instance_data)</code>","text":"<p>Prepare persisted data before wrapping it in <code>CreatedInstance</code>.</p> <p>Parameters:</p> Name Type Description Default <code>instance_data</code> <p>Raw data read from the Katana node.</p> required <p>Returns:</p> Type Description <p>Data used for the collected AYON instance.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>def prepare_collected_instance_data(self, instance_data):\n    \"\"\"Prepare persisted data before wrapping it in ``CreatedInstance``.\n\n    Args:\n        instance_data: Raw data read from the Katana node.\n\n    Returns:\n        Data used for the collected AYON instance.\n    \"\"\"\n    return instance_data\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaCreator.remove_instances","title":"<code>remove_instances(instances)</code>","text":"<p>Delete Katana nodes owned by removed creator instances.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>def remove_instances(self, instances):\n    \"\"\"Delete Katana nodes owned by removed creator instances.\"\"\"\n    for created_inst in instances:\n        instance_node = created_inst.transient_data.get(\"node\")\n        if instance_node is not None:\n            try:\n                instance_node.delete()\n            except Exception:\n                self.log.warning(\n                    \"Failed to delete Katana instance node.\",\n                    exc_info=True,\n                )\n        self._remove_instance_from_context(created_inst)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaCreator.update_instances","title":"<code>update_instances(update_list)</code>","text":"<p>Apply creator-instance changes to their Katana nodes.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>def update_instances(self, update_list):\n    \"\"\"Apply creator-instance changes to their Katana nodes.\"\"\"\n    for created_inst, changes in update_list:\n        instance_node = created_inst.transient_data.get(\"node\")\n        if instance_node is None:\n            self._remove_instance_from_context(created_inst)\n            continue\n\n        try:\n            instance_node.getName()\n        except Exception:\n            self._remove_instance_from_context(created_inst)\n            continue\n\n        if \"productName\" in changes.changed_keys:\n            instance_node.setName(changes[\"productName\"].new_value)\n        created_inst[\"instance_node\"] = instance_node.getName()\n        instances.imprint(instance_node, created_inst.data_to_store())\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaCreatorBase","title":"<code>KatanaCreatorBase</code>","text":"<p>Share node-backed instance behavior across Katana creators.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>class KatanaCreatorBase:\n    \"\"\"Share node-backed instance behavior across Katana creators.\"\"\"\n\n    @staticmethod\n    def cache_instance_data(shared_data):\n        \"\"\"Cache instances for Creators to shared data.\n\n        Create ``katana_cached_instances`` when needed in shared data and fill\n        it with all collected node-backed instances under their respective\n        creator identifiers.\n\n        Args:\n            shared_data: Shared collection data.\n\n        Returns:\n            Shared collection data.\n        \"\"\"\n        if shared_data.get(\"katana_cached_instances\") is None:\n            cache = defaultdict(list)\n            for node, instance_data in instances.iter_instances():\n                creator_identifier = instance_data.get(\"creator_identifier\")\n                if creator_identifier:\n                    cache[creator_identifier].append((node, instance_data))\n            shared_data[\"katana_cached_instances\"] = cache\n\n        return shared_data\n\n    @staticmethod\n    def create_instance_node(node_type, node_name, parent=None):\n        \"\"\"Create node representing instance.\n\n        Args:\n            node_type: Type of the node.\n            node_name: Name of the new node.\n            parent: Optional parent node. Defaults to the root node.\n\n        Returns:\n            Newly created Katana instance node.\n        \"\"\"\n        parent = parent or NodegraphAPI.GetRootNode()\n        instance_node = NodegraphAPI.CreateNode(node_type, parent)\n        instance_node.setName(node_name)\n        return instance_node\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaCreatorBase.cache_instance_data","title":"<code>cache_instance_data(shared_data)</code>  <code>staticmethod</code>","text":"<p>Cache instances for Creators to shared data.</p> <p>Create <code>katana_cached_instances</code> when needed in shared data and fill it with all collected node-backed instances under their respective creator identifiers.</p> <p>Parameters:</p> Name Type Description Default <code>shared_data</code> <p>Shared collection data.</p> required <p>Returns:</p> Type Description <p>Shared collection data.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>@staticmethod\ndef cache_instance_data(shared_data):\n    \"\"\"Cache instances for Creators to shared data.\n\n    Create ``katana_cached_instances`` when needed in shared data and fill\n    it with all collected node-backed instances under their respective\n    creator identifiers.\n\n    Args:\n        shared_data: Shared collection data.\n\n    Returns:\n        Shared collection data.\n    \"\"\"\n    if shared_data.get(\"katana_cached_instances\") is None:\n        cache = defaultdict(list)\n        for node, instance_data in instances.iter_instances():\n            creator_identifier = instance_data.get(\"creator_identifier\")\n            if creator_identifier:\n                cache[creator_identifier].append((node, instance_data))\n        shared_data[\"katana_cached_instances\"] = cache\n\n    return shared_data\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaCreatorBase.create_instance_node","title":"<code>create_instance_node(node_type, node_name, parent=None)</code>  <code>staticmethod</code>","text":"<p>Create node representing instance.</p> <p>Parameters:</p> Name Type Description Default <code>node_type</code> <p>Type of the node.</p> required <code>node_name</code> <p>Name of the new node.</p> required <code>parent</code> <p>Optional parent node. Defaults to the root node.</p> <code>None</code> <p>Returns:</p> Type Description <p>Newly created Katana instance node.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>@staticmethod\ndef create_instance_node(node_type, node_name, parent=None):\n    \"\"\"Create node representing instance.\n\n    Args:\n        node_type: Type of the node.\n        node_name: Name of the new node.\n        parent: Optional parent node. Defaults to the root node.\n\n    Returns:\n        Newly created Katana instance node.\n    \"\"\"\n    parent = parent or NodegraphAPI.GetRootNode()\n    instance_node = NodegraphAPI.CreateNode(node_type, parent)\n    instance_node.setName(node_name)\n    return instance_node\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaExtractorPlugin","title":"<code>KatanaExtractorPlugin</code>","text":"<p>               Bases: <code>Extractor</code></p> <p>Base class for Katana extract plugins.</p> Note <p><code>KatanaExtractorPlugin</code> is a subclass of <code>publish.Extractor</code>, which in turn is a subclass of <code>pyblish.api.InstancePlugin</code>. If a context extractor is needed, it should incorporate the staging functionality provided by <code>publish.Extractor</code>.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>class KatanaExtractorPlugin(publish.Extractor):\n    \"\"\"Base class for Katana extract plugins.\n\n    Note:\n        ``KatanaExtractorPlugin`` is a subclass of ``publish.Extractor``,\n        which in turn is a subclass of ``pyblish.api.InstancePlugin``.\n        If a context extractor is needed, it should incorporate the staging\n        functionality provided by ``publish.Extractor``.\n    \"\"\"\n\n    hosts = [\"katana\"]\n    settings_category = SETTINGS_CATEGORY\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaInstancePlugin","title":"<code>KatanaInstancePlugin</code>","text":"<p>               Bases: <code>InstancePlugin</code></p> <p>Base class for Katana instance publish plugins.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>class KatanaInstancePlugin(pyblish.api.InstancePlugin):\n    \"\"\"Base class for Katana instance publish plugins.\"\"\"\n\n    hosts = [\"katana\"]\n    settings_category = SETTINGS_CATEGORY\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaLoader","title":"<code>KatanaLoader</code>","text":"<p>               Bases: <code>LoaderPlugin</code></p> <p>Base class for Katana load plugins.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>class KatanaLoader(load.LoaderPlugin):\n    \"\"\"Base class for Katana load plugins.\"\"\"\n\n    hosts = [\"katana\"]\n    settings_category = SETTINGS_CATEGORY\n    use_ayon_entity_uri = False\n\n    @classmethod\n    def filepath_from_context(cls, context):\n        \"\"\"Resolve a representation to a path or optional AYON entity URI.\"\"\"\n        if cls.use_ayon_entity_uri:\n            from .usd import get_ayon_entity_uri_from_representation_context\n\n            return get_ayon_entity_uri_from_representation_context(context)\n        path = super().filepath_from_context(context)\n        return os.path.normpath(path).replace(\"\\\\\", \"/\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/plugin.html#client.ayon_katana.api.plugin.KatanaLoader.filepath_from_context","title":"<code>filepath_from_context(context)</code>  <code>classmethod</code>","text":"<p>Resolve a representation to a path or optional AYON entity URI.</p> Source code in <code>client/ayon_katana/api/plugin.py</code> <pre><code>@classmethod\ndef filepath_from_context(cls, context):\n    \"\"\"Resolve a representation to a path or optional AYON entity URI.\"\"\"\n    if cls.use_ayon_entity_uri:\n        from .usd import get_ayon_entity_uri_from_representation_context\n\n        return get_ayon_entity_uri_from_representation_context(context)\n    path = super().filepath_from_context(context)\n    return os.path.normpath(path).replace(\"\\\\\", \"/\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html","title":"render","text":"<p>Katana render-instance graph and output helpers.</p>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.RenderFrameRange","title":"<code>RenderFrameRange</code>  <code>dataclass</code>","text":"<p>Describe the effective native Katana frame range.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>@dataclass(frozen=True)\nclass RenderFrameRange:\n    \"\"\"Describe the effective native Katana frame range.\"\"\"\n\n    start: int\n    end: int\n    step: int\n    source: str\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.RenderOutputDefinition","title":"<code>RenderOutputDefinition</code>  <code>dataclass</code>","text":"<p>Describe one enabled Katana render output.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>@dataclass(frozen=True)\nclass RenderOutputDefinition:\n    \"\"\"Describe one enabled Katana render output.\"\"\"\n\n    node_name: str\n    name: str\n    path: str\n    extension: str\n    channel: str\n    enabled: bool\n\n    @property\n    def aov_identifier(self) -&gt; str:\n        \"\"\"Return the AOV key used by AYON farm integration.\"\"\"\n        if _is_main_output(self.name, self.channel):\n            return \"\"\n        for candidate in (self.channel, self.name):\n            normalized = candidate.strip()\n            if normalized:\n                return normalized\n        return \"\"\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.RenderOutputDefinition.aov_identifier","title":"<code>aov_identifier</code>  <code>property</code>","text":"<p>Return the AOV key used by AYON farm integration.</p>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.RenderResolution","title":"<code>RenderResolution</code>  <code>dataclass</code>","text":"<p>Describe a named Katana resolution preset.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>@dataclass(frozen=True)\nclass RenderResolution:\n    \"\"\"Describe a named Katana resolution preset.\"\"\"\n\n    name: str\n    width: int\n    height: int\n    pixel_aspect: float\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.RenderSettingsValues","title":"<code>RenderSettingsValues</code>  <code>dataclass</code>","text":"<p>Describe the effective settings authored by a RenderSettings node.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>@dataclass(frozen=True)\nclass RenderSettingsValues:\n    \"\"\"Describe the effective settings authored by a RenderSettings node.\"\"\"\n\n    renderer: str\n    camera: str\n    resolution_name: str\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.configure_output_node","title":"<code>configure_output_node(output_node, output_name, output_path, extension, channel)</code>","text":"<p>Configure a renderer-neutral file output definition.</p> <p>Parameters:</p> Name Type Description Default <code>output_node</code> <p>Katana <code>RenderOutputDefine</code> node.</p> required <code>output_name</code> <code>str</code> <p>Render output name.</p> required <code>output_path</code> <code>str</code> <p>Output path containing Katana <code>#</code> frame padding.</p> required <code>extension</code> <code>str</code> <p>File extension without a leading dot.</p> required <code>channel</code> <code>str</code> <p>Renderer channel or AOV name.</p> required Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def configure_output_node(\n    output_node,\n    output_name: str,\n    output_path: str,\n    extension: str,\n    channel: str,\n) -&gt; None:\n    \"\"\"Configure a renderer-neutral file output definition.\n\n    Args:\n        output_node: Katana ``RenderOutputDefine`` node.\n        output_name: Render output name.\n        output_path: Output path containing Katana ``#`` frame padding.\n        extension: File extension without a leading dot.\n        channel: Renderer channel or AOV name.\n    \"\"\"\n    output_node.getParameter(\"outputName\").setValue(output_name, _TIME)\n    lib.set_string_parameter(output_node, \"args.__lastLocationType\", \"file\")\n    base_path = \"args.renderSettings.outputs.outputName\"\n    is_main_output = _is_main_output(output_name, channel)\n    _set_string_attr(\n        output_node,\n        f\"{base_path}.rendererSettings.fileExtension\",\n        extension,\n    )\n    _set_string_attr(output_node, f\"{base_path}.rendererSettings.channel\", channel)\n    _set_number_attr(\n        output_node,\n        f\"{base_path}.rendererSettings.withAlpha\",\n        int(is_main_output),\n    )\n    _set_string_attr(output_node, f\"{base_path}.locationType\", \"file\")\n    _set_string_attr(\n        output_node,\n        f\"{base_path}.locationSettings.renderLocation\",\n        output_path,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.configure_render_settings","title":"<code>configure_render_settings(settings_node, renderer, camera, resolution='')</code>","text":"<p>Configure explicit native renderer, camera, and resolution settings.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def configure_render_settings(\n    settings_node,\n    renderer: str,\n    camera: str,\n    resolution: str = \"\",\n) -&gt; None:\n    \"\"\"Configure explicit native renderer, camera, and resolution settings.\"\"\"\n    _set_optional_string_attr(settings_node, _RENDERER_PARAMETER, renderer)\n    _set_optional_string_attr(settings_node, _CAMERA_PARAMETER, camera)\n    _set_optional_string_attr(settings_node, _RESOLUTION_PARAMETER, resolution)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.create_output_node","title":"<code>create_output_node(instance_node, output_name, output_path, extension, channel, primary=False)</code>","text":"<p>Create, configure, and connect one native Katana render output.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def create_output_node(\n    instance_node,\n    output_name: str,\n    output_path: str,\n    extension: str,\n    channel: str,\n    primary: bool = False,\n):\n    \"\"\"Create, configure, and connect one native Katana render output.\"\"\"\n    existing_output_count = len(get_output_nodes(instance_node))\n    output_node = NodegraphAPI.CreateNode(\"RenderOutputDefine\", instance_node)\n    node_suffix = lib.sanitize_node_name(output_name, \"Output\")\n    output_node.setName(f\"AYON_RenderOutput_{node_suffix}\")\n    set_node_role(output_node, _PRIMARY_OUTPUT_ROLE if primary else _OUTPUT_ROLE)\n    configure_output_node(\n        output_node,\n        output_name=output_name,\n        output_path=output_path,\n        extension=extension,\n        channel=channel,\n    )\n    NodegraphAPI.SetNodePosition(\n        output_node,\n        (float(existing_output_count * 250), 0.0),\n    )\n    reconnect_render_graph(instance_node)\n    return output_node\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.create_render_graph","title":"<code>create_render_graph(instance_node, product_name, output_name, output_path, extension, channel, renderer, camera, frame_start, frame_end, frame_step, resolution='')</code>","text":"<p>Create the native Katana nodes owned by a render instance.</p> <p>Parameters:</p> Name Type Description Default <code>instance_node</code> <p>Outer AYON instance Group.</p> required <code>product_name</code> <code>str</code> <p>AYON product name.</p> required <code>output_name</code> <code>str</code> <p>Render output name.</p> required <code>output_path</code> <code>str</code> <p>Expected output pattern.</p> required <code>extension</code> <code>str</code> <p>File extension.</p> required <code>channel</code> <code>str</code> <p>Render channel.</p> required <code>renderer</code> <code>str</code> <p>Optional renderer identifier.</p> required <code>camera</code> <code>str</code> <p>Optional scenegraph camera path.</p> required <code>frame_start</code> <code>int</code> <p>First frame.</p> required <code>frame_end</code> <code>int</code> <p>Last frame.</p> required <code>frame_step</code> <code>int</code> <p>Native Katana project timeline increment.</p> required <code>resolution</code> <code>str</code> <p>Optional named Katana resolution preset.</p> <code>''</code> <p>Returns:</p> Type Description <p>Created native <code>Render</code> node.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def create_render_graph(\n    instance_node,\n    product_name: str,\n    output_name: str,\n    output_path: str,\n    extension: str,\n    channel: str,\n    renderer: str,\n    camera: str,\n    frame_start: int,\n    frame_end: int,\n    frame_step: int,\n    resolution: str = \"\",\n):\n    \"\"\"Create the native Katana nodes owned by a render instance.\n\n    Args:\n        instance_node: Outer AYON instance Group.\n        product_name: AYON product name.\n        output_name: Render output name.\n        output_path: Expected output pattern.\n        extension: File extension.\n        channel: Render channel.\n        renderer: Optional renderer identifier.\n        camera: Optional scenegraph camera path.\n        frame_start: First frame.\n        frame_end: Last frame.\n        frame_step: Native Katana project timeline increment.\n        resolution: Optional named Katana resolution preset.\n\n    Returns:\n        Created native ``Render`` node.\n    \"\"\"\n    settings_node = NodegraphAPI.CreateNode(\"RenderSettings\", instance_node)\n    settings_node.setName(\"AYON_RenderSettings\")\n    set_node_role(settings_node, _SETTINGS_ROLE)\n\n    render_node = NodegraphAPI.CreateNode(\"Render\", instance_node)\n    render_node.setName(f\"AYON_{product_name}_Render\")\n    set_node_role(render_node, _RENDER_ROLE)\n\n    output_node = create_output_node(\n        instance_node,\n        output_name=output_name,\n        output_path=output_path,\n        extension=extension,\n        channel=channel,\n        primary=True,\n    )\n    update_render_graph(\n        instance_node=instance_node,\n        product_name=product_name,\n        output_name=output_name,\n        output_path=output_path,\n        extension=extension,\n        channel=channel,\n        renderer=renderer,\n        camera=camera,\n        frame_start=frame_start,\n        frame_end=frame_end,\n        frame_step=frame_step,\n        resolution=resolution,\n    )\n\n    NodegraphAPI.SetNodePosition(settings_node, (-300.0, 0.0))\n    NodegraphAPI.SetNodePosition(output_node, (0.0, 0.0))\n    NodegraphAPI.SetNodePosition(render_node, (300.0, -100.0))\n    return render_node\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_default_renderer","title":"<code>get_default_renderer(preferred=None)</code>","text":"<p>Return the preferred registered pixel renderer.</p> <p>Parameters:</p> Name Type Description Default <code>preferred</code> <code>Optional[str]</code> <p>Optional project-settings override.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>Preferred renderer identifier, or an empty string when none are loaded.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_default_renderer(preferred: Optional[str] = None) -&gt; str:\n    \"\"\"Return the preferred registered pixel renderer.\n\n    Args:\n        preferred: Optional project-settings override.\n\n    Returns:\n        Preferred renderer identifier, or an empty string when none are loaded.\n    \"\"\"\n    registered_renderers = get_registered_renderers()\n    for candidate in (preferred, os.environ.get(\"DEFAULT_RENDERER\")):\n        if candidate and candidate in registered_renderers:\n            return candidate\n    return \"\"\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_effective_render_settings","title":"<code>get_effective_render_settings(settings_node)</code>","text":"<p>Return renderer, camera, and resolution from native Katana settings.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_effective_render_settings(settings_node) -&gt; RenderSettingsValues:\n    \"\"\"Return renderer, camera, and resolution from native Katana settings.\"\"\"\n    resolution_name = _get_enabled_string_setting(\n        settings_node,\n        _RESOLUTION_PARAMETER,\n    )\n    if not resolution_name:\n        root_node = NodegraphAPI.GetRootNode()\n        resolution_name = str(_parameter_value(root_node, \"resolution\", \"\") or \"\")\n    return RenderSettingsValues(\n        renderer=_get_enabled_string_setting(settings_node, _RENDERER_PARAMETER),\n        camera=_get_enabled_string_setting(settings_node, _CAMERA_PARAMETER),\n        resolution_name=resolution_name.strip(),\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_katana_executable","title":"<code>get_katana_executable()</code>","text":"<p>Return the Katana executable used by the current AYON launch.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_katana_executable() -&gt; Path:\n    \"\"\"Return the Katana executable used by the current AYON launch.\"\"\"\n    candidates = []\n    katana_root = os.environ.get(\"KATANA_ROOT\")\n    if katana_root:\n        candidates.append(Path(katana_root) / \"bin\" / _katana_executable_name())\n\n    current_executable = Path(sys.executable)\n    if \"katana\" in current_executable.name.casefold():\n        candidates.append(current_executable)\n\n    app_executable = os.environ.get(\"AYON_APP_EXECUTABLE\")\n    if app_executable:\n        candidates.append(Path(app_executable))\n\n    for candidate in candidates:\n        if candidate.is_file():\n            return candidate\n    raise RuntimeError(\"Could not resolve the Katana executable.\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_node_by_role","title":"<code>get_node_by_role(instance_node, role)</code>","text":"<p>Return a direct child with a render graph role.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_node_by_role(instance_node, role: str):\n    \"\"\"Return a direct child with a render graph role.\"\"\"\n    for child in instance_node.getChildren():\n        if lib.get_string_parameter(child, _ROLE_PARAMETER) == role:\n            return child\n    return None\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_output_definition","title":"<code>get_output_definition(output_node)</code>","text":"<p>Return normalized render-product data from a Katana output node.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_output_definition(output_node) -&gt; RenderOutputDefinition:\n    \"\"\"Return normalized render-product data from a Katana output node.\"\"\"\n    output_name = str(_parameter_value(output_node, \"outputName\", \"\") or \"\").strip()\n    output_path = str(\n        _output_attribute_value(\n            output_node,\n            \"locationSettings.renderLocation\",\n            \"\",\n        )\n        or \"\"\n    ).strip()\n    extension = (\n        str(\n            _output_attribute_value(\n                output_node,\n                \"rendererSettings.fileExtension\",\n                \"\",\n            )\n            or \"\"\n        )\n        .strip()\n        .lstrip(\".\")\n    )\n    if not extension and output_path:\n        extension = Path(output_path).suffix.lstrip(\".\")\n    channel = str(\n        _output_attribute_value(\n            output_node,\n            \"rendererSettings.channel\",\n            \"\",\n        )\n        or \"\"\n    ).strip()\n    return RenderOutputDefinition(\n        node_name=output_node.getName(),\n        name=output_name,\n        path=output_path,\n        extension=extension,\n        channel=channel,\n        enabled=_node_is_enabled(output_node),\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_output_definitions","title":"<code>get_output_definitions(instance_node, include_disabled=False)</code>","text":"<p>Return render products defined by the native Katana graph.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_output_definitions(\n    instance_node, include_disabled: bool = False\n) -&gt; list[RenderOutputDefinition]:\n    \"\"\"Return render products defined by the native Katana graph.\"\"\"\n    definitions = [\n        get_output_definition(output_node)\n        for output_node in get_output_nodes(instance_node)\n    ]\n    if include_disabled:\n        return definitions\n    return [definition for definition in definitions if definition.enabled]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_output_node","title":"<code>get_output_node(instance_node)</code>","text":"<p>Return the creator-owned primary RenderOutputDefine child.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_output_node(instance_node):\n    \"\"\"Return the creator-owned primary RenderOutputDefine child.\"\"\"\n    return get_node_by_role(instance_node, _PRIMARY_OUTPUT_ROLE)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_output_nodes","title":"<code>get_output_nodes(instance_node)</code>","text":"<p>Return all direct RenderOutputDefine children in graph order.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_output_nodes(instance_node) -&gt; list:\n    \"\"\"Return all direct RenderOutputDefine children in graph order.\"\"\"\n    output_nodes = [\n        child\n        for child in instance_node.getChildren()\n        if child.getType() == \"RenderOutputDefine\"\n    ]\n\n    def sort_key(node):\n        \"\"\"Return graph position and name for deterministic output ordering.\"\"\"\n        position = NodegraphAPI.GetNodePosition(node)\n        return float(position[0]), node.getName()\n\n    return sorted(output_nodes, key=sort_key)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_output_path","title":"<code>get_output_path(instance_node)</code>","text":"<p>Return the persisted primary render output path.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_output_path(instance_node) -&gt; Optional[str]:\n    \"\"\"Return the persisted primary render output path.\"\"\"\n    return lib.get_string_parameter(instance_node, _OUTPUT_PATH_PARAMETER)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_project_frame_range","title":"<code>get_project_frame_range()</code>","text":"<p>Return the native project timeline in and out frames.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_project_frame_range() -&gt; tuple[int, int]:\n    \"\"\"Return the native project timeline in and out frames.\"\"\"\n    root_node = NodegraphAPI.GetRootNode()\n    start = _parameter_value(root_node, \"inTime\")\n    end = _parameter_value(root_node, \"outTime\")\n    if start is None or end is None:\n        raise RuntimeError(\"Katana project timeline has no inTime/outTime values.\")\n    return int(start), int(end)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_project_frame_step","title":"<code>get_project_frame_step()</code>","text":"<p>Return Katana's native project timeline increment.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_project_frame_step() -&gt; int:\n    \"\"\"Return Katana's native project timeline increment.\"\"\"\n    root_node = NodegraphAPI.GetRootNode()\n    step = _parameter_value(root_node, \"timeIncrement\")\n    if step is None:\n        raise RuntimeError(\"Katana project timeline has no timeIncrement value.\")\n    return int(step)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_registered_renderers","title":"<code>get_registered_renderers(include_internal=False)</code>","text":"<p>Return renderer IDs registered in the current Katana process.</p> <p>Parameters:</p> Name Type Description Default <code>include_internal</code> <code>bool</code> <p>Include Katana's profiling and USD helper renderers.</p> <code>False</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>Sorted renderer identifiers.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_registered_renderers(include_internal: bool = False) -&gt; list[str]:\n    \"\"\"Return renderer IDs registered in the current Katana process.\n\n    Args:\n        include_internal: Include Katana's profiling and USD helper renderers.\n\n    Returns:\n        Sorted renderer identifiers.\n    \"\"\"\n    renderer_names = RenderingAPI.RenderPlugins.GetRendererPluginNames()\n    output = sorted({str(name) for name in renderer_names if name})\n    if include_internal:\n        return output\n    return [name for name in output if name not in _INTERNAL_RENDERERS]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_render_frame_range","title":"<code>get_render_frame_range(render_node)</code>","text":"<p>Return the active native Render range and project frame increment.</p> <p>Katana's Render node explicitly owns only the optional farm range.  When that range is disabled, the project timeline is the native source of truth.  Frame stepping is represented by the project <code>timeIncrement</code> parameter in both cases.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_render_frame_range(render_node) -&gt; RenderFrameRange:\n    \"\"\"Return the active native Render range and project frame increment.\n\n    Katana's Render node explicitly owns only the optional farm range.  When\n    that range is disabled, the project timeline is the native source of\n    truth.  Frame stepping is represented by the project ``timeIncrement``\n    parameter in both cases.\n    \"\"\"\n    active_range = _parameter_value(render_node, \"farmSettings.setActiveFrameRange\")\n    if _is_enabled(active_range):\n        start = _parameter_value(render_node, \"farmSettings.activeFrameRange.start\")\n        end = _parameter_value(render_node, \"farmSettings.activeFrameRange.end\")\n        if start is None or end is None:\n            raise RuntimeError(\"Katana Render active frame range is incomplete.\")\n        source = \"render\"\n    else:\n        start, end = get_project_frame_range()\n        source = \"project\"\n    return RenderFrameRange(\n        start=int(start),\n        end=int(end),\n        step=get_project_frame_step(),\n        source=source,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_render_node","title":"<code>get_render_node(instance_node)</code>","text":"<p>Return the native Render child of an AYON render instance.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_render_node(instance_node):\n    \"\"\"Return the native Render child of an AYON render instance.\"\"\"\n    return get_node_by_role(instance_node, _RENDER_ROLE)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_resolution","title":"<code>get_resolution(name)</code>","text":"<p>Resolve an effective native Katana resolution preset by name.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_resolution(name: str) -&gt; RenderResolution:\n    \"\"\"Resolve an effective native Katana resolution preset by name.\"\"\"\n    if not name:\n        raise RuntimeError(\"Katana RenderSettings has no effective resolution.\")\n    for resolution_path in _resolution_resource_paths():\n        root = element_tree.parse(resolution_path).getroot()\n        for format_element in root.findall(\".//format\"):\n            if format_element.get(\"name\") != name:\n                continue\n            try:\n                return RenderResolution(\n                    name=name,\n                    width=int(format_element.attrib[\"width\"]),\n                    height=int(format_element.attrib[\"height\"]),\n                    pixel_aspect=float(format_element.attrib[\"pixelAspect\"]),\n                )\n            except (KeyError, TypeError, ValueError) as exc:\n                raise RuntimeError(\n                    f\"Katana resolution preset {name!r} is malformed in \"\n                    f\"{resolution_path}.\"\n                ) from exc\n    raise RuntimeError(f\"Katana resolution preset {name!r} was not found.\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_scenegraph_location_type","title":"<code>get_scenegraph_location_type(render_node, location_path)</code>","text":"<p>Return a cooked scenegraph location type from the native Render node.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_scenegraph_location_type(render_node, location_path: str) -&gt; str:\n    \"\"\"Return a cooked scenegraph location type from the native Render node.\"\"\"\n    if not location_path:\n        return \"\"\n    import Nodes3DAPI\n    from Katana import FnGeolib\n\n    runtime = FnGeolib.GetRegisteredRuntimeInstance()\n    transaction = runtime.createTransaction()\n    client = transaction.createClient()\n    operation = Nodes3DAPI.GetOp(transaction, render_node)\n    transaction.setClientOp(client, operation)\n    runtime.commit(transaction)\n    location = client.cookLocation(location_path)\n    if not location:\n        return \"\"\n    attributes = location.getAttrs()\n    if attributes is None:\n        return \"\"\n    type_attribute = attributes.getChildByName(\"type\")\n    if type_attribute is None:\n        return \"\"\n    return str(type_attribute.getValue() or \"\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.get_settings_node","title":"<code>get_settings_node(instance_node)</code>","text":"<p>Return the native RenderSettings child.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def get_settings_node(instance_node):\n    \"\"\"Return the native RenderSettings child.\"\"\"\n    return get_node_by_role(instance_node, _SETTINGS_ROLE)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.is_renderer_registered","title":"<code>is_renderer_registered(renderer_name)</code>","text":"<p>Return whether a pixel renderer is registered in Katana.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def is_renderer_registered(renderer_name: str) -&gt; bool:\n    \"\"\"Return whether a pixel renderer is registered in Katana.\"\"\"\n    return renderer_name in get_registered_renderers()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.reconnect_render_graph","title":"<code>reconnect_render_graph(instance_node)</code>","text":"<p>Connect settings, all render outputs, the Group return, and Render node.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def reconnect_render_graph(instance_node) -&gt; None:\n    \"\"\"Connect settings, all render outputs, the Group return, and Render node.\"\"\"\n    settings_node = get_settings_node(instance_node)\n    render_node = get_render_node(instance_node)\n    output_nodes = get_output_nodes(instance_node)\n    if settings_node is None or render_node is None or not output_nodes:\n        raise RuntimeError(\n            f\"Render instance {instance_node.getName()!r} has an incomplete graph.\"\n        )\n\n    _connect_to_target(\n        instance_node.getSendPort(\"in\"),\n        settings_node.getInputPort(\"input\"),\n    )\n    source_port = settings_node.getOutputPort(\"out\")\n    for output_node in output_nodes:\n        _connect_to_target(source_port, output_node.getInputPort(\"input\"))\n        source_port = output_node.getOutputPort(\"out\")\n    _connect_to_target(source_port, instance_node.getReturnPort(\"out\"))\n    _connect_to_target(source_port, render_node.getInputPort(\"input\"))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.set_node_role","title":"<code>set_node_role(node, role)</code>","text":"<p>Tag a child node with its role in an AYON render graph.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def set_node_role(node, role: str) -&gt; None:\n    \"\"\"Tag a child node with its role in an AYON render graph.\"\"\"\n    lib.set_string_parameter(node, _ROLE_PARAMETER, role)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.set_output_path","title":"<code>set_output_path(instance_node, output_path)</code>","text":"<p>Persist the expected render output path on the instance Group.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def set_output_path(instance_node, output_path: str) -&gt; None:\n    \"\"\"Persist the expected render output path on the instance Group.\"\"\"\n    lib.set_string_parameter(instance_node, _OUTPUT_PATH_PARAMETER, output_path)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.set_project_frame_step","title":"<code>set_project_frame_step(frame_step)</code>","text":"<p>Set Katana's native project timeline increment for a new render graph.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def set_project_frame_step(frame_step: int) -&gt; None:\n    \"\"\"Set Katana's native project timeline increment for a new render graph.\"\"\"\n    root_node = NodegraphAPI.GetRootNode()\n    parameter = root_node.getParameter(\"timeIncrement\")\n    if parameter is None:\n        raise RuntimeError(\"Katana project timeline has no timeIncrement parameter.\")\n    parameter.setValue(int(frame_step), _TIME)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/render.html#client.ayon_katana.api.render.update_render_graph","title":"<code>update_render_graph(instance_node, product_name, output_name, output_path, extension, channel, renderer, camera, frame_start, frame_end, frame_step, resolution='')</code>","text":"<p>Synchronize persisted creator data into native Katana render nodes.</p> <p>Parameters:</p> Name Type Description Default <code>instance_node</code> <p>Outer AYON render instance Group.</p> required <code>product_name</code> <code>str</code> <p>AYON product name.</p> required <code>output_name</code> <code>str</code> <p>Render output name.</p> required <code>output_path</code> <code>str</code> <p>Expected output pattern.</p> required <code>extension</code> <code>str</code> <p>File extension.</p> required <code>channel</code> <code>str</code> <p>Render channel.</p> required <code>renderer</code> <code>str</code> <p>Optional renderer identifier.</p> required <code>camera</code> <code>str</code> <p>Optional scenegraph camera path.</p> required <code>frame_start</code> <code>int</code> <p>First frame.</p> required <code>frame_end</code> <code>int</code> <p>Last frame.</p> required <code>frame_step</code> <code>int</code> <p>Native Katana project timeline increment.</p> required <code>resolution</code> <code>str</code> <p>Optional named Katana resolution preset.</p> <code>''</code> <p>Returns:</p> Type Description <p>Updated native <code>Render</code> node.</p> Source code in <code>client/ayon_katana/api/render.py</code> <pre><code>def update_render_graph(\n    instance_node,\n    product_name: str,\n    output_name: str,\n    output_path: str,\n    extension: str,\n    channel: str,\n    renderer: str,\n    camera: str,\n    frame_start: int,\n    frame_end: int,\n    frame_step: int,\n    resolution: str = \"\",\n):\n    \"\"\"Synchronize persisted creator data into native Katana render nodes.\n\n    Args:\n        instance_node: Outer AYON render instance Group.\n        product_name: AYON product name.\n        output_name: Render output name.\n        output_path: Expected output pattern.\n        extension: File extension.\n        channel: Render channel.\n        renderer: Optional renderer identifier.\n        camera: Optional scenegraph camera path.\n        frame_start: First frame.\n        frame_end: Last frame.\n        frame_step: Native Katana project timeline increment.\n        resolution: Optional named Katana resolution preset.\n\n    Returns:\n        Updated native ``Render`` node.\n    \"\"\"\n    settings_node = get_settings_node(instance_node)\n    output_node = get_output_node(instance_node)\n    render_node = get_render_node(instance_node)\n    if settings_node is None or output_node is None or render_node is None:\n        raise RuntimeError(\n            f\"Render instance {instance_node.getName()!r} has an incomplete graph.\"\n        )\n\n    configure_render_settings(settings_node, renderer, camera, resolution)\n    configure_output_node(\n        output_node,\n        output_name,\n        output_path,\n        extension,\n        channel,\n    )\n    render_node.setName(f\"AYON_{product_name}_Render\")\n    render_node.getParameter(\"passName\").setValue(product_name, _TIME)\n    render_node.getParameter(\"farmSettings.setActiveFrameRange\").setValue(\"Yes\", _TIME)\n    render_node.getParameter(\"farmSettings.activeFrameRange.start\").setValue(\n        frame_start, _TIME\n    )\n    render_node.getParameter(\"farmSettings.activeFrameRange.end\").setValue(\n        frame_end, _TIME\n    )\n    set_project_frame_step(frame_step)\n    set_output_path(instance_node, output_path)\n    return render_node\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/review.html","title":"review","text":"<p>Katana Viewer review-sequence capture helpers.</p>"},{"location":"autoapi/client/ayon_katana/api/review.html#client.ayon_katana.api.review.capture_viewer_sequence","title":"<code>capture_viewer_sequence(viewer_widget, staging_dir, product_name, frame_start, frame_end, frame_step, *, frame_padding=4)</code>","text":"<p>Capture the visible Viewer over a frame range as a PNG sequence.</p> <p>The active Katana frame and Viewer presentation are restored even when capture fails. Files from a failed capture are removed before re-raising.</p> <p>Parameters:</p> Name Type Description Default <code>viewer_widget</code> <code>Any</code> <p>Selected Katana Viewer tab widget.</p> required <code>staging_dir</code> <code>Path</code> <p>Destination directory.</p> required <code>product_name</code> <code>str</code> <p>Prefix used for captured filenames.</p> required <code>frame_start</code> <code>int</code> <p>First frame, including handles when desired.</p> required <code>frame_end</code> <code>int</code> <p>Last frame, including handles when desired.</p> required <code>frame_step</code> <code>int</code> <p>Positive frame increment.</p> required <code>frame_padding</code> <code>int</code> <p>Minimum frame-number padding.</p> <code>4</code> <p>Returns:</p> Type Description <code>list[str]</code> <p>Captured filenames relative to <code>staging_dir</code>.</p> Source code in <code>client/ayon_katana/api/review.py</code> <pre><code>def capture_viewer_sequence(\n    viewer_widget: Any,\n    staging_dir: Path,\n    product_name: str,\n    frame_start: int,\n    frame_end: int,\n    frame_step: int,\n    *,\n    frame_padding: int = 4,\n) -&gt; list[str]:\n    \"\"\"Capture the visible Viewer over a frame range as a PNG sequence.\n\n    The active Katana frame and Viewer presentation are restored even when\n    capture fails. Files from a failed capture are removed before re-raising.\n\n    Args:\n        viewer_widget: Selected Katana Viewer tab widget.\n        staging_dir: Destination directory.\n        product_name: Prefix used for captured filenames.\n        frame_start: First frame, including handles when desired.\n        frame_end: Last frame, including handles when desired.\n        frame_step: Positive frame increment.\n        frame_padding: Minimum frame-number padding.\n\n    Returns:\n        Captured filenames relative to ``staging_dir``.\n    \"\"\"\n    if frame_padding &lt; 1:\n        raise ValueError(\"Review frame padding must be greater than zero.\")\n    if (\n        not product_name\n        or product_name in {\".\", \"..\"}\n        or \"/\" in product_name\n        or \"\\\\\" in product_name\n        or Path(product_name).is_absolute()\n    ):\n        raise ValueError(\n            \"Review product name must be a single non-empty filename component.\"\n        )\n\n    frames = frame_numbers(frame_start, frame_end, frame_step)\n    viewport_widget = _select_review_viewport(viewer_widget)\n\n    from Katana import NodegraphAPI\n\n    staging_dir.mkdir(parents=True, exist_ok=True)\n    original_frame = NodegraphAPI.GetCurrentTime()\n    created_paths: list[Path] = []\n    capture_failed = False\n    try:\n        for frame in frames:\n            NodegraphAPI.SetCurrentTime(frame)\n            _synchronize_viewport(viewport_widget)\n\n            filename = f\"{product_name}.{frame:0{frame_padding}d}.png\"\n            output_path = staging_dir / filename\n            _write_viewport_image(viewport_widget, output_path)\n            created_paths.append(output_path)\n    except Exception:\n        capture_failed = True\n        _remove_paths(created_paths)\n        raise\n    finally:\n        try:\n            NodegraphAPI.SetCurrentTime(original_frame)\n            _synchronize_viewport(viewport_widget)\n        except Exception:\n            if not capture_failed:\n                _remove_paths(created_paths)\n                raise\n\n    return [path.name for path in created_paths]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/review.html#client.ayon_katana.api.review.frame_numbers","title":"<code>frame_numbers(frame_start, frame_end, frame_step)</code>","text":"<p>Return an inclusive validated review frame sequence.</p> <p>Parameters:</p> Name Type Description Default <code>frame_start</code> <code>int</code> <p>First frame to capture.</p> required <code>frame_end</code> <code>int</code> <p>Last frame to capture.</p> required <code>frame_step</code> <code>int</code> <p>Positive frame increment.</p> required <p>Returns:</p> Type Description <code>tuple[int, ...]</code> <p>Inclusive frame numbers.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>The range or step is invalid.</p> Source code in <code>client/ayon_katana/api/review.py</code> <pre><code>def frame_numbers(frame_start: int, frame_end: int, frame_step: int) -&gt; tuple[int, ...]:\n    \"\"\"Return an inclusive validated review frame sequence.\n\n    Args:\n        frame_start: First frame to capture.\n        frame_end: Last frame to capture.\n        frame_step: Positive frame increment.\n\n    Returns:\n        Inclusive frame numbers.\n\n    Raises:\n        ValueError: The range or step is invalid.\n    \"\"\"\n    if frame_end &lt; frame_start:\n        raise ValueError(\n            f\"Review frame end {frame_end} is before frame start {frame_start}.\"\n        )\n    if frame_step &lt; 1:\n        raise ValueError(\"Review frame step must be greater than zero.\")\n    return tuple(range(frame_start, frame_end + 1, frame_step))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/startup.html","title":"startup","text":"<p>Katana-specific startup coordination for AYON launches.</p>"},{"location":"autoapi/client/ayon_katana/api/startup.html#client.ayon_katana.api.startup.has_requested_workfile","title":"<code>has_requested_workfile(environment=None)</code>","text":"<p>Return whether AYON selected a workfile for this Katana launch.</p> <p>Parameters:</p> Name Type Description Default <code>environment</code> <code>Optional[MutableMapping[str, str]]</code> <p>Process environment containing the launch request.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when a non-empty request is waiting to be consumed.</p> Source code in <code>client/ayon_katana/api/startup.py</code> <pre><code>def has_requested_workfile(\n    environment: Optional[MutableMapping[str, str]] = None,\n) -&gt; bool:\n    \"\"\"Return whether AYON selected a workfile for this Katana launch.\n\n    Args:\n        environment: Process environment containing the launch request.\n\n    Returns:\n        ``True`` when a non-empty request is waiting to be consumed.\n    \"\"\"\n    if environment is None:\n        environment = os.environ\n    return bool(environment.get(REQUESTED_WORKFILE_ENV))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/startup.html#client.ayon_katana.api.startup.open_requested_workfile","title":"<code>open_requested_workfile(environment=None, workio_module=None)</code>","text":"<p>Open the launch-requested workfile once after host installation.</p> <p>Parameters:</p> Name Type Description Default <code>environment</code> <code>Optional[MutableMapping[str, str]]</code> <p>Process environment containing the launch request.</p> <code>None</code> <code>workio_module</code> <code>Any</code> <p>Optional workfile API override used by tests.</p> <code>None</code> <p>Returns:</p> Type Description <code>Optional[str]</code> <p>The normalized opened path, or <code>None</code> when no file was opened.</p> Source code in <code>client/ayon_katana/api/startup.py</code> <pre><code>def open_requested_workfile(\n    environment: Optional[MutableMapping[str, str]] = None, workio_module: Any = None\n) -&gt; Optional[str]:\n    \"\"\"Open the launch-requested workfile once after host installation.\n\n    Args:\n        environment: Process environment containing the launch request.\n        workio_module: Optional workfile API override used by tests.\n\n    Returns:\n        The normalized opened path, or ``None`` when no file was opened.\n    \"\"\"\n    if environment is None:\n        environment = os.environ\n    requested_path = environment.pop(REQUESTED_WORKFILE_ENV, None)\n    if not requested_path:\n        return None\n\n    if workio_module is None:\n        from . import workio as workio_module\n\n    try:\n        normalized_path = workio_module.validate_workfile_path(\n            requested_path,\n            require_exists=True,\n        )\n    except ValueError:\n        log.exception(\n            \"AYON rejected the requested Katana startup workfile: %s\",\n            requested_path,\n        )\n        return None\n\n    current_path = workio_module.get_current_workfile()\n    if current_path:\n        try:\n            if workio_module.workfile_paths_match(current_path, normalized_path):\n                log.info(\n                    \"Katana already has the requested workfile open: %s\",\n                    normalized_path,\n                )\n                return normalized_path\n        except ValueError:\n            log.debug(\n                \"The current Katana project is not a comparable filesystem \"\n                \"workfile: %s\",\n                current_path,\n            )\n\n    try:\n        opened_path = workio_module.open_workfile(normalized_path)\n    except Exception:\n        log.exception(\n            \"Katana could not open the requested AYON workfile. \"\n            \"AYON did not save or create a replacement scene: %s\",\n            normalized_path,\n        )\n        return None\n\n    log.info(\"Opened the requested Katana workfile: %s\", opened_path)\n    return opened_path\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/thumbnail.html","title":"thumbnail","text":"<p>Katana Viewer thumbnail helpers.</p>"},{"location":"autoapi/client/ayon_katana/api/thumbnail.html#client.ayon_katana.api.thumbnail.capture_viewer_image","title":"<code>capture_viewer_image(viewer_widget, output_path, *, max_dimension=None)</code>","text":"<p>Capture a Viewer widget directly to a PNG path.</p> <p>Parameters:</p> Name Type Description Default <code>viewer_widget</code> <code>Any</code> <p>Katana Viewer Qt widget.</p> required <code>output_path</code> <code>PathLike[str] | str</code> <p>Destination PNG path.</p> required <code>max_dimension</code> <code>Optional[int]</code> <p>Optional maximum width or height. <code>None</code> preserves the native Viewer widget dimensions.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>Normalized destination path.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>The widget cannot be captured or the PNG cannot be saved.</p> <code>ValueError</code> <p><code>max_dimension</code> is not positive when supplied.</p> Source code in <code>client/ayon_katana/api/thumbnail.py</code> <pre><code>def capture_viewer_image(\n    viewer_widget: Any,\n    output_path: os.PathLike[str] | str,\n    *,\n    max_dimension: Optional[int] = None,\n) -&gt; str:\n    \"\"\"Capture a Viewer widget directly to a PNG path.\n\n    Args:\n        viewer_widget: Katana Viewer Qt widget.\n        output_path: Destination PNG path.\n        max_dimension: Optional maximum width or height. ``None`` preserves\n            the native Viewer widget dimensions.\n\n    Returns:\n        Normalized destination path.\n\n    Raises:\n        RuntimeError: The widget cannot be captured or the PNG cannot be saved.\n        ValueError: ``max_dimension`` is not positive when supplied.\n    \"\"\"\n    if max_dimension is not None and max_dimension &lt; 1:\n        raise ValueError(\"Viewer capture maximum dimension must be positive.\")\n\n    try:\n        pixmap = viewer_widget.grab()\n    except Exception as exc:\n        raise RuntimeError(\"Failed to capture the Katana Viewer.\") from exc\n    if pixmap is None or pixmap.isNull():\n        raise RuntimeError(\"Katana Viewer capture returned an empty image.\")\n\n    width = int(pixmap.width())\n    height = int(pixmap.height())\n    if width &lt; 1 or height &lt; 1:\n        raise RuntimeError(\"Katana Viewer capture has invalid dimensions.\")\n\n    if max_dimension is not None and max(width, height) &gt; max_dimension:\n        try:\n            from qtpy import QtCore\n\n            pixmap = pixmap.scaled(\n                max_dimension,\n                max_dimension,\n                QtCore.Qt.KeepAspectRatio,\n                QtCore.Qt.SmoothTransformation,\n            )\n        except Exception as exc:\n            raise RuntimeError(\"Failed to scale the Katana Viewer capture.\") from exc\n        if pixmap is None or pixmap.isNull():\n            raise RuntimeError(\"Katana Viewer capture scaling returned no image.\")\n\n    destination = Path(output_path)\n    destination.parent.mkdir(parents=True, exist_ok=True)\n    try:\n        saved = pixmap.save(str(destination), \"PNG\")\n    except Exception as exc:\n        with suppress(OSError):\n            destination.unlink()\n        raise RuntimeError(\"Qt could not save the Viewer capture.\") from exc\n    if not saved:\n        with suppress(OSError):\n            destination.unlink()\n        raise RuntimeError(\"Qt could not save the Viewer capture.\")\n    return str(destination)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/thumbnail.html#client.ayon_katana.api.thumbnail.capture_viewer_thumbnail","title":"<code>capture_viewer_thumbnail(viewer_widget, max_dimension=MAX_THUMBNAIL_DIMENSION)</code>","text":"<p>Capture a Viewer widget to a temporary PNG thumbnail.</p> Source code in <code>client/ayon_katana/api/thumbnail.py</code> <pre><code>def capture_viewer_thumbnail(\n    viewer_widget: Any,\n    max_dimension: int = MAX_THUMBNAIL_DIMENSION,\n) -&gt; str:\n    \"\"\"Capture a Viewer widget to a temporary PNG thumbnail.\"\"\"\n    with tempfile.NamedTemporaryFile(suffix=\".png\", delete=False) as stream:\n        output_path = stream.name\n    try:\n        return capture_viewer_image(\n            viewer_widget,\n            output_path,\n            max_dimension=max_dimension,\n        )\n    except Exception:\n        with suppress(OSError):\n            os.remove(output_path)\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/thumbnail.html#client.ayon_katana.api.thumbnail.get_viewer_tabs","title":"<code>get_viewer_tabs()</code>","text":"<p>Return open Viewer tabs without changing the layout.</p> Source code in <code>client/ayon_katana/api/thumbnail.py</code> <pre><code>def get_viewer_tabs() -&gt; list[Any]:\n    \"\"\"Return open Viewer tabs without changing the layout.\"\"\"\n    try:\n        from Katana import UI4\n    except ImportError:\n        return []\n\n    layouts = getattr(UI4.App, \"Layouts\", None)\n    if layouts is not None and hasattr(layouts, \"GetTabs\"):\n        try:\n            return list(\n                layouts.GetTabs(\n                    \"Viewer\",\n                    includeFloating=True,\n                    includeDockWidgets=True,\n                )\n                or []\n            )\n        except Exception:\n            return []\n\n    tabs_api = getattr(UI4.App, \"Tabs\", None)\n    if tabs_api is not None and hasattr(tabs_api, \"GetTabsByType\"):\n        try:\n            return list(tabs_api.GetTabsByType(\"Viewer\") or [])\n        except Exception:\n            return []\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/thumbnail.html#client.ayon_katana.api.thumbnail.select_viewer_widget","title":"<code>select_viewer_widget()</code>","text":"<p>Choose the focused Viewer, or the only visible Viewer.</p> <p>Returns:</p> Type Description <code>Optional[Any]</code> <p>A <code>(widget, reason)</code> pair. <code>widget</code> is <code>None</code> when no Viewer</p> <code>str</code> <p>can be selected; <code>reason</code> explains why.</p> Source code in <code>client/ayon_katana/api/thumbnail.py</code> <pre><code>def select_viewer_widget() -&gt; tuple[Optional[Any], str]:\n    \"\"\"Choose the focused Viewer, or the only visible Viewer.\n\n    Returns:\n        A ``(widget, reason)`` pair. ``widget`` is ``None`` when no Viewer\n        can be selected; ``reason`` explains why.\n    \"\"\"\n    viewers = get_viewer_tabs()\n    if not viewers:\n        return None, \"no Viewer tab is available\"\n\n    visible = [viewer for viewer in viewers if _is_visible(viewer)]\n    if not visible:\n        return None, \"no visible Viewer tab is available\"\n\n    focus_widget = _get_focus_widget()\n    focused = [viewer for viewer in visible if _contains_focus(viewer, focus_widget)]\n    if len(focused) == 1:\n        return focused[0], \"focused Viewer\"\n    if len(focused) &gt; 1:\n        return None, \"multiple visible Viewer tabs contain focus\"\n    if len(visible) == 1:\n        return visible[0], \"sole visible Viewer\"\n    return None, \"multiple visible Viewer tabs are ambiguous\"\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd.html","title":"usd","text":"<p>Katana USD helpers.</p>"},{"location":"autoapi/client/ayon_katana/api/usd.html#client.ayon_katana.api.usd.clear_resolver_cache","title":"<code>clear_resolver_cache()</code>","text":"<p>Clear the optional AYON USD Resolver and flush native UsdIn stages.</p> <p>Returns:</p> Type Description <code>dict[str, int | bool]</code> <p>Resolver availability, clear state, and successfully flushed node count.</p> Notes <p>The resolver is an optional external component. Its absence is a warning and a no-op; this addon never installs it implicitly.</p> Source code in <code>client/ayon_katana/api/usd.py</code> <pre><code>def clear_resolver_cache() -&gt; dict[str, int | bool]:\n    \"\"\"Clear the optional AYON USD Resolver and flush native UsdIn stages.\n\n    Returns:\n        Resolver availability, clear state, and successfully flushed node count.\n\n    Notes:\n        The resolver is an optional external component. Its absence is a\n        warning and a no-op; this addon never installs it implicitly.\n    \"\"\"\n    try:\n        from usdAssetResolver import AyonUsdResolver\n    except ImportError:\n        log.warning(\n            \"AYON USD Resolver is not installed; resolver cache was not cleared.\"\n        )\n        return {\n            \"resolver_available\": False,\n            \"cache_cleared\": False,\n            \"flushed_nodes\": 0,\n        }\n\n    from Katana import NodegraphAPI\n\n    AyonUsdResolver.ResolverContext().ClearCache()\n    graph_state = NodegraphAPI.GetCurrentGraphState()\n    flushed_nodes = 0\n    for node in NodegraphAPI.GetAllNodesByType(\"UsdIn\"):\n        flush_stage = getattr(node, \"flushStage\", None)\n        if flush_stage is None:\n            log.warning(\n                \"UsdIn node %s has no native flushStage method.\", node.getName()\n            )\n            continue\n        try:\n            flush_stage(node, graph_state)\n        except Exception:\n            log.warning(\n                \"Failed to flush UsdIn stage for %s.\",\n                node.getName(),\n                exc_info=True,\n            )\n            continue\n        flushed_nodes += 1\n\n    return {\n        \"resolver_available\": True,\n        \"cache_cleared\": True,\n        \"flushed_nodes\": flushed_nodes,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd.html#client.ayon_katana.api.usd.collect_schema_prim_paths","title":"<code>collect_schema_prim_paths(stage, schema)</code>","text":"<p>Return sorted prim paths matching one concrete or base USD schema.</p> Source code in <code>client/ayon_katana/api/usd.py</code> <pre><code>def collect_schema_prim_paths(stage: Any, schema: Any) -&gt; list[str]:\n    \"\"\"Return sorted prim paths matching one concrete or base USD schema.\"\"\"\n    return sorted(str(prim.GetPath()) for prim in stage.Traverse() if prim.IsA(schema))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd.html#client.ayon_katana.api.usd.configure_usd_layer_export","title":"<code>configure_usd_layer_export(node, *, file_format, time_samples, frame_start=None, frame_end=None, samples_per_frame=1.0, export_method='Keep Composition Arcs', output_path=None)</code>","text":"<p>Configure Katana's <code>UsdLayerExport</code> node.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>Any</code> <p>Katana <code>UsdLayerExport</code> node.</p> required <code>file_format</code> <code>str</code> <p>One of <code>usd</code>, <code>usda</code>, or <code>usdc</code>.</p> required <code>time_samples</code> <code>str</code> <p>Katana time-sampling mode.</p> required <code>frame_start</code> <code>Optional[float]</code> <p>First frame when using <code>Frame Range</code>.</p> <code>None</code> <code>frame_end</code> <code>Optional[float]</code> <p>Last frame when using <code>Frame Range</code>.</p> <code>None</code> <code>samples_per_frame</code> <code>float</code> <p>Positive number of time samples per frame.</p> <code>1.0</code> <code>export_method</code> <code>str</code> <p>Katana layer composition method.</p> <code>'Keep Composition Arcs'</code> <code>output_path</code> <code>Optional[str | Path]</code> <p>Optional destination assigned to <code>saveTo</code>.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>A setting is unsupported or internally inconsistent.</p> <code>RuntimeError</code> <p>A required parameter is unavailable.</p> Source code in <code>client/ayon_katana/api/usd.py</code> <pre><code>def configure_usd_layer_export(\n    node: Any,\n    *,\n    file_format: str,\n    time_samples: str,\n    frame_start: Optional[float] = None,\n    frame_end: Optional[float] = None,\n    samples_per_frame: float = 1.0,\n    export_method: str = \"Keep Composition Arcs\",\n    output_path: Optional[str | Path] = None,\n) -&gt; None:\n    \"\"\"Configure Katana's ``UsdLayerExport`` node.\n\n    Args:\n        node: Katana ``UsdLayerExport`` node.\n        file_format: One of ``usd``, ``usda``, or ``usdc``.\n        time_samples: Katana time-sampling mode.\n        frame_start: First frame when using ``Frame Range``.\n        frame_end: Last frame when using ``Frame Range``.\n        samples_per_frame: Positive number of time samples per frame.\n        export_method: Katana layer composition method.\n        output_path: Optional destination assigned to ``saveTo``.\n\n    Raises:\n        ValueError: A setting is unsupported or internally inconsistent.\n        RuntimeError: A required parameter is unavailable.\n    \"\"\"\n    normalized_format = str(file_format).lower().lstrip(\".\")\n    if normalized_format not in USD_FILE_FORMATS:\n        raise ValueError(f\"Unsupported USD file format: {file_format!r}.\")\n    if time_samples not in USD_TIME_SAMPLE_MODES:\n        raise ValueError(f\"Unsupported USD time-sampling mode: {time_samples!r}.\")\n    if export_method not in USD_EXPORT_METHODS:\n        raise ValueError(f\"Unsupported USD export method: {export_method!r}.\")\n    if float(samples_per_frame) &lt;= 0:\n        raise ValueError(\"USD samples per frame must be greater than zero.\")\n    if time_samples == \"Frame Range\":\n        if frame_start is None or frame_end is None:\n            raise ValueError(\"USD Frame Range requires start and end frames.\")\n        if float(frame_end) &lt; float(frame_start):\n            raise ValueError(f\"Invalid USD frame range: {frame_start}-{frame_end}.\")\n\n    _get_parameter(node, \"usdFileFormat\").setValue(normalized_format, 0.0)\n    _get_parameter(node, \"time.timeSamples\").setValue(time_samples, 0.0)\n    _get_parameter(node, \"time.samplesPerFrame\").setValue(float(samples_per_frame), 0.0)\n    _get_parameter(node, \"exportOptions.exportMethod\").setValue(export_method, 0.0)\n    if frame_start is not None:\n        _get_parameter(node, \"time.frameRange.i0\").setValue(float(frame_start), 0.0)\n    if frame_end is not None:\n        _get_parameter(node, \"time.frameRange.i1\").setValue(float(frame_end), 0.0)\n    if output_path is not None:\n        _get_parameter(node, \"saveTo\").setValue(Path(output_path).as_posix(), 0.0)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd.html#client.ayon_katana.api.usd.export_usd_layer","title":"<code>export_usd_layer(node, output_path)</code>","text":"<p>Write a USD layer without leaving transient path changes.</p> <p>Katana's <code>write</code> method reports some failures by returning <code>None</code> instead of raising. This helper treats both cases as publish failures and verifies that a non-empty file was produced.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>Any</code> <p>Katana <code>UsdLayerExport</code> node.</p> required <code>output_path</code> <code>str | Path</code> <p>Final path in the publish staging directory.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>Output path.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Export fails or does not produce a usable file.</p> <code>ValueError</code> <p>The output extension is not a supported USD format.</p> Source code in <code>client/ayon_katana/api/usd.py</code> <pre><code>def export_usd_layer(node: Any, output_path: str | Path) -&gt; Path:\n    \"\"\"Write a USD layer without leaving transient path changes.\n\n    Katana's ``write`` method reports some failures by returning ``None``\n    instead of raising. This helper treats both cases as publish failures and\n    verifies that a non-empty file was produced.\n\n    Args:\n        node: Katana ``UsdLayerExport`` node.\n        output_path: Final path in the publish staging directory.\n\n    Returns:\n        Output path.\n\n    Raises:\n        RuntimeError: Export fails or does not produce a usable file.\n        ValueError: The output extension is not a supported USD format.\n    \"\"\"\n    destination = Path(output_path)\n    file_format = destination.suffix.lower().lstrip(\".\")\n    if file_format not in USD_FILE_FORMATS:\n        raise ValueError(\n            f\"USD output must end in {USD_FILE_FORMATS}, got {destination.name!r}.\"\n        )\n    destination.parent.mkdir(parents=True, exist_ok=True)\n\n    save_parameter = _get_parameter(node, \"saveTo\")\n    saved_to_parameter = node.getParameter(\"_savedTo\")\n    format_parameter = _get_parameter(node, \"usdFileFormat\")\n    original_save_to = save_parameter.getValue(0.0)\n    original_saved_to = (\n        saved_to_parameter.getValue(0.0) if saved_to_parameter is not None else None\n    )\n    original_format = format_parameter.getValue(0.0)\n    try:\n        _rehydrate_usd_layer_export(node)\n        save_parameter.setValue(destination.as_posix(), 0.0)\n        format_parameter.setValue(file_format, 0.0)\n        if destination.exists():\n            destination.unlink()\n        result = node.write()\n        if result is None:\n            raise RuntimeError(\"Katana UsdLayerExport returned no success result.\")\n        if not destination.is_file():\n            raise RuntimeError(\n                f\"Katana UsdLayerExport did not create {destination.as_posix()!r}.\"\n            )\n        if destination.stat().st_size &lt;= 0:\n            raise RuntimeError(\n                f\"Katana UsdLayerExport created an empty file: {destination}.\"\n            )\n        return destination\n    except Exception:\n        if destination.exists():\n            destination.unlink()\n        raise\n    finally:\n        save_parameter.setValue(original_save_to, 0.0)\n        format_parameter.setValue(original_format, 0.0)\n        if saved_to_parameter is not None:\n            saved_to_parameter.setValue(original_saved_to, 0.0)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd.html#client.ayon_katana.api.usd.get_ayon_entity_uri_from_representation_context","title":"<code>get_ayon_entity_uri_from_representation_context(context)</code>","text":"<p>Return the AYON URI for a representation context.</p> <p>Parameters:</p> Name Type Description Default <code>context</code> <code>dict</code> <p>Loader representation context.</p> required <p>Returns:</p> Type Description <code>str</code> <p>AYON entity URI.</p> <p>Raises:</p> Type Description <code>RuntimeError</code> <p>Unable to resolve to a single valid URI.</p> Source code in <code>client/ayon_katana/api/usd.py</code> <pre><code>def get_ayon_entity_uri_from_representation_context(context: dict) -&gt; str:\n    \"\"\"Return the AYON URI for a representation context.\n\n    Args:\n        context: Loader representation context.\n\n    Returns:\n        AYON entity URI.\n\n    Raises:\n        RuntimeError: Unable to resolve to a single valid URI.\n    \"\"\"\n    project_name = context[\"project\"][\"name\"]\n    representation_id = context[\"representation\"][\"id\"]\n    response = ayon_api.post(\n        f\"projects/{project_name}/uris\",\n        entityType=\"representation\",\n        ids=[representation_id],\n    )\n    if response.status_code != 200:\n        raise RuntimeError(\n            f\"Unable to resolve AYON entity URI for {project_name!r} \"\n            f\"representation {representation_id!r}: {response.text}\"\n        )\n    uris = response.data.get(\"uris\") or []\n    if len(uris) != 1 or not uris[0].get(\"uri\"):\n        raise RuntimeError(\n            f\"Unable to resolve AYON entity URI for {project_name!r} \"\n            f\"representation {representation_id!r} to one URI: \"\n            f\"{response.data!r}\"\n        )\n    return uris[0][\"uri\"]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd.html#client.ayon_katana.api.usd.get_composed_usd_stage","title":"<code>get_composed_usd_stage(source_node)</code>","text":"<p>Return the composed USD stage for a native USD node.</p> <p>Parameters:</p> Name Type Description Default <code>source_node</code> <code>Any</code> <p>Katana node with the <code>nativeusd</code> flavor.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>Katana's pxr/fnpxr stage object.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>The source node is missing or is not a native USD node.</p> <code>RuntimeError</code> <p>Katana returns no valid stage handle or composed stage.</p> <code>Exception</code> <p>Unexpected native Katana USD API failures propagate unchanged.</p> Source code in <code>client/ayon_katana/api/usd.py</code> <pre><code>def get_composed_usd_stage(source_node: Any) -&gt; Any:\n    \"\"\"Return the composed USD stage for a native USD node.\n\n    Args:\n        source_node: Katana node with the ``nativeusd`` flavor.\n\n    Returns:\n        Katana's pxr/fnpxr stage object.\n\n    Raises:\n        ValueError: The source node is missing or is not a native USD node.\n        RuntimeError: Katana returns no valid stage handle or composed stage.\n        Exception: Unexpected native Katana USD API failures propagate unchanged.\n    \"\"\"\n    if source_node is None:\n        raise ValueError(\"A native USD source node is required.\")\n\n    node_name = source_node.getName()\n    node_type = source_node.getType()\n    if not is_native_usd_node(source_node):\n        raise ValueError(\n            f\"Katana node {node_name!r} ({node_type}) is not a native USD node.\"\n        )\n\n    from Katana import NodesUsdAPI\n\n    stage_handle = NodesUsdAPI.GetStage(source_node)\n    if stage_handle is None:\n        raise RuntimeError(f\"Katana returned no USD stage handle for {node_name!r}.\")\n\n    stage = stage_handle.getUsdStage()\n    if stage is None:\n        raise RuntimeError(f\"Katana returned no composed USD stage for {node_name!r}.\")\n    return stage\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd.html#client.ayon_katana.api.usd.get_extracted_usd_layer_path","title":"<code>get_extracted_usd_layer_path(instance_data)</code>","text":"<p>Return the single extracted USD layer path from publish instance data.</p> <p>Parameters:</p> Name Type Description Default <code>instance_data</code> <code>dict[str, Any]</code> <p>AYON publish instance data containing representations.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>Path to the single staged USD layer.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>No single-file USD representation is available.</p> Source code in <code>client/ayon_katana/api/usd.py</code> <pre><code>def get_extracted_usd_layer_path(instance_data: dict[str, Any]) -&gt; Path:\n    \"\"\"Return the single extracted USD layer path from publish instance data.\n\n    Args:\n        instance_data: AYON publish instance data containing representations.\n\n    Returns:\n        Path to the single staged USD layer.\n\n    Raises:\n        ValueError: No single-file USD representation is available.\n    \"\"\"\n    usd_extensions = {\"usd\", \"usda\", \"usdc\", \"usdlc\", \"usdnc\"}\n    candidates = []\n    for representation in instance_data.get(\"representations\") or []:\n        extension = str(representation.get(\"ext\") or \"\").lower().lstrip(\".\")\n        if extension in usd_extensions:\n            candidates.append(representation)\n\n    if not candidates:\n        raise ValueError(\"No extracted USD representation is available for validation.\")\n    if len(candidates) != 1:\n        raise ValueError(\n            \"USD validation requires exactly one extracted USD representation; \"\n            f\"found {len(candidates)}.\"\n        )\n\n    representation = candidates[0]\n    files = representation.get(\"files\")\n    if isinstance(files, (list, tuple)):\n        if len(files) != 1:\n            raise ValueError(\n                \"USD validation requires exactly one extracted USD layer file.\"\n            )\n        files = files[0]\n    if not files:\n        raise ValueError(\n            \"The extracted USD representation does not contain a layer file.\"\n        )\n\n    staging_dir = representation.get(\"stagingDir\") or instance_data.get(\"stagingDir\")\n    if not staging_dir:\n        raise ValueError(\"The extracted USD representation has no staging directory.\")\n    return Path(staging_dir) / str(files)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd.html#client.ayon_katana.api.usd.is_native_usd_node","title":"<code>is_native_usd_node(node)</code>","text":"<p>Return whether a Katana node produces native USD data.</p> <p>Katana tags native USD node types with the <code>nativeusd</code> flavor. Regular Geolib nodes can still be connected to <code>UsdLayerExport</code> at the port level, but the exporter cannot evaluate them as USD layers.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>Any</code> <p>Katana node exposing <code>getType</code>.</p> required <p>Returns:</p> Type Description <code>bool</code> <p>Whether Katana registers the node type as native USD.</p> Source code in <code>client/ayon_katana/api/usd.py</code> <pre><code>def is_native_usd_node(node: Any) -&gt; bool:\n    \"\"\"Return whether a Katana node produces native USD data.\n\n    Katana tags native USD node types with the ``nativeusd`` flavor. Regular\n    Geolib nodes can still be connected to ``UsdLayerExport`` at the port\n    level, but the exporter cannot evaluate them as USD layers.\n\n    Args:\n        node: Katana node exposing ``getType``.\n\n    Returns:\n        Whether Katana registers the node type as native USD.\n    \"\"\"\n    from Katana import NodegraphAPI\n\n    return \"nativeusd\" in NodegraphAPI.GetNodeFlavors(node.getType())\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd.html#client.ayon_katana.api.usd.open_extracted_usd_stage","title":"<code>open_extracted_usd_stage(instance_data)</code>","text":"<p>Open the single extracted USD representation as a composed stage.</p> <p>Parameters:</p> Name Type Description Default <code>instance_data</code> <code>dict[str, Any]</code> <p>AYON publish instance data containing one USD representation.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>Opened <code>Usd.Stage</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Representation metadata does not resolve to one USD layer.</p> <code>RuntimeError</code> <p>USD cannot open the extracted layer as a stage.</p> Source code in <code>client/ayon_katana/api/usd.py</code> <pre><code>def open_extracted_usd_stage(instance_data: dict[str, Any]) -&gt; Any:\n    \"\"\"Open the single extracted USD representation as a composed stage.\n\n    Args:\n        instance_data: AYON publish instance data containing one USD representation.\n\n    Returns:\n        Opened ``Usd.Stage``.\n\n    Raises:\n        ValueError: Representation metadata does not resolve to one USD layer.\n        RuntimeError: USD cannot open the extracted layer as a stage.\n    \"\"\"\n    from pxr import Tf, Usd\n\n    layer_path = get_extracted_usd_layer_path(instance_data)\n    try:\n        stage = Usd.Stage.Open(layer_path.as_posix())\n    except Tf.ErrorException as exc:\n        raise RuntimeError(f\"Failed to open extracted USD stage: {layer_path}\") from exc\n    if stage is None:\n        raise RuntimeError(f\"Failed to open extracted USD stage: {layer_path}\")\n    return stage\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd.html#client.ayon_katana.api.usd.read_usd_layer_export_settings","title":"<code>read_usd_layer_export_settings(node)</code>","text":"<p>Read publish settings from a USD export node.</p> <p>Parameters:</p> Name Type Description Default <code>node</code> <code>Any</code> <p>Katana <code>UsdLayerExport</code> node.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Normalized export settings.</p> Source code in <code>client/ayon_katana/api/usd.py</code> <pre><code>def read_usd_layer_export_settings(node: Any) -&gt; dict[str, Any]:\n    \"\"\"Read publish settings from a USD export node.\n\n    Args:\n        node: Katana ``UsdLayerExport`` node.\n\n    Returns:\n        Normalized export settings.\n    \"\"\"\n    return {\n        \"file_format\": str(_get_parameter(node, \"usdFileFormat\").getValue(0.0)).lower(),\n        \"time_samples\": str(_get_parameter(node, \"time.timeSamples\").getValue(0.0)),\n        \"frame_start\": float(_get_parameter(node, \"time.frameRange.i0\").getValue(0.0)),\n        \"frame_end\": float(_get_parameter(node, \"time.frameRange.i1\").getValue(0.0)),\n        \"samples_per_frame\": float(\n            _get_parameter(node, \"time.samplesPerFrame\").getValue(0.0)\n        ),\n        \"export_method\": str(\n            _get_parameter(node, \"exportOptions.exportMethod\").getValue(0.0)\n        ),\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_look.html","title":"usd_look","text":"<p>USD look inspection helpers for Katana publishing.</p>"},{"location":"autoapi/client/ayon_katana/api/usd_look.html#client.ayon_katana.api.usd_look.collect_disallowed_authored_look_items","title":"<code>collect_disallowed_authored_look_items(layer)</code>","text":"<p>Return authored look prims with disallowed types or composition arcs.</p> Source code in <code>client/ayon_katana/api/usd_look.py</code> <pre><code>def collect_disallowed_authored_look_items(layer: Any) -&gt; dict[str, list[str]]:\n    \"\"\"Return authored look prims with disallowed types or composition arcs.\"\"\"\n    disallowed_names = set()\n    for schema_type in DISALLOWED_LOOK_SCHEMA_TYPES:\n        disallowed_names.update(get_schema_type_names(schema_type))\n\n    invalid: dict[str, list[str]] = {}\n\n    def inspect(path: Any) -&gt; None:\n        \"\"\"Record one disallowed authored prim path and its reasons.\"\"\"\n        if not path.IsPrimPath():\n            return\n        prim_spec = layer.GetPrimAtPath(path)\n        if prim_spec is None:\n            return\n        path_string = str(path)\n        reasons = invalid.setdefault(path_string, [])\n        if prim_spec.typeName in disallowed_names:\n            reasons.append(f\"defines disallowed type {prim_spec.typeName!r}\")\n\n        references = _get_applied_items(prim_spec.referenceList)\n        if references:\n            assets = \", \".join(\n                str(item.assetPath or \"&lt;internal&gt;\") for item in references\n            )\n            reasons.append(f\"authors reference(s): {assets}\")\n\n        payloads = _get_applied_items(prim_spec.payloadList)\n        if payloads:\n            assets = \", \".join(str(item.assetPath or \"&lt;internal&gt;\") for item in payloads)\n            reasons.append(f\"authors payload(s): {assets}\")\n\n        if not reasons:\n            invalid.pop(path_string, None)\n\n    layer.Traverse(\"/\", inspect)\n    return dict(sorted(invalid.items()))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_look.html#client.ayon_katana.api.usd_look.collect_invalid_material_definitions","title":"<code>collect_invalid_material_definitions(layer, stage)</code>","text":"<p>Return authored Material prims that are overs/classes instead of defs.</p> Source code in <code>client/ayon_katana/api/usd_look.py</code> <pre><code>def collect_invalid_material_definitions(\n    layer: Any,\n    stage: Any,\n) -&gt; dict[str, str]:\n    \"\"\"Return authored Material prims that are overs/classes instead of defs.\"\"\"\n    from pxr import Sdf, UsdShade\n\n    material_type_names = set()\n    for schema_type in MATERIAL_SCHEMA_TYPES:\n        material_type_names.update(get_schema_type_names(schema_type))\n\n    specifier_names = {\n        Sdf.SpecifierDef: \"def\",\n        Sdf.SpecifierOver: \"over\",\n        Sdf.SpecifierClass: \"class\",\n    }\n    invalid: dict[str, str] = {}\n\n    def inspect(path: Any) -&gt; None:\n        \"\"\"Record one authored Material that is not a definition.\"\"\"\n        if not path.IsPrimPath():\n            return\n        prim_spec = layer.GetPrimAtPath(path)\n        if prim_spec is None:\n            return\n\n        is_material = prim_spec.typeName in material_type_names\n        if not prim_spec.typeName:\n            composed_prim = stage.GetPrimAtPath(path)\n            is_material = bool(composed_prim and composed_prim.IsA(UsdShade.Material))\n        if not is_material or prim_spec.specifier == Sdf.SpecifierDef:\n            return\n\n        invalid[str(path)] = specifier_names.get(\n            prim_spec.specifier,\n            str(prim_spec.specifier),\n        )\n\n    layer.Traverse(\"/\", inspect)\n    return dict(sorted(invalid.items()))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_look.html#client.ayon_katana.api.usd_look.collect_unbound_geometry_paths","title":"<code>collect_unbound_geometry_paths(stage)</code>","text":"<p>Return composed geometry prims without a material binding.</p> Source code in <code>client/ayon_katana/api/usd_look.py</code> <pre><code>def collect_unbound_geometry_paths(stage: Any) -&gt; list[str]:\n    \"\"\"Return composed geometry prims without a material binding.\"\"\"\n    from pxr import UsdGeom\n\n    invalid = []\n    for prim in stage.Traverse():\n        if not prim.IsA(UsdGeom.Gprim):\n            continue\n        if not has_material_binding(prim):\n            invalid.append(str(prim.GetPath()))\n    return sorted(set(invalid))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_look.html#client.ayon_katana.api.usd_look.get_composed_source_stage","title":"<code>get_composed_source_stage(instance_data)</code>","text":"<p>Return the composed source stage for a USD publish instance.</p> Source code in <code>client/ayon_katana/api/usd_look.py</code> <pre><code>def get_composed_source_stage(instance_data: dict[str, Any]) -&gt; Any:\n    \"\"\"Return the composed source stage for a USD publish instance.\"\"\"\n    from ayon_katana.api import compat\n\n    node_name = instance_data[\"instance_node\"]\n    export_node = compat.get_node(node_name)\n    if export_node is None:\n        raise ValueError(f\"Katana USD export node does not exist: {node_name!r}.\")\n    source_node = get_usd_export_source_node(export_node)\n    return get_composed_usd_stage(source_node)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_look.html#client.ayon_katana.api.usd_look.get_schema_type_names","title":"<code>get_schema_type_names(type_name)</code>","text":"<p>Return a USD schema type name and all concrete derived schema names.</p> Source code in <code>client/ayon_katana/api/usd_look.py</code> <pre><code>def get_schema_type_names(type_name: str) -&gt; set[str]:\n    \"\"\"Return a USD schema type name and all concrete derived schema names.\"\"\"\n    from pxr import Tf, Usd\n\n    schema_registry = Usd.SchemaRegistry\n    type_ = Tf.Type.FindByName(type_name)\n    if type_ == Tf.Type.Unknown:\n        type_ = schema_registry.GetTypeFromSchemaTypeName(type_name)\n        if type_ == Tf.Type.Unknown:\n            return set()\n\n    output = set()\n    for derived_type in itertools.chain([type_], type_.GetAllDerivedTypes()):\n        schema_name = schema_registry.GetSchemaTypeName(derived_type)\n        if schema_name:\n            output.add(str(schema_name))\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_look.html#client.ayon_katana.api.usd_look.get_usd_export_source_node","title":"<code>get_usd_export_source_node(export_node)</code>","text":"<p>Return the single native USD source connected to <code>UsdLayerExport</code>.</p> <p>Parameters:</p> Name Type Description Default <code>export_node</code> <code>Any</code> <p>Katana <code>UsdLayerExport</code> node.</p> required <p>Returns:</p> Type Description <code>Any</code> <p>Connected native source node.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>The export node is missing or does not have one source.</p> Source code in <code>client/ayon_katana/api/usd_look.py</code> <pre><code>def get_usd_export_source_node(export_node: Any) -&gt; Any:\n    \"\"\"Return the single native USD source connected to ``UsdLayerExport``.\n\n    Args:\n        export_node: Katana ``UsdLayerExport`` node.\n\n    Returns:\n        Connected native source node.\n\n    Raises:\n        ValueError: The export node is missing or does not have one source.\n    \"\"\"\n    if export_node is None:\n        raise ValueError(\"Katana USD look export node is missing.\")\n    input_port = export_node.getInputPort(\"in\")\n    connected_ports = (\n        list(input_port.getConnectedPorts()) if input_port is not None else []\n    )\n    if len(connected_ports) != 1:\n        raise ValueError(\n            \"UsdLayerExport requires exactly one native USD source on its 'in' port.\"\n        )\n    return connected_ports[0].getNode()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_look.html#client.ayon_katana.api.usd_look.has_material_binding","title":"<code>has_material_binding(prim)</code>","text":"<p>Return whether a geometry prim or one of its subsets has a material.</p> Source code in <code>client/ayon_katana/api/usd_look.py</code> <pre><code>def has_material_binding(prim: Any) -&gt; bool:\n    \"\"\"Return whether a geometry prim or one of its subsets has a material.\"\"\"\n    from pxr import UsdShade\n\n    search_prims = [prim]\n    binding_api = UsdShade.MaterialBindingAPI(prim)\n    for subset in binding_api.GetMaterialBindSubsets():\n        search_prims.append(subset.GetPrim())\n\n    purposes = (\n        UsdShade.Tokens.full,\n        UsdShade.Tokens.preview,\n        UsdShade.Tokens.allPurpose,\n    )\n    for purpose in purposes:\n        materials, _relationships = UsdShade.MaterialBindingAPI.ComputeBoundMaterials(\n            search_prims,\n            purpose,\n        )\n        for material in materials:\n            material_prim = material.GetPrim()\n            if material_prim and material_prim.IsValid():\n                return True\n    return False\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_look.html#client.ayon_katana.api.usd_look.is_look_instance","title":"<code>is_look_instance(instance_data)</code>","text":"<p>Return whether instance data represents a USD look.</p> Source code in <code>client/ayon_katana/api/usd_look.py</code> <pre><code>def is_look_instance(instance_data: dict[str, Any]) -&gt; bool:\n    \"\"\"Return whether instance data represents a USD look.\"\"\"\n    return (\n        instance_data.get(\"productBaseType\") == \"look\"\n        or instance_data.get(\"productType\") == \"look\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_look.html#client.ayon_katana.api.usd_look.open_extracted_look_layer","title":"<code>open_extracted_look_layer(instance_data)</code>","text":"<p>Open the staged USD look layer.</p> Source code in <code>client/ayon_katana/api/usd_look.py</code> <pre><code>def open_extracted_look_layer(instance_data: dict[str, Any]) -&gt; Any:\n    \"\"\"Open the staged USD look layer.\"\"\"\n    from pxr import Sdf\n\n    layer_path = get_extracted_usd_layer_path(instance_data)\n    layer = Sdf.Layer.FindOrOpen(layer_path.as_posix())\n    if layer is None:\n        raise RuntimeError(f\"Failed to open extracted USD look layer: {layer_path}\")\n    return layer\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_publish.html","title":"usd_publish","text":"<p>Finalize staged Katana USD layers without modifying live source layers.</p>"},{"location":"autoapi/client/ayon_katana/api/usd_publish.html#client.ayon_katana.api.usd_publish.build_publish_path_map","title":"<code>build_publish_path_map(context)</code>","text":"<p>Map all active staged representation files to Core-computed publish paths.</p> Source code in <code>client/ayon_katana/api/usd_publish.py</code> <pre><code>def build_publish_path_map(context: Any) -&gt; dict[str, str]:\n    \"\"\"Map all active staged representation files to Core-computed publish paths.\"\"\"\n    from ayon_core.pipeline.publish.lib import get_instance_expected_output_path\n\n    mapping: dict[str, str] = {}\n    for instance in context:\n        if not instance.data.get(\"active\", True) or not instance.data.get(\n            \"publish\", True\n        ):\n            continue\n        for representation in instance.data.get(\"representations\") or []:\n            name = representation.get(\"name\")\n            if not name:\n                continue\n            extension = representation.get(\"ext\")\n            expected_path = Path(\n                get_instance_expected_output_path(\n                    instance,\n                    representation_name=name,\n                    ext=extension,\n                )\n            )\n            sources = get_representation_source_paths(instance, representation)\n            if len(sources) == 1:\n                destinations = [expected_path]\n            else:\n                destinations = [\n                    expected_path.parent / Path(source).name for source in sources\n                ]\n            for source, destination in zip(sources, destinations):\n                mapping[_mapping_key(source)] = _usd_path(destination)\n    return mapping\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_publish.html#client.ayon_katana.api.usd_publish.build_source_composition_anchors","title":"<code>build_source_composition_anchors(source_stage)</code>","text":"<p>Map source-stage relative composition paths to their original resolutions.</p> Source code in <code>client/ayon_katana/api/usd_publish.py</code> <pre><code>def build_source_composition_anchors(source_stage: Any) -&gt; dict[str, set[str]]:\n    \"\"\"Map source-stage relative composition paths to their original resolutions.\"\"\"\n    if source_stage is None:\n        return {}\n    layers = list(source_stage.GetLayerStack())\n\n    anchors: dict[str, set[str]] = {}\n    for layer in layers:\n        for authored in _iter_layer_composition_paths(layer):\n            filesystem_part = _asset_filesystem_part(authored)\n            if not filesystem_part or Path(filesystem_part).is_absolute():\n                continue\n            if filesystem_part.startswith(\"anon:\"):\n                continue\n            resolved = _resolve_asset_against_layer(layer, authored)\n            if resolved:\n                anchors.setdefault(_mapping_key(filesystem_part), set()).add(resolved)\n    return anchors\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_publish.html#client.ayon_katana.api.usd_publish.get_representation_source_paths","title":"<code>get_representation_source_paths(instance, representation)</code>","text":"<p>Return absolute staged source paths for one representation.</p> Source code in <code>client/ayon_katana/api/usd_publish.py</code> <pre><code>def get_representation_source_paths(instance: Any, representation: dict) -&gt; list[str]:\n    \"\"\"Return absolute staged source paths for one representation.\"\"\"\n    staging_dir = representation.get(\"stagingDir\") or instance.data.get(\"stagingDir\")\n    files = representation.get(\"files_raw\", representation.get(\"files\"))\n    if not files:\n        return []\n    if isinstance(files, str):\n        files = [files]\n    elif not isinstance(files, (list, tuple)):\n        raise ValueError(\n            \"Representation files must be a string, list, or tuple for USD remapping.\"\n        )\n\n    output = []\n    for filename in files:\n        filename = str(filename)\n        filesystem_part = _asset_filesystem_part(filename)\n        if Path(filesystem_part).is_absolute():\n            source = filesystem_part\n        elif staging_dir:\n            source = str(Path(staging_dir) / filesystem_part)\n        else:\n            raise ValueError(\n                \"Representation has relative files but no staging directory: \"\n                f\"{representation!r}\"\n            )\n        output.append(os.path.normpath(source))\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_publish.html#client.ayon_katana.api.usd_publish.rewrite_staged_usd_layer","title":"<code>rewrite_staged_usd_layer(staged_layer_path, *, publish_mapping, asset_remap=None, source_stage=None)</code>","text":"<p>Rewrite a staged USD layer atomically.</p> Source code in <code>client/ayon_katana/api/usd_publish.py</code> <pre><code>def rewrite_staged_usd_layer(\n    staged_layer_path: str | Path,\n    *,\n    publish_mapping: dict[str, str],\n    asset_remap: dict[str, str] | None = None,\n    source_stage: Any = None,\n) -&gt; Path:\n    \"\"\"Rewrite a staged USD layer atomically.\"\"\"\n    from pxr import Sdf, UsdUtils\n\n    staged_path = Path(staged_layer_path)\n    source_layer = Sdf.Layer.FindOrOpen(_usd_path(staged_path))\n    if source_layer is None:\n        raise ValueError(f\"Failed to open staged USD layer: {staged_path}\")\n\n    source_anchors = build_source_composition_anchors(source_stage)\n    copied_layer = Sdf.Layer.CreateAnonymous(f\"ayon_finalize_{staged_path.name}\")\n    copied_layer.TransferContent(source_layer)\n\n    def rewrite(authored: str) -&gt; str:\n        \"\"\"Rewrite one asset path.\"\"\"\n        return _rewrite_asset_path(\n            str(authored),\n            staged_layer_path=staged_path,\n            publish_mapping=publish_mapping,\n            asset_remap=asset_remap or {},\n            source_anchors=source_anchors,\n        )\n\n    UsdUtils.ModifyAssetPaths(copied_layer, rewrite)\n\n    temporary_path = staged_path.with_name(\n        f\".{staged_path.stem}.ayon_finalize{staged_path.suffix}\"\n    )\n    if temporary_path.exists():\n        temporary_path.unlink()\n    try:\n        export_args = {}\n        if staged_path.suffix.lower() == \".usd\":\n            format_id = str(source_layer.GetFileFormat().formatId or \"\")\n            if format_id in {\"usda\", \"usdc\"}:\n                export_args[\"format\"] = format_id\n        exported = copied_layer.Export(_usd_path(temporary_path), args=export_args)\n        if exported is False or not temporary_path.is_file():\n            raise RuntimeError(\n                f\"Failed to export finalized USD layer: {temporary_path}\"\n            )\n        if temporary_path.stat().st_size &lt;= 0:\n            raise RuntimeError(f\"Finalized USD layer is empty: {temporary_path}\")\n        verified = Sdf.Layer.FindOrOpen(_usd_path(temporary_path))\n        if verified is None:\n            raise RuntimeError(\n                f\"Failed to reopen finalized USD layer: {temporary_path}\"\n            )\n        os.replace(temporary_path, staged_path)\n    except Exception:\n        if temporary_path.exists():\n            temporary_path.unlink()\n        raise\n    return staged_path\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/usd_resources.html","title":"usd_resources","text":"<p>Plan resource transfers for extracted Katana USD look layers.</p>"},{"location":"autoapi/client/ayon_katana/api/usd_resources.html#client.ayon_katana.api.usd_resources.plan_look_resources","title":"<code>plan_look_resources(layer, resources_dir, source_stage)</code>","text":"<p>Build resource, remap, and transfer data for a USD look layer.</p> <p>Parameters:</p> Name Type Description Default <code>layer</code> <code>Any</code> <p>Extracted look Sdf layer.</p> required <code>resources_dir</code> <code>str | Path</code> <p>Final publish resources directory.</p> required <code>source_stage</code> <code>Any</code> <p>Original composed source stage used to anchor staged relative asset values.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dictionary containing <code>resources</code>, <code>assetRemap</code>, and <code>transfers</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>A resource is remote, missing, ambiguously anchored, or collides with another resource destination.</p> Source code in <code>client/ayon_katana/api/usd_resources.py</code> <pre><code>def plan_look_resources(\n    layer: Any,\n    resources_dir: str | Path,\n    source_stage: Any,\n) -&gt; dict[str, Any]:\n    \"\"\"Build resource, remap, and transfer data for a USD look layer.\n\n    Args:\n        layer: Extracted look Sdf layer.\n        resources_dir: Final publish resources directory.\n        source_stage: Original composed source stage used to anchor staged\n            relative asset values.\n\n    Returns:\n        Dictionary containing ``resources``, ``assetRemap``, and ``transfers``.\n\n    Raises:\n        ValueError: A resource is remote, missing, ambiguously anchored, or\n            collides with another resource destination.\n    \"\"\"\n    resources_directory = Path(resources_dir)\n    source_anchors = _build_source_anchor_map(source_stage)\n    resources = []\n    remap: dict[str, str] = {}\n    transfers = []\n    transfer_sources = set()\n    destination_sources: dict[str, str] = {}\n    resource_keys = set()\n\n    for path in _iter_property_paths(layer):\n        spec = layer.GetAttributeAtPath(path)\n        if spec is None or \"asset\" not in str(spec.typeName):\n            continue\n        attribute_path = _path_string(path)\n        colorspace = _get_colorspace(spec)\n        for asset in _iter_asset_values(layer, path, spec):\n            authored = _asset_path(asset).replace(\"\\\\\", \"/\")\n            if not authored:\n                continue\n            if Path(authored).suffix.lower() in COMPOSITION_EXTENSIONS:\n                continue\n            if _URI_PATTERN.match(authored):\n                raise ValueError(\n                    f\"USD look resource {attribute_path} uses unsupported remote URI: \"\n                    f\"{authored}\"\n                )\n\n            if Path(authored).is_absolute():\n                resolved = os.path.normpath(authored)\n            else:\n                candidates = source_anchors.get((attribute_path, authored), set())\n                if len(candidates) &gt; 1:\n                    formatted = \", \".join(sorted(candidates))\n                    raise ValueError(\n                        \"USD look resource \"\n                        f\"{attribute_path} is ambiguous: {authored!r} \"\n                        f\"resolves to multiple source files: {formatted}\"\n                    )\n                if candidates:\n                    resolved = next(iter(candidates))\n                else:\n                    resolved = _resolve_relative_asset(\n                        layer,\n                        authored,\n                        _resolved_asset_hint(asset),\n                    )\n            if not resolved:\n                raise ValueError(\n                    f\"Unable to resolve USD look resource {attribute_path}: {authored}\"\n                )\n\n            files = _expand_local_asset(resolved)\n            if not files:\n                raise ValueError(\n                    f\"USD look resource {attribute_path} does not resolve to local \"\n                    f\"files: {authored} -&gt; {resolved}\"\n                )\n\n            remap_path = f\"./resources/{Path(authored).name}\"\n            remap[os.path.normpath(authored)] = remap_path\n            resource_key = (attribute_path, authored, tuple(files), colorspace)\n            if resource_key not in resource_keys:\n                resource_keys.add(resource_key)\n                resources.append(\n                    {\n                        \"attribute\": attribute_path,\n                        \"source\": authored,\n                        \"files\": files,\n                        \"color_space\": colorspace or None,\n                    }\n                )\n\n            for source in files:\n                canonical_source = os.path.normcase(os.path.abspath(source))\n                destination = resources_directory / Path(source).name\n                destination_key = os.path.normcase(str(destination))\n                previous_source = destination_sources.get(destination_key)\n                if previous_source is not None and previous_source != canonical_source:\n                    raise ValueError(\n                        \"USD look resources contain a destination basename collision: \"\n                        f\"{Path(source).name!r} maps from both {previous_source!r} \"\n                        f\"and {canonical_source!r}.\"\n                    )\n                destination_sources[destination_key] = canonical_source\n                if canonical_source in transfer_sources:\n                    continue\n                transfer_sources.add(canonical_source)\n                transfers.append((source, str(destination)))\n\n    resources.sort(key=lambda item: (item[\"source\"], item[\"attribute\"]))\n    transfers.sort(key=lambda item: (item[1], item[0]))\n    return {\n        \"resources\": resources,\n        \"assetRemap\": dict(sorted(remap.items())),\n        \"transfers\": transfers,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html","title":"workfile_template_builder","text":"<p>AYON Workfile Builder integration for Katana.</p>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaPlaceholderPlugin","title":"<code>KatanaPlaceholderPlugin</code>","text":"<p>               Bases: <code>PlaceholderPlugin</code></p> <p>Provide shared Katana behavior for Workfile Builder placeholders.</p> <p>Inherited classes must still implement <code>populate_placeholder</code>.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>class KatanaPlaceholderPlugin(PlaceholderPlugin):\n    \"\"\"Provide shared Katana behavior for Workfile Builder placeholders.\n\n    Inherited classes must still implement ``populate_placeholder``.\n    \"\"\"\n\n    def get_placeholder_node_name(self, placeholder_data) -&gt; str:\n        \"\"\"Return a stable Katana node name for placeholder metadata.\"\"\"\n        product_name = placeholder_data.get(\"product_name\") or \"product\"\n        return lib.sanitize_node_name(\n            f\"AYON_PLACEHOLDER_{product_name}\",\n            \"AYON_PLACEHOLDER\",\n        )\n\n    def create_placeholder_node(self, node_name=None):\n        \"\"\"Create node to be used as placeholder.\"\"\"\n        placeholder_node = NodegraphAPI.CreateNode(\"Group\", NodegraphAPI.GetRootNode())\n        placeholder_node.setName(node_name)\n        placeholder_node.addOutputPort(\"out\")\n        self.ensure_combine_node(placeholder_node)\n        return placeholder_node\n\n    def create_placeholder(self, placeholder_data):\n        \"\"\"Create and imprint a Workfile Builder placeholder node.\"\"\"\n        data = dict(placeholder_data)\n        data[\"plugin_identifier\"] = self.identifier\n        node_name = self.get_placeholder_node_name(data)\n        placeholder_node = self.create_placeholder_node(node_name)\n        self._imprint(placeholder_node, data)\n        return placeholder_node\n\n    def update_placeholder(self, placeholder_item, placeholder_data):\n        \"\"\"Update a placeholder node and its in-memory item.\"\"\"\n        placeholder_node = _node_from_identifier(placeholder_item.scene_identifier)\n        if placeholder_node is None:\n            raise RuntimeError(\n                f\"Katana placeholder no longer exists: \"\n                f\"{placeholder_item.scene_identifier}\"\n            )\n\n        data = dict(placeholder_data)\n        data[\"plugin_identifier\"] = self.identifier\n        self._imprint(placeholder_node, data, update=True)\n        placeholder_node.setName(self.get_placeholder_node_name(data))\n        placeholder_item.data.clear()\n        placeholder_item.data.update(data)\n\n    def collect_scene_placeholders(self) -&gt; list:\n        \"\"\"Return cached placeholder Group nodes from the current project.\"\"\"\n        cached_nodes = self.builder.get_shared_populate_data(\"placeholder_nodes\")\n        if cached_nodes is not None:\n            return cached_nodes\n\n        placeholder_nodes = []\n        for node in NodegraphAPI.GetAllNodesByType(\"Group\"):\n            data = self._read(node)\n            if data:\n                placeholder_nodes.append(node)\n        self.builder.set_shared_populate_data(\"placeholder_nodes\", placeholder_nodes)\n        return placeholder_nodes\n\n    def _imprint(self, placeholder_node, placeholder_data, update=False):\n        data = dict(placeholder_data)\n        data.setdefault(\"schema\", _PLACEHOLDER_SCHEMA)\n        lib.write_json_parameter(\n            placeholder_node,\n            _PLACEHOLDER_PARAMETER,\n            data,\n            skip_unchanged=update,\n        )\n\n    def _read(self, placeholder_node) -&gt; Optional[dict[str, Any]]:\n        \"\"\"Read placeholder metadata from a Katana Group.\"\"\"\n        return lib.read_json_parameter(\n            placeholder_node,\n            _PLACEHOLDER_PARAMETER,\n        )\n\n    def clear_placeholder(self, placeholder_node) -&gt; None:\n        \"\"\"Remove placeholder semantics while keeping its graph Group.\n\n        Args:\n            placeholder_node: Placeholder Group to consume.\n        \"\"\"\n        lib.set_string_parameter(placeholder_node, _PLACEHOLDER_PARAMETER, \"\")\n        name = placeholder_node.getName()\n        if \"PLACEHOLDER\" in name:\n            name = name.replace(\"PLACEHOLDER\", \"LOADED\", 1)\n        else:\n            name = f\"{name}_LOADED\"\n        placeholder_node.setName(\n            lib.sanitize_node_name(name, \"AYON_PLACEHOLDER_LOADED\")\n        )\n\n    def ensure_combine_node(self, placeholder_node):\n        \"\"\"Return the Merge node combining loaded placeholder products.\n\n        Args:\n            placeholder_node: Placeholder Group.\n\n        Returns:\n            Native Katana Merge node.\n        \"\"\"\n        for child in placeholder_node.getChildren():\n            role = lib.get_string_parameter(child, _PLACEHOLDER_ROLE_PARAMETER)\n            if role == _COMBINE_ROLE:\n                return child\n\n        combine_node = NodegraphAPI.CreateNode(\"Merge\", placeholder_node)\n        combine_node.setName(_COMBINE_NAME)\n        lib.set_string_parameter(\n            combine_node,\n            _PLACEHOLDER_ROLE_PARAMETER,\n            _COMBINE_ROLE,\n        )\n        combine_node.getOutputPort(\"out\").connect(placeholder_node.getReturnPort(\"out\"))\n        return combine_node\n\n    def connect_loaded_container(self, placeholder_node, container_node) -&gt; None:\n        \"\"\"Move a loaded container into a placeholder and connect its output.\n\n        Args:\n            placeholder_node: Destination placeholder Group.\n            container_node: AYON container Group returned by a loader.\n        \"\"\"\n        combine_node = self.ensure_combine_node(placeholder_node)\n        compat.set_parent(container_node, placeholder_node)\n\n        input_port = next(\n            (\n                port\n                for port in combine_node.getInputPorts()\n                if not port.getConnectedPorts()\n            ),\n            None,\n        )\n        if input_port is None:\n            input_port = combine_node.addInputPort(\n                f\"i{len(combine_node.getInputPorts())}\"\n            )\n        container_node.getOutputPort(\"out\").connect(input_port)\n\n        index = len(self.get_loaded_containers(placeholder_node)) - 1\n        NodegraphAPI.SetNodePosition(container_node, (-250.0, index * 120.0))\n        NodegraphAPI.SetNodePosition(combine_node, (100.0, 0.0))\n\n    def get_loaded_containers(self, placeholder_node) -&gt; list:\n        \"\"\"Return AYON containers nested beneath a placeholder Group.\n\n        Args:\n            placeholder_node: Placeholder Group.\n\n        Returns:\n            Parsed AYON container dictionaries.\n        \"\"\"\n        loaded_containers = []\n        for node in NodegraphAPI.GetAllNodesByType(\"Group\"):\n            if not compat.is_descendant(node, placeholder_node):\n                continue\n            container = containers.parse_container(node)\n            if container is not None:\n                loaded_containers.append(container)\n        return loaded_containers\n\n    def get_loaded_representation_ids(self, placeholder_node) -&gt; set[str]:\n        \"\"\"Return representation IDs already loaded by a placeholder.\n\n        Args:\n            placeholder_node: Placeholder Group.\n\n        Returns:\n            Set of AYON representation IDs.\n        \"\"\"\n        return {\n            container[\"representation\"]\n            for container in self.get_loaded_containers(placeholder_node)\n            if container.get(\"representation\")\n        }\n\n    def delete_placeholder(self, placeholder) -&gt; None:\n        \"\"\"Consume or delete a completed placeholder.\n\n        Loaded containers remain beneath a stable Group so external template\n        connections are preserved. Empty placeholders are deleted normally.\n\n        Args:\n            placeholder: AYON placeholder item.\n        \"\"\"\n        placeholder_node = _node_from_identifier(placeholder.scene_identifier)\n        if placeholder_node is None:\n            return\n        if self.get_loaded_containers(placeholder_node):\n            self.clear_placeholder(placeholder_node)\n            return\n        placeholder_node.delete()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaPlaceholderPlugin.clear_placeholder","title":"<code>clear_placeholder(placeholder_node)</code>","text":"<p>Remove placeholder semantics while keeping its graph Group.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder_node</code> <p>Placeholder Group to consume.</p> required Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def clear_placeholder(self, placeholder_node) -&gt; None:\n    \"\"\"Remove placeholder semantics while keeping its graph Group.\n\n    Args:\n        placeholder_node: Placeholder Group to consume.\n    \"\"\"\n    lib.set_string_parameter(placeholder_node, _PLACEHOLDER_PARAMETER, \"\")\n    name = placeholder_node.getName()\n    if \"PLACEHOLDER\" in name:\n        name = name.replace(\"PLACEHOLDER\", \"LOADED\", 1)\n    else:\n        name = f\"{name}_LOADED\"\n    placeholder_node.setName(\n        lib.sanitize_node_name(name, \"AYON_PLACEHOLDER_LOADED\")\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaPlaceholderPlugin.collect_scene_placeholders","title":"<code>collect_scene_placeholders()</code>","text":"<p>Return cached placeholder Group nodes from the current project.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def collect_scene_placeholders(self) -&gt; list:\n    \"\"\"Return cached placeholder Group nodes from the current project.\"\"\"\n    cached_nodes = self.builder.get_shared_populate_data(\"placeholder_nodes\")\n    if cached_nodes is not None:\n        return cached_nodes\n\n    placeholder_nodes = []\n    for node in NodegraphAPI.GetAllNodesByType(\"Group\"):\n        data = self._read(node)\n        if data:\n            placeholder_nodes.append(node)\n    self.builder.set_shared_populate_data(\"placeholder_nodes\", placeholder_nodes)\n    return placeholder_nodes\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaPlaceholderPlugin.connect_loaded_container","title":"<code>connect_loaded_container(placeholder_node, container_node)</code>","text":"<p>Move a loaded container into a placeholder and connect its output.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder_node</code> <p>Destination placeholder Group.</p> required <code>container_node</code> <p>AYON container Group returned by a loader.</p> required Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def connect_loaded_container(self, placeholder_node, container_node) -&gt; None:\n    \"\"\"Move a loaded container into a placeholder and connect its output.\n\n    Args:\n        placeholder_node: Destination placeholder Group.\n        container_node: AYON container Group returned by a loader.\n    \"\"\"\n    combine_node = self.ensure_combine_node(placeholder_node)\n    compat.set_parent(container_node, placeholder_node)\n\n    input_port = next(\n        (\n            port\n            for port in combine_node.getInputPorts()\n            if not port.getConnectedPorts()\n        ),\n        None,\n    )\n    if input_port is None:\n        input_port = combine_node.addInputPort(\n            f\"i{len(combine_node.getInputPorts())}\"\n        )\n    container_node.getOutputPort(\"out\").connect(input_port)\n\n    index = len(self.get_loaded_containers(placeholder_node)) - 1\n    NodegraphAPI.SetNodePosition(container_node, (-250.0, index * 120.0))\n    NodegraphAPI.SetNodePosition(combine_node, (100.0, 0.0))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaPlaceholderPlugin.create_placeholder","title":"<code>create_placeholder(placeholder_data)</code>","text":"<p>Create and imprint a Workfile Builder placeholder node.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def create_placeholder(self, placeholder_data):\n    \"\"\"Create and imprint a Workfile Builder placeholder node.\"\"\"\n    data = dict(placeholder_data)\n    data[\"plugin_identifier\"] = self.identifier\n    node_name = self.get_placeholder_node_name(data)\n    placeholder_node = self.create_placeholder_node(node_name)\n    self._imprint(placeholder_node, data)\n    return placeholder_node\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaPlaceholderPlugin.create_placeholder_node","title":"<code>create_placeholder_node(node_name=None)</code>","text":"<p>Create node to be used as placeholder.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def create_placeholder_node(self, node_name=None):\n    \"\"\"Create node to be used as placeholder.\"\"\"\n    placeholder_node = NodegraphAPI.CreateNode(\"Group\", NodegraphAPI.GetRootNode())\n    placeholder_node.setName(node_name)\n    placeholder_node.addOutputPort(\"out\")\n    self.ensure_combine_node(placeholder_node)\n    return placeholder_node\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaPlaceholderPlugin.delete_placeholder","title":"<code>delete_placeholder(placeholder)</code>","text":"<p>Consume or delete a completed placeholder.</p> <p>Loaded containers remain beneath a stable Group so external template connections are preserved. Empty placeholders are deleted normally.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder</code> <p>AYON placeholder item.</p> required Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def delete_placeholder(self, placeholder) -&gt; None:\n    \"\"\"Consume or delete a completed placeholder.\n\n    Loaded containers remain beneath a stable Group so external template\n    connections are preserved. Empty placeholders are deleted normally.\n\n    Args:\n        placeholder: AYON placeholder item.\n    \"\"\"\n    placeholder_node = _node_from_identifier(placeholder.scene_identifier)\n    if placeholder_node is None:\n        return\n    if self.get_loaded_containers(placeholder_node):\n        self.clear_placeholder(placeholder_node)\n        return\n    placeholder_node.delete()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaPlaceholderPlugin.ensure_combine_node","title":"<code>ensure_combine_node(placeholder_node)</code>","text":"<p>Return the Merge node combining loaded placeholder products.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder_node</code> <p>Placeholder Group.</p> required <p>Returns:</p> Type Description <p>Native Katana Merge node.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def ensure_combine_node(self, placeholder_node):\n    \"\"\"Return the Merge node combining loaded placeholder products.\n\n    Args:\n        placeholder_node: Placeholder Group.\n\n    Returns:\n        Native Katana Merge node.\n    \"\"\"\n    for child in placeholder_node.getChildren():\n        role = lib.get_string_parameter(child, _PLACEHOLDER_ROLE_PARAMETER)\n        if role == _COMBINE_ROLE:\n            return child\n\n    combine_node = NodegraphAPI.CreateNode(\"Merge\", placeholder_node)\n    combine_node.setName(_COMBINE_NAME)\n    lib.set_string_parameter(\n        combine_node,\n        _PLACEHOLDER_ROLE_PARAMETER,\n        _COMBINE_ROLE,\n    )\n    combine_node.getOutputPort(\"out\").connect(placeholder_node.getReturnPort(\"out\"))\n    return combine_node\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaPlaceholderPlugin.get_loaded_containers","title":"<code>get_loaded_containers(placeholder_node)</code>","text":"<p>Return AYON containers nested beneath a placeholder Group.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder_node</code> <p>Placeholder Group.</p> required <p>Returns:</p> Type Description <code>list</code> <p>Parsed AYON container dictionaries.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def get_loaded_containers(self, placeholder_node) -&gt; list:\n    \"\"\"Return AYON containers nested beneath a placeholder Group.\n\n    Args:\n        placeholder_node: Placeholder Group.\n\n    Returns:\n        Parsed AYON container dictionaries.\n    \"\"\"\n    loaded_containers = []\n    for node in NodegraphAPI.GetAllNodesByType(\"Group\"):\n        if not compat.is_descendant(node, placeholder_node):\n            continue\n        container = containers.parse_container(node)\n        if container is not None:\n            loaded_containers.append(container)\n    return loaded_containers\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaPlaceholderPlugin.get_loaded_representation_ids","title":"<code>get_loaded_representation_ids(placeholder_node)</code>","text":"<p>Return representation IDs already loaded by a placeholder.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder_node</code> <p>Placeholder Group.</p> required <p>Returns:</p> Type Description <code>set[str]</code> <p>Set of AYON representation IDs.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def get_loaded_representation_ids(self, placeholder_node) -&gt; set[str]:\n    \"\"\"Return representation IDs already loaded by a placeholder.\n\n    Args:\n        placeholder_node: Placeholder Group.\n\n    Returns:\n        Set of AYON representation IDs.\n    \"\"\"\n    return {\n        container[\"representation\"]\n        for container in self.get_loaded_containers(placeholder_node)\n        if container.get(\"representation\")\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaPlaceholderPlugin.get_placeholder_node_name","title":"<code>get_placeholder_node_name(placeholder_data)</code>","text":"<p>Return a stable Katana node name for placeholder metadata.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def get_placeholder_node_name(self, placeholder_data) -&gt; str:\n    \"\"\"Return a stable Katana node name for placeholder metadata.\"\"\"\n    product_name = placeholder_data.get(\"product_name\") or \"product\"\n    return lib.sanitize_node_name(\n        f\"AYON_PLACEHOLDER_{product_name}\",\n        \"AYON_PLACEHOLDER\",\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaPlaceholderPlugin.update_placeholder","title":"<code>update_placeholder(placeholder_item, placeholder_data)</code>","text":"<p>Update a placeholder node and its in-memory item.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def update_placeholder(self, placeholder_item, placeholder_data):\n    \"\"\"Update a placeholder node and its in-memory item.\"\"\"\n    placeholder_node = _node_from_identifier(placeholder_item.scene_identifier)\n    if placeholder_node is None:\n        raise RuntimeError(\n            f\"Katana placeholder no longer exists: \"\n            f\"{placeholder_item.scene_identifier}\"\n        )\n\n    data = dict(placeholder_data)\n    data[\"plugin_identifier\"] = self.identifier\n    self._imprint(placeholder_node, data, update=True)\n    placeholder_node.setName(self.get_placeholder_node_name(data))\n    placeholder_item.data.clear()\n    placeholder_item.data.update(data)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaTemplateBuilder","title":"<code>KatanaTemplateBuilder</code>","text":"<p>               Bases: <code>AbstractTemplateBuilder</code></p> <p>Implement the AYON Workfile Builder contract for Katana.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>class KatanaTemplateBuilder(AbstractTemplateBuilder):\n    \"\"\"Implement the AYON Workfile Builder contract for Katana.\"\"\"\n\n    def import_template(self, path):\n        \"\"\"Import a Katana template into the current project.\n\n        Args:\n            path: Template ``.katana`` path.\n\n        Returns:\n            ``True`` when Katana imported the template.\n        \"\"\"\n        try:\n            compat.import_katana_file(path, float_nodes=False)\n        except (OSError, RuntimeError, ValueError):\n            log.warning(\"Failed to import Katana template: %s\", path, exc_info=True)\n            return False\n        return True\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.KatanaTemplateBuilder.import_template","title":"<code>import_template(path)</code>","text":"<p>Import a Katana template into the current project.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <p>Template <code>.katana</code> path.</p> required <p>Returns:</p> Type Description <p><code>True</code> when Katana imported the template.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def import_template(self, path):\n    \"\"\"Import a Katana template into the current project.\n\n    Args:\n        path: Template ``.katana`` path.\n\n    Returns:\n        ``True`` when Katana imported the template.\n    \"\"\"\n    try:\n        compat.import_katana_file(path, float_nodes=False)\n    except (OSError, RuntimeError, ValueError):\n        log.warning(\"Failed to import Katana template: %s\", path, exc_info=True)\n        return False\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.build_workfile_template","title":"<code>build_workfile_template(*args, **kwargs)</code>","text":"<p>Build the matching template explicitly in the current scene.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when a valid matching template was built.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def build_workfile_template(*args, **kwargs) -&gt; bool:\n    \"\"\"Build the matching template explicitly in the current scene.\n\n    Returns:\n        ``True`` when a valid matching template was built.\n    \"\"\"\n    builder = KatanaTemplateBuilder(registered_host())\n    try:\n        preset = builder.get_template_preset()\n    except TemplateProfileNotFound:\n        log.info(\"No Katana Workfile Builder profile matches the current task.\")\n        return False\n    if not preset.has_valid_path():\n        log.warning(\n            \"Katana Workfile Builder template path is unavailable: %s\",\n            preset.path,\n        )\n        return False\n    builder.build_template(preset=preset)\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.create_placeholder","title":"<code>create_placeholder(*args)</code>","text":"<p>Open the Workfile Builder dialog in create-placeholder mode.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def create_placeholder(*args):\n    \"\"\"Open the Workfile Builder dialog in create-placeholder mode.\"\"\"\n    from ayon_core.tools.workfile_template_build import (\n        WorkfileBuildPlaceholderDialog,\n    )\n\n    from .menu import get_main_window\n\n    host = registered_host()\n    builder = KatanaTemplateBuilder(host)\n    window = WorkfileBuildPlaceholderDialog(host, builder, parent=get_main_window())\n    window.show()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.open_workfile_template","title":"<code>open_workfile_template(*args)</code>","text":"<p>Open the matching template after Core's destructive-action prompt.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def open_workfile_template(*args) -&gt; None:\n    \"\"\"Open the matching template after Core's destructive-action prompt.\"\"\"\n    from ayon_core.tools.workfile_template_build import open_template_ui\n\n    from .menu import get_main_window\n\n    builder = KatanaTemplateBuilder(registered_host())\n    open_template_ui(builder, get_main_window())\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.trigger_on_app_launch","title":"<code>trigger_on_app_launch()</code>","text":"<p>Run the Core Workfile Builder application-launch trigger.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when a matching profile was evaluated, otherwise <code>False</code>.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def trigger_on_app_launch() -&gt; bool:\n    \"\"\"Run the Core Workfile Builder application-launch trigger.\n\n    Returns:\n        ``True`` when a matching profile was evaluated, otherwise ``False``.\n    \"\"\"\n    builder = KatanaTemplateBuilder(registered_host())\n    try:\n        builder.trigger_on_app_launch()\n    except TemplateProfileNotFound:\n        log.info(\"No Katana Workfile Builder profile matches the current task.\")\n        return False\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.trigger_on_new_file","title":"<code>trigger_on_new_file()</code>","text":"<p>Run the Core Workfile Builder new-file trigger.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> when a matching profile was evaluated, otherwise <code>False</code>.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def trigger_on_new_file() -&gt; bool:\n    \"\"\"Run the Core Workfile Builder new-file trigger.\n\n    Returns:\n        ``True`` when a matching profile was evaluated, otherwise ``False``.\n    \"\"\"\n    builder = KatanaTemplateBuilder(registered_host())\n    try:\n        builder.trigger_on_new_file()\n    except TemplateProfileNotFound:\n        log.info(\"No Katana Workfile Builder profile matches the current task.\")\n        return False\n    return True\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.update_placeholder","title":"<code>update_placeholder(*args)</code>","text":"<p>Open the Workfile Builder dialog for one selected placeholder.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def update_placeholder(*args):\n    \"\"\"Open the Workfile Builder dialog for one selected placeholder.\"\"\"\n    from ayon_core.tools.utils import show_message_dialog\n    from ayon_core.tools.workfile_template_build import (\n        WorkfileBuildPlaceholderDialog,\n    )\n\n    from .menu import get_main_window\n\n    host = registered_host()\n    builder = KatanaTemplateBuilder(host)\n    placeholder_items_by_id = {\n        placeholder_item.scene_identifier: placeholder_item\n        for placeholder_item in builder.get_placeholders()\n    }\n    placeholder_items = []\n    for node in compat.get_selected_nodes():\n        if node.getName() in placeholder_items_by_id:\n            placeholder_items.append(placeholder_items_by_id[node.getName()])\n\n    parent = get_main_window()\n    if len(placeholder_items) == 0:\n        show_message_dialog(\n            \"Workfile Placeholder Manager\",\n            \"Select one Katana placeholder Group.\",\n            \"warning\",\n            parent,\n        )\n        return\n    if len(placeholder_items) &gt; 1:\n        show_message_dialog(\n            \"Workfile Placeholder Manager\",\n            \"Select only one Katana placeholder Group.\",\n            \"warning\",\n            parent,\n        )\n        return\n\n    placeholder_item = placeholder_items[0]\n    window = WorkfileBuildPlaceholderDialog(host, builder, parent=parent)\n    window.set_update_mode(placeholder_item)\n    window.exec_()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workfile_template_builder.html#client.ayon_katana.api.workfile_template_builder.update_workfile_template","title":"<code>update_workfile_template(*args)</code>","text":"<p>Rebuild the current project from its matching template profile.</p> Source code in <code>client/ayon_katana/api/workfile_template_builder.py</code> <pre><code>def update_workfile_template(*args):\n    \"\"\"Rebuild the current project from its matching template profile.\"\"\"\n    builder = KatanaTemplateBuilder(registered_host())\n    builder.rebuild_template()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workio.html","title":"workio","text":"<p>Katana workfile operations.</p>"},{"location":"autoapi/client/ayon_katana/api/workio.html#client.ayon_katana.api.workio.get_current_workfile","title":"<code>get_current_workfile()</code>","text":"<p>Return the current Katana project path, or <code>None</code> when unsaved.</p> Source code in <code>client/ayon_katana/api/workio.py</code> <pre><code>def get_current_workfile() -&gt; Optional[str]:\n    \"\"\"Return the current Katana project path, or ``None`` when unsaved.\"\"\"\n    return NodegraphAPI.GetProjectFile() or None\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workio.html#client.ayon_katana.api.workio.get_workfile_extensions","title":"<code>get_workfile_extensions()</code>","text":"<p>Return supported Katana project extensions.</p> Source code in <code>client/ayon_katana/api/workio.py</code> <pre><code>def get_workfile_extensions() -&gt; list[str]:\n    \"\"\"Return supported Katana project extensions.\"\"\"\n    return list(WORKFILE_EXTENSIONS)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workio.html#client.ayon_katana.api.workio.is_filesystem_workfile_path","title":"<code>is_filesystem_workfile_path(value)</code>","text":"<p>Return whether a value is a filesystem path to a Katana workfile.</p> <p>Asset identifiers and other URI-style values are intentionally unsupported until Katana has a safe, explicit asset-backed workfile workflow.</p> Source code in <code>client/ayon_katana/api/workio.py</code> <pre><code>def is_filesystem_workfile_path(value: str) -&gt; bool:\n    \"\"\"Return whether a value is a filesystem path to a Katana workfile.\n\n    Asset identifiers and other URI-style values are intentionally unsupported\n    until Katana has a safe, explicit asset-backed workfile workflow.\n    \"\"\"\n    return bool(\n        value\n        and not _URI_SCHEME_PATTERN.match(value)\n        and os.path.splitext(value)[1].lower() in WORKFILE_EXTENSIONS\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workio.html#client.ayon_katana.api.workio.new_workfile","title":"<code>new_workfile()</code>","text":"<p>Create a new Katana project.</p> Source code in <code>client/ayon_katana/api/workio.py</code> <pre><code>def new_workfile() -&gt; None:\n    \"\"\"Create a new Katana project.\"\"\"\n    KatanaFile.New()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workio.html#client.ayon_katana.api.workio.normalize_workfile_path","title":"<code>normalize_workfile_path(value)</code>","text":"<p>Return a normalized filesystem workfile path.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>Filesystem path to normalize.</p> required <p>Raises:</p> Type Description <code>ValueError</code> <p>The value is empty, URI-style, or not a <code>.katana</code> file.</p> Source code in <code>client/ayon_katana/api/workio.py</code> <pre><code>def normalize_workfile_path(value: str) -&gt; str:\n    \"\"\"Return a normalized filesystem workfile path.\n\n    Args:\n        value: Filesystem path to normalize.\n\n    Raises:\n        ValueError: The value is empty, URI-style, or not a ``.katana`` file.\n    \"\"\"\n    if not value:\n        raise ValueError(\"Katana workfile path is empty.\")\n    if _URI_SCHEME_PATTERN.match(value):\n        raise ValueError(\n            \"Katana asset identifiers are not supported for workfile publishing.\"\n        )\n    if not is_filesystem_workfile_path(value):\n        raise ValueError(\"Katana workfile path must use the .katana extension.\")\n    return os.path.abspath(os.path.normpath(value))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workio.html#client.ayon_katana.api.workio.open_workfile","title":"<code>open_workfile(filepath)</code>","text":"<p>Open a Katana project and return its normalized filesystem path.</p> Source code in <code>client/ayon_katana/api/workio.py</code> <pre><code>def open_workfile(filepath: str) -&gt; str:\n    \"\"\"Open a Katana project and return its normalized filesystem path.\"\"\"\n    normalized_path = validate_workfile_path(filepath, require_exists=True)\n    KatanaFile.Load(normalized_path)\n    return normalized_path\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workio.html#client.ayon_katana.api.workio.save_workfile","title":"<code>save_workfile(dst_path=None)</code>","text":"<p>Save the current Katana project.</p> <p>Parameters:</p> Name Type Description Default <code>dst_path</code> <code>Optional[str]</code> <p>Optional destination filesystem path.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>Saved project path.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>No supported filesystem destination is available.</p> Source code in <code>client/ayon_katana/api/workio.py</code> <pre><code>def save_workfile(dst_path: Optional[str] = None) -&gt; str:\n    \"\"\"Save the current Katana project.\n\n    Args:\n        dst_path: Optional destination filesystem path.\n\n    Returns:\n        Saved project path.\n\n    Raises:\n        ValueError: No supported filesystem destination is available.\n    \"\"\"\n    destination = validate_workfile_path(dst_path or get_current_workfile())\n    KatanaFile.Save(destination)\n    return destination\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workio.html#client.ayon_katana.api.workio.validate_workfile_path","title":"<code>validate_workfile_path(value, *, require_exists=False)</code>","text":"<p>Normalize and validate a Katana filesystem workfile path.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>Workfile path to validate.</p> required <code>require_exists</code> <code>bool</code> <p>Whether the path must exist on disk.</p> <code>False</code> <p>Returns:</p> Type Description <code>str</code> <p>Normalized filesystem path.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>The path is not a supported existing Katana workfile.</p> Source code in <code>client/ayon_katana/api/workio.py</code> <pre><code>def validate_workfile_path(value: str, *, require_exists: bool = False) -&gt; str:\n    \"\"\"Normalize and validate a Katana filesystem workfile path.\n\n    Args:\n        value: Workfile path to validate.\n        require_exists: Whether the path must exist on disk.\n\n    Returns:\n        Normalized filesystem path.\n\n    Raises:\n        ValueError: The path is not a supported existing Katana workfile.\n    \"\"\"\n    normalized_path = normalize_workfile_path(value)\n    if require_exists and not os.path.isfile(normalized_path):\n        raise ValueError(f\"Katana workfile does not exist on disk: {normalized_path}\")\n    return normalized_path\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workio.html#client.ayon_katana.api.workio.workfile_has_unsaved_changes","title":"<code>workfile_has_unsaved_changes()</code>","text":"<p>Return whether the current Katana project is dirty.</p> Source code in <code>client/ayon_katana/api/workio.py</code> <pre><code>def workfile_has_unsaved_changes() -&gt; bool:\n    \"\"\"Return whether the current Katana project is dirty.\"\"\"\n    return bool(KatanaFile.IsFileDirty())\n</code></pre>"},{"location":"autoapi/client/ayon_katana/api/workio.html#client.ayon_katana.api.workio.workfile_paths_match","title":"<code>workfile_paths_match(first_path, second_path)</code>","text":"<p>Return whether two validated workfile paths identify the same file.</p> Source code in <code>client/ayon_katana/api/workio.py</code> <pre><code>def workfile_paths_match(first_path: str, second_path: str) -&gt; bool:\n    \"\"\"Return whether two validated workfile paths identify the same file.\"\"\"\n    return os.path.normcase(normalize_workfile_path(first_path)) == os.path.normcase(\n        normalize_workfile_path(second_path)\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/hooks/index.html","title":"hooks","text":"<p>AYON Katana application launch hooks.</p>"},{"location":"autoapi/client/ayon_katana/hooks/pre_add_workfile_arg.html","title":"pre_add_workfile_arg","text":"<p>Prepare Katana startup workfiles for AYON Applications launches.</p>"},{"location":"autoapi/client/ayon_katana/hooks/pre_add_workfile_arg.html#client.ayon_katana.hooks.pre_add_workfile_arg.PrepareKatanaWorkfileLaunch","title":"<code>PrepareKatanaWorkfileLaunch</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Pass a validated workfile to Katana's startup coordinator.</p> <p>Katana executes <code>--script</code> before loading positional workfile arguments. The selected path is therefore passed through the environment and opened by the Katana startup resource only after the AYON host has been installed.</p> Source code in <code>client/ayon_katana/hooks/pre_add_workfile_arg.py</code> <pre><code>class PrepareKatanaWorkfileLaunch(PreLaunchHook):\n    \"\"\"Pass a validated workfile to Katana's startup coordinator.\n\n    Katana executes ``--script`` before loading positional workfile arguments.\n    The selected path is therefore passed through the environment and opened by\n    the Katana startup resource only after the AYON host has been installed.\n    \"\"\"\n\n    order = 10\n    app_groups = {\"katana\"}\n    launch_types = {LaunchTypes.local}\n\n    def execute(self) -&gt; None:\n        \"\"\"Prepare the requested Katana workfile without changing arguments.\"\"\"\n        launch_environment = self.launch_context.env\n        launch_environment.pop(REQUESTED_WORKFILE_ENV, None)\n\n        workfile_path = self.data.get(\"workfile_path\")\n        if not workfile_path:\n            if not self.data.get(\"start_last_workfile\"):\n                self.log.info(\"Katana will start without opening the last workfile.\")\n                return\n            workfile_path = self.data.get(\"last_workfile_path\")\n\n        validated_path = self._validate_workfile_path(workfile_path)\n        if validated_path is None:\n            return\n        launch_environment[REQUESTED_WORKFILE_ENV] = validated_path\n\n    def _validate_workfile_path(self, workfile_path: object) -&gt; Optional[str]:\n        \"\"\"Return a normalized existing Katana workfile path.\n\n        Args:\n            workfile_path: Candidate path supplied by AYON Applications.\n\n        Returns:\n            Normalized path, or ``None`` when the value is unsupported.\n        \"\"\"\n        if not isinstance(workfile_path, str) or not workfile_path:\n            self.log.warning(\"No Katana workfile path was prepared for launch.\")\n            return None\n        if _URI_SCHEME_PATTERN.match(workfile_path):\n            self.log.warning(\n                \"Katana startup does not support workfile URIs: %s\",\n                workfile_path,\n            )\n            return None\n        if os.path.splitext(workfile_path)[1].lower() != \".katana\":\n            self.log.warning(\n                \"Katana startup requires a .katana workfile: %s\",\n                workfile_path,\n            )\n            return None\n\n        normalized_path = os.path.abspath(os.path.normpath(workfile_path))\n        if not os.path.isfile(normalized_path):\n            self.log.warning(\n                \"The requested Katana workfile does not exist: %s\",\n                normalized_path,\n            )\n            return None\n        return normalized_path\n</code></pre>"},{"location":"autoapi/client/ayon_katana/hooks/pre_add_workfile_arg.html#client.ayon_katana.hooks.pre_add_workfile_arg.PrepareKatanaWorkfileLaunch.execute","title":"<code>execute()</code>","text":"<p>Prepare the requested Katana workfile without changing arguments.</p> Source code in <code>client/ayon_katana/hooks/pre_add_workfile_arg.py</code> <pre><code>def execute(self) -&gt; None:\n    \"\"\"Prepare the requested Katana workfile without changing arguments.\"\"\"\n    launch_environment = self.launch_context.env\n    launch_environment.pop(REQUESTED_WORKFILE_ENV, None)\n\n    workfile_path = self.data.get(\"workfile_path\")\n    if not workfile_path:\n        if not self.data.get(\"start_last_workfile\"):\n            self.log.info(\"Katana will start without opening the last workfile.\")\n            return\n        workfile_path = self.data.get(\"last_workfile_path\")\n\n    validated_path = self._validate_workfile_path(workfile_path)\n    if validated_path is None:\n        return\n    launch_environment[REQUESTED_WORKFILE_ENV] = validated_path\n</code></pre>"},{"location":"autoapi/client/ayon_katana/hooks/set_default_display_and_view.html","title":"set_default_display_and_view","text":"<p>Apply optional Katana OCIO display/view launch overrides.</p>"},{"location":"autoapi/client/ayon_katana/hooks/set_default_display_and_view.html#client.ayon_katana.hooks.set_default_display_and_view.SetDefaultDisplayView","title":"<code>SetDefaultDisplayView</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Prepend optional OCIO active display/view values for Katana.</p> Source code in <code>client/ayon_katana/hooks/set_default_display_and_view.py</code> <pre><code>class SetDefaultDisplayView(PreLaunchHook):\n    \"\"\"Prepend optional OCIO active display/view values for Katana.\"\"\"\n\n    app_groups = {\"katana\"}\n    launch_types = {LaunchTypes.local}\n\n    def execute(self) -&gt; None:\n        \"\"\"Apply enabled workfile display/view overrides to launch environment.\"\"\"\n        environment = self.launch_context.env\n        if not environment.get(\"OCIO\"):\n            return\n\n        katana_settings = (self.data.get(\"project_settings\") or {}).get(\"katana\", {})\n        imageio_settings = katana_settings.get(\"imageio\") or {}\n        if not imageio_settings.get(\"activate_host_color_management\", True):\n            return\n\n        workfile_settings = imageio_settings.get(\"workfile\") or {}\n        if not workfile_settings.get(\"enabled\", False):\n            return\n\n        for setting_key, environment_key in (\n            (\"default_display\", \"OCIO_ACTIVE_DISPLAYS\"),\n            (\"default_view\", \"OCIO_ACTIVE_VIEWS\"),\n        ):\n            configured = str(workfile_settings.get(setting_key) or \"\")\n            if not configured:\n                continue\n            environment[environment_key] = prepend_unique_ocio_values(\n                configured,\n                environment.get(environment_key, \"\"),\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/hooks/set_default_display_and_view.html#client.ayon_katana.hooks.set_default_display_and_view.SetDefaultDisplayView.execute","title":"<code>execute()</code>","text":"<p>Apply enabled workfile display/view overrides to launch environment.</p> Source code in <code>client/ayon_katana/hooks/set_default_display_and_view.py</code> <pre><code>def execute(self) -&gt; None:\n    \"\"\"Apply enabled workfile display/view overrides to launch environment.\"\"\"\n    environment = self.launch_context.env\n    if not environment.get(\"OCIO\"):\n        return\n\n    katana_settings = (self.data.get(\"project_settings\") or {}).get(\"katana\", {})\n    imageio_settings = katana_settings.get(\"imageio\") or {}\n    if not imageio_settings.get(\"activate_host_color_management\", True):\n        return\n\n    workfile_settings = imageio_settings.get(\"workfile\") or {}\n    if not workfile_settings.get(\"enabled\", False):\n        return\n\n    for setting_key, environment_key in (\n        (\"default_display\", \"OCIO_ACTIVE_DISPLAYS\"),\n        (\"default_view\", \"OCIO_ACTIVE_VIEWS\"),\n    ):\n        configured = str(workfile_settings.get(setting_key) or \"\")\n        if not configured:\n            continue\n        environment[environment_key] = prepend_unique_ocio_values(\n            configured,\n            environment.get(environment_key, \"\"),\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/hooks/set_default_display_and_view.html#client.ayon_katana.hooks.set_default_display_and_view.prepend_unique_ocio_values","title":"<code>prepend_unique_ocio_values(configured, existing='')</code>","text":"<p>Prepend colon-separated OCIO values without introducing duplicates.</p> <p>Parameters:</p> Name Type Description Default <code>configured</code> <code>str</code> <p>Preferred display or view values from Katana settings.</p> required <code>existing</code> <code>str</code> <p>Existing environment value to preserve after preferences.</p> <code>''</code> <p>Returns:</p> Type Description <code>str</code> <p>Colon-separated values in first-seen order.</p> Source code in <code>client/ayon_katana/hooks/set_default_display_and_view.py</code> <pre><code>def prepend_unique_ocio_values(configured: str, existing: str = \"\") -&gt; str:\n    \"\"\"Prepend colon-separated OCIO values without introducing duplicates.\n\n    Args:\n        configured: Preferred display or view values from Katana settings.\n        existing: Existing environment value to preserve after preferences.\n\n    Returns:\n        Colon-separated values in first-seen order.\n    \"\"\"\n    output = []\n    seen = set()\n    for raw_value in (configured, existing):\n        for value in str(raw_value or \"\").split(\":\"):\n            value = value.strip()\n            if not value or value in seen:\n                continue\n            seen.add(value)\n            output.append(value)\n    return \":\".join(output)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/hooks/set_paths.html","title":"set_paths","text":"<p>Set Katana's process working directory before launch.</p>"},{"location":"autoapi/client/ayon_katana/hooks/set_paths.html#client.ayon_katana.hooks.set_paths.SetPath","title":"<code>SetPath</code>","text":"<p>               Bases: <code>PreLaunchHook</code></p> <p>Set current dir to workdir.</p> <p>Hook <code>GlobalHostDataHook</code> must be executed before this hook.</p> Source code in <code>client/ayon_katana/hooks/set_paths.py</code> <pre><code>class SetPath(PreLaunchHook):\n    \"\"\"Set current dir to workdir.\n\n    Hook `GlobalHostDataHook` must be executed before this hook.\n    \"\"\"\n\n    app_groups = {\"katana\"}\n    launch_types = {LaunchTypes.local}\n\n    def execute(self):\n        \"\"\"Set the process working directory from ``AYON_WORKDIR``.\"\"\"\n        workdir = self.launch_context.env.get(\"AYON_WORKDIR\", \"\")\n        if not workdir:\n            self.log.warning(\"BUG: Workdir is not filled.\")\n            return\n\n        self.launch_context.kwargs[\"cwd\"] = workdir\n</code></pre>"},{"location":"autoapi/client/ayon_katana/hooks/set_paths.html#client.ayon_katana.hooks.set_paths.SetPath.execute","title":"<code>execute()</code>","text":"<p>Set the process working directory from <code>AYON_WORKDIR</code>.</p> Source code in <code>client/ayon_katana/hooks/set_paths.py</code> <pre><code>def execute(self):\n    \"\"\"Set the process working directory from ``AYON_WORKDIR``.\"\"\"\n    workdir = self.launch_context.env.get(\"AYON_WORKDIR\", \"\")\n    if not workdir:\n        self.log.warning(\"BUG: Workdir is not filled.\")\n        return\n\n    self.launch_context.kwargs[\"cwd\"] = workdir\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/index.html","title":"plugins","text":"<p>AYON Katana host plugins.</p>"},{"location":"autoapi/client/ayon_katana/plugins/create/index.html","title":"create","text":"<p>Katana creator plugins.</p>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_image.html","title":"create_image","text":"<p>Create Katana ImageWrite publish instances.</p>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_image.html#client.ayon_katana.plugins.create.create_image.CreateImage","title":"<code>CreateImage</code>","text":"<p>               Bases: <code>KatanaCreator</code></p> <p>Create an <code>ImageWrite</code> publish instance.</p> Source code in <code>client/ayon_katana/plugins/create/create_image.py</code> <pre><code>class CreateImage(plugin.KatanaCreator):\n    \"\"\"Create an ``ImageWrite`` publish instance.\"\"\"\n\n    identifier = \"io.ayon.creators.katana.image\"\n    label = \"Image\"\n    product_base_type = \"image\"\n    product_type = product_base_type\n    icon = \"picture-o\"\n    description = \"Publish a selected image stream locally or on the farm\"\n\n    default_render_target = \"local\"\n    default_extension = \"exr\"\n    default_colorspace = \"\"\n    default_frame_padding = 4\n    default_review = False\n\n    def _current_frame_range(self) -&gt; tuple[int, int]:\n        folder_entity = self.create_context.get_current_folder_entity()\n        attributes = (folder_entity or {}).get(\"attrib\") or {}\n        frame_start = attributes.get(\"frameStart\")\n        if frame_start is None:\n            frame_start = 1001\n        frame_end = attributes.get(\"frameEnd\")\n        if frame_end is None:\n            frame_end = frame_start\n        return int(frame_start), int(frame_end)\n\n    def _default_output_path(\n        self,\n        product_name: str,\n        extension: str,\n        *,\n        single_frame: bool,\n        frame: int,\n    ) -&gt; str:\n        current_workfile = self.create_context.host.get_current_workfile()\n        base_directory = (\n            Path(current_workfile).parent if current_workfile else Path.cwd()\n        )\n        frame_component = (\n            str(frame).zfill(self.default_frame_padding)\n            if single_frame\n            else \"#\" * self.default_frame_padding\n        )\n        return str(\n            base_directory\n            / \"images\"\n            / product_name\n            / f\"{product_name}.{frame_component}.{extension}\"\n        ).replace(\"\\\\\", \"/\")\n\n    @staticmethod\n    def _selected_output_port():\n        selected_nodes = compat.get_selected_nodes()\n        if len(selected_nodes) != 1:\n            raise CreatorError(\n                \"Select exactly one Katana image node to create an ImageWrite \"\n                \"publish instance.\"\n            )\n        selected_node = selected_nodes[0]\n        output_port = selected_node.getOutputPort(\"out\")\n        if output_port is None:\n            output_ports = compat.get_output_ports(selected_node)\n            output_port = output_ports[0] if output_ports else None\n        if output_port is None:\n            raise CreatorError(\n                f\"Selected node {selected_node.getName()!r} has no output port.\"\n            )\n        return output_port\n\n    def create(\n        self,\n        product_name: str,\n        instance_data: dict[str, Any],\n        pre_create_data: dict[str, Any],\n    ):\n        \"\"\"Create and connect an ``ImageWrite`` node.\"\"\"\n        use_selection = bool(pre_create_data.get(\"use_selection\", True))\n        source_port = self._selected_output_port() if use_selection else None\n        frame_start, _frame_end = self._current_frame_range()\n        frame = int(pre_create_data.get(\"frame\", frame_start))\n        single_frame = bool(pre_create_data.get(\"single_frame\", False))\n        extension = (\n            (pre_create_data.get(\"extension\") or self.default_extension)\n            .lower()\n            .lstrip(\".\")\n        )\n        output_path = pre_create_data.get(\"output_path\") or self._default_output_path(\n            product_name,\n            extension,\n            single_frame=single_frame,\n            frame=frame,\n        )\n        render_target = pre_create_data.get(\"render_target\", self.default_render_target)\n        if render_target not in {\"farm\", \"local\", \"local_no_render\"}:\n            raise CreatorError(f\"Unsupported image render target: {render_target!r}.\")\n        review = bool(pre_create_data.get(\"review\", self.default_review))\n        colorspace = pre_create_data.get(\"colorspace\") or self.default_colorspace\n\n        creator_attributes = instance_data.setdefault(\"creator_attributes\", {})\n        creator_attributes.update(\n            {\n                \"render_target\": render_target,\n                \"review\": review,\n            }\n        )\n        instance_data[\"node_type\"] = \"ImageWrite\"\n        instance_data[\"farm\"] = render_target == \"farm\"\n        families = instance_data.setdefault(\"families\", [])\n        for family in (\"image\", \"katana.image\"):\n            if family not in families:\n                families.append(family)\n        if instance_data[\"farm\"] and \"render.farm\" not in families:\n            families.append(\"render.farm\")\n        if not instance_data[\"farm\"] and \"render.farm\" in families:\n            families.remove(\"render.farm\")\n\n        created_instance = None\n        image_write_node = None\n        try:\n            created_instance = super().create(\n                product_name,\n                instance_data,\n                pre_create_data,\n            )\n            image_write_node = created_instance.transient_data[\"node\"]\n            try:\n                configure_image_write(\n                    image_write_node,\n                    output_path=output_path,\n                    file_format=extension,\n                    colorspace=colorspace,\n                    single_frame=single_frame,\n                    frame=frame,\n                )\n            except ValueError as exc:\n                raise CreatorError(f\"Failed to create ImageWrite: {exc}\") from exc\n            if source_port is not None:\n                input_port = image_write_node.getInputPort(\"in\")\n                if input_port is None:\n                    raise RuntimeError(\"ImageWrite has no 'in' port.\")\n                source_port.connect(input_port)\n\n            return created_instance\n        except Exception:\n            if image_write_node is not None:\n                with suppress(Exception):\n                    image_write_node.delete()\n            if created_instance is not None:\n                with suppress(Exception):\n                    self._remove_instance_from_context(created_instance)\n            raise\n\n    def get_pre_create_attr_defs(self):\n        \"\"\"Return pre-create ImageWrite settings.\"\"\"\n        frame_start, _frame_end = self._current_frame_range()\n        return [\n            BoolDef(\n                \"use_selection\",\n                label=\"Connect selected node\",\n                default=not self.create_context.headless,\n            ),\n            EnumDef(\n                \"render_target\",\n                label=\"Render target\",\n                default=self.default_render_target,\n                items={\n                    \"local\": \"Local\",\n                    \"farm\": \"Farm\",\n                    \"local_no_render\": \"Use existing frames\",\n                },\n            ),\n            BoolDef(\"review\", label=\"Review\", default=self.default_review),\n            BoolDef(\"single_frame\", label=\"Single frame\", default=False),\n            NumberDef(\n                \"frame\",\n                label=\"Single frame\",\n                default=frame_start,\n                decimals=0,\n            ),\n            TextDef(\n                \"output_path\",\n                label=\"Output path\",\n                default=\"\",\n                placeholder=\"Leave empty for a workfile-relative image sequence\",\n            ),\n            EnumDef(\n                \"extension\",\n                label=\"Extension\",\n                default=self.default_extension,\n                items=[\"exr\", \"png\", \"tif\", \"jpg\", \"dpx\", \"cin\"],\n            ),\n            TextDef(\n                \"colorspace\",\n                label=\"Output colorspace\",\n                default=self.default_colorspace,\n                placeholder=\"Required when Review is enabled\",\n            ),\n        ]\n\n    def get_instance_attr_defs(self):\n        \"\"\"Return instance publish settings.\"\"\"\n        return [\n            EnumDef(\n                \"render_target\",\n                label=\"Render target\",\n                default=self.default_render_target,\n                items={\n                    \"local\": \"Local\",\n                    \"farm\": \"Farm\",\n                    \"local_no_render\": \"Use existing frames\",\n                },\n            ),\n            BoolDef(\"review\", label=\"Review\", default=self.default_review),\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_image.html#client.ayon_katana.plugins.create.create_image.CreateImage.create","title":"<code>create(product_name, instance_data, pre_create_data)</code>","text":"<p>Create and connect an <code>ImageWrite</code> node.</p> Source code in <code>client/ayon_katana/plugins/create/create_image.py</code> <pre><code>def create(\n    self,\n    product_name: str,\n    instance_data: dict[str, Any],\n    pre_create_data: dict[str, Any],\n):\n    \"\"\"Create and connect an ``ImageWrite`` node.\"\"\"\n    use_selection = bool(pre_create_data.get(\"use_selection\", True))\n    source_port = self._selected_output_port() if use_selection else None\n    frame_start, _frame_end = self._current_frame_range()\n    frame = int(pre_create_data.get(\"frame\", frame_start))\n    single_frame = bool(pre_create_data.get(\"single_frame\", False))\n    extension = (\n        (pre_create_data.get(\"extension\") or self.default_extension)\n        .lower()\n        .lstrip(\".\")\n    )\n    output_path = pre_create_data.get(\"output_path\") or self._default_output_path(\n        product_name,\n        extension,\n        single_frame=single_frame,\n        frame=frame,\n    )\n    render_target = pre_create_data.get(\"render_target\", self.default_render_target)\n    if render_target not in {\"farm\", \"local\", \"local_no_render\"}:\n        raise CreatorError(f\"Unsupported image render target: {render_target!r}.\")\n    review = bool(pre_create_data.get(\"review\", self.default_review))\n    colorspace = pre_create_data.get(\"colorspace\") or self.default_colorspace\n\n    creator_attributes = instance_data.setdefault(\"creator_attributes\", {})\n    creator_attributes.update(\n        {\n            \"render_target\": render_target,\n            \"review\": review,\n        }\n    )\n    instance_data[\"node_type\"] = \"ImageWrite\"\n    instance_data[\"farm\"] = render_target == \"farm\"\n    families = instance_data.setdefault(\"families\", [])\n    for family in (\"image\", \"katana.image\"):\n        if family not in families:\n            families.append(family)\n    if instance_data[\"farm\"] and \"render.farm\" not in families:\n        families.append(\"render.farm\")\n    if not instance_data[\"farm\"] and \"render.farm\" in families:\n        families.remove(\"render.farm\")\n\n    created_instance = None\n    image_write_node = None\n    try:\n        created_instance = super().create(\n            product_name,\n            instance_data,\n            pre_create_data,\n        )\n        image_write_node = created_instance.transient_data[\"node\"]\n        try:\n            configure_image_write(\n                image_write_node,\n                output_path=output_path,\n                file_format=extension,\n                colorspace=colorspace,\n                single_frame=single_frame,\n                frame=frame,\n            )\n        except ValueError as exc:\n            raise CreatorError(f\"Failed to create ImageWrite: {exc}\") from exc\n        if source_port is not None:\n            input_port = image_write_node.getInputPort(\"in\")\n            if input_port is None:\n                raise RuntimeError(\"ImageWrite has no 'in' port.\")\n            source_port.connect(input_port)\n\n        return created_instance\n    except Exception:\n        if image_write_node is not None:\n            with suppress(Exception):\n                image_write_node.delete()\n        if created_instance is not None:\n            with suppress(Exception):\n                self._remove_instance_from_context(created_instance)\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_image.html#client.ayon_katana.plugins.create.create_image.CreateImage.get_instance_attr_defs","title":"<code>get_instance_attr_defs()</code>","text":"<p>Return instance publish settings.</p> Source code in <code>client/ayon_katana/plugins/create/create_image.py</code> <pre><code>def get_instance_attr_defs(self):\n    \"\"\"Return instance publish settings.\"\"\"\n    return [\n        EnumDef(\n            \"render_target\",\n            label=\"Render target\",\n            default=self.default_render_target,\n            items={\n                \"local\": \"Local\",\n                \"farm\": \"Farm\",\n                \"local_no_render\": \"Use existing frames\",\n            },\n        ),\n        BoolDef(\"review\", label=\"Review\", default=self.default_review),\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_image.html#client.ayon_katana.plugins.create.create_image.CreateImage.get_pre_create_attr_defs","title":"<code>get_pre_create_attr_defs()</code>","text":"<p>Return pre-create ImageWrite settings.</p> Source code in <code>client/ayon_katana/plugins/create/create_image.py</code> <pre><code>def get_pre_create_attr_defs(self):\n    \"\"\"Return pre-create ImageWrite settings.\"\"\"\n    frame_start, _frame_end = self._current_frame_range()\n    return [\n        BoolDef(\n            \"use_selection\",\n            label=\"Connect selected node\",\n            default=not self.create_context.headless,\n        ),\n        EnumDef(\n            \"render_target\",\n            label=\"Render target\",\n            default=self.default_render_target,\n            items={\n                \"local\": \"Local\",\n                \"farm\": \"Farm\",\n                \"local_no_render\": \"Use existing frames\",\n            },\n        ),\n        BoolDef(\"review\", label=\"Review\", default=self.default_review),\n        BoolDef(\"single_frame\", label=\"Single frame\", default=False),\n        NumberDef(\n            \"frame\",\n            label=\"Single frame\",\n            default=frame_start,\n            decimals=0,\n        ),\n        TextDef(\n            \"output_path\",\n            label=\"Output path\",\n            default=\"\",\n            placeholder=\"Leave empty for a workfile-relative image sequence\",\n        ),\n        EnumDef(\n            \"extension\",\n            label=\"Extension\",\n            default=self.default_extension,\n            items=[\"exr\", \"png\", \"tif\", \"jpg\", \"dpx\", \"cin\"],\n        ),\n        TextDef(\n            \"colorspace\",\n            label=\"Output colorspace\",\n            default=self.default_colorspace,\n            placeholder=\"Required when Review is enabled\",\n        ),\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_nodegraph.html","title":"create_nodegraph","text":"<p>Create publish instances for reusable Katana node graphs.</p>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_nodegraph.html#client.ayon_katana.plugins.create.create_nodegraph.CreateNodegraph","title":"<code>CreateNodegraph</code>","text":"<p>               Bases: <code>KatanaCreator</code></p> <p>Create a publish instance for exactly one selected Katana Group.</p> Source code in <code>client/ayon_katana/plugins/create/create_nodegraph.py</code> <pre><code>class CreateNodegraph(plugin.KatanaCreator):\n    \"\"\"Create a publish instance for exactly one selected Katana Group.\"\"\"\n\n    identifier = \"io.ayon.creators.katana.nodegraph\"\n    label = \"Node Graph\"\n    product_base_type = \"nodegraph\"\n    product_type = product_base_type\n    icon = \"sitemap\"\n    description = \"Publish one selected Katana Group as a reusable node graph\"\n\n    def _selected_group(self):\n        \"\"\"Return the selected Group used as the node graph source.\n\n        Raises:\n            CreatorError: The selection is not one publishable Katana Group.\n        \"\"\"\n        selected_nodes = compat.get_selected_nodes()\n        if len(selected_nodes) != 1:\n            raise CreatorError(\n                \"Select exactly one Katana Group node to create a node graph \"\n                \"publish instance.\"\n            )\n\n        selected_node = selected_nodes[0]\n        if selected_node.getType() != \"Group\":\n            raise CreatorError(\n                \"The selected node graph source must be a Katana Group node.\"\n            )\n        if containers.parse_container(selected_node) is not None:\n            raise CreatorError(\n                \"The selected Group is an outer AYON container and cannot be \"\n                \"published as a node graph source.\"\n            )\n        return selected_node\n\n    def create(\n        self,\n        product_name: str,\n        instance_data: dict[str, Any],\n        pre_create_data: dict[str, Any],\n    ):\n        \"\"\"Create an AYON instance referencing one selected Group.\"\"\"\n        selected_group = self._selected_group()\n        instance_data[\"nodegraph_node\"] = selected_group.getName()\n        families = instance_data.setdefault(\"families\", [])\n        if \"nodegraph\" not in families:\n            families.append(\"nodegraph\")\n\n        return super().create(\n            product_name,\n            instance_data,\n            pre_create_data,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_nodegraph.html#client.ayon_katana.plugins.create.create_nodegraph.CreateNodegraph.create","title":"<code>create(product_name, instance_data, pre_create_data)</code>","text":"<p>Create an AYON instance referencing one selected Group.</p> Source code in <code>client/ayon_katana/plugins/create/create_nodegraph.py</code> <pre><code>def create(\n    self,\n    product_name: str,\n    instance_data: dict[str, Any],\n    pre_create_data: dict[str, Any],\n):\n    \"\"\"Create an AYON instance referencing one selected Group.\"\"\"\n    selected_group = self._selected_group()\n    instance_data[\"nodegraph_node\"] = selected_group.getName()\n    families = instance_data.setdefault(\"families\", [])\n    if \"nodegraph\" not in families:\n        families.append(\"nodegraph\")\n\n    return super().create(\n        product_name,\n        instance_data,\n        pre_create_data,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_nodegraph.html#client.ayon_katana.plugins.create.create_nodegraph.CreateRenderSetup","title":"<code>CreateRenderSetup</code>","text":"<p>               Bases: <code>CreateNodegraph</code></p> <p>Publish reusable Katana render and lighting setup graphs.</p> Source code in <code>client/ayon_katana/plugins/create/create_nodegraph.py</code> <pre><code>class CreateRenderSetup(CreateNodegraph):\n    \"\"\"Publish reusable Katana render and lighting setup graphs.\"\"\"\n\n    identifier = \"io.ayon.creators.katana.render_setup\"\n    label = \"Render Setup\"\n    product_base_type = \"rendersetup\"\n    product_type = product_base_type\n    icon = \"sliders\"\n    description = \"Publish one reusable Katana render or lighting setup Group\"\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_render.html","title":"create_render","text":"<p>Create Katana render publish instances.</p>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_render.html#client.ayon_katana.plugins.create.create_render.CreateRender","title":"<code>CreateRender</code>","text":"<p>               Bases: <code>KatanaCreator</code></p> <p>Create a Katana render graph and AYON publish instance.</p> Source code in <code>client/ayon_katana/plugins/create/create_render.py</code> <pre><code>class CreateRender(plugin.KatanaCreator):\n    \"\"\"Create a Katana render graph and AYON publish instance.\"\"\"\n\n    identifier = \"io.ayon.creators.katana.render\"\n    label = \"Render\"\n    product_base_type = \"render\"\n    product_type = product_base_type\n    icon = \"fa5.images\"\n    description = \"Create a Katana render node graph\"\n\n    default_variant = \"Main\"\n    default_renderer = \"\"\n    default_render_target = \"farm\"\n    default_extension = \"exr\"\n    default_channel = \"rgba\"\n    default_camera = \"/root/world/cam/camera\"\n    default_frame_padding = 4\n\n    def create_instance_node(\n        self,\n        node_type: str,\n        node_name: str,\n        parent=None,\n    ):\n        \"\"\"Create a render instance Group with pass-through ports.\"\"\"\n        instance_node = super().create_instance_node(\n            node_type,\n            node_name,\n            parent=parent,\n        )\n        instance_node.addInputPort(\"in\")\n        instance_node.addOutputPort(\"out\")\n        return instance_node\n\n    def _current_frame_range(self) -&gt; tuple[int, int]:\n        folder_entity = self.create_context.get_current_folder_entity()\n        attributes = (folder_entity or {}).get(\"attrib\") or {}\n        frame_start = attributes.get(\"frameStart\")\n        if frame_start is None:\n            frame_start = 1001\n        frame_end = attributes.get(\"frameEnd\")\n        if frame_end is None:\n            frame_end = frame_start\n        return int(frame_start), int(frame_end)\n\n    def _current_task_handles(self) -&gt; tuple[int, int]:\n        \"\"\"Return active task handles, falling back to folder attributes.\n\n        The creator's frame inputs are cut frames. The Render range expands\n        them when handles are enabled.\n        \"\"\"\n        task_entity = self.create_context.get_current_task_entity()\n        folder_entity = self.create_context.get_current_folder_entity()\n        task_attributes = (task_entity or {}).get(\"attrib\") or {}\n        folder_attributes = (folder_entity or {}).get(\"attrib\") or {}\n\n        def get_handle(name: str) -&gt; int:\n            \"\"\"Return one task or folder handle.\"\"\"\n            value = task_attributes.get(name)\n            if value is None:\n                value = folder_attributes.get(name)\n            if value is None:\n                return 0\n            try:\n                return int(value)\n            except (TypeError, ValueError) as exc:\n                raise CreatorError(\n                    f\"AYON {name} handle is not a whole number: {value!r}\"\n                ) from exc\n\n        return get_handle(\"handleStart\"), get_handle(\"handleEnd\")\n\n    def _default_output_path(self, product_name: str, extension: str) -&gt; str:\n        current_workfile = self.create_context.host.get_current_workfile()\n        base_directory = (\n            Path(current_workfile).parent if current_workfile else Path.cwd()\n        )\n        padding = \"#\" * self.default_frame_padding\n        return str(\n            base_directory\n            / \"renders\"\n            / product_name\n            / f\"{product_name}.{padding}.{extension}\"\n        ).replace(\"\\\\\", \"/\")\n\n    def _get_required_renderer(self, configured_renderer: Optional[str]) -&gt; str:\n        renderer_name = configured_renderer or render.get_default_renderer(\n            self.default_renderer\n        )\n        if not renderer_name:\n            raise CreatorError(\n                \"The renderer is not configured. Set the CreateRender default \"\n                \"renderer or DEFAULT_RENDERER to a registered renderer.\"\n            )\n        return renderer_name\n\n    def create(\n        self,\n        product_name: str,\n        instance_data: dict[str, Any],\n        pre_create_data: dict[str, Any],\n    ):\n        \"\"\"Create a render graph and publish instance.\"\"\"\n        frame_start_default, frame_end_default = self._current_frame_range()\n        frame_start = int(pre_create_data.get(\"frame_start\", frame_start_default))\n        frame_end = int(pre_create_data.get(\"frame_end\", frame_end_default))\n        frame_step = int(pre_create_data.get(\"frame_step\", 1))\n        extension = pre_create_data.get(\"extension\") or self.default_extension\n        output_path = pre_create_data.get(\"output_path\") or self._default_output_path(\n            product_name, extension\n        )\n        renderer_name = self._get_required_renderer(pre_create_data.get(\"renderer\"))\n        creator_attributes = instance_data.setdefault(\"creator_attributes\", {})\n        use_handles = bool(pre_create_data.get(\"use_handles\", True))\n        render_target = pre_create_data.get(\n            \"render_target\",\n            self.default_render_target,\n        )\n        if render_target not in {\"farm\", \"local\", \"local_no_render\"}:\n            raise CreatorError(f\"Unsupported Katana render target: {render_target!r}.\")\n        creator_attributes.update(\n            {\n                \"render_target\": render_target,\n                \"review\": bool(pre_create_data.get(\"review\", True)),\n            }\n        )\n        handle_publish_attributes = instance_data.setdefault(\n            \"publish_attributes\", {}\n        ).setdefault(_TASK_HANDLES_PUBLISH_PLUGIN, {})\n        handle_publish_attributes[\"use_handles\"] = use_handles\n        families = instance_data.setdefault(\"families\", [])\n        for family in (\"render\", \"katana.render\"):\n            if family not in families:\n                families.append(family)\n        if render_target == \"farm\" and \"render.farm\" not in families:\n            families.append(\"render.farm\")\n        if render_target != \"farm\" and \"render.farm\" in families:\n            families.remove(\"render.farm\")\n        instance_data[\"node_type\"] = \"Group\"\n        instance_data[\"farm\"] = render_target == \"farm\"\n\n        handle_start, handle_end = (\n            self._current_task_handles() if use_handles else (0, 0)\n        )\n        render_start = frame_start - handle_start\n        render_end = frame_end + handle_end\n        created_instance = None\n        instance_node = None\n        try:\n            created_instance = super().create(\n                product_name,\n                instance_data,\n                pre_create_data,\n            )\n            instance_node = created_instance.transient_data[\"node\"]\n            render_node = render.create_render_graph(\n                instance_node=instance_node,\n                product_name=product_name,\n                output_name=pre_create_data.get(\"output_name\") or \"primary\",\n                output_path=output_path,\n                extension=extension,\n                channel=pre_create_data.get(\"channel\") or self.default_channel,\n                renderer=renderer_name,\n                camera=pre_create_data.get(\"camera\") or self.default_camera,\n                frame_start=render_start,\n                frame_end=render_end,\n                frame_step=frame_step,\n                resolution=pre_create_data.get(\"resolution\") or \"\",\n            )\n            created_instance[\"instance_node\"] = instance_node.getName()\n            created_instance[\"render_node\"] = render_node.getName()\n            settings_node = render.get_settings_node(instance_node)\n            if settings_node is None:\n                raise RuntimeError(\"Katana render graph has no RenderSettings node.\")\n            created_instance[\"render_settings_node\"] = settings_node.getName()\n            instances.imprint(instance_node, created_instance.data_to_store())\n\n            if pre_create_data.get(\"use_selection\", True):\n                selected_nodes = compat.get_selected_nodes()\n                if selected_nodes:\n                    source_port = selected_nodes[0].getOutputPort(\"out\")\n                    target_port = instance_node.getInputPort(\"in\")\n                    if source_port is not None and target_port is not None:\n                        source_port.connect(target_port)\n            return created_instance\n        except Exception:\n            if instance_node is not None:\n                with suppress(Exception):\n                    instance_node.delete()\n            if created_instance is not None:\n                with suppress(Exception):\n                    self._remove_instance_from_context(created_instance)\n            raise\n\n    def update_instances(self, update_list) -&gt; None:\n        \"\"\"Update persisted render instance identity after creator edits.\"\"\"\n        for created_instance, changes in update_list:\n            if \"productName\" not in changes.changed_keys:\n                continue\n            instance_node = created_instance.transient_data.get(\"node\")\n            if instance_node is None:\n                continue\n            if render.get_render_node(instance_node) is None:\n                raise RuntimeError(\"Katana render graph has no Render node.\")\n            if render.get_settings_node(instance_node) is None:\n                raise RuntimeError(\"Katana render graph has no RenderSettings node.\")\n\n        native_creator_keys = {\n            \"output_path\",\n            \"output_name\",\n            \"extension\",\n            \"channel\",\n            \"renderer\",\n            \"camera\",\n            \"frame_start\",\n            \"frame_end\",\n            \"frame_step\",\n            \"resolution\",\n        }\n        for created_instance, _changes in update_list:\n            creator_attributes = created_instance.get(\"creator_attributes\") or {}\n            for key in native_creator_keys:\n                creator_attributes.pop(key, None)\n        super().update_instances(update_list)\n\n        for created_instance, changes in update_list:\n            if \"productName\" not in changes.changed_keys:\n                continue\n            instance_node = created_instance.transient_data.get(\"node\")\n            if instance_node is None:\n                continue\n            render_node = render.get_render_node(instance_node)\n            if render_node is None:\n                raise RuntimeError(\"Katana render graph has no Render node.\")\n            settings_node = render.get_settings_node(instance_node)\n            if settings_node is None:\n                raise RuntimeError(\"Katana render graph has no RenderSettings node.\")\n            created_instance[\"render_node\"] = render_node.getName()\n            created_instance[\"render_settings_node\"] = settings_node.getName()\n            instances.imprint(instance_node, created_instance.data_to_store())\n\n    def _get_initial_render_attr_defs(self):\n        frame_start, frame_end = self._current_frame_range()\n        return [\n            TextDef(\n                \"output_path\",\n                label=\"Output path\",\n                default=\"\",\n                placeholder=\"Leave empty for a workfile-relative EXR sequence\",\n            ),\n            TextDef(\"output_name\", label=\"Output name\", default=\"primary\"),\n            EnumDef(\n                \"extension\",\n                label=\"Extension\",\n                default=self.default_extension,\n                items=[\"exr\", \"png\", \"tif\", \"jpg\"],\n            ),\n            TextDef(\"channel\", label=\"Channel / AOV\", default=self.default_channel),\n            self._get_renderer_attr_def(),\n            TextDef(\"camera\", label=\"Camera path\", default=self.default_camera),\n            TextDef(\n                \"resolution\",\n                label=\"Resolution preset\",\n                default=\"\",\n                placeholder=\"Leave empty to use Katana project resolution\",\n            ),\n            NumberDef(\n                \"frame_start\",\n                label=\"Frame start\",\n                default=frame_start,\n                decimals=0,\n            ),\n            NumberDef(\n                \"frame_end\",\n                label=\"Frame end\",\n                default=frame_end,\n                decimals=0,\n            ),\n            NumberDef(\n                \"frame_step\",\n                label=\"Frame step\",\n                default=1,\n                minimum=1,\n                decimals=0,\n            ),\n        ]\n\n    def _get_renderer_attr_def(self):\n        renderer_names = render.get_registered_renderers()\n        default_renderer = render.get_default_renderer(self.default_renderer)\n        if renderer_names:\n            return EnumDef(\n                \"renderer\",\n                label=\"Renderer\",\n                default=default_renderer,\n                items={name: name for name in renderer_names},\n            )\n        return TextDef(\n            \"renderer\",\n            label=\"Renderer\",\n            default=default_renderer,\n            placeholder=\"No renderer plugins are registered\",\n        )\n\n    def get_pre_create_attr_defs(self):\n        \"\"\"Return render controls shown before instance creation.\"\"\"\n        return [\n            BoolDef(\n                \"use_selection\",\n                label=\"Connect selected node\",\n                default=not self.create_context.headless,\n            ),\n            EnumDef(\n                \"render_target\",\n                label=\"Render target\",\n                default=self.default_render_target,\n                items={\n                    \"farm\": \"Farm\",\n                    \"local\": \"Local\",\n                    \"local_no_render\": \"Use existing frames\",\n                },\n            ),\n            BoolDef(\"review\", label=\"Review\", default=True),\n            BoolDef(\"use_handles\", label=\"Use task handles\", default=True),\n            *self._get_initial_render_attr_defs(),\n        ]\n\n    def get_instance_attr_defs(self):\n        \"\"\"Return render controls editable after instance creation.\"\"\"\n        return [\n            EnumDef(\n                \"render_target\",\n                label=\"Render target\",\n                default=self.default_render_target,\n                items={\n                    \"farm\": \"Farm\",\n                    \"local\": \"Local\",\n                    \"local_no_render\": \"Use existing frames\",\n                },\n            ),\n            BoolDef(\"review\", label=\"Review\", default=True),\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_render.html#client.ayon_katana.plugins.create.create_render.CreateRender.create","title":"<code>create(product_name, instance_data, pre_create_data)</code>","text":"<p>Create a render graph and publish instance.</p> Source code in <code>client/ayon_katana/plugins/create/create_render.py</code> <pre><code>def create(\n    self,\n    product_name: str,\n    instance_data: dict[str, Any],\n    pre_create_data: dict[str, Any],\n):\n    \"\"\"Create a render graph and publish instance.\"\"\"\n    frame_start_default, frame_end_default = self._current_frame_range()\n    frame_start = int(pre_create_data.get(\"frame_start\", frame_start_default))\n    frame_end = int(pre_create_data.get(\"frame_end\", frame_end_default))\n    frame_step = int(pre_create_data.get(\"frame_step\", 1))\n    extension = pre_create_data.get(\"extension\") or self.default_extension\n    output_path = pre_create_data.get(\"output_path\") or self._default_output_path(\n        product_name, extension\n    )\n    renderer_name = self._get_required_renderer(pre_create_data.get(\"renderer\"))\n    creator_attributes = instance_data.setdefault(\"creator_attributes\", {})\n    use_handles = bool(pre_create_data.get(\"use_handles\", True))\n    render_target = pre_create_data.get(\n        \"render_target\",\n        self.default_render_target,\n    )\n    if render_target not in {\"farm\", \"local\", \"local_no_render\"}:\n        raise CreatorError(f\"Unsupported Katana render target: {render_target!r}.\")\n    creator_attributes.update(\n        {\n            \"render_target\": render_target,\n            \"review\": bool(pre_create_data.get(\"review\", True)),\n        }\n    )\n    handle_publish_attributes = instance_data.setdefault(\n        \"publish_attributes\", {}\n    ).setdefault(_TASK_HANDLES_PUBLISH_PLUGIN, {})\n    handle_publish_attributes[\"use_handles\"] = use_handles\n    families = instance_data.setdefault(\"families\", [])\n    for family in (\"render\", \"katana.render\"):\n        if family not in families:\n            families.append(family)\n    if render_target == \"farm\" and \"render.farm\" not in families:\n        families.append(\"render.farm\")\n    if render_target != \"farm\" and \"render.farm\" in families:\n        families.remove(\"render.farm\")\n    instance_data[\"node_type\"] = \"Group\"\n    instance_data[\"farm\"] = render_target == \"farm\"\n\n    handle_start, handle_end = (\n        self._current_task_handles() if use_handles else (0, 0)\n    )\n    render_start = frame_start - handle_start\n    render_end = frame_end + handle_end\n    created_instance = None\n    instance_node = None\n    try:\n        created_instance = super().create(\n            product_name,\n            instance_data,\n            pre_create_data,\n        )\n        instance_node = created_instance.transient_data[\"node\"]\n        render_node = render.create_render_graph(\n            instance_node=instance_node,\n            product_name=product_name,\n            output_name=pre_create_data.get(\"output_name\") or \"primary\",\n            output_path=output_path,\n            extension=extension,\n            channel=pre_create_data.get(\"channel\") or self.default_channel,\n            renderer=renderer_name,\n            camera=pre_create_data.get(\"camera\") or self.default_camera,\n            frame_start=render_start,\n            frame_end=render_end,\n            frame_step=frame_step,\n            resolution=pre_create_data.get(\"resolution\") or \"\",\n        )\n        created_instance[\"instance_node\"] = instance_node.getName()\n        created_instance[\"render_node\"] = render_node.getName()\n        settings_node = render.get_settings_node(instance_node)\n        if settings_node is None:\n            raise RuntimeError(\"Katana render graph has no RenderSettings node.\")\n        created_instance[\"render_settings_node\"] = settings_node.getName()\n        instances.imprint(instance_node, created_instance.data_to_store())\n\n        if pre_create_data.get(\"use_selection\", True):\n            selected_nodes = compat.get_selected_nodes()\n            if selected_nodes:\n                source_port = selected_nodes[0].getOutputPort(\"out\")\n                target_port = instance_node.getInputPort(\"in\")\n                if source_port is not None and target_port is not None:\n                    source_port.connect(target_port)\n        return created_instance\n    except Exception:\n        if instance_node is not None:\n            with suppress(Exception):\n                instance_node.delete()\n        if created_instance is not None:\n            with suppress(Exception):\n                self._remove_instance_from_context(created_instance)\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_render.html#client.ayon_katana.plugins.create.create_render.CreateRender.create_instance_node","title":"<code>create_instance_node(node_type, node_name, parent=None)</code>","text":"<p>Create a render instance Group with pass-through ports.</p> Source code in <code>client/ayon_katana/plugins/create/create_render.py</code> <pre><code>def create_instance_node(\n    self,\n    node_type: str,\n    node_name: str,\n    parent=None,\n):\n    \"\"\"Create a render instance Group with pass-through ports.\"\"\"\n    instance_node = super().create_instance_node(\n        node_type,\n        node_name,\n        parent=parent,\n    )\n    instance_node.addInputPort(\"in\")\n    instance_node.addOutputPort(\"out\")\n    return instance_node\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_render.html#client.ayon_katana.plugins.create.create_render.CreateRender.get_instance_attr_defs","title":"<code>get_instance_attr_defs()</code>","text":"<p>Return render controls editable after instance creation.</p> Source code in <code>client/ayon_katana/plugins/create/create_render.py</code> <pre><code>def get_instance_attr_defs(self):\n    \"\"\"Return render controls editable after instance creation.\"\"\"\n    return [\n        EnumDef(\n            \"render_target\",\n            label=\"Render target\",\n            default=self.default_render_target,\n            items={\n                \"farm\": \"Farm\",\n                \"local\": \"Local\",\n                \"local_no_render\": \"Use existing frames\",\n            },\n        ),\n        BoolDef(\"review\", label=\"Review\", default=True),\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_render.html#client.ayon_katana.plugins.create.create_render.CreateRender.get_pre_create_attr_defs","title":"<code>get_pre_create_attr_defs()</code>","text":"<p>Return render controls shown before instance creation.</p> Source code in <code>client/ayon_katana/plugins/create/create_render.py</code> <pre><code>def get_pre_create_attr_defs(self):\n    \"\"\"Return render controls shown before instance creation.\"\"\"\n    return [\n        BoolDef(\n            \"use_selection\",\n            label=\"Connect selected node\",\n            default=not self.create_context.headless,\n        ),\n        EnumDef(\n            \"render_target\",\n            label=\"Render target\",\n            default=self.default_render_target,\n            items={\n                \"farm\": \"Farm\",\n                \"local\": \"Local\",\n                \"local_no_render\": \"Use existing frames\",\n            },\n        ),\n        BoolDef(\"review\", label=\"Review\", default=True),\n        BoolDef(\"use_handles\", label=\"Use task handles\", default=True),\n        *self._get_initial_render_attr_defs(),\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_render.html#client.ayon_katana.plugins.create.create_render.CreateRender.update_instances","title":"<code>update_instances(update_list)</code>","text":"<p>Update persisted render instance identity after creator edits.</p> Source code in <code>client/ayon_katana/plugins/create/create_render.py</code> <pre><code>def update_instances(self, update_list) -&gt; None:\n    \"\"\"Update persisted render instance identity after creator edits.\"\"\"\n    for created_instance, changes in update_list:\n        if \"productName\" not in changes.changed_keys:\n            continue\n        instance_node = created_instance.transient_data.get(\"node\")\n        if instance_node is None:\n            continue\n        if render.get_render_node(instance_node) is None:\n            raise RuntimeError(\"Katana render graph has no Render node.\")\n        if render.get_settings_node(instance_node) is None:\n            raise RuntimeError(\"Katana render graph has no RenderSettings node.\")\n\n    native_creator_keys = {\n        \"output_path\",\n        \"output_name\",\n        \"extension\",\n        \"channel\",\n        \"renderer\",\n        \"camera\",\n        \"frame_start\",\n        \"frame_end\",\n        \"frame_step\",\n        \"resolution\",\n    }\n    for created_instance, _changes in update_list:\n        creator_attributes = created_instance.get(\"creator_attributes\") or {}\n        for key in native_creator_keys:\n            creator_attributes.pop(key, None)\n    super().update_instances(update_list)\n\n    for created_instance, changes in update_list:\n        if \"productName\" not in changes.changed_keys:\n            continue\n        instance_node = created_instance.transient_data.get(\"node\")\n        if instance_node is None:\n            continue\n        render_node = render.get_render_node(instance_node)\n        if render_node is None:\n            raise RuntimeError(\"Katana render graph has no Render node.\")\n        settings_node = render.get_settings_node(instance_node)\n        if settings_node is None:\n            raise RuntimeError(\"Katana render graph has no RenderSettings node.\")\n        created_instance[\"render_node\"] = render_node.getName()\n        created_instance[\"render_settings_node\"] = settings_node.getName()\n        instances.imprint(instance_node, created_instance.data_to_store())\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_review.html","title":"create_review","text":"<p>Create Katana Viewer review publish instances.</p>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_review.html#client.ayon_katana.plugins.create.create_review.CreateReview","title":"<code>CreateReview</code>","text":"<p>               Bases: <code>KatanaCreator</code></p> <p>Create a persistent Scene Review marker for the active Katana Viewer.</p> Source code in <code>client/ayon_katana/plugins/create/create_review.py</code> <pre><code>class CreateReview(plugin.KatanaCreator):\n    \"\"\"Create a persistent Scene Review marker for the active Katana Viewer.\"\"\"\n\n    identifier = \"io.ayon.creators.katana.review\"\n    label = \"Scene Review\"\n    product_base_type = \"review\"\n    product_type = product_base_type\n    icon = \"video-camera\"\n    description = \"Capture the active Katana Viewer as an AYON review sequence\"\n\n    def create(\n        self,\n        product_name: str,\n        instance_data: dict[str, Any],\n        pre_create_data: dict[str, Any],\n    ):\n        \"\"\"Create a node-backed review instance without changing the Viewer.\"\"\"\n        instance_data[\"review\"] = True\n        families = instance_data.setdefault(\"families\", [])\n        for family in (\"review\", \"katana.review\"):\n            if family not in families:\n                families.append(family)\n        return super().create(product_name, instance_data, pre_create_data)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_review.html#client.ayon_katana.plugins.create.create_review.CreateReview.create","title":"<code>create(product_name, instance_data, pre_create_data)</code>","text":"<p>Create a node-backed review instance without changing the Viewer.</p> Source code in <code>client/ayon_katana/plugins/create/create_review.py</code> <pre><code>def create(\n    self,\n    product_name: str,\n    instance_data: dict[str, Any],\n    pre_create_data: dict[str, Any],\n):\n    \"\"\"Create a node-backed review instance without changing the Viewer.\"\"\"\n    instance_data[\"review\"] = True\n    families = instance_data.setdefault(\"families\", [])\n    for family in (\"review\", \"katana.review\"):\n        if family not in families:\n            families.append(family)\n    return super().create(product_name, instance_data, pre_create_data)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_usd_layer.html","title":"create_usd_layer","text":"<p>Create native Katana USD layer publish instances.</p>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_usd_layer.html#client.ayon_katana.plugins.create.create_usd_layer.CreateUsdAssembly","title":"<code>CreateUsdAssembly</code>","text":"<p>               Bases: <code>CreateUsdLayer</code></p> <p>Publish a Katana-authored USD asset assembly layer.</p> Source code in <code>client/ayon_katana/plugins/create/create_usd_layer.py</code> <pre><code>class CreateUsdAssembly(CreateUsdLayer):\n    \"\"\"Publish a Katana-authored USD asset assembly layer.\"\"\"\n\n    identifier = \"io.ayon.creators.katana.usd_assembly\"\n    label = \"USD Assembly\"\n    product_base_type = \"assembly\"\n    product_type = product_base_type\n    icon = \"cubes\"\n    description = \"Publish referenced USD assets as an AYON assembly\"\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_usd_layer.html#client.ayon_katana.plugins.create.create_usd_layer.CreateUsdCamera","title":"<code>CreateUsdCamera</code>","text":"<p>               Bases: <code>CreateUsdLayer</code></p> <p>Publish Katana-authored USD camera data.</p> Source code in <code>client/ayon_katana/plugins/create/create_usd_layer.py</code> <pre><code>class CreateUsdCamera(CreateUsdLayer):\n    \"\"\"Publish Katana-authored USD camera data.\"\"\"\n\n    identifier = \"io.ayon.creators.katana.usd_camera\"\n    label = \"USD Camera\"\n    product_base_type = \"camera\"\n    product_type = product_base_type\n    icon = \"camera\"\n    description = \"Publish native USD camera definitions and animation\"\n    default_time_samples = \"Frame Range\"\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_usd_layer.html#client.ayon_katana.plugins.create.create_usd_layer.CreateUsdLayer","title":"<code>CreateUsdLayer</code>","text":"<p>               Bases: <code>KatanaCreator</code></p> <p>Create a native <code>UsdLayerExport</code> publish instance.</p> Source code in <code>client/ayon_katana/plugins/create/create_usd_layer.py</code> <pre><code>class CreateUsdLayer(plugin.KatanaCreator):\n    \"\"\"Create a native ``UsdLayerExport`` publish instance.\"\"\"\n\n    identifier = \"io.ayon.creators.katana.usd_layer\"\n    label = \"USD Layer\"\n    product_base_type = \"usd\"\n    product_type = product_base_type\n    icon = \"cubes\"\n    description = \"Publish selected native USD graph output as one USD layer\"\n\n    default_usd_format = \"usd\"\n    default_time_samples = \"Current Frame\"\n    default_export_method = \"Keep Composition Arcs\"\n    default_samples_per_frame = 1.0\n\n    def _current_frame_range(self) -&gt; tuple[int, int]:\n        \"\"\"Return cut-frame defaults from the current folder context.\"\"\"\n        folder_entity = self.create_context.get_current_folder_entity()\n        attributes = (folder_entity or {}).get(\"attrib\") or {}\n        frame_start = attributes.get(\"frameStart\")\n        if frame_start is None:\n            frame_start = 1001\n        frame_end = attributes.get(\"frameEnd\")\n        if frame_end is None:\n            frame_end = frame_start\n        return int(frame_start), int(frame_end)\n\n    @staticmethod\n    def _selected_output_port():\n        \"\"\"Return the output port of exactly one selected Katana node.\n\n        Returns:\n            Selected native output port.\n\n        Raises:\n            CreatorError: Selection is missing, ambiguous, or has no output.\n        \"\"\"\n        selected_nodes = compat.get_selected_nodes()\n        if len(selected_nodes) != 1:\n            raise CreatorError(\n                \"Select exactly one native USD node to create a USD layer \"\n                \"publish instance.\"\n            )\n        selected_node = selected_nodes[0]\n        if not is_native_usd_node(selected_node):\n            raise CreatorError(\n                f\"Selected node {selected_node.getName()!r} is not a native USD \"\n                f\"node (type: {selected_node.getType()!r}). Select a node with \"\n                \"Katana's 'nativeusd' flavor.\"\n            )\n        output_port = selected_node.getOutputPort(\"out\")\n        if output_port is None:\n            output_ports = compat.get_output_ports(selected_node)\n            output_port = output_ports[0] if output_ports else None\n        if output_port is None:\n            raise CreatorError(\n                f\"Selected node {selected_node.getName()!r} has no output port.\"\n            )\n        return output_port\n\n    def create(\n        self,\n        product_name: str,\n        instance_data: dict[str, Any],\n        pre_create_data: dict[str, Any],\n    ):\n        \"\"\"Create, configure, and optionally connect ``UsdLayerExport``.\"\"\"\n        use_selection = bool(pre_create_data.get(\"use_selection\", True))\n        source_port = self._selected_output_port() if use_selection else None\n        frame_start_default, frame_end_default = self._current_frame_range()\n        frame_start = float(pre_create_data.get(\"frame_start\", frame_start_default))\n        frame_end = float(pre_create_data.get(\"frame_end\", frame_end_default))\n        file_format = pre_create_data.get(\"usd_format\") or self.default_usd_format\n        time_samples = pre_create_data.get(\"time_samples\") or self.default_time_samples\n        samples_per_frame = float(\n            pre_create_data.get(\n                \"samples_per_frame\",\n                self.default_samples_per_frame,\n            )\n        )\n        export_method = (\n            pre_create_data.get(\"export_method\") or self.default_export_method\n        )\n\n        instance_data[\"node_type\"] = \"UsdLayerExport\"\n        families = instance_data.setdefault(\"families\", [])\n        for family in (\"usd\", \"katana.usd\"):\n            if family not in families:\n                families.append(family)\n\n        created_instance = None\n        export_node = None\n        try:\n            created_instance = super().create(\n                product_name,\n                instance_data,\n                pre_create_data,\n            )\n            export_node = created_instance.transient_data[\"node\"]\n            try:\n                configure_usd_layer_export(\n                    export_node,\n                    file_format=file_format,\n                    time_samples=time_samples,\n                    frame_start=frame_start,\n                    frame_end=frame_end,\n                    samples_per_frame=samples_per_frame,\n                    export_method=export_method,\n                )\n            except ValueError as exc:\n                raise CreatorError(f\"Katana USD layer creator failed: {exc}\") from exc\n            if source_port is not None:\n                input_port = export_node.getInputPort(\"in\")\n                if input_port is None:\n                    raise RuntimeError(\n                        \"UsdLayerExport does not expose its native 'in' port.\"\n                    )\n                source_port.connect(input_port)\n\n            return created_instance\n        except Exception:\n            if export_node is not None:\n                with suppress(Exception):\n                    export_node.delete()\n            if created_instance is not None:\n                with suppress(Exception):\n                    self._remove_instance_from_context(created_instance)\n            raise\n\n    def get_pre_create_attr_defs(self):\n        \"\"\"Return native USD export settings used during creation.\"\"\"\n        frame_start, frame_end = self._current_frame_range()\n        return [\n            BoolDef(\n                \"use_selection\",\n                label=\"Connect selected node\",\n                default=not self.create_context.headless,\n            ),\n            EnumDef(\n                \"usd_format\",\n                label=\"USD format\",\n                default=self.default_usd_format,\n                items={\n                    \"usd\": \"USD\",\n                    \"usda\": \"USD ASCII\",\n                    \"usdc\": \"USD Crate\",\n                },\n            ),\n            EnumDef(\n                \"time_samples\",\n                label=\"Time samples\",\n                default=self.default_time_samples,\n                items={\n                    \"Current Frame\": \"Current Frame\",\n                    \"Frame Range\": \"Frame Range\",\n                    \"Project Settings\": \"Project Settings\",\n                    \"Defined Layer Metadata\": \"Defined Layer Metadata\",\n                },\n            ),\n            NumberDef(\n                \"frame_start\",\n                label=\"Frame start\",\n                default=frame_start,\n                decimals=0,\n            ),\n            NumberDef(\n                \"frame_end\",\n                label=\"Frame end\",\n                default=frame_end,\n                decimals=0,\n            ),\n            NumberDef(\n                \"samples_per_frame\",\n                label=\"Samples per frame\",\n                default=self.default_samples_per_frame,\n                minimum=0.001,\n                decimals=3,\n            ),\n            EnumDef(\n                \"export_method\",\n                label=\"Export method\",\n                default=self.default_export_method,\n                items={\n                    \"Keep Composition Arcs\": \"Keep Composition Arcs\",\n                    \"Flatten Sublayers, Keep References\": (\n                        \"Flatten Sublayers, Keep References\"\n                    ),\n                    \"Flatten All\": \"Flatten All\",\n                },\n            ),\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_usd_layer.html#client.ayon_katana.plugins.create.create_usd_layer.CreateUsdLayer.create","title":"<code>create(product_name, instance_data, pre_create_data)</code>","text":"<p>Create, configure, and optionally connect <code>UsdLayerExport</code>.</p> Source code in <code>client/ayon_katana/plugins/create/create_usd_layer.py</code> <pre><code>def create(\n    self,\n    product_name: str,\n    instance_data: dict[str, Any],\n    pre_create_data: dict[str, Any],\n):\n    \"\"\"Create, configure, and optionally connect ``UsdLayerExport``.\"\"\"\n    use_selection = bool(pre_create_data.get(\"use_selection\", True))\n    source_port = self._selected_output_port() if use_selection else None\n    frame_start_default, frame_end_default = self._current_frame_range()\n    frame_start = float(pre_create_data.get(\"frame_start\", frame_start_default))\n    frame_end = float(pre_create_data.get(\"frame_end\", frame_end_default))\n    file_format = pre_create_data.get(\"usd_format\") or self.default_usd_format\n    time_samples = pre_create_data.get(\"time_samples\") or self.default_time_samples\n    samples_per_frame = float(\n        pre_create_data.get(\n            \"samples_per_frame\",\n            self.default_samples_per_frame,\n        )\n    )\n    export_method = (\n        pre_create_data.get(\"export_method\") or self.default_export_method\n    )\n\n    instance_data[\"node_type\"] = \"UsdLayerExport\"\n    families = instance_data.setdefault(\"families\", [])\n    for family in (\"usd\", \"katana.usd\"):\n        if family not in families:\n            families.append(family)\n\n    created_instance = None\n    export_node = None\n    try:\n        created_instance = super().create(\n            product_name,\n            instance_data,\n            pre_create_data,\n        )\n        export_node = created_instance.transient_data[\"node\"]\n        try:\n            configure_usd_layer_export(\n                export_node,\n                file_format=file_format,\n                time_samples=time_samples,\n                frame_start=frame_start,\n                frame_end=frame_end,\n                samples_per_frame=samples_per_frame,\n                export_method=export_method,\n            )\n        except ValueError as exc:\n            raise CreatorError(f\"Katana USD layer creator failed: {exc}\") from exc\n        if source_port is not None:\n            input_port = export_node.getInputPort(\"in\")\n            if input_port is None:\n                raise RuntimeError(\n                    \"UsdLayerExport does not expose its native 'in' port.\"\n                )\n            source_port.connect(input_port)\n\n        return created_instance\n    except Exception:\n        if export_node is not None:\n            with suppress(Exception):\n                export_node.delete()\n        if created_instance is not None:\n            with suppress(Exception):\n                self._remove_instance_from_context(created_instance)\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_usd_layer.html#client.ayon_katana.plugins.create.create_usd_layer.CreateUsdLayer.get_pre_create_attr_defs","title":"<code>get_pre_create_attr_defs()</code>","text":"<p>Return native USD export settings used during creation.</p> Source code in <code>client/ayon_katana/plugins/create/create_usd_layer.py</code> <pre><code>def get_pre_create_attr_defs(self):\n    \"\"\"Return native USD export settings used during creation.\"\"\"\n    frame_start, frame_end = self._current_frame_range()\n    return [\n        BoolDef(\n            \"use_selection\",\n            label=\"Connect selected node\",\n            default=not self.create_context.headless,\n        ),\n        EnumDef(\n            \"usd_format\",\n            label=\"USD format\",\n            default=self.default_usd_format,\n            items={\n                \"usd\": \"USD\",\n                \"usda\": \"USD ASCII\",\n                \"usdc\": \"USD Crate\",\n            },\n        ),\n        EnumDef(\n            \"time_samples\",\n            label=\"Time samples\",\n            default=self.default_time_samples,\n            items={\n                \"Current Frame\": \"Current Frame\",\n                \"Frame Range\": \"Frame Range\",\n                \"Project Settings\": \"Project Settings\",\n                \"Defined Layer Metadata\": \"Defined Layer Metadata\",\n            },\n        ),\n        NumberDef(\n            \"frame_start\",\n            label=\"Frame start\",\n            default=frame_start,\n            decimals=0,\n        ),\n        NumberDef(\n            \"frame_end\",\n            label=\"Frame end\",\n            default=frame_end,\n            decimals=0,\n        ),\n        NumberDef(\n            \"samples_per_frame\",\n            label=\"Samples per frame\",\n            default=self.default_samples_per_frame,\n            minimum=0.001,\n            decimals=3,\n        ),\n        EnumDef(\n            \"export_method\",\n            label=\"Export method\",\n            default=self.default_export_method,\n            items={\n                \"Keep Composition Arcs\": \"Keep Composition Arcs\",\n                \"Flatten Sublayers, Keep References\": (\n                    \"Flatten Sublayers, Keep References\"\n                ),\n                \"Flatten All\": \"Flatten All\",\n            },\n        ),\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_usd_layer.html#client.ayon_katana.plugins.create.create_usd_layer.CreateUsdLayout","title":"<code>CreateUsdLayout</code>","text":"<p>               Bases: <code>CreateUsdLayer</code></p> <p>Publish a Katana-authored USD shot layout layer.</p> Source code in <code>client/ayon_katana/plugins/create/create_usd_layer.py</code> <pre><code>class CreateUsdLayout(CreateUsdLayer):\n    \"\"\"Publish a Katana-authored USD shot layout layer.\"\"\"\n\n    identifier = \"io.ayon.creators.katana.usd_layout\"\n    label = \"USD Layout\"\n    product_base_type = \"layout\"\n    product_type = product_base_type\n    icon = \"object-group\"\n    description = \"Publish native USD shot composition and placement as layout\"\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_usd_layer.html#client.ayon_katana.plugins.create.create_usd_layer.CreateUsdLook","title":"<code>CreateUsdLook</code>","text":"<p>               Bases: <code>CreateUsdLayer</code></p> <p>Publish a Katana-authored USD material and assignment layer.</p> Source code in <code>client/ayon_katana/plugins/create/create_usd_layer.py</code> <pre><code>class CreateUsdLook(CreateUsdLayer):\n    \"\"\"Publish a Katana-authored USD material and assignment layer.\"\"\"\n\n    identifier = \"io.ayon.creators.katana.usd_look\"\n    label = \"USD Look\"\n    product_base_type = \"look\"\n    product_type = product_base_type\n    icon = \"paint-brush\"\n    description = \"Publish native USD materials and assignments as an AYON look\"\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_workfile.html","title":"create_workfile","text":"<p>Creator plugin for creating workfiles.</p>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_workfile.html#client.ayon_katana.plugins.create.create_workfile.CreateWorkfile","title":"<code>CreateWorkfile</code>","text":"<p>               Bases: <code>KatanaCreatorBase</code>, <code>AutoCreator</code></p> <p>Workfile auto-creator.</p> Source code in <code>client/ayon_katana/plugins/create/create_workfile.py</code> <pre><code>class CreateWorkfile(plugin.KatanaCreatorBase, AutoCreator):\n    \"\"\"Workfile auto-creator.\"\"\"\n\n    settings_category = \"katana\"\n    identifier = \"io.ayon.creators.katana.workfile\"\n    label = \"Workfile\"\n    product_base_type = \"workfile\"\n    product_type = product_base_type\n    icon = \"fa5.file\"\n\n    default_variant = \"Main\"\n    is_mandatory = False\n\n    def create(self):\n        \"\"\"Create or synchronize the mandatory workfile instance.\"\"\"\n        variant = self.default_variant\n        current_instance = next(\n            (\n                instance\n                for instance in self.create_context.instances\n                if instance.creator_identifier == self.identifier\n            ),\n            None,\n        )\n\n        project_entity = self.create_context.get_current_project_entity()\n        folder_entity = self.create_context.get_current_folder_entity()\n        task_entity = self.create_context.get_current_task_entity()\n\n        project_name = project_entity[\"name\"]\n        folder_path = folder_entity[\"path\"]\n        task_name = task_entity[\"name\"]\n        host_name = self.create_context.host_name\n\n        if current_instance is None:\n            product_name = self.get_product_name(\n                project_name=project_name,\n                project_entity=project_entity,\n                folder_entity=folder_entity,\n                task_entity=task_entity,\n                variant=variant,\n                host_name=host_name,\n                product_type=self.product_type,\n            )\n            data = {\n                \"folderPath\": folder_path,\n                \"task\": task_name,\n                \"productType\": self.product_type,\n                \"variant\": variant,\n            }\n\n            self.log.info(\"Auto-creating workfile instance...\")\n            current_instance = CreatedInstance(\n                product_base_type=self.product_base_type,\n                product_type=self.product_type,\n                product_name=product_name,\n                data=data,\n                creator=self,\n            )\n            self._add_instance_to_context(current_instance)\n        elif (\n            current_instance[\"folderPath\"] != folder_path\n            or current_instance[\"task\"] != task_name\n        ):\n            product_name = self.get_product_name(\n                project_name=project_name,\n                project_entity=project_entity,\n                folder_entity=folder_entity,\n                task_entity=task_entity,\n                variant=variant,\n                host_name=host_name,\n                product_type=self.product_type,\n            )\n            current_instance[\"folderPath\"] = folder_path\n            current_instance[\"task\"] = task_name\n            current_instance[\"productType\"] = self.product_type\n            current_instance[\"productName\"] = product_name\n\n        if hasattr(current_instance, \"set_mandatory\"):\n            current_instance.set_mandatory(self.is_mandatory)\n\n        context.set_workfile_instance_data(current_instance.data_to_store())\n\n    def collect_instances(self):\n        \"\"\"Collect the workfile instance embedded in the Katana project.\"\"\"\n        workfile = context.get_workfile_instance_data()\n        if not workfile:\n            return\n\n        created_instance = CreatedInstance.from_existing(workfile, self)\n        self._add_instance_to_context(created_instance)\n\n    def update_instances(self, update_list):\n        \"\"\"Persist workfile creator-instance changes in the project.\"\"\"\n        for created_inst, _changes in update_list:\n            if created_inst[\"creator_identifier\"] != self.identifier:\n                continue\n            context.set_workfile_instance_data(created_inst.data_to_store())\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_workfile.html#client.ayon_katana.plugins.create.create_workfile.CreateWorkfile.collect_instances","title":"<code>collect_instances()</code>","text":"<p>Collect the workfile instance embedded in the Katana project.</p> Source code in <code>client/ayon_katana/plugins/create/create_workfile.py</code> <pre><code>def collect_instances(self):\n    \"\"\"Collect the workfile instance embedded in the Katana project.\"\"\"\n    workfile = context.get_workfile_instance_data()\n    if not workfile:\n        return\n\n    created_instance = CreatedInstance.from_existing(workfile, self)\n    self._add_instance_to_context(created_instance)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_workfile.html#client.ayon_katana.plugins.create.create_workfile.CreateWorkfile.create","title":"<code>create()</code>","text":"<p>Create or synchronize the mandatory workfile instance.</p> Source code in <code>client/ayon_katana/plugins/create/create_workfile.py</code> <pre><code>def create(self):\n    \"\"\"Create or synchronize the mandatory workfile instance.\"\"\"\n    variant = self.default_variant\n    current_instance = next(\n        (\n            instance\n            for instance in self.create_context.instances\n            if instance.creator_identifier == self.identifier\n        ),\n        None,\n    )\n\n    project_entity = self.create_context.get_current_project_entity()\n    folder_entity = self.create_context.get_current_folder_entity()\n    task_entity = self.create_context.get_current_task_entity()\n\n    project_name = project_entity[\"name\"]\n    folder_path = folder_entity[\"path\"]\n    task_name = task_entity[\"name\"]\n    host_name = self.create_context.host_name\n\n    if current_instance is None:\n        product_name = self.get_product_name(\n            project_name=project_name,\n            project_entity=project_entity,\n            folder_entity=folder_entity,\n            task_entity=task_entity,\n            variant=variant,\n            host_name=host_name,\n            product_type=self.product_type,\n        )\n        data = {\n            \"folderPath\": folder_path,\n            \"task\": task_name,\n            \"productType\": self.product_type,\n            \"variant\": variant,\n        }\n\n        self.log.info(\"Auto-creating workfile instance...\")\n        current_instance = CreatedInstance(\n            product_base_type=self.product_base_type,\n            product_type=self.product_type,\n            product_name=product_name,\n            data=data,\n            creator=self,\n        )\n        self._add_instance_to_context(current_instance)\n    elif (\n        current_instance[\"folderPath\"] != folder_path\n        or current_instance[\"task\"] != task_name\n    ):\n        product_name = self.get_product_name(\n            project_name=project_name,\n            project_entity=project_entity,\n            folder_entity=folder_entity,\n            task_entity=task_entity,\n            variant=variant,\n            host_name=host_name,\n            product_type=self.product_type,\n        )\n        current_instance[\"folderPath\"] = folder_path\n        current_instance[\"task\"] = task_name\n        current_instance[\"productType\"] = self.product_type\n        current_instance[\"productName\"] = product_name\n\n    if hasattr(current_instance, \"set_mandatory\"):\n        current_instance.set_mandatory(self.is_mandatory)\n\n    context.set_workfile_instance_data(current_instance.data_to_store())\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/create/create_workfile.html#client.ayon_katana.plugins.create.create_workfile.CreateWorkfile.update_instances","title":"<code>update_instances(update_list)</code>","text":"<p>Persist workfile creator-instance changes in the project.</p> Source code in <code>client/ayon_katana/plugins/create/create_workfile.py</code> <pre><code>def update_instances(self, update_list):\n    \"\"\"Persist workfile creator-instance changes in the project.\"\"\"\n    for created_inst, _changes in update_list:\n        if created_inst[\"creator_identifier\"] != self.identifier:\n            continue\n        context.set_workfile_instance_data(created_inst.data_to_store())\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/deadline/index.html","title":"deadline","text":"<p>Deadline submission plugins for AYON Katana.</p>"},{"location":"autoapi/client/ayon_katana/plugins/deadline/submit_katana_deadline.html","title":"submit_katana_deadline","text":"<p>Submit Katana outputs to Deadline through CommandLine.</p>"},{"location":"autoapi/client/ayon_katana/plugins/deadline/submit_katana_deadline.html#client.ayon_katana.plugins.deadline.submit_katana_deadline.KatanaSubmitDeadline","title":"<code>KatanaSubmitDeadline</code>","text":"<p>               Bases: <code>AbstractSubmitDeadline</code>, <code>AYONPyblishPluginMixin</code></p> <p>Submit Render and ImageWrite outputs through CommandLine.</p> Source code in <code>client/ayon_katana/plugins/deadline/submit_katana_deadline.py</code> <pre><code>class KatanaSubmitDeadline(\n    abstract_submit_deadline.AbstractSubmitDeadline,\n    AYONPyblishPluginMixin,\n):\n    \"\"\"Submit Render and ImageWrite outputs through CommandLine.\"\"\"\n\n    label = \"Submit Output to Deadline\"\n    # Core's workfile integration must finish before Deadline resolves\n    # ``use_published=True`` to the published scene path.\n    order = pyblish.api.IntegratorOrder + 0.05\n    hosts = [\"katana\"]\n    families = [\"render\", \"katana.render\", \"image\", \"katana.image\"]\n    targets = [\"local\"]\n    settings_category = \"deadline\"\n\n    def process(self, instance):\n        \"\"\"Submit farm output instances.\"\"\"\n        if not instance.data.get(\"farm\"):\n            self.log.debug(\"Katana farm rendering is disabled; skipping Deadline.\")\n            return\n        super().process(instance)\n        instance.data[\"outputDir\"] = os.path.dirname(instance.data[\"files\"][0])\n\n    def get_job_info(self, job_info=None, **_kwargs):\n        \"\"\"Build Deadline job information for Katana batch mode.\"\"\"\n        instance = self._instance\n        context = instance.context\n        job_info.Plugin = \"CommandLine\"\n        if not job_info.Frames:\n            start = int(instance.data[\"frameStartHandle\"])\n            end = int(instance.data[\"frameEndHandle\"])\n            step = int(instance.data[\"byFrameStep\"])\n            job_info.Frames = f\"{start}-{end}x{step}\"\n\n        filename = os.path.basename(context.data[\"currentFile\"])\n        families = set(instance.data.get(\"families\") or [])\n        job_kind = \"IMAGE\" if {\"image\", \"katana.image\"} &amp; families else \"RENDER\"\n        job_info.Name = f\"{filename} - {instance.name} [{job_kind}]\"\n        job_info.BatchName = filename\n        return job_info\n\n    def get_plugin_info(self, **_kwargs):\n        \"\"\"Build Deadline CommandLine plugin data.\"\"\"\n        executable = render.get_katana_executable()\n        render_node = self._instance.data[\"render_node\"]\n        if not render_node:\n            raise RuntimeError(\n                \"Katana Deadline submission has no Render or ImageWrite node.\"\n            )\n        arguments = (\n            f'--batch --katana-file=\"{self.scene_path}\" '\n            f'-t &lt;STARTFRAME&gt;-&lt;ENDFRAME&gt; --render-node=\"{render_node}\"'\n        )\n        return {\n            \"Executable\": str(executable),\n            \"Arguments\": arguments,\n            \"SceneFile\": self.scene_path,\n            \"StartupDirectory\": str(executable.parent),\n            \"ShellExecute\": False,\n            \"SingleFramesOnly\": False,\n        }\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/deadline/submit_katana_deadline.html#client.ayon_katana.plugins.deadline.submit_katana_deadline.KatanaSubmitDeadline.get_job_info","title":"<code>get_job_info(job_info=None, **_kwargs)</code>","text":"<p>Build Deadline job information for Katana batch mode.</p> Source code in <code>client/ayon_katana/plugins/deadline/submit_katana_deadline.py</code> <pre><code>def get_job_info(self, job_info=None, **_kwargs):\n    \"\"\"Build Deadline job information for Katana batch mode.\"\"\"\n    instance = self._instance\n    context = instance.context\n    job_info.Plugin = \"CommandLine\"\n    if not job_info.Frames:\n        start = int(instance.data[\"frameStartHandle\"])\n        end = int(instance.data[\"frameEndHandle\"])\n        step = int(instance.data[\"byFrameStep\"])\n        job_info.Frames = f\"{start}-{end}x{step}\"\n\n    filename = os.path.basename(context.data[\"currentFile\"])\n    families = set(instance.data.get(\"families\") or [])\n    job_kind = \"IMAGE\" if {\"image\", \"katana.image\"} &amp; families else \"RENDER\"\n    job_info.Name = f\"{filename} - {instance.name} [{job_kind}]\"\n    job_info.BatchName = filename\n    return job_info\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/deadline/submit_katana_deadline.html#client.ayon_katana.plugins.deadline.submit_katana_deadline.KatanaSubmitDeadline.get_plugin_info","title":"<code>get_plugin_info(**_kwargs)</code>","text":"<p>Build Deadline CommandLine plugin data.</p> Source code in <code>client/ayon_katana/plugins/deadline/submit_katana_deadline.py</code> <pre><code>def get_plugin_info(self, **_kwargs):\n    \"\"\"Build Deadline CommandLine plugin data.\"\"\"\n    executable = render.get_katana_executable()\n    render_node = self._instance.data[\"render_node\"]\n    if not render_node:\n        raise RuntimeError(\n            \"Katana Deadline submission has no Render or ImageWrite node.\"\n        )\n    arguments = (\n        f'--batch --katana-file=\"{self.scene_path}\" '\n        f'-t &lt;STARTFRAME&gt;-&lt;ENDFRAME&gt; --render-node=\"{render_node}\"'\n    )\n    return {\n        \"Executable\": str(executable),\n        \"Arguments\": arguments,\n        \"SceneFile\": self.scene_path,\n        \"StartupDirectory\": str(executable.parent),\n        \"ShellExecute\": False,\n        \"SingleFramesOnly\": False,\n    }\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/deadline/submit_katana_deadline.html#client.ayon_katana.plugins.deadline.submit_katana_deadline.KatanaSubmitDeadline.process","title":"<code>process(instance)</code>","text":"<p>Submit farm output instances.</p> Source code in <code>client/ayon_katana/plugins/deadline/submit_katana_deadline.py</code> <pre><code>def process(self, instance):\n    \"\"\"Submit farm output instances.\"\"\"\n    if not instance.data.get(\"farm\"):\n        self.log.debug(\"Katana farm rendering is disabled; skipping Deadline.\")\n        return\n    super().process(instance)\n    instance.data[\"outputDir\"] = os.path.dirname(instance.data[\"files\"][0])\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/inventory/index.html","title":"inventory","text":"<p>Katana inventory action plugins.</p>"},{"location":"autoapi/client/ayon_katana/plugins/inventory/select_containers.html","title":"select_containers","text":"<p>Select Katana nodes owned by chosen AYON containers.</p>"},{"location":"autoapi/client/ayon_katana/plugins/inventory/select_containers.html#client.ayon_katana.plugins.inventory.select_containers.SelectInScene","title":"<code>SelectInScene</code>","text":"<p>               Bases: <code>InventoryAction</code></p> <p>Select Katana nodes represented by Scene Inventory containers.</p> Source code in <code>client/ayon_katana/plugins/inventory/select_containers.py</code> <pre><code>class SelectInScene(InventoryAction):\n    \"\"\"Select Katana nodes represented by Scene Inventory containers.\"\"\"\n\n    label = \"Select in scene\"\n    icon = \"search\"\n    color = \"#888888\"\n    order = 99\n\n    @staticmethod\n    def is_compatible(container):\n        \"\"\"Return whether a container resolves to a Katana node.\"\"\"\n        return _get_container_node(container) is not None\n\n    def process(self, containers):\n        \"\"\"Select the Katana nodes for the chosen containers.\"\"\"\n        nodes = [\n            node\n            for node in (_get_container_node(container) for container in containers)\n            if node is not None\n        ]\n        if not nodes:\n            return\n\n        compat.set_selected_nodes(nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/inventory/select_containers.html#client.ayon_katana.plugins.inventory.select_containers.SelectInScene.is_compatible","title":"<code>is_compatible(container)</code>  <code>staticmethod</code>","text":"<p>Return whether a container resolves to a Katana node.</p> Source code in <code>client/ayon_katana/plugins/inventory/select_containers.py</code> <pre><code>@staticmethod\ndef is_compatible(container):\n    \"\"\"Return whether a container resolves to a Katana node.\"\"\"\n    return _get_container_node(container) is not None\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/inventory/select_containers.html#client.ayon_katana.plugins.inventory.select_containers.SelectInScene.process","title":"<code>process(containers)</code>","text":"<p>Select the Katana nodes for the chosen containers.</p> Source code in <code>client/ayon_katana/plugins/inventory/select_containers.py</code> <pre><code>def process(self, containers):\n    \"\"\"Select the Katana nodes for the chosen containers.\"\"\"\n    nodes = [\n        node\n        for node in (_get_container_node(container) for container in containers)\n        if node is not None\n    ]\n    if not nodes:\n        return\n\n    compat.set_selected_nodes(nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/inventory/select_managed_sources.html","title":"select_managed_sources","text":"<p>Scene Inventory action for selecting loader-owned source nodes.</p>"},{"location":"autoapi/client/ayon_katana/plugins/inventory/select_managed_sources.html#client.ayon_katana.plugins.inventory.select_managed_sources.SelectManagedSources","title":"<code>SelectManagedSources</code>","text":"<p>               Bases: <code>InventoryAction</code></p> <p>Select the managed source nodes for loaded containers.</p> Source code in <code>client/ayon_katana/plugins/inventory/select_managed_sources.py</code> <pre><code>class SelectManagedSources(InventoryAction):\n    \"\"\"Select the managed source nodes for loaded containers.\"\"\"\n\n    label = \"Select Managed Sources\"\n    icon = \"code-fork\"\n    color = \"#888888\"\n    order = 100\n\n    @staticmethod\n    def is_compatible(container) -&gt; bool:\n        \"\"\"Return whether the container has a managed source node.\"\"\"\n        container_node = container.get(\"node\")\n        if container_node is None:\n            object_name = container.get(\"objectName\")\n            container_node = compat.get_node(object_name) if object_name else None\n        if container_node is None:\n            return False\n        return (\n            container_api.find_managed_node(container_node, container_api.SOURCE_ROLE)\n            is not None\n        )\n\n    def process(self, containers) -&gt; None:\n        \"\"\"Replace Katana's current selection with managed source nodes.\"\"\"\n        source_nodes = []\n        for container in containers:\n            container_node = container.get(\"node\")\n            if container_node is None:\n                object_name = container.get(\"objectName\")\n                container_node = compat.get_node(object_name) if object_name else None\n            if container_node is None:\n                continue\n\n            source_node = container_api.find_managed_node(\n                container_node, container_api.SOURCE_ROLE\n            )\n            if source_node is not None:\n                source_nodes.append(source_node)\n\n        if source_nodes:\n            compat.set_selected_nodes(source_nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/inventory/select_managed_sources.html#client.ayon_katana.plugins.inventory.select_managed_sources.SelectManagedSources.is_compatible","title":"<code>is_compatible(container)</code>  <code>staticmethod</code>","text":"<p>Return whether the container has a managed source node.</p> Source code in <code>client/ayon_katana/plugins/inventory/select_managed_sources.py</code> <pre><code>@staticmethod\ndef is_compatible(container) -&gt; bool:\n    \"\"\"Return whether the container has a managed source node.\"\"\"\n    container_node = container.get(\"node\")\n    if container_node is None:\n        object_name = container.get(\"objectName\")\n        container_node = compat.get_node(object_name) if object_name else None\n    if container_node is None:\n        return False\n    return (\n        container_api.find_managed_node(container_node, container_api.SOURCE_ROLE)\n        is not None\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/inventory/select_managed_sources.html#client.ayon_katana.plugins.inventory.select_managed_sources.SelectManagedSources.process","title":"<code>process(containers)</code>","text":"<p>Replace Katana's current selection with managed source nodes.</p> Source code in <code>client/ayon_katana/plugins/inventory/select_managed_sources.py</code> <pre><code>def process(self, containers) -&gt; None:\n    \"\"\"Replace Katana's current selection with managed source nodes.\"\"\"\n    source_nodes = []\n    for container in containers:\n        container_node = container.get(\"node\")\n        if container_node is None:\n            object_name = container.get(\"objectName\")\n            container_node = compat.get_node(object_name) if object_name else None\n        if container_node is None:\n            continue\n\n        source_node = container_api.find_managed_node(\n            container_node, container_api.SOURCE_ROLE\n        )\n        if source_node is not None:\n            source_nodes.append(source_node)\n\n    if source_nodes:\n        compat.set_selected_nodes(source_nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/index.html","title":"load","text":"<p>Katana representation loader plugins.</p>"},{"location":"autoapi/client/ayon_katana/plugins/load/clear_usd_resolver_cache.html","title":"clear_usd_resolver_cache","text":"<p>Loader action for the optional AYON USD Resolver cache.</p>"},{"location":"autoapi/client/ayon_katana/plugins/load/clear_usd_resolver_cache.html#client.ayon_katana.plugins.load.clear_usd_resolver_cache.ClearUsdResolverCache","title":"<code>ClearUsdResolverCache</code>","text":"<p>               Bases: <code>KatanaLoader</code></p> <p>Clear the optional resolver cache and flush native UsdIn stages.</p> Source code in <code>client/ayon_katana/plugins/load/clear_usd_resolver_cache.py</code> <pre><code>class ClearUsdResolverCache(plugin.KatanaLoader):\n    \"\"\"Clear the optional resolver cache and flush native UsdIn stages.\"\"\"\n\n    product_base_types = USD_PRODUCT_BASE_TYPES\n    product_types = product_base_types\n    representations = {\"*\"}\n    extensions = {\"usd\", \"usda\", \"usdc\", \"usdlc\", \"usdnc\", \"usdz\"}\n    label = \"Clear AYON USD Resolver Cache\"\n    order = 12\n    icon = \"refresh\"\n    color = \"white\"\n\n    def load(self, context, name=None, namespace=None, options=None):\n        \"\"\"Clear caches without creating or changing a container.\"\"\"\n        return clear_resolver_cache()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/clear_usd_resolver_cache.html#client.ayon_katana.plugins.load.clear_usd_resolver_cache.ClearUsdResolverCache.load","title":"<code>load(context, name=None, namespace=None, options=None)</code>","text":"<p>Clear caches without creating or changing a container.</p> Source code in <code>client/ayon_katana/plugins/load/clear_usd_resolver_cache.py</code> <pre><code>def load(self, context, name=None, namespace=None, options=None):\n    \"\"\"Clear caches without creating or changing a container.\"\"\"\n    return clear_resolver_cache()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_alembic.html","title":"load_alembic","text":"<p>Load Alembic representations into managed Katana node graphs.</p>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_alembic.html#client.ayon_katana.plugins.load.load_alembic.AbcLoader","title":"<code>AbcLoader</code>","text":"<p>               Bases: <code>KatanaLoader</code></p> <p>Load and manage Alembic data through a Katana Alembic_In node.</p> Source code in <code>client/ayon_katana/plugins/load/load_alembic.py</code> <pre><code>class AbcLoader(plugin.KatanaLoader):\n    \"\"\"Load and manage Alembic data through a Katana Alembic_In node.\"\"\"\n\n    product_base_types = {\n        \"alembic\",\n        \"animation\",\n        \"camera\",\n        \"model\",\n        \"pointcache\",\n    }\n    product_types = product_base_types\n    label = \"Load Alembic\"\n    representations = {\"*\"}\n    extensions = {\"abc\"}\n    order = 2\n\n    icon = \"database\"\n    color = \"orange\"\n\n    @classmethod\n    def get_options(cls, contexts):\n        \"\"\"Return loader options shared by the selected representations.\"\"\"\n        return [\n            TextDef(\n                \"location\",\n                label=\"Scenegraph location\",\n                default=\"\",\n                tooltip=(\n                    \"Optional Alembic scenegraph location, for example \"\n                    \"'/root/world/geo'. Leave empty to use Katana's native \"\n                    \"default.\"\n                ),\n            )\n        ]\n\n    def load(self, context, name=None, namespace=None, options=None):\n        \"\"\"Load an Alembic representation and return its container node.\"\"\"\n        options = options or {}\n        product_name = name or context[\"product\"][\"name\"]\n        namespace = namespace or context[\"folder\"][\"name\"]\n        container_node = None\n        try:\n            container_node = containers.containerise(\n                name=product_name,\n                namespace=namespace,\n                context=context,\n                loader=self.__class__.__name__,\n            )\n            managed_group = containers.get_managed_group(container_node)\n            if managed_group is None:\n                raise RuntimeError(\"Failed to create the AYON managed group.\")\n\n            source_node = NodegraphAPI.CreateNode(\"Alembic_In\", managed_group)\n            source_node.setName(f\"{namespace}_{product_name}_AlembicIn\")\n            source_node.getParameter(\"abcAsset\").setValue(\n                self.filepath_from_context(context), 0.0\n            )\n            location = options.get(\"location\")\n            if location:\n                source_node.getParameter(\"name\").setValue(location, 0.0)\n            containers.set_managed_node_role(source_node, containers.SOURCE_ROLE)\n            source_node.getOutputPort(\"out\").connect(managed_group.getReturnPort(\"out\"))\n            self[:] = [container_node, source_node]\n            return container_node\n        except Exception:\n            if container_node is not None:\n                with suppress(Exception):\n                    container_node.delete()\n            raise\n\n    def update(self, container, context):\n        \"\"\"Update an Alembic container to a new representation.\"\"\"\n        container_node = container[\"node\"]\n        source_node = containers.find_managed_node(\n            container_node, containers.SOURCE_ROLE\n        )\n        if source_node is None:\n            raise RuntimeError(\n                \"The AYON container is missing its managed Alembic_In node.\"\n            )\n        filepath = self.filepath_from_context(context)\n        file_parameter = source_node.getParameter(\"abcAsset\")\n        old_filepath = file_parameter.getValue(0.0)\n        old_container_data = containers.parse_container(container_node)\n        if old_container_data is None:\n            raise RuntimeError(\"The AYON Alembic container has invalid metadata.\")\n        old_container_data.pop(\"node\", None)\n        old_container_data.pop(\"objectName\", None)\n        project = context.get(\"project\") or {}\n        try:\n            file_parameter.setValue(filepath, 0.0)\n            containers.update_container(\n                container_node,\n                {\n                    \"representation\": context[\"representation\"][\"id\"],\n                    \"project_name\": project.get(\"name\"),\n                    \"loader\": self.__class__.__name__,\n                },\n            )\n        except Exception:\n            with suppress(Exception):\n                file_parameter.setValue(old_filepath, 0.0)\n            with suppress(Exception):\n                containers.update_container(container_node, old_container_data)\n            raise\n\n    def remove(self, container):\n        \"\"\"Remove an Alembic container from the Katana project.\"\"\"\n        container[\"node\"].delete()\n\n    def switch(self, container, context):\n        \"\"\"Switch an Alembic container to another representation.\"\"\"\n        self.update(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_alembic.html#client.ayon_katana.plugins.load.load_alembic.AbcLoader.get_options","title":"<code>get_options(contexts)</code>  <code>classmethod</code>","text":"<p>Return loader options shared by the selected representations.</p> Source code in <code>client/ayon_katana/plugins/load/load_alembic.py</code> <pre><code>@classmethod\ndef get_options(cls, contexts):\n    \"\"\"Return loader options shared by the selected representations.\"\"\"\n    return [\n        TextDef(\n            \"location\",\n            label=\"Scenegraph location\",\n            default=\"\",\n            tooltip=(\n                \"Optional Alembic scenegraph location, for example \"\n                \"'/root/world/geo'. Leave empty to use Katana's native \"\n                \"default.\"\n            ),\n        )\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_alembic.html#client.ayon_katana.plugins.load.load_alembic.AbcLoader.load","title":"<code>load(context, name=None, namespace=None, options=None)</code>","text":"<p>Load an Alembic representation and return its container node.</p> Source code in <code>client/ayon_katana/plugins/load/load_alembic.py</code> <pre><code>def load(self, context, name=None, namespace=None, options=None):\n    \"\"\"Load an Alembic representation and return its container node.\"\"\"\n    options = options or {}\n    product_name = name or context[\"product\"][\"name\"]\n    namespace = namespace or context[\"folder\"][\"name\"]\n    container_node = None\n    try:\n        container_node = containers.containerise(\n            name=product_name,\n            namespace=namespace,\n            context=context,\n            loader=self.__class__.__name__,\n        )\n        managed_group = containers.get_managed_group(container_node)\n        if managed_group is None:\n            raise RuntimeError(\"Failed to create the AYON managed group.\")\n\n        source_node = NodegraphAPI.CreateNode(\"Alembic_In\", managed_group)\n        source_node.setName(f\"{namespace}_{product_name}_AlembicIn\")\n        source_node.getParameter(\"abcAsset\").setValue(\n            self.filepath_from_context(context), 0.0\n        )\n        location = options.get(\"location\")\n        if location:\n            source_node.getParameter(\"name\").setValue(location, 0.0)\n        containers.set_managed_node_role(source_node, containers.SOURCE_ROLE)\n        source_node.getOutputPort(\"out\").connect(managed_group.getReturnPort(\"out\"))\n        self[:] = [container_node, source_node]\n        return container_node\n    except Exception:\n        if container_node is not None:\n            with suppress(Exception):\n                container_node.delete()\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_alembic.html#client.ayon_katana.plugins.load.load_alembic.AbcLoader.remove","title":"<code>remove(container)</code>","text":"<p>Remove an Alembic container from the Katana project.</p> Source code in <code>client/ayon_katana/plugins/load/load_alembic.py</code> <pre><code>def remove(self, container):\n    \"\"\"Remove an Alembic container from the Katana project.\"\"\"\n    container[\"node\"].delete()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_alembic.html#client.ayon_katana.plugins.load.load_alembic.AbcLoader.switch","title":"<code>switch(container, context)</code>","text":"<p>Switch an Alembic container to another representation.</p> Source code in <code>client/ayon_katana/plugins/load/load_alembic.py</code> <pre><code>def switch(self, container, context):\n    \"\"\"Switch an Alembic container to another representation.\"\"\"\n    self.update(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_alembic.html#client.ayon_katana.plugins.load.load_alembic.AbcLoader.update","title":"<code>update(container, context)</code>","text":"<p>Update an Alembic container to a new representation.</p> Source code in <code>client/ayon_katana/plugins/load/load_alembic.py</code> <pre><code>def update(self, container, context):\n    \"\"\"Update an Alembic container to a new representation.\"\"\"\n    container_node = container[\"node\"]\n    source_node = containers.find_managed_node(\n        container_node, containers.SOURCE_ROLE\n    )\n    if source_node is None:\n        raise RuntimeError(\n            \"The AYON container is missing its managed Alembic_In node.\"\n        )\n    filepath = self.filepath_from_context(context)\n    file_parameter = source_node.getParameter(\"abcAsset\")\n    old_filepath = file_parameter.getValue(0.0)\n    old_container_data = containers.parse_container(container_node)\n    if old_container_data is None:\n        raise RuntimeError(\"The AYON Alembic container has invalid metadata.\")\n    old_container_data.pop(\"node\", None)\n    old_container_data.pop(\"objectName\", None)\n    project = context.get(\"project\") or {}\n    try:\n        file_parameter.setValue(filepath, 0.0)\n        containers.update_container(\n            container_node,\n            {\n                \"representation\": context[\"representation\"][\"id\"],\n                \"project_name\": project.get(\"name\"),\n                \"loader\": self.__class__.__name__,\n            },\n        )\n    except Exception:\n        with suppress(Exception):\n            file_parameter.setValue(old_filepath, 0.0)\n        with suppress(Exception):\n            containers.update_container(container_node, old_container_data)\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_image.html","title":"load_image","text":"<p>Load AYON image products through Katana ImageRead.</p>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_image.html#client.ayon_katana.plugins.load.load_image.ImageLoader","title":"<code>ImageLoader</code>","text":"<p>               Bases: <code>KatanaLoader</code></p> <p>Load still images and sequences into a managed ImageRead node.</p> Source code in <code>client/ayon_katana/plugins/load/load_image.py</code> <pre><code>class ImageLoader(plugin.KatanaLoader):\n    \"\"\"Load still images and sequences into a managed ImageRead node.\"\"\"\n\n    product_base_types = {\n        \"image\",\n        \"imagesequence\",\n        \"online\",\n        \"plate\",\n        \"render\",\n        \"review\",\n    }\n    product_types = product_base_types\n    label = \"Load Image\"\n    representations = {\"*\"}\n    extensions = {\n        \"bmp\",\n        \"cin\",\n        \"dpx\",\n        \"exr\",\n        \"gif\",\n        \"hdr\",\n        \"jpeg\",\n        \"jpg\",\n        \"png\",\n        \"psd\",\n        \"sgi\",\n        \"tga\",\n        \"tif\",\n        \"tiff\",\n        \"tx\",\n    }\n    order = -9\n\n    icon = \"picture-o\"\n    color = \"orange\"\n\n    @classmethod\n    def _path_from_context(cls, context: dict) -&gt; str:\n        filepath = cls.filepath_from_context(context)\n        return normalize_image_sequence_path(filepath, context[\"representation\"])\n\n    def load(self, context, name=None, namespace=None, options=None):\n        \"\"\"Load an image representation and return its container node.\"\"\"\n        product_name = name or context[\"product\"][\"name\"]\n        namespace = namespace or context[\"folder\"][\"name\"]\n        container_node = None\n        try:\n            container_node = containers.containerise(\n                name=product_name,\n                namespace=namespace,\n                context=context,\n                loader=self.__class__.__name__,\n            )\n            managed_group = containers.get_managed_group(container_node)\n            if managed_group is None:\n                raise RuntimeError(\"Failed to create the AYON managed group.\")\n\n            source_node = NodegraphAPI.CreateNode(\"ImageRead\", managed_group)\n            source_node.setName(f\"{namespace}_{product_name}_ImageRead\")\n            filepath = self._path_from_context(context)\n            source_node.getParameter(\"file\").setValue(filepath, 0.0)\n            colorspace = _image_colorspace(filepath, context)\n            if colorspace:\n                source_node.getParameter(\"image.colorspace\").setValue(\n                    colorspace,\n                    0.0,\n                )\n            containers.set_managed_node_role(source_node, containers.SOURCE_ROLE)\n            source_node.getOutputPort(\"out\").connect(managed_group.getReturnPort(\"out\"))\n            self[:] = [container_node, source_node]\n            return container_node\n        except Exception:\n            if container_node is not None:\n                with suppress(Exception):\n                    container_node.delete()\n            raise\n\n    def update(self, container, context):\n        \"\"\"Update the managed path, colorspace, and container transactionally.\"\"\"\n        container_node = container[\"node\"]\n        source_node = containers.find_managed_node(\n            container_node,\n            containers.SOURCE_ROLE,\n        )\n        if source_node is None:\n            raise RuntimeError(\n                \"The AYON container is missing its managed ImageRead node.\"\n            )\n\n        file_parameter = source_node.getParameter(\"file\")\n        colorspace_parameter = source_node.getParameter(\"image.colorspace\")\n        old_filepath = file_parameter.getValue(0.0)\n        old_colorspace = colorspace_parameter.getValue(0.0)\n        old_container_data = containers.parse_container(container_node)\n        if old_container_data is None:\n            raise RuntimeError(\"The AYON image container has invalid metadata.\")\n        old_container_data.pop(\"node\", None)\n        old_container_data.pop(\"objectName\", None)\n\n        filepath = self._path_from_context(context)\n        colorspace = _image_colorspace(filepath, context)\n        project = context.get(\"project\") or {}\n        try:\n            file_parameter.setValue(filepath, 0.0)\n            colorspace_parameter.setValue(colorspace, 0.0)\n            containers.update_container(\n                container_node,\n                {\n                    \"representation\": context[\"representation\"][\"id\"],\n                    \"project_name\": project.get(\"name\"),\n                    \"loader\": self.__class__.__name__,\n                },\n            )\n        except Exception:\n            with suppress(Exception):\n                file_parameter.setValue(old_filepath, 0.0)\n            with suppress(Exception):\n                colorspace_parameter.setValue(old_colorspace, 0.0)\n            with suppress(Exception):\n                containers.update_container(container_node, old_container_data)\n            raise\n\n    def remove(self, container):\n        \"\"\"Remove the complete AYON image container.\"\"\"\n        container[\"node\"].delete()\n\n    def switch(self, container, context):\n        \"\"\"Switch the container through the transactional update path.\"\"\"\n        self.update(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_image.html#client.ayon_katana.plugins.load.load_image.ImageLoader.load","title":"<code>load(context, name=None, namespace=None, options=None)</code>","text":"<p>Load an image representation and return its container node.</p> Source code in <code>client/ayon_katana/plugins/load/load_image.py</code> <pre><code>def load(self, context, name=None, namespace=None, options=None):\n    \"\"\"Load an image representation and return its container node.\"\"\"\n    product_name = name or context[\"product\"][\"name\"]\n    namespace = namespace or context[\"folder\"][\"name\"]\n    container_node = None\n    try:\n        container_node = containers.containerise(\n            name=product_name,\n            namespace=namespace,\n            context=context,\n            loader=self.__class__.__name__,\n        )\n        managed_group = containers.get_managed_group(container_node)\n        if managed_group is None:\n            raise RuntimeError(\"Failed to create the AYON managed group.\")\n\n        source_node = NodegraphAPI.CreateNode(\"ImageRead\", managed_group)\n        source_node.setName(f\"{namespace}_{product_name}_ImageRead\")\n        filepath = self._path_from_context(context)\n        source_node.getParameter(\"file\").setValue(filepath, 0.0)\n        colorspace = _image_colorspace(filepath, context)\n        if colorspace:\n            source_node.getParameter(\"image.colorspace\").setValue(\n                colorspace,\n                0.0,\n            )\n        containers.set_managed_node_role(source_node, containers.SOURCE_ROLE)\n        source_node.getOutputPort(\"out\").connect(managed_group.getReturnPort(\"out\"))\n        self[:] = [container_node, source_node]\n        return container_node\n    except Exception:\n        if container_node is not None:\n            with suppress(Exception):\n                container_node.delete()\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_image.html#client.ayon_katana.plugins.load.load_image.ImageLoader.remove","title":"<code>remove(container)</code>","text":"<p>Remove the complete AYON image container.</p> Source code in <code>client/ayon_katana/plugins/load/load_image.py</code> <pre><code>def remove(self, container):\n    \"\"\"Remove the complete AYON image container.\"\"\"\n    container[\"node\"].delete()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_image.html#client.ayon_katana.plugins.load.load_image.ImageLoader.switch","title":"<code>switch(container, context)</code>","text":"<p>Switch the container through the transactional update path.</p> Source code in <code>client/ayon_katana/plugins/load/load_image.py</code> <pre><code>def switch(self, container, context):\n    \"\"\"Switch the container through the transactional update path.\"\"\"\n    self.update(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_image.html#client.ayon_katana.plugins.load.load_image.ImageLoader.update","title":"<code>update(container, context)</code>","text":"<p>Update the managed path, colorspace, and container transactionally.</p> Source code in <code>client/ayon_katana/plugins/load/load_image.py</code> <pre><code>def update(self, container, context):\n    \"\"\"Update the managed path, colorspace, and container transactionally.\"\"\"\n    container_node = container[\"node\"]\n    source_node = containers.find_managed_node(\n        container_node,\n        containers.SOURCE_ROLE,\n    )\n    if source_node is None:\n        raise RuntimeError(\n            \"The AYON container is missing its managed ImageRead node.\"\n        )\n\n    file_parameter = source_node.getParameter(\"file\")\n    colorspace_parameter = source_node.getParameter(\"image.colorspace\")\n    old_filepath = file_parameter.getValue(0.0)\n    old_colorspace = colorspace_parameter.getValue(0.0)\n    old_container_data = containers.parse_container(container_node)\n    if old_container_data is None:\n        raise RuntimeError(\"The AYON image container has invalid metadata.\")\n    old_container_data.pop(\"node\", None)\n    old_container_data.pop(\"objectName\", None)\n\n    filepath = self._path_from_context(context)\n    colorspace = _image_colorspace(filepath, context)\n    project = context.get(\"project\") or {}\n    try:\n        file_parameter.setValue(filepath, 0.0)\n        colorspace_parameter.setValue(colorspace, 0.0)\n        containers.update_container(\n            container_node,\n            {\n                \"representation\": context[\"representation\"][\"id\"],\n                \"project_name\": project.get(\"name\"),\n                \"loader\": self.__class__.__name__,\n            },\n        )\n    except Exception:\n        with suppress(Exception):\n            file_parameter.setValue(old_filepath, 0.0)\n        with suppress(Exception):\n            colorspace_parameter.setValue(old_colorspace, 0.0)\n        with suppress(Exception):\n            containers.update_container(container_node, old_container_data)\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_image.html#client.ayon_katana.plugins.load.load_image.normalize_image_sequence_path","title":"<code>normalize_image_sequence_path(filepath, representation)</code>","text":"<p>Normalize an AYON sequence path to Katana's hash-token syntax.</p> <p>Parameters:</p> Name Type Description Default <code>filepath</code> <code>str</code> <p>Resolved representation file path.</p> required <code>representation</code> <code>dict</code> <p>AYON representation entity.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Forward-slash path with a hash token for sequence representations.</p> Source code in <code>client/ayon_katana/plugins/load/load_image.py</code> <pre><code>def normalize_image_sequence_path(filepath: str, representation: dict) -&gt; str:\n    \"\"\"Normalize an AYON sequence path to Katana's hash-token syntax.\n\n    Args:\n        filepath: Resolved representation file path.\n        representation: AYON representation entity.\n\n    Returns:\n        Forward-slash path with a hash token for sequence representations.\n    \"\"\"\n    path = filepath.replace(\"\\\\\", \"/\")\n    frame = (representation.get(\"context\") or {}).get(\"frame\")\n    if frame is None:\n        return path\n\n    printf_match = _PRINTF_TOKEN.search(path)\n    if printf_match:\n        return _PRINTF_TOKEN.sub(\"#\" * int(printf_match.group(1)), path, count=1)\n    dollar_f_match = _DOLLAR_F_TOKEN.search(path)\n    if dollar_f_match:\n        padding = int(dollar_f_match.group(1) or 1)\n        return _DOLLAR_F_TOKEN.sub(\"#\" * padding, path, count=1)\n\n    filename = Path(path).name\n    frame_match = _FRAME_NUMBER.match(filename)\n    if frame_match is None:\n        return path\n    try:\n        if int(frame_match.group(2)) != int(frame):\n            return path\n    except (TypeError, ValueError):\n        return path\n    tokenized_name = (\n        frame_match.group(1) + (\"#\" * len(frame_match.group(2))) + frame_match.group(3)\n    )\n    return path[: -len(filename)] + tokenized_name\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_katana.html","title":"load_katana","text":"<p>Load published Katana node graphs into managed containers.</p>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_katana.html#client.ayon_katana.plugins.load.load_katana.KatanaImportLoader","title":"<code>KatanaImportLoader</code>","text":"<p>               Bases: <code>KatanaLoader</code></p> <p>Import a published <code>.katana</code> graph below an AYON container.</p> Source code in <code>client/ayon_katana/plugins/load/load_katana.py</code> <pre><code>class KatanaImportLoader(plugin.KatanaLoader):\n    \"\"\"Import a published ``.katana`` graph below an AYON container.\"\"\"\n\n    product_base_types = {\"katana\", \"nodegraph\", \"rendersetup\", \"workfile\"}\n    product_types = product_base_types\n    label = \"Import Katana Graph\"\n    representations = {\"katana\"}\n    extensions = {\"katana\"}\n    order = 3\n\n    icon = \"sitemap\"\n    color = \"orange\"\n\n    def _import_graph(self, managed_group, filepath: str) -&gt; list:\n        \"\"\"Import nodes and connect scenegraph outputs to the managed return.\"\"\"\n        imported_nodes = compat.import_katana_file(\n            filepath,\n            parent_node=managed_group,\n            float_nodes=False,\n        )\n        if not imported_nodes:\n            raise RuntimeError(\"The Katana import did not create any nodes.\")\n\n        imported_set = set(imported_nodes)\n        terminal_outputs = []\n        for node in imported_nodes:\n            for output_port in compat.get_output_ports(node):\n                connected_inside_import = any(\n                    connected_port.getNode() in imported_set\n                    for connected_port in output_port.getConnectedPorts()\n                )\n                if not connected_inside_import:\n                    terminal_outputs.append((node, output_port))\n        if not terminal_outputs:\n            raise RuntimeError(\n                \"The imported Katana graph has no usable terminal output port.\"\n            )\n\n        if len(terminal_outputs) == 1:\n            output_node, output_port = terminal_outputs[0]\n            output_port.connect(managed_group.getReturnPort(\"out\"))\n            containers.set_managed_node_role(output_node, containers.SOURCE_ROLE)\n            return imported_nodes\n\n        combine_node = NodegraphAPI.CreateNode(\"Merge\", managed_group)\n        combine_node.setName(\"AYON_IMPORTED_MERGE\")\n        containers.set_managed_node_role(combine_node, _IMPORT_ROLE)\n        for index, (_output_node, output_port) in enumerate(terminal_outputs):\n            input_port = combine_node.addInputPort(f\"i{index}\")\n            output_port.connect(input_port)\n        combine_node.getOutputPort(\"out\").connect(managed_group.getReturnPort(\"out\"))\n        return imported_nodes + [combine_node]\n\n    @classmethod\n    def _representation_path(cls, context) -&gt; str:\n        \"\"\"Resolve and validate the representation path before graph mutation.\"\"\"\n        filepath = cls.filepath_from_context(context)\n        if not filepath:\n            raise RuntimeError(\"The Katana representation does not have a filepath.\")\n\n        try:\n            resolved_path = Path(filepath).expanduser().resolve(strict=True)\n        except OSError as exc:\n            raise RuntimeError(\n                f\"Katana representation file does not exist: {filepath!r}\"\n            ) from exc\n        if not resolved_path.is_file():\n            raise RuntimeError(\n                f\"Katana representation path is not a file: {filepath!r}\"\n            )\n        return resolved_path.as_posix()\n\n    @staticmethod\n    def _validate_update_candidate(managed_group, user_group) -&gt; None:\n        \"\"\"Validate that a staged graph can feed the existing user graph.\"\"\"\n        managed_output = managed_group.getOutputPort(\"out\")\n        managed_return = managed_group.getReturnPort(\"out\")\n        user_input = user_group.getInputPort(\"in\")\n        if managed_output is None or managed_return is None or user_input is None:\n            raise RuntimeError(\"The AYON container has incomplete managed/user ports.\")\n        if not managed_return.getConnectedPorts():\n            raise RuntimeError(\n                \"The imported Katana graph does not connect to its managed output.\"\n            )\n\n    def load(self, context, name=None, namespace=None, options=None):\n        \"\"\"Import a Katana representation and return its container node.\"\"\"\n        filepath = self._representation_path(context)\n        product_name = name or context[\"product\"][\"name\"]\n        namespace = namespace or context[\"folder\"][\"name\"]\n        container_node = None\n        try:\n            container_node = containers.containerise(\n                name=product_name,\n                namespace=namespace,\n                context=context,\n                loader=self.__class__.__name__,\n            )\n            managed_group = containers.get_managed_group(container_node)\n            if managed_group is None:\n                raise RuntimeError(\"Failed to create the AYON managed group.\")\n            imported_nodes = self._import_graph(managed_group, filepath)\n            self[:] = [container_node, *imported_nodes]\n            return container_node\n        except Exception:\n            if container_node is not None:\n                with suppress(Exception):\n                    container_node.delete()\n            raise\n\n    def update(self, container, context):\n        \"\"\"Replace a container's managed graph with a new representation.\"\"\"\n        container_node = container[\"node\"]\n        user_group = containers.get_user_group(container_node)\n        if containers.get_managed_group(container_node) is None or user_group is None:\n            raise RuntimeError(\"The AYON container is missing its managed/user groups.\")\n\n        filepath = self._representation_path(context)\n        temporary_group = containers.create_managed_group(\n            container_node,\n            name=_TEMP_MANAGED_GROUP_NAME,\n            role=None,\n        )\n        try:\n            imported_nodes = self._import_graph(temporary_group, filepath)\n            self._validate_update_candidate(temporary_group, user_group)\n        except Exception:\n            with suppress(Exception):\n                temporary_group.delete()\n            raise\n\n        project = context.get(\"project\") or {}\n        containers.replace_managed_group(\n            container_node,\n            temporary_group,\n            {\n                \"representation\": context[\"representation\"][\"id\"],\n                \"project_name\": project.get(\"name\"),\n            },\n        )\n        return imported_nodes\n\n    def remove(self, container):\n        \"\"\"Remove an imported graph container from the Katana project.\"\"\"\n        container[\"node\"].delete()\n\n    def switch(self, container, context):\n        \"\"\"Switch an imported graph container to another representation.\"\"\"\n        self.update(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_katana.html#client.ayon_katana.plugins.load.load_katana.KatanaImportLoader.load","title":"<code>load(context, name=None, namespace=None, options=None)</code>","text":"<p>Import a Katana representation and return its container node.</p> Source code in <code>client/ayon_katana/plugins/load/load_katana.py</code> <pre><code>def load(self, context, name=None, namespace=None, options=None):\n    \"\"\"Import a Katana representation and return its container node.\"\"\"\n    filepath = self._representation_path(context)\n    product_name = name or context[\"product\"][\"name\"]\n    namespace = namespace or context[\"folder\"][\"name\"]\n    container_node = None\n    try:\n        container_node = containers.containerise(\n            name=product_name,\n            namespace=namespace,\n            context=context,\n            loader=self.__class__.__name__,\n        )\n        managed_group = containers.get_managed_group(container_node)\n        if managed_group is None:\n            raise RuntimeError(\"Failed to create the AYON managed group.\")\n        imported_nodes = self._import_graph(managed_group, filepath)\n        self[:] = [container_node, *imported_nodes]\n        return container_node\n    except Exception:\n        if container_node is not None:\n            with suppress(Exception):\n                container_node.delete()\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_katana.html#client.ayon_katana.plugins.load.load_katana.KatanaImportLoader.remove","title":"<code>remove(container)</code>","text":"<p>Remove an imported graph container from the Katana project.</p> Source code in <code>client/ayon_katana/plugins/load/load_katana.py</code> <pre><code>def remove(self, container):\n    \"\"\"Remove an imported graph container from the Katana project.\"\"\"\n    container[\"node\"].delete()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_katana.html#client.ayon_katana.plugins.load.load_katana.KatanaImportLoader.switch","title":"<code>switch(container, context)</code>","text":"<p>Switch an imported graph container to another representation.</p> Source code in <code>client/ayon_katana/plugins/load/load_katana.py</code> <pre><code>def switch(self, container, context):\n    \"\"\"Switch an imported graph container to another representation.\"\"\"\n    self.update(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_katana.html#client.ayon_katana.plugins.load.load_katana.KatanaImportLoader.update","title":"<code>update(container, context)</code>","text":"<p>Replace a container's managed graph with a new representation.</p> Source code in <code>client/ayon_katana/plugins/load/load_katana.py</code> <pre><code>def update(self, container, context):\n    \"\"\"Replace a container's managed graph with a new representation.\"\"\"\n    container_node = container[\"node\"]\n    user_group = containers.get_user_group(container_node)\n    if containers.get_managed_group(container_node) is None or user_group is None:\n        raise RuntimeError(\"The AYON container is missing its managed/user groups.\")\n\n    filepath = self._representation_path(context)\n    temporary_group = containers.create_managed_group(\n        container_node,\n        name=_TEMP_MANAGED_GROUP_NAME,\n        role=None,\n    )\n    try:\n        imported_nodes = self._import_graph(temporary_group, filepath)\n        self._validate_update_candidate(temporary_group, user_group)\n    except Exception:\n        with suppress(Exception):\n            temporary_group.delete()\n        raise\n\n    project = context.get(\"project\") or {}\n    containers.replace_managed_group(\n        container_node,\n        temporary_group,\n        {\n            \"representation\": context[\"representation\"][\"id\"],\n            \"project_name\": project.get(\"name\"),\n        },\n    )\n    return imported_nodes\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_usd.html","title":"load_usd","text":"<p>Load USD representations into a managed Katana node graph.</p>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_usd.html#client.ayon_katana.plugins.load.load_usd.UsdLoader","title":"<code>UsdLoader</code>","text":"<p>               Bases: <code>KatanaLoader</code></p> <p>Load and manage a USD representation through a Katana UsdIn node.</p> Source code in <code>client/ayon_katana/plugins/load/load_usd.py</code> <pre><code>class UsdLoader(plugin.KatanaLoader):\n    \"\"\"Load and manage a USD representation through a Katana UsdIn node.\"\"\"\n\n    product_base_types = USD_PRODUCT_BASE_TYPES\n    product_types = product_base_types\n    label = \"Load USD (Scene Graph)\"\n    representations = {\"*\"}\n    extensions = {\"usd\", \"usda\", \"usdc\", \"usdlc\", \"usdnc\", \"usdz\"}\n    order = 1\n\n    icon = \"code-fork\"\n    color = \"orange\"\n\n    source_node_type = \"UsdIn\"\n    source_node_suffix = \"UsdIn\"\n    filepath_parameter = \"fileName\"\n\n    @classmethod\n    def get_options(cls, contexts):\n        \"\"\"Return loader options shared by the selected representations.\"\"\"\n        return [\n            TextDef(\n                \"location\",\n                label=\"Scenegraph location\",\n                default=\"\",\n                tooltip=(\n                    \"Optional USD scenegraph location, for example \"\n                    \"'/root/world/geo'. Leave empty to use Katana's native \"\n                    \"default.\"\n                ),\n            )\n        ]\n\n    def load(self, context, name=None, namespace=None, options=None):\n        \"\"\"Load a USD representation and return its AYON container node.\"\"\"\n        options = options or {}\n        product_name = name or context[\"product\"][\"name\"]\n        namespace = namespace or context[\"folder\"][\"name\"]\n        container_node = None\n        try:\n            container_node = containers.containerise(\n                name=product_name,\n                namespace=namespace,\n                context=context,\n                loader=self.__class__.__name__,\n            )\n            managed_group = containers.get_managed_group(container_node)\n            if managed_group is None:\n                raise RuntimeError(\"Failed to create the AYON managed group.\")\n\n            source_node = NodegraphAPI.CreateNode(\n                self.source_node_type,\n                managed_group,\n            )\n            source_node.setName(f\"{namespace}_{product_name}_{self.source_node_suffix}\")\n            source_node.getParameter(self.filepath_parameter).setValue(\n                self.filepath_from_context(context), 0.0\n            )\n            self._apply_options(source_node, options)\n\n            containers.set_managed_node_role(source_node, containers.SOURCE_ROLE)\n            source_node.getOutputPort(\"out\").connect(managed_group.getReturnPort(\"out\"))\n            self[:] = [container_node, source_node]\n            return container_node\n        except Exception:\n            if container_node is not None:\n                with suppress(Exception):\n                    container_node.delete()\n            raise\n\n    def _apply_options(self, source_node, options) -&gt; None:\n        \"\"\"Apply loader-specific settings to a newly created source node.\"\"\"\n        location = options.get(\"location\")\n        if location:\n            source_node.getParameter(\"location\").setValue(location, 0.0)\n\n    def update(self, container, context):\n        \"\"\"Update a USD container to a new representation.\"\"\"\n        container_node = container[\"node\"]\n        source_node = containers.find_managed_node(\n            container_node, containers.SOURCE_ROLE\n        )\n        if source_node is None:\n            raise RuntimeError(\n                \"The AYON container is missing its managed \"\n                f\"{self.source_node_type} node.\"\n            )\n\n        filepath = self.filepath_from_context(context)\n        file_parameter = source_node.getParameter(self.filepath_parameter)\n        old_filepath = file_parameter.getValue(0.0)\n        old_container_data = containers.parse_container(container_node)\n        if old_container_data is None:\n            raise RuntimeError(\"The AYON USD container has invalid metadata.\")\n        old_container_data.pop(\"node\", None)\n        old_container_data.pop(\"objectName\", None)\n        project = context.get(\"project\") or {}\n        try:\n            file_parameter.setValue(filepath, 0.0)\n            containers.update_container(\n                container_node,\n                {\n                    \"representation\": context[\"representation\"][\"id\"],\n                    \"project_name\": project.get(\"name\"),\n                    \"loader\": self.__class__.__name__,\n                },\n            )\n        except Exception:\n            with suppress(Exception):\n                file_parameter.setValue(old_filepath, 0.0)\n            with suppress(Exception):\n                containers.update_container(container_node, old_container_data)\n            raise\n\n    def remove(self, container):\n        \"\"\"Remove a USD container from the Katana project.\"\"\"\n        container[\"node\"].delete()\n\n    def switch(self, container, context):\n        \"\"\"Switch a USD container to another representation.\"\"\"\n        self.update(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_usd.html#client.ayon_katana.plugins.load.load_usd.UsdLoader.get_options","title":"<code>get_options(contexts)</code>  <code>classmethod</code>","text":"<p>Return loader options shared by the selected representations.</p> Source code in <code>client/ayon_katana/plugins/load/load_usd.py</code> <pre><code>@classmethod\ndef get_options(cls, contexts):\n    \"\"\"Return loader options shared by the selected representations.\"\"\"\n    return [\n        TextDef(\n            \"location\",\n            label=\"Scenegraph location\",\n            default=\"\",\n            tooltip=(\n                \"Optional USD scenegraph location, for example \"\n                \"'/root/world/geo'. Leave empty to use Katana's native \"\n                \"default.\"\n            ),\n        )\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_usd.html#client.ayon_katana.plugins.load.load_usd.UsdLoader.load","title":"<code>load(context, name=None, namespace=None, options=None)</code>","text":"<p>Load a USD representation and return its AYON container node.</p> Source code in <code>client/ayon_katana/plugins/load/load_usd.py</code> <pre><code>def load(self, context, name=None, namespace=None, options=None):\n    \"\"\"Load a USD representation and return its AYON container node.\"\"\"\n    options = options or {}\n    product_name = name or context[\"product\"][\"name\"]\n    namespace = namespace or context[\"folder\"][\"name\"]\n    container_node = None\n    try:\n        container_node = containers.containerise(\n            name=product_name,\n            namespace=namespace,\n            context=context,\n            loader=self.__class__.__name__,\n        )\n        managed_group = containers.get_managed_group(container_node)\n        if managed_group is None:\n            raise RuntimeError(\"Failed to create the AYON managed group.\")\n\n        source_node = NodegraphAPI.CreateNode(\n            self.source_node_type,\n            managed_group,\n        )\n        source_node.setName(f\"{namespace}_{product_name}_{self.source_node_suffix}\")\n        source_node.getParameter(self.filepath_parameter).setValue(\n            self.filepath_from_context(context), 0.0\n        )\n        self._apply_options(source_node, options)\n\n        containers.set_managed_node_role(source_node, containers.SOURCE_ROLE)\n        source_node.getOutputPort(\"out\").connect(managed_group.getReturnPort(\"out\"))\n        self[:] = [container_node, source_node]\n        return container_node\n    except Exception:\n        if container_node is not None:\n            with suppress(Exception):\n                container_node.delete()\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_usd.html#client.ayon_katana.plugins.load.load_usd.UsdLoader.remove","title":"<code>remove(container)</code>","text":"<p>Remove a USD container from the Katana project.</p> Source code in <code>client/ayon_katana/plugins/load/load_usd.py</code> <pre><code>def remove(self, container):\n    \"\"\"Remove a USD container from the Katana project.\"\"\"\n    container[\"node\"].delete()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_usd.html#client.ayon_katana.plugins.load.load_usd.UsdLoader.switch","title":"<code>switch(container, context)</code>","text":"<p>Switch a USD container to another representation.</p> Source code in <code>client/ayon_katana/plugins/load/load_usd.py</code> <pre><code>def switch(self, container, context):\n    \"\"\"Switch a USD container to another representation.\"\"\"\n    self.update(container, context)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_usd.html#client.ayon_katana.plugins.load.load_usd.UsdLoader.update","title":"<code>update(container, context)</code>","text":"<p>Update a USD container to a new representation.</p> Source code in <code>client/ayon_katana/plugins/load/load_usd.py</code> <pre><code>def update(self, container, context):\n    \"\"\"Update a USD container to a new representation.\"\"\"\n    container_node = container[\"node\"]\n    source_node = containers.find_managed_node(\n        container_node, containers.SOURCE_ROLE\n    )\n    if source_node is None:\n        raise RuntimeError(\n            \"The AYON container is missing its managed \"\n            f\"{self.source_node_type} node.\"\n        )\n\n    filepath = self.filepath_from_context(context)\n    file_parameter = source_node.getParameter(self.filepath_parameter)\n    old_filepath = file_parameter.getValue(0.0)\n    old_container_data = containers.parse_container(container_node)\n    if old_container_data is None:\n        raise RuntimeError(\"The AYON USD container has invalid metadata.\")\n    old_container_data.pop(\"node\", None)\n    old_container_data.pop(\"objectName\", None)\n    project = context.get(\"project\") or {}\n    try:\n        file_parameter.setValue(filepath, 0.0)\n        containers.update_container(\n            container_node,\n            {\n                \"representation\": context[\"representation\"][\"id\"],\n                \"project_name\": project.get(\"name\"),\n                \"loader\": self.__class__.__name__,\n            },\n        )\n    except Exception:\n        with suppress(Exception):\n            file_parameter.setValue(old_filepath, 0.0)\n        with suppress(Exception):\n            containers.update_container(container_node, old_container_data)\n        raise\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_usd.html#client.ayon_katana.plugins.load.load_usd.UsdSublayerLoader","title":"<code>UsdSublayerLoader</code>","text":"<p>               Bases: <code>UsdLoader</code></p> <p>Compose a USD representation into Katana's native USD graph.</p> Source code in <code>client/ayon_katana/plugins/load/load_usd.py</code> <pre><code>class UsdSublayerLoader(UsdLoader):\n    \"\"\"Compose a USD representation into Katana's native USD graph.\"\"\"\n\n    label = \"Sublayer USD (Native Graph)\"\n    extensions = {\"usd\", \"usda\", \"usdc\", \"usdlc\", \"usdnc\"}\n    order = 0\n\n    source_node_type = \"UsdSubLayerAdd\"\n    source_node_suffix = \"UsdSubLayerAdd\"\n    filepath_parameter = \"asset\"\n\n    @classmethod\n    def get_options(cls, contexts):\n        \"\"\"Return no Geolib-only options for native USD composition.\"\"\"\n        return []\n\n    def _apply_options(self, source_node, options) -&gt; None:\n        \"\"\"Keep native sublayer composition on Katana's node defaults.\"\"\"\n        _ = source_node, options\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/load/load_usd.html#client.ayon_katana.plugins.load.load_usd.UsdSublayerLoader.get_options","title":"<code>get_options(contexts)</code>  <code>classmethod</code>","text":"<p>Return no Geolib-only options for native USD composition.</p> Source code in <code>client/ayon_katana/plugins/load/load_usd.py</code> <pre><code>@classmethod\ndef get_options(cls, contexts):\n    \"\"\"Return no Geolib-only options for native USD composition.\"\"\"\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/index.html","title":"publish","text":"<p>Katana publishing plugins.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/actions.html","title":"actions","text":"<p>Deterministic Pyblish actions for Katana render validation failures.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/actions.html#client.ayon_katana.plugins.publish.actions.ReconnectAYONRenderGraphAction","title":"<code>ReconnectAYONRenderGraphAction</code>","text":"<p>               Bases: <code>Action</code></p> <p>Reconnect failed render graphs created by the AYON Katana creator.</p> Source code in <code>client/ayon_katana/plugins/publish/actions.py</code> <pre><code>class ReconnectAYONRenderGraphAction(pyblish.api.Action):\n    \"\"\"Reconnect failed render graphs created by the AYON Katana creator.\"\"\"\n\n    label = \"Reconnect AYON render graph\"\n    icon = \"refresh\"\n    on = \"failed\"\n\n    def process(self, context, plugin) -&gt; None:\n        \"\"\"Reconnect only failed instances explicitly created by AYON.\"\"\"\n        reconnected_nodes = []\n        for instance in _invalid_instances(context, plugin):\n            if instance.data.get(\"creator_identifier\") != _RENDER_CREATOR_IDENTIFIER:\n                continue\n            instance_node = _node_from_instance(instance)\n            if instance_node is None:\n                continue\n            render.reconnect_render_graph(instance_node)\n            reconnected_nodes.append(instance_node)\n\n        if reconnected_nodes:\n            compat.set_selected_nodes(_unique_nodes(reconnected_nodes))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/actions.html#client.ayon_katana.plugins.publish.actions.ReconnectAYONRenderGraphAction.process","title":"<code>process(context, plugin)</code>","text":"<p>Reconnect only failed instances explicitly created by AYON.</p> Source code in <code>client/ayon_katana/plugins/publish/actions.py</code> <pre><code>def process(self, context, plugin) -&gt; None:\n    \"\"\"Reconnect only failed instances explicitly created by AYON.\"\"\"\n    reconnected_nodes = []\n    for instance in _invalid_instances(context, plugin):\n        if instance.data.get(\"creator_identifier\") != _RENDER_CREATOR_IDENTIFIER:\n            continue\n        instance_node = _node_from_instance(instance)\n        if instance_node is None:\n            continue\n        render.reconnect_render_graph(instance_node)\n        reconnected_nodes.append(instance_node)\n\n    if reconnected_nodes:\n        compat.set_selected_nodes(_unique_nodes(reconnected_nodes))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/actions.html#client.ayon_katana.plugins.publish.actions.SelectInvalidInstanceNodes","title":"<code>SelectInvalidInstanceNodes</code>","text":"<p>               Bases: <code>Action</code></p> <p>Select outer AYON instance nodes from a failed validator.</p> Source code in <code>client/ayon_katana/plugins/publish/actions.py</code> <pre><code>class SelectInvalidInstanceNodes(pyblish.api.Action):\n    \"\"\"Select outer AYON instance nodes from a failed validator.\"\"\"\n\n    label = \"Select invalid instance nodes\"\n    icon = \"search\"\n    on = \"failed\"\n\n    def process(self, context, plugin) -&gt; None:\n        \"\"\"Replace the Katana selection with invalid outer instance nodes.\"\"\"\n        nodes = _invalid_nodes_for_context_plugin(context, plugin, \"get_invalid\")\n        if not nodes:\n            nodes = [\n                _node_from_instance(instance)\n                for instance in _invalid_instances(context, plugin)\n            ]\n            nodes = _unique_nodes(nodes)\n        if nodes:\n            compat.set_selected_nodes(nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/actions.html#client.ayon_katana.plugins.publish.actions.SelectInvalidInstanceNodes.process","title":"<code>process(context, plugin)</code>","text":"<p>Replace the Katana selection with invalid outer instance nodes.</p> Source code in <code>client/ayon_katana/plugins/publish/actions.py</code> <pre><code>def process(self, context, plugin) -&gt; None:\n    \"\"\"Replace the Katana selection with invalid outer instance nodes.\"\"\"\n    nodes = _invalid_nodes_for_context_plugin(context, plugin, \"get_invalid\")\n    if not nodes:\n        nodes = [\n            _node_from_instance(instance)\n            for instance in _invalid_instances(context, plugin)\n        ]\n        nodes = _unique_nodes(nodes)\n    if nodes:\n        compat.set_selected_nodes(nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/actions.html#client.ayon_katana.plugins.publish.actions.SelectInvalidOutputNodes","title":"<code>SelectInvalidOutputNodes</code>","text":"<p>               Bases: <code>Action</code></p> <p>Select concrete Katana output nodes from a failed context validator.</p> Source code in <code>client/ayon_katana/plugins/publish/actions.py</code> <pre><code>class SelectInvalidOutputNodes(pyblish.api.Action):\n    \"\"\"Select concrete Katana output nodes from a failed context validator.\"\"\"\n\n    label = \"Select invalid render outputs\"\n    icon = \"search\"\n    on = \"failed\"\n\n    def process(self, context, plugin) -&gt; None:\n        \"\"\"Replace the Katana selection with invalid output nodes.\"\"\"\n        nodes = _invalid_output_nodes(context, plugin)\n        if nodes:\n            compat.set_selected_nodes(nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/actions.html#client.ayon_katana.plugins.publish.actions.SelectInvalidOutputNodes.process","title":"<code>process(context, plugin)</code>","text":"<p>Replace the Katana selection with invalid output nodes.</p> Source code in <code>client/ayon_katana/plugins/publish/actions.py</code> <pre><code>def process(self, context, plugin) -&gt; None:\n    \"\"\"Replace the Katana selection with invalid output nodes.\"\"\"\n    nodes = _invalid_output_nodes(context, plugin)\n    if nodes:\n        compat.set_selected_nodes(nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_current_file.html","title":"collect_current_file","text":"<p>Collect the current Katana project path for publishing.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_current_file.html#client.ayon_katana.plugins.publish.collect_current_file.CollectKatanaCurrentFile","title":"<code>CollectKatanaCurrentFile</code>","text":"<p>               Bases: <code>KatanaContextPlugin</code></p> <p>Inject the current working file into context</p> Source code in <code>client/ayon_katana/plugins/publish/collect_current_file.py</code> <pre><code>class CollectKatanaCurrentFile(plugin.KatanaContextPlugin):\n    \"\"\"Inject the current working file into context\"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.5\n    label = \"Katana Current File\"\n\n    def process(self, context):\n        \"\"\"Inject the current working file\"\"\"\n        current_file = workio.get_current_workfile()\n        try:\n            current_file = workio.normalize_workfile_path(current_file)\n        except ValueError:\n            # Keep the collected value so the validator can report the actual\n            # unsupported path instead of disguising it as a never-saved scene.\n            self.log.warning(\"Katana workfile is not a supported saved path.\")\n\n        context.data[\"currentFile\"] = current_file\n        self.log.info(\"Current workfile path: %s\", current_file)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_current_file.html#client.ayon_katana.plugins.publish.collect_current_file.CollectKatanaCurrentFile.process","title":"<code>process(context)</code>","text":"<p>Inject the current working file</p> Source code in <code>client/ayon_katana/plugins/publish/collect_current_file.py</code> <pre><code>def process(self, context):\n    \"\"\"Inject the current working file\"\"\"\n    current_file = workio.get_current_workfile()\n    try:\n        current_file = workio.normalize_workfile_path(current_file)\n    except ValueError:\n        # Keep the collected value so the validator can report the actual\n        # unsupported path instead of disguising it as a never-saved scene.\n        self.log.warning(\"Katana workfile is not a supported saved path.\")\n\n    context.data[\"currentFile\"] = current_file\n    self.log.info(\"Current workfile path: %s\", current_file)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_image.html","title":"collect_image","text":"<p>Collect Katana ImageWrite publish data.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_image.html#client.ayon_katana.plugins.publish.collect_image.CollectImage","title":"<code>CollectImage</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Collect one ImageWrite output for local or farm processing.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_image.py</code> <pre><code>class CollectImage(plugin.KatanaInstancePlugin):\n    \"\"\"Collect one ImageWrite output for local or farm processing.\"\"\"\n\n    label = \"Collect ImageWrite\"\n    order = pyblish.api.CollectorOrder + 0.410\n    families = [\"image\", \"katana.image\"]\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Collect live node settings, range, expected files, and metadata.\"\"\"\n        node_name = instance.data[\"instance_node\"]\n        image_write_node = compat.get_node(node_name)\n        if image_write_node is None:\n            raise RuntimeError(\n                f\"Katana ImageWrite instance node does not exist: {node_name!r}\"\n            )\n\n        settings = read_image_write_settings(image_write_node)\n        if settings[\"single_frame\"]:\n            frame_start = frame_end = settings[\"frame\"]\n            frame_step = 1\n            range_source = \"image_write\"\n        else:\n            frame_start, frame_end = render.get_project_frame_range()\n            frame_step = render.get_project_frame_step()\n            range_source = \"project\"\n        expected_files = expand_image_output_pattern(\n            settings[\"output_path\"],\n            frame_start,\n            frame_end,\n            frame_step,\n        )\n\n        creator_attributes = instance.data.get(\"creator_attributes\") or {}\n        render_target = creator_attributes.get(\"render_target\", \"local\")\n        if render_target not in {\"local\", \"farm\", \"local_no_render\"}:\n            raise RuntimeError(f\"Unsupported Katana image target: {render_target!r}.\")\n        review = bool(creator_attributes.get(\"review\", False))\n        render_products = colorspace.ARenderProduct(\n            [\"\"],\n            settings[\"colorspace\"],\n        )\n        effective_colorspace = render_products.layer_data.products[0].colorspace\n        color_data = lib.get_color_management_preferences() or {}\n        if color_data:\n            instance.data[\"colorspaceConfig\"] = color_data[\"config\"]\n\n        instance.data.update(\n            {\n                \"render_node\": image_write_node.getName(),\n                \"imageOutputPath\": settings[\"output_path\"],\n                \"imageFileFormat\": settings[\"file_format\"],\n                \"imageOutputColorspace\": settings[\"colorspace\"],\n                \"imageSingleFrame\": settings[\"single_frame\"],\n                \"frameStartHandle\": frame_start,\n                \"frameEndHandle\": frame_end,\n                \"byFrameStep\": frame_step,\n                \"katanaFrameRangeSource\": range_source,\n                \"expectedFiles\": [{\"\": expected_files}],\n                \"files\": [settings[\"output_path\"]],\n                \"outputDir\": str(Path(expected_files[0]).parent),\n                \"multipartExr\": False,\n                \"attachTo\": [],\n                \"farm\": render_target == \"farm\",\n                \"colorspace\": effective_colorspace,\n                \"renderProducts\": render_products,\n            }\n        )\n        families = instance.data.setdefault(\"families\", [])\n        for family in (\"image\", \"katana.image\"):\n            if family not in families:\n                families.append(family)\n        if instance.data[\"farm\"] and \"render.farm\" not in families:\n            families.append(\"render.farm\")\n        if not instance.data[\"farm\"] and \"render.farm\" in families:\n            families.remove(\"render.farm\")\n        if review and \"review\" not in families:\n            families.append(\"review\")\n        if not review and \"review\" in families:\n            families.remove(\"review\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_image.html#client.ayon_katana.plugins.publish.collect_image.CollectImage.process","title":"<code>process(instance)</code>","text":"<p>Collect live node settings, range, expected files, and metadata.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_image.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Collect live node settings, range, expected files, and metadata.\"\"\"\n    node_name = instance.data[\"instance_node\"]\n    image_write_node = compat.get_node(node_name)\n    if image_write_node is None:\n        raise RuntimeError(\n            f\"Katana ImageWrite instance node does not exist: {node_name!r}\"\n        )\n\n    settings = read_image_write_settings(image_write_node)\n    if settings[\"single_frame\"]:\n        frame_start = frame_end = settings[\"frame\"]\n        frame_step = 1\n        range_source = \"image_write\"\n    else:\n        frame_start, frame_end = render.get_project_frame_range()\n        frame_step = render.get_project_frame_step()\n        range_source = \"project\"\n    expected_files = expand_image_output_pattern(\n        settings[\"output_path\"],\n        frame_start,\n        frame_end,\n        frame_step,\n    )\n\n    creator_attributes = instance.data.get(\"creator_attributes\") or {}\n    render_target = creator_attributes.get(\"render_target\", \"local\")\n    if render_target not in {\"local\", \"farm\", \"local_no_render\"}:\n        raise RuntimeError(f\"Unsupported Katana image target: {render_target!r}.\")\n    review = bool(creator_attributes.get(\"review\", False))\n    render_products = colorspace.ARenderProduct(\n        [\"\"],\n        settings[\"colorspace\"],\n    )\n    effective_colorspace = render_products.layer_data.products[0].colorspace\n    color_data = lib.get_color_management_preferences() or {}\n    if color_data:\n        instance.data[\"colorspaceConfig\"] = color_data[\"config\"]\n\n    instance.data.update(\n        {\n            \"render_node\": image_write_node.getName(),\n            \"imageOutputPath\": settings[\"output_path\"],\n            \"imageFileFormat\": settings[\"file_format\"],\n            \"imageOutputColorspace\": settings[\"colorspace\"],\n            \"imageSingleFrame\": settings[\"single_frame\"],\n            \"frameStartHandle\": frame_start,\n            \"frameEndHandle\": frame_end,\n            \"byFrameStep\": frame_step,\n            \"katanaFrameRangeSource\": range_source,\n            \"expectedFiles\": [{\"\": expected_files}],\n            \"files\": [settings[\"output_path\"]],\n            \"outputDir\": str(Path(expected_files[0]).parent),\n            \"multipartExr\": False,\n            \"attachTo\": [],\n            \"farm\": render_target == \"farm\",\n            \"colorspace\": effective_colorspace,\n            \"renderProducts\": render_products,\n        }\n    )\n    families = instance.data.setdefault(\"families\", [])\n    for family in (\"image\", \"katana.image\"):\n        if family not in families:\n            families.append(family)\n    if instance.data[\"farm\"] and \"render.farm\" not in families:\n        families.append(\"render.farm\")\n    if not instance.data[\"farm\"] and \"render.farm\" in families:\n        families.remove(\"render.farm\")\n    if review and \"review\" not in families:\n        families.append(\"review\")\n    if not review and \"review\" in families:\n        families.remove(\"review\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_local_render_instances.html","title":"collect_local_render_instances","text":"<p>Collect integration instances for local Katana outputs.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_local_render_instances.html#client.ayon_katana.plugins.publish.collect_local_render_instances.CollectLocalRenderInstances","title":"<code>CollectLocalRenderInstances</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Create integration instances for local outputs.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_local_render_instances.py</code> <pre><code>class CollectLocalRenderInstances(plugin.KatanaInstancePlugin):\n    \"\"\"Create integration instances for local outputs.\"\"\"\n\n    label = \"Collect Local Output Instances\"\n    order = pyblish.api.CollectorOrder + 0.430\n    families = [\"render\", \"katana.render\", \"image\", \"katana.image\"]\n\n    def process(self, instance):\n        \"\"\"Create integration instances from local outputs.\"\"\"\n        if instance.data.get(\"farm\"):\n            self.log.debug(\"Katana output is configured for farm processing.\")\n            return\n\n        expected_files = instance.data.get(\"expectedFiles\") or []\n        if not expected_files or not expected_files[0]:\n            raise RuntimeError(\"Local output has no expected files.\")\n\n        skeleton = create_skeleton_instance(\n            instance,\n            families_transfer=[],\n            instance_transfer={},\n        )\n        for key in (\"creator_attributes\", \"publish_attributes\"):\n            if key in instance.data:\n                skeleton[key] = instance.data[key]\n\n        review = bool((instance.data.get(\"creator_attributes\") or {}).get(\"review\"))\n        aov_instances = create_instances_for_aov(\n            instance=instance,\n            skeleton=skeleton,\n            aov_filter={},\n            skip_integration_repre_list=[],\n            do_not_add_review=True,\n        )\n\n        source_families = set(instance.data.get(\"families\") or [])\n        is_image = bool({\"image\", \"katana.image\"} &amp; source_families)\n        context = instance.context\n        anatomy = context.data[\"anatomy\"]\n        for instance_data in aov_instances:\n            is_review = (\n                review and instance_data[\"productName\"] == skeleton[\"productName\"]\n            )\n            for representation in instance_data[\"representations\"]:\n                representation[\"stagingDir\"] = anatomy.fill_root(\n                    representation[\"stagingDir\"]\n                )\n                if is_review:\n                    tags = representation.setdefault(\"tags\", [])\n                    if \"review\" not in tags:\n                        tags.append(\"review\")\n\n            aov_instance = context.create_instance(instance_data[\"productName\"])\n            aov_instance.data.update(instance_data)\n            families = [\"image.local.katana\" if is_image else \"render.local.katana\"]\n            if is_review:\n                families.append(\"review\")\n            aov_instance.data[\"families\"] = families\n\n        instance.data[\"integrate\"] = False\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_local_render_instances.html#client.ayon_katana.plugins.publish.collect_local_render_instances.CollectLocalRenderInstances.process","title":"<code>process(instance)</code>","text":"<p>Create integration instances from local outputs.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_local_render_instances.py</code> <pre><code>def process(self, instance):\n    \"\"\"Create integration instances from local outputs.\"\"\"\n    if instance.data.get(\"farm\"):\n        self.log.debug(\"Katana output is configured for farm processing.\")\n        return\n\n    expected_files = instance.data.get(\"expectedFiles\") or []\n    if not expected_files or not expected_files[0]:\n        raise RuntimeError(\"Local output has no expected files.\")\n\n    skeleton = create_skeleton_instance(\n        instance,\n        families_transfer=[],\n        instance_transfer={},\n    )\n    for key in (\"creator_attributes\", \"publish_attributes\"):\n        if key in instance.data:\n            skeleton[key] = instance.data[key]\n\n    review = bool((instance.data.get(\"creator_attributes\") or {}).get(\"review\"))\n    aov_instances = create_instances_for_aov(\n        instance=instance,\n        skeleton=skeleton,\n        aov_filter={},\n        skip_integration_repre_list=[],\n        do_not_add_review=True,\n    )\n\n    source_families = set(instance.data.get(\"families\") or [])\n    is_image = bool({\"image\", \"katana.image\"} &amp; source_families)\n    context = instance.context\n    anatomy = context.data[\"anatomy\"]\n    for instance_data in aov_instances:\n        is_review = (\n            review and instance_data[\"productName\"] == skeleton[\"productName\"]\n        )\n        for representation in instance_data[\"representations\"]:\n            representation[\"stagingDir\"] = anatomy.fill_root(\n                representation[\"stagingDir\"]\n            )\n            if is_review:\n                tags = representation.setdefault(\"tags\", [])\n                if \"review\" not in tags:\n                    tags.append(\"review\")\n\n        aov_instance = context.create_instance(instance_data[\"productName\"])\n        aov_instance.data.update(instance_data)\n        families = [\"image.local.katana\" if is_image else \"render.local.katana\"]\n        if is_review:\n            families.append(\"review\")\n        aov_instance.data[\"families\"] = families\n\n    instance.data[\"integrate\"] = False\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_nodegraph.html","title":"collect_nodegraph","text":"<p>Collect selected Katana node graph publish data.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_nodegraph.html#client.ayon_katana.plugins.publish.collect_nodegraph.CollectNodegraph","title":"<code>CollectNodegraph</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Collect the source Group for node graph publishing.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_nodegraph.py</code> <pre><code>class CollectNodegraph(plugin.KatanaInstancePlugin):\n    \"\"\"Collect the source Group for node graph publishing.\"\"\"\n\n    label = \"Collect Node Graph\"\n    order = pyblish.api.CollectorOrder + 0.01\n    families = [\"nodegraph\"]\n\n    def process(self, instance):\n        \"\"\"Collect node graph source data from its creator instance.\"\"\"\n        node_name = instance.data.get(\"nodegraph_node\")\n        if not node_name:\n            return\n        node = compat.get_node(node_name)\n        if node is not None:\n            instance.data[\"nodegraph_node\"] = node.getName()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_nodegraph.html#client.ayon_katana.plugins.publish.collect_nodegraph.CollectNodegraph.process","title":"<code>process(instance)</code>","text":"<p>Collect node graph source data from its creator instance.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_nodegraph.py</code> <pre><code>def process(self, instance):\n    \"\"\"Collect node graph source data from its creator instance.\"\"\"\n    node_name = instance.data.get(\"nodegraph_node\")\n    if not node_name:\n        return\n    node = compat.get_node(node_name)\n    if node is not None:\n        instance.data[\"nodegraph_node\"] = node.getName()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_nodegraph.html#client.ayon_katana.plugins.publish.collect_nodegraph.CollectNodegraphDependencies","title":"<code>CollectNodegraphDependencies</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Collect AYON containers nested in the published Group when discoverable.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_nodegraph.py</code> <pre><code>class CollectNodegraphDependencies(plugin.KatanaInstancePlugin):\n    \"\"\"Collect AYON containers nested in the published Group when discoverable.\"\"\"\n\n    label = \"Collect Node Graph Dependencies\"\n    order = pyblish.api.CollectorOrder + 0.02\n    families = [\"nodegraph\"]\n\n    def process(self, instance):\n        \"\"\"Collect representation dependencies from nested containers.\"\"\"\n        node_name = instance.data.get(\"nodegraph_node\")\n        source_node = compat.get_node(node_name) if node_name else None\n        if source_node is None:\n            return\n\n        representation_ids = {\n            container.get(\"representation\")\n            for container in containers.ls()\n            if container.get(\"representation\")\n            and compat.is_descendant(container[\"node\"], source_node)\n        }\n        inputs = sorted(representation_ids)\n        instance.data[\"inputRepresentations\"] = inputs\n        self.log.debug(\"Collected inputs: %s\", inputs)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_nodegraph.html#client.ayon_katana.plugins.publish.collect_nodegraph.CollectNodegraphDependencies.process","title":"<code>process(instance)</code>","text":"<p>Collect representation dependencies from nested containers.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_nodegraph.py</code> <pre><code>def process(self, instance):\n    \"\"\"Collect representation dependencies from nested containers.\"\"\"\n    node_name = instance.data.get(\"nodegraph_node\")\n    source_node = compat.get_node(node_name) if node_name else None\n    if source_node is None:\n        return\n\n    representation_ids = {\n        container.get(\"representation\")\n        for container in containers.ls()\n        if container.get(\"representation\")\n        and compat.is_descendant(container[\"node\"], source_node)\n    }\n    inputs = sorted(representation_ids)\n    instance.data[\"inputRepresentations\"] = inputs\n    self.log.debug(\"Collected inputs: %s\", inputs)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render.html","title":"collect_render","text":"<p>Collect render products from Katana output nodes.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render.html#client.ayon_katana.plugins.publish.collect_render.CollectRender","title":"<code>CollectRender</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Collect enabled render products.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render.py</code> <pre><code>class CollectRender(plugin.KatanaInstancePlugin):\n    \"\"\"Collect enabled render products.\"\"\"\n\n    label = \"Collect Render Products\"\n    order = pyblish.api.CollectorOrder + 0.410\n    families = [\"render\", \"katana.render\"]\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Collect render outputs and expected files.\"\"\"\n        creator_attributes = instance.data.get(\"creator_attributes\") or {}\n        render_target = creator_attributes.get(\"render_target\", \"farm\")\n        if render_target not in {\"farm\", \"local\", \"local_no_render\"}:\n            raise RuntimeError(f\"Unsupported Katana render target: {render_target!r}.\")\n        instance_node = compat.get_node(instance.data.get(\"instance_node\"))\n        if instance_node is None:\n            raise RuntimeError(\"Katana render instance node must be collected first.\")\n        output_definitions = render.get_output_definitions(instance_node)\n        if not output_definitions:\n            raise RuntimeError(\"Katana render instance has no enabled outputs.\")\n        for output_definition in output_definitions:\n            if not output_definition.path:\n                raise RuntimeError(\n                    f\"Katana output {output_definition.name!r} has no render path.\"\n                )\n\n        expected_files_by_aov = collect_expected_files_by_aov(\n            output_definitions,\n            int(instance.data[\"frameStartHandle\"]),\n            int(instance.data[\"frameEndHandle\"]),\n            int(instance.data[\"byFrameStep\"]),\n        )\n        output_patterns = [definition.path for definition in output_definitions]\n        first_expected_files = next(iter(expected_files_by_aov.values()))\n        output_directory = str(Path(first_expected_files[0]).parent)\n        instance.data[\"farm\"] = render_target == \"farm\"\n        instance.data.update(\n            {\n                \"expectedFiles\": [expected_files_by_aov],\n                \"files\": output_patterns,\n                \"outputDir\": output_directory,\n                \"multipartExr\": False,\n                \"attachTo\": [],\n            }\n        )\n        families = instance.data.setdefault(\"families\", [])\n        for family in (\"render\", \"katana.render\"):\n            if family not in families:\n                families.append(family)\n        if instance.data[\"farm\"] and \"render.farm\" not in families:\n            families.append(\"render.farm\")\n        if not instance.data[\"farm\"] and \"render.farm\" in families:\n            families.remove(\"render.farm\")\n        review = bool((instance.data.get(\"creator_attributes\") or {}).get(\"review\"))\n        if review and \"review\" not in families:\n            families.append(\"review\")\n        if not review and \"review\" in families:\n            families.remove(\"review\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render.html#client.ayon_katana.plugins.publish.collect_render.CollectRender.process","title":"<code>process(instance)</code>","text":"<p>Collect render outputs and expected files.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Collect render outputs and expected files.\"\"\"\n    creator_attributes = instance.data.get(\"creator_attributes\") or {}\n    render_target = creator_attributes.get(\"render_target\", \"farm\")\n    if render_target not in {\"farm\", \"local\", \"local_no_render\"}:\n        raise RuntimeError(f\"Unsupported Katana render target: {render_target!r}.\")\n    instance_node = compat.get_node(instance.data.get(\"instance_node\"))\n    if instance_node is None:\n        raise RuntimeError(\"Katana render instance node must be collected first.\")\n    output_definitions = render.get_output_definitions(instance_node)\n    if not output_definitions:\n        raise RuntimeError(\"Katana render instance has no enabled outputs.\")\n    for output_definition in output_definitions:\n        if not output_definition.path:\n            raise RuntimeError(\n                f\"Katana output {output_definition.name!r} has no render path.\"\n            )\n\n    expected_files_by_aov = collect_expected_files_by_aov(\n        output_definitions,\n        int(instance.data[\"frameStartHandle\"]),\n        int(instance.data[\"frameEndHandle\"]),\n        int(instance.data[\"byFrameStep\"]),\n    )\n    output_patterns = [definition.path for definition in output_definitions]\n    first_expected_files = next(iter(expected_files_by_aov.values()))\n    output_directory = str(Path(first_expected_files[0]).parent)\n    instance.data[\"farm\"] = render_target == \"farm\"\n    instance.data.update(\n        {\n            \"expectedFiles\": [expected_files_by_aov],\n            \"files\": output_patterns,\n            \"outputDir\": output_directory,\n            \"multipartExr\": False,\n            \"attachTo\": [],\n        }\n    )\n    families = instance.data.setdefault(\"families\", [])\n    for family in (\"render\", \"katana.render\"):\n        if family not in families:\n            families.append(family)\n    if instance.data[\"farm\"] and \"render.farm\" not in families:\n        families.append(\"render.farm\")\n    if not instance.data[\"farm\"] and \"render.farm\" in families:\n        families.remove(\"render.farm\")\n    review = bool((instance.data.get(\"creator_attributes\") or {}).get(\"review\"))\n    if review and \"review\" not in families:\n        families.append(\"review\")\n    if not review and \"review\" in families:\n        families.remove(\"review\")\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render.html#client.ayon_katana.plugins.publish.collect_render.collect_expected_files_by_aov","title":"<code>collect_expected_files_by_aov(output_definitions, frame_start, frame_end, frame_step)</code>","text":"<p>Return AYON's expected-files mapping from Katana outputs.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render.py</code> <pre><code>def collect_expected_files_by_aov(\n    output_definitions: list[render.RenderOutputDefinition],\n    frame_start: int,\n    frame_end: int,\n    frame_step: int,\n) -&gt; dict[str, list[str]]:\n    \"\"\"Return AYON's expected-files mapping from Katana outputs.\"\"\"\n    expected_files_by_aov: dict[str, list[str]] = {}\n    for output_definition in output_definitions:\n        aov_identifier = output_definition.aov_identifier\n        if aov_identifier in expected_files_by_aov:\n            label = aov_identifier or \"main\"\n            raise RuntimeError(f\"Multiple Katana outputs use AOV identifier {label!r}.\")\n        expected_files_by_aov[aov_identifier] = expand_output_pattern(\n            output_definition.path,\n            frame_start,\n            frame_end,\n            frame_step,\n        )\n    return expected_files_by_aov\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render.html#client.ayon_katana.plugins.publish.collect_render.expand_output_pattern","title":"<code>expand_output_pattern(output_pattern, frame_start, frame_end, frame_step)</code>","text":"<p>Expand Katana <code>#</code> padding into concrete output file paths.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render.py</code> <pre><code>def expand_output_pattern(\n    output_pattern: str,\n    frame_start: int,\n    frame_end: int,\n    frame_step: int,\n) -&gt; list[str]:\n    \"\"\"Expand Katana ``#`` padding into concrete output file paths.\"\"\"\n    if frame_end &lt; frame_start:\n        raise RuntimeError(f\"Invalid Katana render range: {frame_start}-{frame_end}.\")\n    if frame_step &lt; 1:\n        raise RuntimeError(f\"Invalid Katana render frame step: {frame_step}.\")\n    output = []\n    for frame in range(frame_start, frame_end + 1, frame_step):\n        filepath = _FRAME_TOKEN_PATTERN.sub(\n            lambda match, value=str(frame): value.zfill(len(match.group(1))),\n            output_pattern,\n        )\n        output.append(os.path.normpath(filepath))\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_frame_range.html","title":"collect_render_frame_range","text":"<p>Collect the frame range for Katana render instances.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_frame_range.html#client.ayon_katana.plugins.publish.collect_render_frame_range.CollectRenderFrameRange","title":"<code>CollectRenderFrameRange</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Collect the Render node frame range.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render_frame_range.py</code> <pre><code>class CollectRenderFrameRange(plugin.KatanaInstancePlugin):\n    \"\"\"Collect the Render node frame range.\"\"\"\n\n    label = \"Collect Frame Range\"\n    order = pyblish.api.CollectorOrder + 0.11\n    families = [\"render\", \"katana.render\"]\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Collect frame start, end, and step.\"\"\"\n        render_node = compat.get_node(instance.data.get(\"render_node\"))\n        if render_node is None:\n            raise RuntimeError(\"Katana Render node must be collected before frames.\")\n        frame_range = render.get_render_frame_range(render_node)\n        instance.data.update(\n            {\n                \"frameStartHandle\": frame_range.start,\n                \"frameEndHandle\": frame_range.end,\n                \"byFrameStep\": frame_range.step,\n                \"katanaFrameRangeSource\": frame_range.source,\n            }\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_frame_range.html#client.ayon_katana.plugins.publish.collect_render_frame_range.CollectRenderFrameRange.process","title":"<code>process(instance)</code>","text":"<p>Collect frame start, end, and step.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render_frame_range.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Collect frame start, end, and step.\"\"\"\n    render_node = compat.get_node(instance.data.get(\"render_node\"))\n    if render_node is None:\n        raise RuntimeError(\"Katana Render node must be collected before frames.\")\n    frame_range = render.get_render_frame_range(render_node)\n    instance.data.update(\n        {\n            \"frameStartHandle\": frame_range.start,\n            \"frameEndHandle\": frame_range.end,\n            \"byFrameStep\": frame_range.step,\n            \"katanaFrameRangeSource\": frame_range.source,\n        }\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_node.html","title":"collect_render_node","text":"<p>Collect nodes for Katana render instances.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_node.html#client.ayon_katana.plugins.publish.collect_render_node.CollectRenderNode","title":"<code>CollectRenderNode</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Collect the Render and RenderSettings nodes.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render_node.py</code> <pre><code>class CollectRenderNode(plugin.KatanaInstancePlugin):\n    \"\"\"Collect the Render and RenderSettings nodes.\"\"\"\n\n    label = \"Collect Render Node\"\n    order = pyblish.api.CollectorOrder + 0.1\n    families = [\"render\", \"katana.render\"]\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Resolve the render graph nodes.\"\"\"\n        instance_node_name = instance.data.get(\"instance_node\")\n        instance_node = compat.get_node(instance_node_name)\n        if instance_node is None:\n            raise RuntimeError(\n                f\"Katana render instance node does not exist: {instance_node_name}\"\n            )\n        render_node = render.get_render_node(instance_node)\n        settings_node = render.get_settings_node(instance_node)\n        if render_node is None or settings_node is None:\n            raise RuntimeError(\n                f\"Katana render graph is incomplete: {instance_node_name}\"\n            )\n        instance.data.update(\n            {\n                \"instance_node\": instance_node.getName(),\n                \"render_node\": render_node.getName(),\n                \"render_settings_node\": settings_node.getName(),\n            }\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_node.html#client.ayon_katana.plugins.publish.collect_render_node.CollectRenderNode.process","title":"<code>process(instance)</code>","text":"<p>Resolve the render graph nodes.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render_node.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Resolve the render graph nodes.\"\"\"\n    instance_node_name = instance.data.get(\"instance_node\")\n    instance_node = compat.get_node(instance_node_name)\n    if instance_node is None:\n        raise RuntimeError(\n            f\"Katana render instance node does not exist: {instance_node_name}\"\n        )\n    render_node = render.get_render_node(instance_node)\n    settings_node = render.get_settings_node(instance_node)\n    if render_node is None or settings_node is None:\n        raise RuntimeError(\n            f\"Katana render graph is incomplete: {instance_node_name}\"\n        )\n    instance.data.update(\n        {\n            \"instance_node\": instance_node.getName(),\n            \"render_node\": render_node.getName(),\n            \"render_settings_node\": settings_node.getName(),\n        }\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_settings.html","title":"collect_render_settings","text":"<p>Collect Katana render settings and colorspace metadata.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_settings.html#client.ayon_katana.plugins.publish.collect_render_settings.CollectRenderSettings","title":"<code>CollectRenderSettings</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Collect render settings and colorspace metadata.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render_settings.py</code> <pre><code>class CollectRenderSettings(plugin.KatanaInstancePlugin):\n    \"\"\"Collect render settings and colorspace metadata.\"\"\"\n\n    label = \"Collect Render Settings\"\n    order = pyblish.api.CollectorOrder + 0.415\n    families = [\"render\", \"katana.render\"]\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Collect renderer, camera, resolution, and colorspace.\"\"\"\n        settings_node = compat.get_node(instance.data.get(\"render_settings_node\"))\n        if settings_node is None:\n            raise RuntimeError(\"Katana RenderSettings node must be collected first.\")\n        settings = render.get_effective_render_settings(settings_node)\n        resolution = render.get_resolution(settings.resolution_name)\n        instance.data.update(\n            {\n                \"renderer\": settings.renderer,\n                \"camera\": settings.camera,\n                \"katanaResolution\": resolution.name,\n                \"resolutionWidth\": resolution.width,\n                \"resolutionHeight\": resolution.height,\n                \"pixelAspect\": resolution.pixel_aspect,\n            }\n        )\n\n        color_data = lib.get_color_management_preferences() or {}\n        colorspace_name = color_data.get(\"colorspace\", \"\")\n        if color_data:\n            instance.data[\"colorspaceConfig\"] = color_data[\"config\"]\n            instance.data[\"colorspace\"] = colorspace_name\n\n        expected_files = instance.data.get(\"expectedFiles\") or []\n        if expected_files:\n            instance.data[\"renderProducts\"] = colorspace.ARenderProduct(\n                list(expected_files[0]),\n                colorspace_name,\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_settings.html#client.ayon_katana.plugins.publish.collect_render_settings.CollectRenderSettings.process","title":"<code>process(instance)</code>","text":"<p>Collect renderer, camera, resolution, and colorspace.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render_settings.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Collect renderer, camera, resolution, and colorspace.\"\"\"\n    settings_node = compat.get_node(instance.data.get(\"render_settings_node\"))\n    if settings_node is None:\n        raise RuntimeError(\"Katana RenderSettings node must be collected first.\")\n    settings = render.get_effective_render_settings(settings_node)\n    resolution = render.get_resolution(settings.resolution_name)\n    instance.data.update(\n        {\n            \"renderer\": settings.renderer,\n            \"camera\": settings.camera,\n            \"katanaResolution\": resolution.name,\n            \"resolutionWidth\": resolution.width,\n            \"resolutionHeight\": resolution.height,\n            \"pixelAspect\": resolution.pixel_aspect,\n        }\n    )\n\n    color_data = lib.get_color_management_preferences() or {}\n    colorspace_name = color_data.get(\"colorspace\", \"\")\n    if color_data:\n        instance.data[\"colorspaceConfig\"] = color_data[\"config\"]\n        instance.data[\"colorspace\"] = colorspace_name\n\n    expected_files = instance.data.get(\"expectedFiles\") or []\n    if expected_files:\n        instance.data[\"renderProducts\"] = colorspace.ARenderProduct(\n            list(expected_files[0]),\n            colorspace_name,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_task_handles.html","title":"collect_render_task_handles","text":"<p>Collector plugin for frame data on Katana render instances.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_task_handles.html#client.ayon_katana.plugins.publish.collect_render_task_handles.CollectAssetHandles","title":"<code>CollectAssetHandles</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>AYONPyblishPluginMixin</code></p> <p>Apply instance's task entity handles.</p> <p>If the instance has a handle-inclusive native render range, retrieve the task handles to compute the exclusive frame range and actual handle values.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render_task_handles.py</code> <pre><code>class CollectAssetHandles(\n    plugin.KatanaInstancePlugin,\n    AYONPyblishPluginMixin,\n):\n    \"\"\"Apply instance's task entity handles.\n\n    If the instance has a handle-inclusive native render range, retrieve the\n    task handles to compute the exclusive frame range and actual handle values.\n    \"\"\"\n\n    label = \"Collect Task Handles\"\n    order = pyblish.api.CollectorOrder + 0.499\n    families = [\"render\", \"katana.render\"]\n    use_asset_handles = True\n\n    def process(self, instance):\n        \"\"\"Apply optional task handles to the render frame range.\"\"\"\n        if \"frameStartHandle\" not in instance.data:\n            return\n        if \"frameEndHandle\" not in instance.data:\n            return\n\n        attr_values = self.get_attr_values_from_data(instance.data)\n        use_handles = bool(attr_values.get(\"use_handles\", self.use_asset_handles))\n        entity = instance.data.get(\"taskEntity\") or instance.data.get(\"folderEntity\")\n        attributes = (entity or {}).get(\"attrib\") or {}\n        handle_start = _handle_value(attributes, \"handleStart\") if use_handles else 0\n        handle_end = _handle_value(attributes, \"handleEnd\") if use_handles else 0\n        frame_start = int(instance.data[\"frameStartHandle\"]) + handle_start\n        frame_end = int(instance.data[\"frameEndHandle\"]) - handle_end\n        instance.data.update(\n            {\n                \"handleStart\": handle_start,\n                \"handleEnd\": handle_end,\n                \"frameStart\": frame_start,\n                \"frameEnd\": frame_end,\n            }\n        )\n\n    @classmethod\n    def get_attr_defs_for_instance(cls, create_context, instance):\n        \"\"\"Return task-handle controls for a render creator instance.\"\"\"\n        if not cls.instance_matches_plugin_families(instance):\n            return []\n        return [\n            BoolDef(\n                \"use_handles\",\n                label=\"Use task handles\",\n                default=cls.use_asset_handles,\n                tooltip=(\n                    \"Apply task handles while deriving the AYON cut range \"\n                    \"from Katana's native Render range.\"\n                ),\n            )\n        ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_task_handles.html#client.ayon_katana.plugins.publish.collect_render_task_handles.CollectAssetHandles.get_attr_defs_for_instance","title":"<code>get_attr_defs_for_instance(create_context, instance)</code>  <code>classmethod</code>","text":"<p>Return task-handle controls for a render creator instance.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render_task_handles.py</code> <pre><code>@classmethod\ndef get_attr_defs_for_instance(cls, create_context, instance):\n    \"\"\"Return task-handle controls for a render creator instance.\"\"\"\n    if not cls.instance_matches_plugin_families(instance):\n        return []\n    return [\n        BoolDef(\n            \"use_handles\",\n            label=\"Use task handles\",\n            default=cls.use_asset_handles,\n            tooltip=(\n                \"Apply task handles while deriving the AYON cut range \"\n                \"from Katana's native Render range.\"\n            ),\n        )\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_render_task_handles.html#client.ayon_katana.plugins.publish.collect_render_task_handles.CollectAssetHandles.process","title":"<code>process(instance)</code>","text":"<p>Apply optional task handles to the render frame range.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_render_task_handles.py</code> <pre><code>def process(self, instance):\n    \"\"\"Apply optional task handles to the render frame range.\"\"\"\n    if \"frameStartHandle\" not in instance.data:\n        return\n    if \"frameEndHandle\" not in instance.data:\n        return\n\n    attr_values = self.get_attr_values_from_data(instance.data)\n    use_handles = bool(attr_values.get(\"use_handles\", self.use_asset_handles))\n    entity = instance.data.get(\"taskEntity\") or instance.data.get(\"folderEntity\")\n    attributes = (entity or {}).get(\"attrib\") or {}\n    handle_start = _handle_value(attributes, \"handleStart\") if use_handles else 0\n    handle_end = _handle_value(attributes, \"handleEnd\") if use_handles else 0\n    frame_start = int(instance.data[\"frameStartHandle\"]) + handle_start\n    frame_end = int(instance.data[\"frameEndHandle\"]) - handle_end\n    instance.data.update(\n        {\n            \"handleStart\": handle_start,\n            \"handleEnd\": handle_end,\n            \"frameStart\": frame_start,\n            \"frameEnd\": frame_end,\n        }\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_review.html","title":"collect_review","text":"<p>Collect Katana Viewer Scene Review publish data.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_review.html#client.ayon_katana.plugins.publish.collect_review.CollectReview","title":"<code>CollectReview</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Collect cut/handle range and FPS for a Katana Viewer review.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_review.py</code> <pre><code>class CollectReview(plugin.KatanaInstancePlugin):\n    \"\"\"Collect cut/handle range and FPS for a Katana Viewer review.\"\"\"\n\n    label = \"Collect Scene Review\"\n    order = pyblish.api.CollectorOrder + 0.410\n    families = [\"katana.review\"]\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Normalize Scene Review data for capture and AYON Core review output.\"\"\"\n        frame_start, frame_end, handle_start, handle_end = _task_frame_data(instance)\n        frame_start_handle = frame_start - handle_start\n        frame_end_handle = frame_end + handle_end\n        if frame_end_handle &lt; frame_start_handle:\n            raise RuntimeError(\n                \"Scene Review frame range is invalid after applying task handles.\"\n            )\n\n        instance.data.update(\n            {\n                \"frameStart\": frame_start,\n                \"frameEnd\": frame_end,\n                \"handleStart\": handle_start,\n                \"handleEnd\": handle_end,\n                \"frameStartHandle\": frame_start_handle,\n                \"frameEndHandle\": frame_end_handle,\n                \"byFrameStep\": 1,\n                \"fps\": _instance_fps(instance),\n                \"review\": True,\n            }\n        )\n        families = instance.data.setdefault(\"families\", [])\n        for family in (\"review\", \"katana.review\"):\n            if family not in families:\n                families.append(family)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_review.html#client.ayon_katana.plugins.publish.collect_review.CollectReview.process","title":"<code>process(instance)</code>","text":"<p>Normalize Scene Review data for capture and AYON Core review output.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_review.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Normalize Scene Review data for capture and AYON Core review output.\"\"\"\n    frame_start, frame_end, handle_start, handle_end = _task_frame_data(instance)\n    frame_start_handle = frame_start - handle_start\n    frame_end_handle = frame_end + handle_end\n    if frame_end_handle &lt; frame_start_handle:\n        raise RuntimeError(\n            \"Scene Review frame range is invalid after applying task handles.\"\n        )\n\n    instance.data.update(\n        {\n            \"frameStart\": frame_start,\n            \"frameEnd\": frame_end,\n            \"handleStart\": handle_start,\n            \"handleEnd\": handle_end,\n            \"frameStartHandle\": frame_start_handle,\n            \"frameEndHandle\": frame_end_handle,\n            \"byFrameStep\": 1,\n            \"fps\": _instance_fps(instance),\n            \"review\": True,\n        }\n    )\n    families = instance.data.setdefault(\"families\", [])\n    for family in (\"review\", \"katana.review\"):\n        if family not in families:\n            families.append(family)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_usd_layer.html","title":"collect_usd_layer","text":"<p>Collect Katana USD layer export settings.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_usd_layer.html#client.ayon_katana.plugins.publish.collect_usd_layer.CollectUsdLayer","title":"<code>CollectUsdLayer</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Collect the <code>UsdLayerExport</code> node and its settings.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_usd_layer.py</code> <pre><code>class CollectUsdLayer(plugin.KatanaInstancePlugin):\n    \"\"\"Collect the ``UsdLayerExport`` node and its settings.\"\"\"\n\n    label = \"Collect USD Layer\"\n    order = pyblish.api.CollectorOrder + 0.01\n    families = [\"katana.usd\"]\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Collect the export node settings.\"\"\"\n        node_name = instance.data[\"instance_node\"]\n        export_node = compat.get_node(node_name)\n        if export_node is None:\n            return\n\n        settings = read_usd_layer_export_settings(export_node)\n        instance.data.update(\n            {\n                \"usdFileFormat\": settings[\"file_format\"],\n                \"usdTimeSamples\": settings[\"time_samples\"],\n                \"usdFrameStart\": settings[\"frame_start\"],\n                \"usdFrameEnd\": settings[\"frame_end\"],\n                \"usdSamplesPerFrame\": settings[\"samples_per_frame\"],\n                \"usdExportMethod\": settings[\"export_method\"],\n            }\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_usd_layer.html#client.ayon_katana.plugins.publish.collect_usd_layer.CollectUsdLayer.process","title":"<code>process(instance)</code>","text":"<p>Collect the export node settings.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_usd_layer.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Collect the export node settings.\"\"\"\n    node_name = instance.data[\"instance_node\"]\n    export_node = compat.get_node(node_name)\n    if export_node is None:\n        return\n\n    settings = read_usd_layer_export_settings(export_node)\n    instance.data.update(\n        {\n            \"usdFileFormat\": settings[\"file_format\"],\n            \"usdTimeSamples\": settings[\"time_samples\"],\n            \"usdFrameStart\": settings[\"frame_start\"],\n            \"usdFrameEnd\": settings[\"frame_end\"],\n            \"usdSamplesPerFrame\": settings[\"samples_per_frame\"],\n            \"usdExportMethod\": settings[\"export_method\"],\n        }\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_workfile.html","title":"collect_workfile","text":"<p>Collect workfile data from the current Katana project.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_workfile.html#client.ayon_katana.plugins.publish.collect_workfile.CollectWorkfile","title":"<code>CollectWorkfile</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Inject workfile representation into instance</p> Source code in <code>client/ayon_katana/plugins/publish/collect_workfile.py</code> <pre><code>class CollectWorkfile(plugin.KatanaInstancePlugin):\n    \"\"\"Inject workfile representation into instance\"\"\"\n\n    order = pyblish.api.CollectorOrder - 0.01\n    label = \"Katana Workfile Data\"\n    families = [\"workfile\"]\n\n    def process(self, instance):\n        \"\"\"Add the staged Katana workfile representation to an instance.\"\"\"\n        current_file = instance.context.data.get(\"currentFile\", \"\")\n        instance.data[\"setMembers\"] = [current_file] if current_file else []\n        instance.data.setdefault(\"productType\", \"workfile\")\n        instance.data.setdefault(\"productBaseType\", \"workfile\")\n\n        for key in (\"frameStart\", \"frameEnd\", \"handleStart\", \"handleEnd\"):\n            if key in instance.context.data:\n                instance.data[key] = instance.context.data[key]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_workfile.html#client.ayon_katana.plugins.publish.collect_workfile.CollectWorkfile.process","title":"<code>process(instance)</code>","text":"<p>Add the staged Katana workfile representation to an instance.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_workfile.py</code> <pre><code>def process(self, instance):\n    \"\"\"Add the staged Katana workfile representation to an instance.\"\"\"\n    current_file = instance.context.data.get(\"currentFile\", \"\")\n    instance.data[\"setMembers\"] = [current_file] if current_file else []\n    instance.data.setdefault(\"productType\", \"workfile\")\n    instance.data.setdefault(\"productBaseType\", \"workfile\")\n\n    for key in (\"frameStart\", \"frameEnd\", \"handleStart\", \"handleEnd\"):\n        if key in instance.context.data:\n            instance.data[key] = instance.context.data[key]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_workfile_dependencies.html","title":"collect_workfile_dependencies","text":"<p>Collect AYON representations loaded into the Katana workfile.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_workfile_dependencies.html#client.ayon_katana.plugins.publish.collect_workfile_dependencies.CollectWorkfileDependencies","title":"<code>CollectWorkfileDependencies</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Record all loaded AYON representations as workfile inputs.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_workfile_dependencies.py</code> <pre><code>class CollectWorkfileDependencies(plugin.KatanaInstancePlugin):\n    \"\"\"Record all loaded AYON representations as workfile inputs.\"\"\"\n\n    order = pyblish.api.CollectorOrder + 0.4\n    label = \"Katana Workfile Dependencies\"\n    families = [\"workfile\"]\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Collect representation dependencies referenced by the workfile.\"\"\"\n        host = registered_host()\n        representations = {\n            container.get(\"representation\")\n            for container in host.get_containers()\n            if container.get(\"representation\")\n        }\n        instance.data[\"inputRepresentations\"] = sorted(representations)\n        self.log.debug(\n            \"Collected workfile input representations: %s\",\n            instance.data[\"inputRepresentations\"],\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/collect_workfile_dependencies.html#client.ayon_katana.plugins.publish.collect_workfile_dependencies.CollectWorkfileDependencies.process","title":"<code>process(instance)</code>","text":"<p>Collect representation dependencies referenced by the workfile.</p> Source code in <code>client/ayon_katana/plugins/publish/collect_workfile_dependencies.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Collect representation dependencies referenced by the workfile.\"\"\"\n    host = registered_host()\n    representations = {\n        container.get(\"representation\")\n        for container in host.get_containers()\n        if container.get(\"representation\")\n    }\n    instance.data[\"inputRepresentations\"] = sorted(representations)\n    self.log.debug(\n        \"Collected workfile input representations: %s\",\n        instance.data[\"inputRepresentations\"],\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_local_render.html","title":"extract_local_render","text":"<p>Process Katana outputs locally during publishing.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_local_render.html#client.ayon_katana.plugins.publish.extract_local_render.ExtractLocalRender","title":"<code>ExtractLocalRender</code>","text":"<p>               Bases: <code>KatanaExtractorPlugin</code></p> <p>Process a non-farm Render or ImageWrite through batch mode.</p> Source code in <code>client/ayon_katana/plugins/publish/extract_local_render.py</code> <pre><code>class ExtractLocalRender(plugin.KatanaExtractorPlugin):\n    \"\"\"Process a non-farm Render or ImageWrite through batch mode.\"\"\"\n\n    label = \"Extract Local Output\"\n    order = pyblish.api.ExtractorOrder - 0.4\n    families = [\"render\", \"katana.render\", \"image\", \"katana.image\"]\n\n    def process(self, instance):\n        \"\"\"Run Katana in batch mode and verify expected files.\"\"\"\n        if instance.data.get(\"farm\"):\n            self.log.debug(\"Katana output is configured for farm processing.\")\n            return\n\n        render_target = (instance.data.get(\"creator_attributes\") or {}).get(\n            \"render_target\",\n            \"farm\",\n        )\n        expected_files = self._expected_files(instance)\n        if render_target == \"local_no_render\":\n            self._validate_output_files(\n                expected_files,\n                failure_message=(\n                    \"Katana existing-frame publish has missing, empty, unreadable, \"\n                    \"or corrupt output files.\"\n                ),\n                logger=self.log,\n            )\n            self.log.info(\n                \"Validated %d existing Katana output files without rendering.\",\n                len(expected_files),\n            )\n            return\n        if render_target != \"local\":\n            self.log.debug(\"Katana output target is not local; skipping extraction.\")\n            return\n\n        try:\n            scene_path = workio.validate_workfile_path(\n                instance.context.data.get(\"currentFile\", \"\"),\n                require_exists=True,\n            )\n        except ValueError as exc:\n            raise PublishError(\n                f\"Katana local render has no valid scene: {exc}\"\n            ) from exc\n\n        render_node = instance.data.get(\"render_node\")\n        if not render_node:\n            raise PublishError(\n                \"Katana local output has no collected Render or ImageWrite node.\"\n            )\n\n        for filepath in expected_files:\n            filepath.parent.mkdir(parents=True, exist_ok=True)\n\n        executable = render.get_katana_executable()\n        frame_start = int(instance.data[\"frameStartHandle\"])\n        frame_end = int(instance.data[\"frameEndHandle\"])\n        frame_step = int(instance.data[\"byFrameStep\"])\n        frame_range = self._format_frame_range(\n            frame_start,\n            frame_end,\n            frame_step,\n        )\n        environment = os.environ.copy()\n        environment.pop(\"AYON_KATANA_WORKFILE_PATH\", None)\n        command = [\n            str(executable),\n            \"--batch\",\n            f\"--katana-file={scene_path}\",\n            \"-t\",\n            frame_range,\n            f\"--render-node={render_node}\",\n        ]\n        self.log.info(\"Rendering Katana frames %s locally.\", frame_range)\n        with tempfile.TemporaryFile(\n            mode=\"w+t\",\n            encoding=\"utf-8\",\n            errors=\"replace\",\n        ) as render_log:\n            completed = subprocess.run(\n                command,\n                cwd=executable.parent,\n                env=environment,\n                check=False,\n                stdout=render_log,\n                stderr=subprocess.STDOUT,\n                text=True,\n            )\n            if completed.returncode != 0:\n                render_log.seek(0)\n                log_tail = render_log.read()[-8000:]\n                raise PublishError(\n                    f\"Katana local render failed for frames {frame_range} with \"\n                    f\"exit code {completed.returncode}.\",\n                    detail=f\"Katana batch output:\\n{log_tail}\",\n                )\n\n        self._validate_output_files(\n            expected_files,\n            failure_message=(\n                \"Katana local render did not produce all expected output files.\"\n            ),\n            logger=self.log,\n        )\n\n    @classmethod\n    def _validate_output_files(\n        cls,\n        filepaths,\n        *,\n        failure_message: str,\n        logger=None,\n    ) -&gt; None:\n        invalid_files = []\n        for filepath in filepaths:\n            if not filepath.is_file():\n                invalid_files.append((filepath, \"missing\"))\n                continue\n            try:\n                with filepath.open(\"rb\") as stream:\n                    if not stream.read(1):\n                        invalid_files.append((filepath, \"empty\"))\n                        continue\n            except OSError as exc:\n                invalid_files.append((filepath, f\"unreadable: {exc}\"))\n                continue\n\n            try:\n                cls._decode_output_file(filepath, logger=logger)\n            except Exception as exc:\n                invalid_files.append(\n                    (\n                        filepath,\n                        f\"OpenImageIO decode failed: {type(exc).__name__}: {exc}\",\n                    )\n                )\n\n        if invalid_files:\n            formatted_files = \"\\n\".join(\n                f\"- {path}: {reason}\" for path, reason in invalid_files\n            )\n            raise PublishError(\n                failure_message,\n                detail=f\"Invalid files:\\n{formatted_files}\",\n            )\n\n    @staticmethod\n    def _decode_output_file(filepath: Path, *, logger=None) -&gt; None:\n        from ayon_core.lib import get_oiio_tool_args, run_subprocess\n\n        arguments = get_oiio_tool_args(\n            \"oiiotool\",\n            \"--hash\",\n            str(filepath),\n        )\n        run_subprocess(arguments, logger=logger)\n\n    @staticmethod\n    def _format_frame_range(frame_start, frame_end, frame_step):\n        if frame_end &lt; frame_start:\n            raise PublishError(\n                f\"Invalid Katana render range: {frame_start}-{frame_end}.\"\n            )\n        if frame_step &lt; 1:\n            raise PublishError(f\"Invalid Katana render frame step: {frame_step}.\")\n        if frame_start == frame_end:\n            return str(frame_start)\n        if frame_step == 1:\n            return f\"{frame_start}-{frame_end}\"\n        return \",\".join(\n            str(frame) for frame in range(frame_start, frame_end + 1, frame_step)\n        )\n\n    @staticmethod\n    def _expected_files(instance):\n        expected_files = instance.data.get(\"expectedFiles\") or []\n        if not expected_files:\n            raise PublishError(\"Katana local output has no expected output files.\")\n\n        output = []\n        for filepaths in expected_files[0].values():\n            if isinstance(filepaths, str):\n                output.append(Path(filepaths))\n            else:\n                output.extend(Path(filepath) for filepath in filepaths)\n        if not output:\n            raise PublishError(\"Katana local output has no expected output files.\")\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_local_render.html#client.ayon_katana.plugins.publish.extract_local_render.ExtractLocalRender.process","title":"<code>process(instance)</code>","text":"<p>Run Katana in batch mode and verify expected files.</p> Source code in <code>client/ayon_katana/plugins/publish/extract_local_render.py</code> <pre><code>def process(self, instance):\n    \"\"\"Run Katana in batch mode and verify expected files.\"\"\"\n    if instance.data.get(\"farm\"):\n        self.log.debug(\"Katana output is configured for farm processing.\")\n        return\n\n    render_target = (instance.data.get(\"creator_attributes\") or {}).get(\n        \"render_target\",\n        \"farm\",\n    )\n    expected_files = self._expected_files(instance)\n    if render_target == \"local_no_render\":\n        self._validate_output_files(\n            expected_files,\n            failure_message=(\n                \"Katana existing-frame publish has missing, empty, unreadable, \"\n                \"or corrupt output files.\"\n            ),\n            logger=self.log,\n        )\n        self.log.info(\n            \"Validated %d existing Katana output files without rendering.\",\n            len(expected_files),\n        )\n        return\n    if render_target != \"local\":\n        self.log.debug(\"Katana output target is not local; skipping extraction.\")\n        return\n\n    try:\n        scene_path = workio.validate_workfile_path(\n            instance.context.data.get(\"currentFile\", \"\"),\n            require_exists=True,\n        )\n    except ValueError as exc:\n        raise PublishError(\n            f\"Katana local render has no valid scene: {exc}\"\n        ) from exc\n\n    render_node = instance.data.get(\"render_node\")\n    if not render_node:\n        raise PublishError(\n            \"Katana local output has no collected Render or ImageWrite node.\"\n        )\n\n    for filepath in expected_files:\n        filepath.parent.mkdir(parents=True, exist_ok=True)\n\n    executable = render.get_katana_executable()\n    frame_start = int(instance.data[\"frameStartHandle\"])\n    frame_end = int(instance.data[\"frameEndHandle\"])\n    frame_step = int(instance.data[\"byFrameStep\"])\n    frame_range = self._format_frame_range(\n        frame_start,\n        frame_end,\n        frame_step,\n    )\n    environment = os.environ.copy()\n    environment.pop(\"AYON_KATANA_WORKFILE_PATH\", None)\n    command = [\n        str(executable),\n        \"--batch\",\n        f\"--katana-file={scene_path}\",\n        \"-t\",\n        frame_range,\n        f\"--render-node={render_node}\",\n    ]\n    self.log.info(\"Rendering Katana frames %s locally.\", frame_range)\n    with tempfile.TemporaryFile(\n        mode=\"w+t\",\n        encoding=\"utf-8\",\n        errors=\"replace\",\n    ) as render_log:\n        completed = subprocess.run(\n            command,\n            cwd=executable.parent,\n            env=environment,\n            check=False,\n            stdout=render_log,\n            stderr=subprocess.STDOUT,\n            text=True,\n        )\n        if completed.returncode != 0:\n            render_log.seek(0)\n            log_tail = render_log.read()[-8000:]\n            raise PublishError(\n                f\"Katana local render failed for frames {frame_range} with \"\n                f\"exit code {completed.returncode}.\",\n                detail=f\"Katana batch output:\\n{log_tail}\",\n            )\n\n    self._validate_output_files(\n        expected_files,\n        failure_message=(\n            \"Katana local render did not produce all expected output files.\"\n        ),\n        logger=self.log,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_nodegraph.html","title":"extract_nodegraph","text":"<p>Export selected Katana Groups as reusable node graph representations.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_nodegraph.html#client.ayon_katana.plugins.publish.extract_nodegraph.ExtractNodegraph","title":"<code>ExtractNodegraph</code>","text":"<p>               Bases: <code>KatanaExtractorPlugin</code></p> <p>Extract the validated Katana node graph.</p> Source code in <code>client/ayon_katana/plugins/publish/extract_nodegraph.py</code> <pre><code>class ExtractNodegraph(plugin.KatanaExtractorPlugin):\n    \"\"\"Extract the validated Katana node graph.\"\"\"\n\n    label = \"Extract Node Graph\"\n    order = pyblish.api.ExtractorOrder\n    families = [\"nodegraph\"]\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Stage one selected Group without saving the active workfile.\"\"\"\n        source_name = instance.data.get(\"nodegraph_node\")\n        source_node = compat.get_node(source_name) if source_name else None\n        if source_node is None:\n            raise PublishError(\n                f\"Katana node graph source does not exist: {source_name!r}\"\n            )\n\n        product_name = instance.data.get(\"productName\") or source_node.getName()\n        filename = f\"{product_name}.katana\"\n        staging_dir = Path(self.staging_dir(instance))\n        staging_dir.mkdir(parents=True, exist_ok=True)\n        destination_path = staging_dir / filename\n\n        try:\n            exported_path = KatanaFile.Export(\n                destination_path.as_posix(),\n                [source_node],\n            )\n        except Exception as exc:\n            raise PublishError(\n                f\"Katana node graph export failed for {source_node.getName()!r}: {exc}\"\n            ) from exc\n\n        if exported_path is None or not destination_path.is_file():\n            raise PublishError(\n                \"Katana node graph export did not create \"\n                f\"{destination_path.as_posix()!r}.\"\n            )\n\n        representation = {\n            \"name\": \"katana\",\n            \"ext\": \"katana\",\n            \"files\": filename,\n            \"stagingDir\": str(staging_dir),\n        }\n        instance.data.setdefault(\"representations\", []).append(representation)\n        instance.data[\"setMembers\"] = [destination_path.as_posix()]\n        self.log.info(\"Exported Katana node graph: %s\", destination_path)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_nodegraph.html#client.ayon_katana.plugins.publish.extract_nodegraph.ExtractNodegraph.process","title":"<code>process(instance)</code>","text":"<p>Stage one selected Group without saving the active workfile.</p> Source code in <code>client/ayon_katana/plugins/publish/extract_nodegraph.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Stage one selected Group without saving the active workfile.\"\"\"\n    source_name = instance.data.get(\"nodegraph_node\")\n    source_node = compat.get_node(source_name) if source_name else None\n    if source_node is None:\n        raise PublishError(\n            f\"Katana node graph source does not exist: {source_name!r}\"\n        )\n\n    product_name = instance.data.get(\"productName\") or source_node.getName()\n    filename = f\"{product_name}.katana\"\n    staging_dir = Path(self.staging_dir(instance))\n    staging_dir.mkdir(parents=True, exist_ok=True)\n    destination_path = staging_dir / filename\n\n    try:\n        exported_path = KatanaFile.Export(\n            destination_path.as_posix(),\n            [source_node],\n        )\n    except Exception as exc:\n        raise PublishError(\n            f\"Katana node graph export failed for {source_node.getName()!r}: {exc}\"\n        ) from exc\n\n    if exported_path is None or not destination_path.is_file():\n        raise PublishError(\n            \"Katana node graph export did not create \"\n            f\"{destination_path.as_posix()!r}.\"\n        )\n\n    representation = {\n        \"name\": \"katana\",\n        \"ext\": \"katana\",\n        \"files\": filename,\n        \"stagingDir\": str(staging_dir),\n    }\n    instance.data.setdefault(\"representations\", []).append(representation)\n    instance.data[\"setMembers\"] = [destination_path.as_posix()]\n    self.log.info(\"Exported Katana node graph: %s\", destination_path)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_review_capture.html","title":"extract_review_capture","text":"<p>Capture the active Katana Viewer as a review image sequence.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_review_capture.html#client.ayon_katana.plugins.publish.extract_review_capture.ExtractReviewCapture","title":"<code>ExtractReviewCapture</code>","text":"<p>               Bases: <code>KatanaExtractorPlugin</code></p> <p>Create the source PNG sequence consumed by AYON Core ExtractReview.</p> Source code in <code>client/ayon_katana/plugins/publish/extract_review_capture.py</code> <pre><code>class ExtractReviewCapture(plugin.KatanaExtractorPlugin):\n    \"\"\"Create the source PNG sequence consumed by AYON Core ExtractReview.\"\"\"\n\n    label = \"Extract Scene Review Capture\"\n    order = pyblish.api.ExtractorOrder - 0.1\n    families = [\"katana.review\"]\n    frame_padding = 4\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Capture every review frame from one unambiguous visible Viewer.\"\"\"\n        try:\n            frame_start = int(instance.data[\"frameStartHandle\"])\n            frame_end = int(instance.data[\"frameEndHandle\"])\n            frame_step = int(instance.data[\"byFrameStep\"])\n        except (KeyError, TypeError, ValueError) as exc:\n            raise PublishError(\"Scene Review frame data was not collected.\") from exc\n\n        try:\n            fps = float(instance.data[\"fps\"])\n        except (KeyError, TypeError, ValueError) as exc:\n            raise PublishError(\"Scene Review FPS was not collected.\") from exc\n        if not math.isfinite(fps) or fps &lt;= 0:\n            raise PublishError(\"Scene Review requires a finite positive AYON task FPS.\")\n\n        viewer_widget, reason = thumbnail.select_viewer_widget()\n        if viewer_widget is None:\n            raise PublishError(f\"Scene Review cannot select a Katana Viewer: {reason}.\")\n\n        staging_dir = Path(self.staging_dir(instance))\n        product_name = str(instance.data.get(\"productName\") or \"review\")\n        try:\n            filenames = review.capture_viewer_sequence(\n                viewer_widget,\n                staging_dir,\n                product_name,\n                frame_start,\n                frame_end,\n                frame_step,\n                frame_padding=self.frame_padding,\n            )\n        except Exception as exc:\n            raise PublishError(f\"Katana Viewer review capture failed: {exc}\") from exc\n        if not filenames:\n            raise PublishError(\"Katana Viewer review capture produced no frames.\")\n\n        representation = {\n            \"name\": \"png\",\n            \"ext\": \"png\",\n            \"files\": filenames,\n            \"stagingDir\": str(staging_dir),\n            \"tags\": [\"review\"],\n            \"frameStart\": frame_start,\n            \"frameEnd\": frame_end,\n            \"fps\": fps,\n        }\n        instance.data.setdefault(\"representations\", []).append(representation)\n        self.log.info(\n            \"Captured %d Katana Viewer review frames to %s.\",\n            len(filenames),\n            staging_dir,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_review_capture.html#client.ayon_katana.plugins.publish.extract_review_capture.ExtractReviewCapture.process","title":"<code>process(instance)</code>","text":"<p>Capture every review frame from one unambiguous visible Viewer.</p> Source code in <code>client/ayon_katana/plugins/publish/extract_review_capture.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Capture every review frame from one unambiguous visible Viewer.\"\"\"\n    try:\n        frame_start = int(instance.data[\"frameStartHandle\"])\n        frame_end = int(instance.data[\"frameEndHandle\"])\n        frame_step = int(instance.data[\"byFrameStep\"])\n    except (KeyError, TypeError, ValueError) as exc:\n        raise PublishError(\"Scene Review frame data was not collected.\") from exc\n\n    try:\n        fps = float(instance.data[\"fps\"])\n    except (KeyError, TypeError, ValueError) as exc:\n        raise PublishError(\"Scene Review FPS was not collected.\") from exc\n    if not math.isfinite(fps) or fps &lt;= 0:\n        raise PublishError(\"Scene Review requires a finite positive AYON task FPS.\")\n\n    viewer_widget, reason = thumbnail.select_viewer_widget()\n    if viewer_widget is None:\n        raise PublishError(f\"Scene Review cannot select a Katana Viewer: {reason}.\")\n\n    staging_dir = Path(self.staging_dir(instance))\n    product_name = str(instance.data.get(\"productName\") or \"review\")\n    try:\n        filenames = review.capture_viewer_sequence(\n            viewer_widget,\n            staging_dir,\n            product_name,\n            frame_start,\n            frame_end,\n            frame_step,\n            frame_padding=self.frame_padding,\n        )\n    except Exception as exc:\n        raise PublishError(f\"Katana Viewer review capture failed: {exc}\") from exc\n    if not filenames:\n        raise PublishError(\"Katana Viewer review capture produced no frames.\")\n\n    representation = {\n        \"name\": \"png\",\n        \"ext\": \"png\",\n        \"files\": filenames,\n        \"stagingDir\": str(staging_dir),\n        \"tags\": [\"review\"],\n        \"frameStart\": frame_start,\n        \"frameEnd\": frame_end,\n        \"fps\": fps,\n    }\n    instance.data.setdefault(\"representations\", []).append(representation)\n    self.log.info(\n        \"Captured %d Katana Viewer review frames to %s.\",\n        len(filenames),\n        staging_dir,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_usd_layer.html","title":"extract_usd_layer","text":"<p>Extract Katana USD layers into AYON representations.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_usd_layer.html#client.ayon_katana.plugins.publish.extract_usd_layer.ExtractUsdLayer","title":"<code>ExtractUsdLayer</code>","text":"<p>               Bases: <code>KatanaExtractorPlugin</code></p> <p>Write a USD representation with <code>UsdLayerExport.write</code>.</p> Source code in <code>client/ayon_katana/plugins/publish/extract_usd_layer.py</code> <pre><code>class ExtractUsdLayer(plugin.KatanaExtractorPlugin):\n    \"\"\"Write a USD representation with ``UsdLayerExport.write``.\"\"\"\n\n    label = \"Extract USD Layer\"\n    # UsdLayerExport temporarily edits its parameters. Run before\n    # SaveCurrentScene so restored values are saved before workfile staging.\n    order = pyblish.api.ExtractorOrder - 0.5\n    families = [\"katana.usd\"]\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Export the USD layer into the instance staging path.\"\"\"\n        node_name = instance.data[\"instance_node\"]\n        export_node = compat.get_node(node_name)\n        if export_node is None:\n            raise PublishError(f\"Katana USD export node does not exist: {node_name!r}\")\n\n        existing_representations = instance.data.get(\"representations\") or []\n        if existing_representations:\n            raise PublishError(\"USD layer instance already has representations.\")\n\n        settings = read_usd_layer_export_settings(export_node)\n        file_format = settings[\"file_format\"]\n        product_name = instance.data.get(\"productName\") or export_node.getName()\n        filename = f\"{product_name}.{file_format}\"\n        staging_dir = Path(self.staging_dir(instance))\n        destination = staging_dir / filename\n        try:\n            export_usd_layer(export_node, destination)\n        except Exception as exc:\n            raise PublishError(\n                f\"Katana USD layer export failed for {product_name!r}: {exc}\"\n            ) from exc\n\n        instance.data[\"stagingDir\"] = str(staging_dir)\n        instance.data[\"representations\"] = [\n            {\n                \"name\": \"usd\",\n                \"ext\": file_format,\n                \"files\": filename,\n                \"stagingDir\": str(staging_dir),\n            }\n        ]\n        instance.data[\"setMembers\"] = [destination.as_posix()]\n        self.log.info(\"Exported USD layer: %s\", destination)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_usd_layer.html#client.ayon_katana.plugins.publish.extract_usd_layer.ExtractUsdLayer.process","title":"<code>process(instance)</code>","text":"<p>Export the USD layer into the instance staging path.</p> Source code in <code>client/ayon_katana/plugins/publish/extract_usd_layer.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Export the USD layer into the instance staging path.\"\"\"\n    node_name = instance.data[\"instance_node\"]\n    export_node = compat.get_node(node_name)\n    if export_node is None:\n        raise PublishError(f\"Katana USD export node does not exist: {node_name!r}\")\n\n    existing_representations = instance.data.get(\"representations\") or []\n    if existing_representations:\n        raise PublishError(\"USD layer instance already has representations.\")\n\n    settings = read_usd_layer_export_settings(export_node)\n    file_format = settings[\"file_format\"]\n    product_name = instance.data.get(\"productName\") or export_node.getName()\n    filename = f\"{product_name}.{file_format}\"\n    staging_dir = Path(self.staging_dir(instance))\n    destination = staging_dir / filename\n    try:\n        export_usd_layer(export_node, destination)\n    except Exception as exc:\n        raise PublishError(\n            f\"Katana USD layer export failed for {product_name!r}: {exc}\"\n        ) from exc\n\n    instance.data[\"stagingDir\"] = str(staging_dir)\n    instance.data[\"representations\"] = [\n        {\n            \"name\": \"usd\",\n            \"ext\": file_format,\n            \"files\": filename,\n            \"stagingDir\": str(staging_dir),\n        }\n    ]\n    instance.data[\"setMembers\"] = [destination.as_posix()]\n    self.log.info(\"Exported USD layer: %s\", destination)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_workfile.html","title":"extract_workfile","text":"<p>Stage a copy of the current Katana workfile for publishing.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_workfile.html#client.ayon_katana.plugins.publish.extract_workfile.ExtractWorkfile","title":"<code>ExtractWorkfile</code>","text":"<p>               Bases: <code>KatanaExtractorPlugin</code></p> <p>Copy the saved Katana project into the instance staging directory.</p> Source code in <code>client/ayon_katana/plugins/publish/extract_workfile.py</code> <pre><code>class ExtractWorkfile(plugin.KatanaExtractorPlugin):\n    \"\"\"Copy the saved Katana project into the instance staging directory.\"\"\"\n\n    label = \"Extract Katana Workfile\"\n    order = pyblish.api.ExtractorOrder\n    families = [\"workfile\"]\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Copy the clean current project without changing Katana's active path.\"\"\"\n        context = instance.context\n        try:\n            source_path = workio.validate_workfile_path(\n                context.data.get(\"currentFile\", \"\"),\n                require_exists=True,\n            )\n            active_path = workio.validate_workfile_path(\n                registered_host().get_current_workfile(),\n                require_exists=True,\n            )\n        except ValueError as exc:\n            raise PublishError(f\"Katana workfile cannot be staged: {exc}\") from exc\n\n        if not workio.workfile_paths_match(source_path, active_path):\n            raise PublishError(\n                \"Katana workfile path changed during publishing; refusing to \"\n                \"stage a different scene.\"\n            )\n        if registered_host().workfile_has_unsaved_changes():\n            raise PublishError(\n                \"Katana workfile has unsaved changes; refusing to stage stale data.\"\n            )\n\n        context.data[\"currentFile\"] = source_path\n        staging_dir = self.staging_dir(instance)\n        filename = os.path.basename(source_path)\n        destination_path = os.path.join(staging_dir, filename)\n\n        destination_path = os.path.abspath(os.path.normpath(destination_path))\n        if not workio.workfile_paths_match(source_path, destination_path):\n            shutil.copy2(source_path, destination_path)\n\n        extension = os.path.splitext(filename)[1].lstrip(\".\")\n        representation = {\n            \"name\": extension,\n            \"ext\": extension,\n            \"files\": filename,\n            \"stagingDir\": staging_dir,\n        }\n        instance.data.setdefault(\"representations\", []).append(representation)\n        self.log.info(\"Staged Katana workfile: %s\", destination_path)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_workfile.html#client.ayon_katana.plugins.publish.extract_workfile.ExtractWorkfile.process","title":"<code>process(instance)</code>","text":"<p>Copy the clean current project without changing Katana's active path.</p> Source code in <code>client/ayon_katana/plugins/publish/extract_workfile.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Copy the clean current project without changing Katana's active path.\"\"\"\n    context = instance.context\n    try:\n        source_path = workio.validate_workfile_path(\n            context.data.get(\"currentFile\", \"\"),\n            require_exists=True,\n        )\n        active_path = workio.validate_workfile_path(\n            registered_host().get_current_workfile(),\n            require_exists=True,\n        )\n    except ValueError as exc:\n        raise PublishError(f\"Katana workfile cannot be staged: {exc}\") from exc\n\n    if not workio.workfile_paths_match(source_path, active_path):\n        raise PublishError(\n            \"Katana workfile path changed during publishing; refusing to \"\n            \"stage a different scene.\"\n        )\n    if registered_host().workfile_has_unsaved_changes():\n        raise PublishError(\n            \"Katana workfile has unsaved changes; refusing to stage stale data.\"\n        )\n\n    context.data[\"currentFile\"] = source_path\n    staging_dir = self.staging_dir(instance)\n    filename = os.path.basename(source_path)\n    destination_path = os.path.join(staging_dir, filename)\n\n    destination_path = os.path.abspath(os.path.normpath(destination_path))\n    if not workio.workfile_paths_match(source_path, destination_path):\n        shutil.copy2(source_path, destination_path)\n\n    extension = os.path.splitext(filename)[1].lstrip(\".\")\n    representation = {\n        \"name\": extension,\n        \"ext\": extension,\n        \"files\": filename,\n        \"stagingDir\": staging_dir,\n    }\n    instance.data.setdefault(\"representations\", []).append(representation)\n    self.log.info(\"Staged Katana workfile: %s\", destination_path)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_workfile_thumbnail.html","title":"extract_workfile_thumbnail","text":"<p>Capture a Katana Viewer thumbnail for workfile publishing.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_workfile_thumbnail.html#client.ayon_katana.plugins.publish.extract_workfile_thumbnail.ExtractWorkfileThumbnail","title":"<code>ExtractWorkfileThumbnail</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Capture a workfile thumbnail from the current Viewer.</p> Source code in <code>client/ayon_katana/plugins/publish/extract_workfile_thumbnail.py</code> <pre><code>class ExtractWorkfileThumbnail(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Capture a workfile thumbnail from the current Viewer.\"\"\"\n\n    label = \"Extract Workfile Thumbnail\"\n    order = pyblish.api.ExtractorOrder + 0.49\n    families = [\"workfile\"]\n    optional = True\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Capture the Viewer thumbnail.\"\"\"\n        if not self.is_active(instance.data):\n            return\n        if instance.data.get(\"thumbnailPath\"):\n            self.log.debug(\"Keeping the existing workfile thumbnail.\")\n            return\n\n        viewer_widget, reason = thumbnail.select_viewer_widget()\n        if viewer_widget is None:\n            self.log.debug(\"Skipping workfile thumbnail: %s.\", reason)\n            return\n\n        try:\n            thumbnail_path = thumbnail.capture_viewer_thumbnail(viewer_widget)\n        except Exception as exc:\n            self.log.warning(\"Katana Viewer thumbnail capture failed: %s\", exc)\n            return\n\n        instance.data[\"thumbnailPath\"] = thumbnail_path\n        cleanup_paths = instance.context.data.setdefault(\"cleanupFullPaths\", [])\n        if thumbnail_path not in cleanup_paths:\n            cleanup_paths.append(thumbnail_path)\n        self.log.debug(\n            \"Captured workfile thumbnail from %s: %s\",\n            reason,\n            thumbnail_path,\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/extract_workfile_thumbnail.html#client.ayon_katana.plugins.publish.extract_workfile_thumbnail.ExtractWorkfileThumbnail.process","title":"<code>process(instance)</code>","text":"<p>Capture the Viewer thumbnail.</p> Source code in <code>client/ayon_katana/plugins/publish/extract_workfile_thumbnail.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Capture the Viewer thumbnail.\"\"\"\n    if not self.is_active(instance.data):\n        return\n    if instance.data.get(\"thumbnailPath\"):\n        self.log.debug(\"Keeping the existing workfile thumbnail.\")\n        return\n\n    viewer_widget, reason = thumbnail.select_viewer_widget()\n    if viewer_widget is None:\n        self.log.debug(\"Skipping workfile thumbnail: %s.\", reason)\n        return\n\n    try:\n        thumbnail_path = thumbnail.capture_viewer_thumbnail(viewer_widget)\n    except Exception as exc:\n        self.log.warning(\"Katana Viewer thumbnail capture failed: %s\", exc)\n        return\n\n    instance.data[\"thumbnailPath\"] = thumbnail_path\n    cleanup_paths = instance.context.data.setdefault(\"cleanupFullPaths\", [])\n    if thumbnail_path not in cleanup_paths:\n        cleanup_paths.append(thumbnail_path)\n    self.log.debug(\n        \"Captured workfile thumbnail from %s: %s\",\n        reason,\n        thumbnail_path,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/finalize_usd_publish.html","title":"finalize_usd_publish","text":"<p>Finalize staged USD outputs.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/finalize_usd_publish.html#client.ayon_katana.plugins.publish.finalize_usd_publish.FinalizeUsdPublish","title":"<code>FinalizeUsdPublish</code>","text":"<p>               Bases: <code>KatanaContextPlugin</code></p> <p>Finalize staged paths and look resources.</p> Source code in <code>client/ayon_katana/plugins/publish/finalize_usd_publish.py</code> <pre><code>class FinalizeUsdPublish(plugin.KatanaContextPlugin):\n    \"\"\"Finalize staged paths and look resources.\"\"\"\n\n    label = \"Finalize USD\"\n    order = pyblish.api.ExtractorOrder - 0.495\n\n    def process(self, context) -&gt; None:\n        \"\"\"Finalize active USD instances.\"\"\"\n        try:\n            publish_mapping = build_publish_path_map(context)\n        except Exception as exc:\n            raise PublishError(f\"Failed to build USD publish path map: {exc}\") from exc\n\n        for instance in context:\n            if not instance.data.get(\"active\", True) or not instance.data.get(\n                \"publish\", True\n            ):\n                continue\n            families = set(instance.data.get(\"families\") or [])\n            if \"katana.usd\" not in families:\n                continue\n\n            try:\n                staged_path = get_extracted_usd_layer_path(instance.data)\n                source_stage = get_composed_source_stage(instance.data)\n                asset_remap = dict(instance.data.get(\"assetRemap\") or {})\n\n                if is_look_instance(instance.data):\n                    from pxr import Sdf\n\n                    layer = Sdf.Layer.FindOrOpen(staged_path.as_posix())\n                    if layer is None:\n                        raise RuntimeError(\n                            f\"Failed to open staged USD look layer: {staged_path}\"\n                        )\n                    resources_dir = instance.data.get(\"resourcesDir\")\n                    if not resources_dir:\n                        raise RuntimeError(\"USD look instance has no resourcesDir.\")\n                    plan = plan_look_resources(layer, resources_dir, source_stage)\n                    self._merge_resource_plan(instance, plan)\n                    asset_remap.update(plan[\"assetRemap\"])\n\n                rewrite_staged_usd_layer(\n                    staged_path,\n                    publish_mapping=publish_mapping,\n                    asset_remap=asset_remap,\n                    source_stage=source_stage,\n                )\n            except Exception as exc:\n                product_name = instance.data.get(\"productName\") or instance.name\n                raise PublishError(\n                    f\"Failed to finalize USD product {product_name!r}: {exc}\"\n                ) from exc\n\n    @staticmethod\n    def _merge_resource_plan(instance, plan: dict) -&gt; None:\n        resources = instance.data.setdefault(\"resources\", [])\n        existing_keys = {\n            (\n                resource.get(\"attribute\"),\n                resource.get(\"source\"),\n                tuple(resource.get(\"files\") or []),\n                resource.get(\"color_space\"),\n            )\n            for resource in resources\n        }\n        for resource in plan[\"resources\"]:\n            key = (\n                resource.get(\"attribute\"),\n                resource.get(\"source\"),\n                tuple(resource.get(\"files\") or []),\n                resource.get(\"color_space\"),\n            )\n            if key not in existing_keys:\n                resources.append(resource)\n                existing_keys.add(key)\n\n        remap = instance.data.setdefault(\"assetRemap\", {})\n        for source, destination in plan[\"assetRemap\"].items():\n            existing = remap.get(source)\n            if existing is not None and existing != destination:\n                raise ValueError(\n                    f\"Conflicting USD asset remap for {source!r}: \"\n                    f\"{existing!r} vs {destination!r}.\"\n                )\n            remap[source] = destination\n\n        transfers = instance.data.setdefault(\"transfers\", [])\n        destination_sources = {}\n        for existing_transfer in transfers:\n            if len(existing_transfer) != 2:\n                continue\n            source, destination = existing_transfer\n            destination_sources[FinalizeUsdPublish._transfer_path_key(destination)] = (\n                FinalizeUsdPublish._transfer_path_key(source)\n            )\n\n        for source, destination in plan[\"transfers\"]:\n            source_key = FinalizeUsdPublish._transfer_path_key(source)\n            destination_key = FinalizeUsdPublish._transfer_path_key(destination)\n            existing_source = destination_sources.get(destination_key)\n            if existing_source is not None:\n                if existing_source != source_key:\n                    raise ValueError(\n                        \"Conflicting USD resource transfer destination \"\n                        f\"{destination!r}.\"\n                    )\n                continue\n            transfers.append((source, destination))\n            destination_sources[destination_key] = source_key\n\n    @staticmethod\n    def _transfer_path_key(path: str) -&gt; str:\n        return os.path.normcase(os.path.normpath(str(path)))\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/finalize_usd_publish.html#client.ayon_katana.plugins.publish.finalize_usd_publish.FinalizeUsdPublish.process","title":"<code>process(context)</code>","text":"<p>Finalize active USD instances.</p> Source code in <code>client/ayon_katana/plugins/publish/finalize_usd_publish.py</code> <pre><code>def process(self, context) -&gt; None:\n    \"\"\"Finalize active USD instances.\"\"\"\n    try:\n        publish_mapping = build_publish_path_map(context)\n    except Exception as exc:\n        raise PublishError(f\"Failed to build USD publish path map: {exc}\") from exc\n\n    for instance in context:\n        if not instance.data.get(\"active\", True) or not instance.data.get(\n            \"publish\", True\n        ):\n            continue\n        families = set(instance.data.get(\"families\") or [])\n        if \"katana.usd\" not in families:\n            continue\n\n        try:\n            staged_path = get_extracted_usd_layer_path(instance.data)\n            source_stage = get_composed_source_stage(instance.data)\n            asset_remap = dict(instance.data.get(\"assetRemap\") or {})\n\n            if is_look_instance(instance.data):\n                from pxr import Sdf\n\n                layer = Sdf.Layer.FindOrOpen(staged_path.as_posix())\n                if layer is None:\n                    raise RuntimeError(\n                        f\"Failed to open staged USD look layer: {staged_path}\"\n                    )\n                resources_dir = instance.data.get(\"resourcesDir\")\n                if not resources_dir:\n                    raise RuntimeError(\"USD look instance has no resourcesDir.\")\n                plan = plan_look_resources(layer, resources_dir, source_stage)\n                self._merge_resource_plan(instance, plan)\n                asset_remap.update(plan[\"assetRemap\"])\n\n            rewrite_staged_usd_layer(\n                staged_path,\n                publish_mapping=publish_mapping,\n                asset_remap=asset_remap,\n                source_stage=source_stage,\n            )\n        except Exception as exc:\n            product_name = instance.data.get(\"productName\") or instance.name\n            raise PublishError(\n                f\"Failed to finalize USD product {product_name!r}: {exc}\"\n            ) from exc\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/increment_current_file.html","title":"increment_current_file","text":"<p>Optionally save the next workfile version after publishing.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/increment_current_file.html#client.ayon_katana.plugins.publish.increment_current_file.IncrementCurrentFile","title":"<code>IncrementCurrentFile</code>","text":"<p>               Bases: <code>KatanaContextPlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Save the next workfile version after a successful publish.</p> Source code in <code>client/ayon_katana/plugins/publish/increment_current_file.py</code> <pre><code>class IncrementCurrentFile(\n    plugin.KatanaContextPlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Save the next workfile version after a successful publish.\"\"\"\n\n    label = \"Increment current file\"\n    order = pyblish.api.IntegratorOrder + 9.0\n    optional = True\n\n    def process(self, context) -&gt; None:\n        \"\"\"Increment the published workfile once.\"\"\"\n        if not self.is_active(context.data):\n            return\n        if context.data.get(_INCREMENT_ATTEMPTED_KEY):\n            self.log.debug(\"Workfile increment already attempted for this publish.\")\n            return\n        if get_errored_plugins_from_context(context):\n            self.log.debug(\"Skipping workfile increment after publish failure.\")\n            return\n        if not self._has_active_workfile_instance(context):\n            self.log.debug(\"No workfile instance was published; skipping increment.\")\n            return\n\n        host = registered_host()\n        collected_path = context.data.get(\"currentFile\") or \"\"\n        current_path = host.get_current_workfile() or \"\"\n        try:\n            paths_match = workio.workfile_paths_match(collected_path, current_path)\n        except ValueError as exc:\n            raise PublishError(f\"Invalid workfile path: {exc}\") from exc\n        if not paths_match:\n            raise PublishError(\"Collected workfile differs from the active scene.\")\n\n        context.data[_INCREMENT_ATTEMPTED_KEY] = True\n        try:\n            from ayon_core.host.interfaces import SaveWorkfileOptionalData\n            from ayon_core.pipeline.workfile import save_next_version\n\n            prepared_data = SaveWorkfileOptionalData(\n                project_entity=context.data.get(\"projectEntity\"),\n                anatomy=context.data.get(\"anatomy\"),\n                project_settings=context.data.get(\"project_settings\"),\n            )\n            save_next_version(\n                description=(\n                    f\"Incremented by publishing from {os.path.basename(current_path)}\"\n                ),\n                prepared_data=prepared_data,\n            )\n        except Exception as exc:\n            raise PublishError(\n                \"Publishing completed, but saving the next workfile version \"\n                f\"failed: {exc}\"\n            ) from exc\n\n    @staticmethod\n    def _has_active_workfile_instance(context) -&gt; bool:\n        for instance in context:\n            data = instance.data\n            if not data.get(\"active\", True) or not data.get(\"publish\", True):\n                continue\n            if (\n                data.get(\"productBaseType\") == \"workfile\"\n                or data.get(\"productType\") == \"workfile\"\n            ):\n                return True\n            if \"workfile\" in set(data.get(\"families\") or []):\n                return True\n        return False\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/increment_current_file.html#client.ayon_katana.plugins.publish.increment_current_file.IncrementCurrentFile.process","title":"<code>process(context)</code>","text":"<p>Increment the published workfile once.</p> Source code in <code>client/ayon_katana/plugins/publish/increment_current_file.py</code> <pre><code>def process(self, context) -&gt; None:\n    \"\"\"Increment the published workfile once.\"\"\"\n    if not self.is_active(context.data):\n        return\n    if context.data.get(_INCREMENT_ATTEMPTED_KEY):\n        self.log.debug(\"Workfile increment already attempted for this publish.\")\n        return\n    if get_errored_plugins_from_context(context):\n        self.log.debug(\"Skipping workfile increment after publish failure.\")\n        return\n    if not self._has_active_workfile_instance(context):\n        self.log.debug(\"No workfile instance was published; skipping increment.\")\n        return\n\n    host = registered_host()\n    collected_path = context.data.get(\"currentFile\") or \"\"\n    current_path = host.get_current_workfile() or \"\"\n    try:\n        paths_match = workio.workfile_paths_match(collected_path, current_path)\n    except ValueError as exc:\n        raise PublishError(f\"Invalid workfile path: {exc}\") from exc\n    if not paths_match:\n        raise PublishError(\"Collected workfile differs from the active scene.\")\n\n    context.data[_INCREMENT_ATTEMPTED_KEY] = True\n    try:\n        from ayon_core.host.interfaces import SaveWorkfileOptionalData\n        from ayon_core.pipeline.workfile import save_next_version\n\n        prepared_data = SaveWorkfileOptionalData(\n            project_entity=context.data.get(\"projectEntity\"),\n            anatomy=context.data.get(\"anatomy\"),\n            project_settings=context.data.get(\"project_settings\"),\n        )\n        save_next_version(\n            description=(\n                f\"Incremented by publishing from {os.path.basename(current_path)}\"\n            ),\n            prepared_data=prepared_data,\n        )\n    except Exception as exc:\n        raise PublishError(\n            \"Publishing completed, but saving the next workfile version \"\n            f\"failed: {exc}\"\n        ) from exc\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/save_current_scene.html","title":"save_current_scene","text":"<p>Save a dirty Katana workfile without changing its active path.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/save_current_scene.html#client.ayon_katana.plugins.publish.save_current_scene.SaveCurrentScene","title":"<code>SaveCurrentScene</code>","text":"<p>               Bases: <code>KatanaContextPlugin</code></p> <p>Save unsaved scene edits before the workfile extractor stages the file.</p> Source code in <code>client/ayon_katana/plugins/publish/save_current_scene.py</code> <pre><code>class SaveCurrentScene(plugin.KatanaContextPlugin):\n    \"\"\"Save unsaved scene edits before the workfile extractor stages the file.\"\"\"\n\n    order = pyblish.api.ExtractorOrder - 0.49\n    label = \"Save Katana Current Scene\"\n\n    def process(self, context) -&gt; None:\n        \"\"\"Save only the collected current scene after checking its path.\"\"\"\n        host = registered_host()\n        try:\n            collected_path = workio.validate_workfile_path(\n                context.data.get(\"currentFile\", \"\"),\n                require_exists=True,\n            )\n            current_path = workio.validate_workfile_path(\n                host.get_current_workfile(),\n                require_exists=True,\n            )\n        except ValueError as exc:\n            raise PublishError(\n                f\"Katana workfile cannot be saved for publishing: {exc}\"\n            ) from exc\n\n        if not workio.workfile_paths_match(collected_path, current_path):\n            raise PublishError(\n                \"Collected Katana workfile path differs from the active scene; \"\n                \"reset publishing and try again without changing workfiles.\"\n            )\n\n        if host.workfile_has_unsaved_changes():\n            self.log.info(\"Saving current Katana workfile: %s\", current_path)\n            host.save_workfile(current_path)\n\n        try:\n            saved_path = workio.validate_workfile_path(\n                host.get_current_workfile(),\n                require_exists=True,\n            )\n        except ValueError as exc:\n            raise PublishError(\n                f\"Katana workfile save did not leave a valid scene path: {exc}\"\n            ) from exc\n\n        if not workio.workfile_paths_match(collected_path, saved_path):\n            raise PublishError(\n                \"Katana workfile path changed while saving; refusing to stage a \"\n                \"different scene.\"\n            )\n        if host.workfile_has_unsaved_changes():\n            raise PublishError(\n                \"Katana workfile is still dirty after saving; refusing to stage \"\n                \"stale data.\"\n            )\n\n        context.data[\"currentFile\"] = saved_path\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/save_current_scene.html#client.ayon_katana.plugins.publish.save_current_scene.SaveCurrentScene.process","title":"<code>process(context)</code>","text":"<p>Save only the collected current scene after checking its path.</p> Source code in <code>client/ayon_katana/plugins/publish/save_current_scene.py</code> <pre><code>def process(self, context) -&gt; None:\n    \"\"\"Save only the collected current scene after checking its path.\"\"\"\n    host = registered_host()\n    try:\n        collected_path = workio.validate_workfile_path(\n            context.data.get(\"currentFile\", \"\"),\n            require_exists=True,\n        )\n        current_path = workio.validate_workfile_path(\n            host.get_current_workfile(),\n            require_exists=True,\n        )\n    except ValueError as exc:\n        raise PublishError(\n            f\"Katana workfile cannot be saved for publishing: {exc}\"\n        ) from exc\n\n    if not workio.workfile_paths_match(collected_path, current_path):\n        raise PublishError(\n            \"Collected Katana workfile path differs from the active scene; \"\n            \"reset publishing and try again without changing workfiles.\"\n        )\n\n    if host.workfile_has_unsaved_changes():\n        self.log.info(\"Saving current Katana workfile: %s\", current_path)\n        host.save_workfile(current_path)\n\n    try:\n        saved_path = workio.validate_workfile_path(\n            host.get_current_workfile(),\n            require_exists=True,\n        )\n    except ValueError as exc:\n        raise PublishError(\n            f\"Katana workfile save did not leave a valid scene path: {exc}\"\n        ) from exc\n\n    if not workio.workfile_paths_match(collected_path, saved_path):\n        raise PublishError(\n            \"Katana workfile path changed while saving; refusing to stage a \"\n            \"different scene.\"\n        )\n    if host.workfile_has_unsaved_changes():\n        raise PublishError(\n            \"Katana workfile is still dirty after saving; refusing to stage \"\n            \"stale data.\"\n        )\n\n    context.data[\"currentFile\"] = saved_path\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_image.html","title":"validate_image","text":"<p>Validate ImageWrite outputs before publishing.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_image.html#client.ayon_katana.plugins.publish.validate_image.ValidateImage","title":"<code>ValidateImage</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate ImageWrite connectivity, output, range, and review colorspace.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_image.py</code> <pre><code>class ValidateImage(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate ImageWrite connectivity, output, range, and review colorspace.\"\"\"\n\n    label = \"Validate ImageWrite\"\n    order = pyblish.api.ValidatorOrder\n    families = [\"image\", \"katana.image\"]\n    optional = False\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Validate the node, connection, and output settings.\"\"\"\n        if not self.is_active(instance.data):\n            return\n        node_name = instance.data[\"instance_node\"]\n        image_write_node = compat.get_node(node_name)\n        if image_write_node is None:\n            raise PublishValidationError(\n                f\"Katana ImageWrite node does not exist: {node_name!r}\",\n                title=\"ImageWrite node missing\",\n            )\n        if image_write_node.getType() != \"ImageWrite\":\n            raise PublishValidationError(\n                \"Image publish instance is not an ImageWrite node.\",\n                title=\"ImageWrite node invalid\",\n            )\n        input_port = image_write_node.getInputPort(\"in\")\n        connected_ports = (\n            list(input_port.getConnectedPorts()) if input_port is not None else []\n        )\n        if len(connected_ports) != 1:\n            raise PublishValidationError(\n                \"ImageWrite requires exactly one source on its 'in' port.\",\n                title=\"ImageWrite source missing\",\n            )\n\n        settings = read_image_write_settings(image_write_node)\n        if not settings[\"output_path\"]:\n            raise PublishValidationError(\n                \"ImageWrite has no output path.\",\n                title=\"ImageWrite output missing\",\n            )\n        if settings[\"file_format\"] not in IMAGE_FILE_FORMATS:\n            raise PublishValidationError(\n                f\"Unsupported ImageWrite format: {settings['file_format']!r}.\",\n                title=\"ImageWrite format invalid\",\n            )\n        extension = Path(settings[\"output_path\"]).suffix.lower().lstrip(\".\")\n        if extension != settings[\"file_format\"]:\n            raise PublishValidationError(\n                \"ImageWrite path extension does not match its file format.\",\n                title=\"ImageWrite format mismatch\",\n            )\n\n        frame_start = int(instance.data[\"frameStartHandle\"])\n        frame_end = int(instance.data[\"frameEndHandle\"])\n        frame_step = int(instance.data[\"byFrameStep\"])\n        try:\n            expected_files = expand_image_output_pattern(\n                settings[\"output_path\"],\n                frame_start,\n                frame_end,\n                frame_step,\n            )\n        except ValueError as exc:\n            raise PublishValidationError(\n                str(exc),\n                title=\"ImageWrite frame pattern invalid\",\n            ) from exc\n        collected_files = (instance.data.get(\"expectedFiles\") or [{}])[0].get(\"\", [])\n        if expected_files != collected_files:\n            raise PublishValidationError(\n                \"ImageWrite expected files are stale; recollect the instance.\",\n                title=\"ImageWrite output stale\",\n            )\n\n        review = bool(\n            (instance.data.get(\"creator_attributes\") or {}).get(\"review\", False)\n        )\n        if review and not str(instance.data.get(\"imageOutputColorspace\") or \"\").strip():\n            raise PublishValidationError(\n                \"Review images require an explicit ImageWrite output colorspace.\",\n                title=\"Review colorspace missing\",\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_image.html#client.ayon_katana.plugins.publish.validate_image.ValidateImage.process","title":"<code>process(instance)</code>","text":"<p>Validate the node, connection, and output settings.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_image.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Validate the node, connection, and output settings.\"\"\"\n    if not self.is_active(instance.data):\n        return\n    node_name = instance.data[\"instance_node\"]\n    image_write_node = compat.get_node(node_name)\n    if image_write_node is None:\n        raise PublishValidationError(\n            f\"Katana ImageWrite node does not exist: {node_name!r}\",\n            title=\"ImageWrite node missing\",\n        )\n    if image_write_node.getType() != \"ImageWrite\":\n        raise PublishValidationError(\n            \"Image publish instance is not an ImageWrite node.\",\n            title=\"ImageWrite node invalid\",\n        )\n    input_port = image_write_node.getInputPort(\"in\")\n    connected_ports = (\n        list(input_port.getConnectedPorts()) if input_port is not None else []\n    )\n    if len(connected_ports) != 1:\n        raise PublishValidationError(\n            \"ImageWrite requires exactly one source on its 'in' port.\",\n            title=\"ImageWrite source missing\",\n        )\n\n    settings = read_image_write_settings(image_write_node)\n    if not settings[\"output_path\"]:\n        raise PublishValidationError(\n            \"ImageWrite has no output path.\",\n            title=\"ImageWrite output missing\",\n        )\n    if settings[\"file_format\"] not in IMAGE_FILE_FORMATS:\n        raise PublishValidationError(\n            f\"Unsupported ImageWrite format: {settings['file_format']!r}.\",\n            title=\"ImageWrite format invalid\",\n        )\n    extension = Path(settings[\"output_path\"]).suffix.lower().lstrip(\".\")\n    if extension != settings[\"file_format\"]:\n        raise PublishValidationError(\n            \"ImageWrite path extension does not match its file format.\",\n            title=\"ImageWrite format mismatch\",\n        )\n\n    frame_start = int(instance.data[\"frameStartHandle\"])\n    frame_end = int(instance.data[\"frameEndHandle\"])\n    frame_step = int(instance.data[\"byFrameStep\"])\n    try:\n        expected_files = expand_image_output_pattern(\n            settings[\"output_path\"],\n            frame_start,\n            frame_end,\n            frame_step,\n        )\n    except ValueError as exc:\n        raise PublishValidationError(\n            str(exc),\n            title=\"ImageWrite frame pattern invalid\",\n        ) from exc\n    collected_files = (instance.data.get(\"expectedFiles\") or [{}])[0].get(\"\", [])\n    if expected_files != collected_files:\n        raise PublishValidationError(\n            \"ImageWrite expected files are stale; recollect the instance.\",\n            title=\"ImageWrite output stale\",\n        )\n\n    review = bool(\n        (instance.data.get(\"creator_attributes\") or {}).get(\"review\", False)\n    )\n    if review and not str(instance.data.get(\"imageOutputColorspace\") or \"\").strip():\n        raise PublishValidationError(\n            \"Review images require an explicit ImageWrite output colorspace.\",\n            title=\"Review colorspace missing\",\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_instance_in_context.html","title":"validate_instance_in_context","text":"<p>Validate that persisted Katana instances match the publish context.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_instance_in_context.html#client.ayon_katana.plugins.publish.validate_instance_in_context.ValidateInstanceInContextKatana","title":"<code>ValidateInstanceInContextKatana</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate that an instance publishes to the current folder and task.</p> <p>Runtime instances that spawn from a persisted creator instance are skipped because their source instance is validated instead.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_instance_in_context.py</code> <pre><code>class ValidateInstanceInContextKatana(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate that an instance publishes to the current folder and task.\n\n    Runtime instances that spawn from a persisted creator instance are skipped\n    because their source\n    instance is validated instead.\n    \"\"\"\n\n    order = ValidateContentsOrder\n    label = \"Instance in Same Context\"\n    optional = True\n    actions = [SelectInvalidInstanceNodes, RepairAction]\n\n    def process(self, instance):\n        \"\"\"Reject persisted instances outside the active publish context.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        attr_values = self.get_attr_values_from_data(instance.data)\n        if not attr_values and not instance.data.get(\"instance_node\"):\n            return\n\n        folder_path = instance.data.get(\"folderPath\")\n        task = instance.data.get(\"task\")\n        context = self.get_context(instance)\n        if (folder_path, task) != context:\n            context_label = f\"{context[0]} &gt; {context[1]}\"\n            instance_label = f\"{folder_path} &gt; {task}\"\n\n            raise PublishValidationError(\n                message=(\n                    f\"Instance {instance.name!r} publishes to different asset \"\n                    f\"than current context: {instance_label}. Current context: \"\n                    f\"{context_label}\"\n                ),\n                description=(\n                    \"## Publishing to a different asset\\n\"\n                    \"There are publish instances present which are publishing \"\n                    \"into a different asset than your current context.\\n\\n\"\n                    \"Usually this is not what you want but there can be cases \"\n                    \"where you might want to publish into another asset or \"\n                    \"shot. If that's the case you can disable the validation \"\n                    \"on the instance to ignore it.\"\n                ),\n            )\n\n    @classmethod\n    def repair(cls, instance):\n        \"\"\"Move persisted instance metadata to the active publish context.\"\"\"\n        context_folder, context_task = cls.get_context(instance)\n\n        create_context = instance.context.data[\"create_context\"]\n        instance_id = instance.data[\"instance_id\"]\n        created_instance = create_context.get_instance_by_id(instance_id)\n        created_instance[\"folderPath\"] = context_folder\n        created_instance[\"task\"] = context_task\n        create_context.save_changes()\n\n    @staticmethod\n    def get_context(instance):\n        \"\"\"Return the current publish folder path and task name.\"\"\"\n        context = instance.context\n        return context.data[\"folderPath\"], context.data[\"task\"]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_instance_in_context.html#client.ayon_katana.plugins.publish.validate_instance_in_context.ValidateInstanceInContextKatana.get_context","title":"<code>get_context(instance)</code>  <code>staticmethod</code>","text":"<p>Return the current publish folder path and task name.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_instance_in_context.py</code> <pre><code>@staticmethod\ndef get_context(instance):\n    \"\"\"Return the current publish folder path and task name.\"\"\"\n    context = instance.context\n    return context.data[\"folderPath\"], context.data[\"task\"]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_instance_in_context.html#client.ayon_katana.plugins.publish.validate_instance_in_context.ValidateInstanceInContextKatana.process","title":"<code>process(instance)</code>","text":"<p>Reject persisted instances outside the active publish context.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_instance_in_context.py</code> <pre><code>def process(self, instance):\n    \"\"\"Reject persisted instances outside the active publish context.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    attr_values = self.get_attr_values_from_data(instance.data)\n    if not attr_values and not instance.data.get(\"instance_node\"):\n        return\n\n    folder_path = instance.data.get(\"folderPath\")\n    task = instance.data.get(\"task\")\n    context = self.get_context(instance)\n    if (folder_path, task) != context:\n        context_label = f\"{context[0]} &gt; {context[1]}\"\n        instance_label = f\"{folder_path} &gt; {task}\"\n\n        raise PublishValidationError(\n            message=(\n                f\"Instance {instance.name!r} publishes to different asset \"\n                f\"than current context: {instance_label}. Current context: \"\n                f\"{context_label}\"\n            ),\n            description=(\n                \"## Publishing to a different asset\\n\"\n                \"There are publish instances present which are publishing \"\n                \"into a different asset than your current context.\\n\\n\"\n                \"Usually this is not what you want but there can be cases \"\n                \"where you might want to publish into another asset or \"\n                \"shot. If that's the case you can disable the validation \"\n                \"on the instance to ignore it.\"\n            ),\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_instance_in_context.html#client.ayon_katana.plugins.publish.validate_instance_in_context.ValidateInstanceInContextKatana.repair","title":"<code>repair(instance)</code>  <code>classmethod</code>","text":"<p>Move persisted instance metadata to the active publish context.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_instance_in_context.py</code> <pre><code>@classmethod\ndef repair(cls, instance):\n    \"\"\"Move persisted instance metadata to the active publish context.\"\"\"\n    context_folder, context_task = cls.get_context(instance)\n\n    create_context = instance.context.data[\"create_context\"]\n    instance_id = instance.data[\"instance_id\"]\n    created_instance = create_context.get_instance_by_id(instance_id)\n    created_instance[\"folderPath\"] = context_folder\n    created_instance[\"task\"] = context_task\n    create_context.save_changes()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_nodegraph.html","title":"validate_nodegraph","text":"<p>Validate Katana node graph publish instances.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_nodegraph.html#client.ayon_katana.plugins.publish.validate_nodegraph.ValidateNodegraph","title":"<code>ValidateNodegraph</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate the selected source Group.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_nodegraph.py</code> <pre><code>class ValidateNodegraph(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate the selected source Group.\"\"\"\n\n    label = \"Validate Node Graph\"\n    order = pyblish.api.ValidatorOrder\n    families = [\"nodegraph\"]\n    optional = True\n\n    def process(self, instance):\n        \"\"\"Validate the source Group.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        source_name = instance.data.get(\"nodegraph_node\")\n        if not source_name:\n            raise PublishValidationError(\n                \"Node graph instance has no selected Group identifier.\",\n                title=\"Node graph source missing\",\n            )\n        source_node = compat.get_node(source_name)\n        if source_node is None:\n            raise PublishValidationError(\n                f\"Node graph source no longer exists: {source_name}\",\n                title=\"Node graph source missing\",\n            )\n        if source_node.getType() != \"Group\":\n            raise PublishValidationError(\n                \"Node graph source must remain a Group node.\",\n                title=\"Node graph source invalid\",\n            )\n        if containers.parse_container(source_node) is not None:\n            raise PublishValidationError(\n                \"Node graph source cannot be an outer AYON container.\",\n                title=\"Node graph source invalid\",\n            )\n\n        instance_name = instance.data.get(\"instance_node\")\n        instance_node = compat.get_node(instance_name) if instance_name else None\n        if instance_node is not None and (\n            source_node is instance_node\n            or compat.is_descendant(instance_node, source_node)\n        ):\n            raise PublishValidationError(\n                \"Node graph source contains its publishing instance.\",\n                title=\"Node graph source recursive\",\n            )\n\n        if not compat.get_output_ports(source_node):\n            raise PublishValidationError(\n                \"Node graph source must expose at least one output port.\",\n                title=\"Node graph output missing\",\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_nodegraph.html#client.ayon_katana.plugins.publish.validate_nodegraph.ValidateNodegraph.process","title":"<code>process(instance)</code>","text":"<p>Validate the source Group.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_nodegraph.py</code> <pre><code>def process(self, instance):\n    \"\"\"Validate the source Group.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    source_name = instance.data.get(\"nodegraph_node\")\n    if not source_name:\n        raise PublishValidationError(\n            \"Node graph instance has no selected Group identifier.\",\n            title=\"Node graph source missing\",\n        )\n    source_node = compat.get_node(source_name)\n    if source_node is None:\n        raise PublishValidationError(\n            f\"Node graph source no longer exists: {source_name}\",\n            title=\"Node graph source missing\",\n        )\n    if source_node.getType() != \"Group\":\n        raise PublishValidationError(\n            \"Node graph source must remain a Group node.\",\n            title=\"Node graph source invalid\",\n        )\n    if containers.parse_container(source_node) is not None:\n        raise PublishValidationError(\n            \"Node graph source cannot be an outer AYON container.\",\n            title=\"Node graph source invalid\",\n        )\n\n    instance_name = instance.data.get(\"instance_node\")\n    instance_node = compat.get_node(instance_name) if instance_name else None\n    if instance_node is not None and (\n        source_node is instance_node\n        or compat.is_descendant(instance_node, source_node)\n    ):\n        raise PublishValidationError(\n            \"Node graph source contains its publishing instance.\",\n            title=\"Node graph source recursive\",\n        )\n\n    if not compat.get_output_ports(source_node):\n        raise PublishValidationError(\n            \"Node graph source must expose at least one output port.\",\n            title=\"Node graph output missing\",\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render.html","title":"validate_render","text":"<p>Validate render instances before local or farm execution.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render.html#client.ayon_katana.plugins.publish.validate_render.ValidateRender","title":"<code>ValidateRender</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate the render graph and enabled outputs.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render.py</code> <pre><code>class ValidateRender(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate the render graph and enabled outputs.\"\"\"\n\n    label = \"Validate Render\"\n    order = pyblish.api.ValidatorOrder\n    families = [\"render\", \"katana.render\"]\n    actions = [SelectInvalidInstanceNodes, ReconnectAYONRenderGraphAction]\n    optional = True\n\n    def process(self, instance):\n        \"\"\"Validate the instance graph and renderer.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        instance_node_name = instance.data.get(\"instance_node\")\n        instance_node = compat.get_node(instance_node_name)\n        if instance_node is None:\n            raise PublishValidationError(\n                f\"Katana render instance node was not found: {instance_node_name}\"\n            )\n        if render.get_render_node(instance_node) is None:\n            raise PublishValidationError(\n                f\"Render instance {instance_node_name!r} has no Render node.\"\n            )\n        if render.get_settings_node(instance_node) is None:\n            raise PublishValidationError(\n                f\"Render instance {instance_node_name!r} has no RenderSettings node.\"\n            )\n\n        renderer_name = instance.data.get(\"renderer\") or \"\"\n        if not renderer_name:\n            raise PublishValidationError(\"RenderSettings has no renderer.\")\n        render_target = (instance.data.get(\"creator_attributes\") or {}).get(\n            \"render_target\",\n            \"farm\",\n        )\n        if render_target != \"local_no_render\" and not render.is_renderer_registered(\n            renderer_name\n        ):\n            raise PublishValidationError(\n                f\"Renderer {renderer_name!r} is not registered in this process.\"\n            )\n\n        output_definitions = render.get_output_definitions(instance_node)\n        if not output_definitions:\n            raise PublishValidationError(\"Render instance has no enabled outputs.\")\n\n        for output_definition in output_definitions:\n            output_node = compat.get_node(output_definition.node_name)\n            input_port = output_node.getInputPort(\"input\") if output_node else None\n            if input_port is None or not input_port.getConnectedPorts():\n                raise PublishValidationError(\n                    f\"Render output {output_definition.node_name!r} is disconnected.\"\n                )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render.html#client.ayon_katana.plugins.publish.validate_render.ValidateRender.process","title":"<code>process(instance)</code>","text":"<p>Validate the instance graph and renderer.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render.py</code> <pre><code>def process(self, instance):\n    \"\"\"Validate the instance graph and renderer.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    instance_node_name = instance.data.get(\"instance_node\")\n    instance_node = compat.get_node(instance_node_name)\n    if instance_node is None:\n        raise PublishValidationError(\n            f\"Katana render instance node was not found: {instance_node_name}\"\n        )\n    if render.get_render_node(instance_node) is None:\n        raise PublishValidationError(\n            f\"Render instance {instance_node_name!r} has no Render node.\"\n        )\n    if render.get_settings_node(instance_node) is None:\n        raise PublishValidationError(\n            f\"Render instance {instance_node_name!r} has no RenderSettings node.\"\n        )\n\n    renderer_name = instance.data.get(\"renderer\") or \"\"\n    if not renderer_name:\n        raise PublishValidationError(\"RenderSettings has no renderer.\")\n    render_target = (instance.data.get(\"creator_attributes\") or {}).get(\n        \"render_target\",\n        \"farm\",\n    )\n    if render_target != \"local_no_render\" and not render.is_renderer_registered(\n        renderer_name\n    ):\n        raise PublishValidationError(\n            f\"Renderer {renderer_name!r} is not registered in this process.\"\n        )\n\n    output_definitions = render.get_output_definitions(instance_node)\n    if not output_definitions:\n        raise PublishValidationError(\"Render instance has no enabled outputs.\")\n\n    for output_definition in output_definitions:\n        output_node = compat.get_node(output_definition.node_name)\n        input_port = output_node.getInputPort(\"input\") if output_node else None\n        if input_port is None or not input_port.getConnectedPorts():\n            raise PublishValidationError(\n                f\"Render output {output_definition.node_name!r} is disconnected.\"\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_camera.html","title":"validate_render_camera","text":"<p>Validate the native Katana render camera location.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_camera.html#client.ayon_katana.plugins.publish.validate_render_camera.ValidateRenderCamera","title":"<code>ValidateRenderCamera</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate the Render node camera setting.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_camera.py</code> <pre><code>class ValidateRenderCamera(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate the Render node camera setting.\"\"\"\n\n    label = \"Validate Render Camera\"\n    order = pyblish.api.ValidatorOrder - 0.1\n    families = [\"render\", \"katana.render\"]\n    optional = True\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Reject render instances without a valid camera location.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        camera_path = str(instance.data.get(\"camera\") or \"\").strip()\n        if not camera_path:\n            raise PublishValidationError(\n                \"Katana RenderSettings has no camera location.\"\n            )\n        render_node = compat.get_node(instance.data.get(\"render_node\"))\n        if render_node is None:\n            raise PublishValidationError(\n                \"Katana Render node is unavailable for camera cook.\"\n            )\n        location_type = render.get_scenegraph_location_type(\n            render_node,\n            camera_path,\n        )\n        if not location_type:\n            raise PublishValidationError(\n                f\"Katana camera location could not be resolved: {camera_path}\"\n            )\n        if location_type.casefold() != \"camera\":\n            raise PublishValidationError(\n                f\"Katana camera location is not a camera: {camera_path}\"\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_camera.html#client.ayon_katana.plugins.publish.validate_render_camera.ValidateRenderCamera.process","title":"<code>process(instance)</code>","text":"<p>Reject render instances without a valid camera location.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_camera.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Reject render instances without a valid camera location.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    camera_path = str(instance.data.get(\"camera\") or \"\").strip()\n    if not camera_path:\n        raise PublishValidationError(\n            \"Katana RenderSettings has no camera location.\"\n        )\n    render_node = compat.get_node(instance.data.get(\"render_node\"))\n    if render_node is None:\n        raise PublishValidationError(\n            \"Katana Render node is unavailable for camera cook.\"\n        )\n    location_type = render.get_scenegraph_location_type(\n        render_node,\n        camera_path,\n    )\n    if not location_type:\n        raise PublishValidationError(\n            f\"Katana camera location could not be resolved: {camera_path}\"\n        )\n    if location_type.casefold() != \"camera\":\n        raise PublishValidationError(\n            f\"Katana camera location is not a camera: {camera_path}\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_colorspace.html","title":"validate_render_colorspace","text":"<p>Validate colorspace metadata collected for Katana render products.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_colorspace.html#client.ayon_katana.plugins.publish.validate_render_colorspace.ValidateRenderColorspace","title":"<code>ValidateRenderColorspace</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate render colorspace settings.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_colorspace.py</code> <pre><code>class ValidateRenderColorspace(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate render colorspace settings.\"\"\"\n\n    label = \"Validate Render Colorspace\"\n    order = pyblish.api.ValidatorOrder + 0.02\n    families = [\"render\", \"katana.render\"]\n    optional = True\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Reject review outputs without a declared colorspace.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        config_path = str(instance.data.get(\"colorspaceConfig\") or \"\")\n        colorspace = str(instance.data.get(\"colorspace\") or \"\")\n        if not config_path or not Path(config_path).is_file() or not colorspace:\n            raise PublishValidationError(\n                \"Katana has no valid active OCIO config with a scene-linear role.\"\n            )\n        render_products = instance.data.get(\"renderProducts\")\n        if render_products is None:\n            raise PublishValidationError(\n                \"Katana render products have no colorspace data.\"\n            )\n        products = render_products.layer_data.products\n        if not products:\n            raise PublishValidationError(\n                \"Katana render products have no colorspace data.\"\n            )\n        if any(product.colorspace != colorspace for product in products):\n            raise PublishValidationError(\n                \"Katana render products do not share the scene-linear colorspace.\"\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_colorspace.html#client.ayon_katana.plugins.publish.validate_render_colorspace.ValidateRenderColorspace.process","title":"<code>process(instance)</code>","text":"<p>Reject review outputs without a declared colorspace.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_colorspace.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Reject review outputs without a declared colorspace.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    config_path = str(instance.data.get(\"colorspaceConfig\") or \"\")\n    colorspace = str(instance.data.get(\"colorspace\") or \"\")\n    if not config_path or not Path(config_path).is_file() or not colorspace:\n        raise PublishValidationError(\n            \"Katana has no valid active OCIO config with a scene-linear role.\"\n        )\n    render_products = instance.data.get(\"renderProducts\")\n    if render_products is None:\n        raise PublishValidationError(\n            \"Katana render products have no colorspace data.\"\n        )\n    products = render_products.layer_data.products\n    if not products:\n        raise PublishValidationError(\n            \"Katana render products have no colorspace data.\"\n        )\n    if any(product.colorspace != colorspace for product in products):\n        raise PublishValidationError(\n            \"Katana render products do not share the scene-linear colorspace.\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_frame_range.html","title":"validate_render_frame_range","text":"<p>Validate Katana render frames after applying task handles.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_frame_range.html#client.ayon_katana.plugins.publish.validate_render_frame_range.DisableUseTaskHandlesAction","title":"<code>DisableUseTaskHandlesAction</code>","text":"<p>               Bases: <code>RepairAction</code></p> <p>Disable task handles on a render creator instance.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_frame_range.py</code> <pre><code>class DisableUseTaskHandlesAction(RepairAction):\n    \"\"\"Disable task handles on a render creator instance.\"\"\"\n\n    label = \"Disable use task handles\"\n    icon = \"mdi.toggle-switch-off\"\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_frame_range.html#client.ayon_katana.plugins.publish.validate_render_frame_range.ValidateFrameRange","title":"<code>ValidateFrameRange</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Validate Frame Range.</p> <p>Due to the usage of start and end handles, the cut frame range must remain valid after handles are applied to the native Katana render range.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_frame_range.py</code> <pre><code>class ValidateFrameRange(plugin.KatanaInstancePlugin):\n    \"\"\"Validate Frame Range.\n\n    Due to the usage of start and end handles, the cut frame range must remain\n    valid after handles are applied to the native Katana render range.\n    \"\"\"\n\n    label = \"Validate Frame Range\"\n    order = pyblish.api.ValidatorOrder - 0.1\n    families = [\"render\", \"katana.render\"]\n    actions = [DisableUseTaskHandlesAction, SelectInvalidInstanceNodes]\n\n    def process(self, instance):\n        \"\"\"Reject native or handled frame ranges that cannot be rendered.\"\"\"\n        frame_start_handle = int(instance.data[\"frameStartHandle\"])\n        frame_end_handle = int(instance.data[\"frameEndHandle\"])\n        frame_step = int(instance.data[\"byFrameStep\"])\n        if frame_end_handle &lt; frame_start_handle:\n            raise PublishValidationError(\n                \"Katana native frame end is before its native frame start.\"\n            )\n        if frame_step &lt; 1:\n            raise PublishValidationError(\"Katana frame step must be at least 1.\")\n        if self.get_invalid(instance):\n            raise PublishValidationError(\n                title=\"Invalid Frame Range\",\n                message=(\n                    \"Invalid frame range because the instance start frame \"\n                    f\"({instance.data['frameStart']}) is higher than the end \"\n                    f\"frame ({instance.data['frameEnd']}).\"\n                ),\n                description=(\n                    \"## Invalid Frame Range\\n\"\n                    \"The frame range is invalid after task handles were applied. \"\n                    \"Disable task handles or extend the native Katana Render range.\"\n                ),\n            )\n\n    @classmethod\n    def get_invalid(cls, instance):\n        \"\"\"Return the instance when its handled frame range is reversed.\"\"\"\n        frame_start = instance.data.get(\"frameStart\")\n        frame_end = instance.data.get(\"frameEnd\")\n        if frame_start is None or frame_end is None:\n            return []\n        if frame_start &gt; frame_end:\n            return [instance]\n        return []\n\n    @classmethod\n    def repair(cls, instance):\n        \"\"\"Disable task handles on an invalid creator instance.\"\"\"\n        if not cls.get_invalid(instance):\n            return\n\n        context = instance.context\n        create_context = context.data.get(\"create_context\")\n        if create_context is None:\n            cls.log.warning(\n                \"Create context is unavailable; task handles were not changed.\"\n            )\n            return\n\n        instance_id = instance.data.get(\"instance_id\")\n        if not instance_id:\n            cls.log.warning(\"Publish instance has no creator instance id.\")\n            return\n\n        created_instance = create_context.get_instance_by_id(instance_id)\n        if created_instance is None:\n            cls.log.warning(\"Creator instance %s was not found.\", instance_id)\n            return\n\n        created_instance.publish_attributes[\"CollectAssetHandles\"][\"use_handles\"] = (\n            False\n        )\n        create_context.save_changes()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_frame_range.html#client.ayon_katana.plugins.publish.validate_render_frame_range.ValidateFrameRange.get_invalid","title":"<code>get_invalid(instance)</code>  <code>classmethod</code>","text":"<p>Return the instance when its handled frame range is reversed.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_frame_range.py</code> <pre><code>@classmethod\ndef get_invalid(cls, instance):\n    \"\"\"Return the instance when its handled frame range is reversed.\"\"\"\n    frame_start = instance.data.get(\"frameStart\")\n    frame_end = instance.data.get(\"frameEnd\")\n    if frame_start is None or frame_end is None:\n        return []\n    if frame_start &gt; frame_end:\n        return [instance]\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_frame_range.html#client.ayon_katana.plugins.publish.validate_render_frame_range.ValidateFrameRange.process","title":"<code>process(instance)</code>","text":"<p>Reject native or handled frame ranges that cannot be rendered.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_frame_range.py</code> <pre><code>def process(self, instance):\n    \"\"\"Reject native or handled frame ranges that cannot be rendered.\"\"\"\n    frame_start_handle = int(instance.data[\"frameStartHandle\"])\n    frame_end_handle = int(instance.data[\"frameEndHandle\"])\n    frame_step = int(instance.data[\"byFrameStep\"])\n    if frame_end_handle &lt; frame_start_handle:\n        raise PublishValidationError(\n            \"Katana native frame end is before its native frame start.\"\n        )\n    if frame_step &lt; 1:\n        raise PublishValidationError(\"Katana frame step must be at least 1.\")\n    if self.get_invalid(instance):\n        raise PublishValidationError(\n            title=\"Invalid Frame Range\",\n            message=(\n                \"Invalid frame range because the instance start frame \"\n                f\"({instance.data['frameStart']}) is higher than the end \"\n                f\"frame ({instance.data['frameEnd']}).\"\n            ),\n            description=(\n                \"## Invalid Frame Range\\n\"\n                \"The frame range is invalid after task handles were applied. \"\n                \"Disable task handles or extend the native Katana Render range.\"\n            ),\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_frame_range.html#client.ayon_katana.plugins.publish.validate_render_frame_range.ValidateFrameRange.repair","title":"<code>repair(instance)</code>  <code>classmethod</code>","text":"<p>Disable task handles on an invalid creator instance.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_frame_range.py</code> <pre><code>@classmethod\ndef repair(cls, instance):\n    \"\"\"Disable task handles on an invalid creator instance.\"\"\"\n    if not cls.get_invalid(instance):\n        return\n\n    context = instance.context\n    create_context = context.data.get(\"create_context\")\n    if create_context is None:\n        cls.log.warning(\n            \"Create context is unavailable; task handles were not changed.\"\n        )\n        return\n\n    instance_id = instance.data.get(\"instance_id\")\n    if not instance_id:\n        cls.log.warning(\"Publish instance has no creator instance id.\")\n        return\n\n    created_instance = create_context.get_instance_by_id(instance_id)\n    if created_instance is None:\n        cls.log.warning(\"Creator instance %s was not found.\", instance_id)\n        return\n\n    created_instance.publish_attributes[\"CollectAssetHandles\"][\"use_handles\"] = (\n        False\n    )\n    create_context.save_changes()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_extensions.html","title":"validate_render_output_extensions","text":"<p>Validate render output file extensions.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_extensions.html#client.ayon_katana.plugins.publish.validate_render_output_extensions.ValidateRenderOutputExtensions","title":"<code>ValidateRenderOutputExtensions</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate output paths match their native file extensions.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_output_extensions.py</code> <pre><code>class ValidateRenderOutputExtensions(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate output paths match their native file extensions.\"\"\"\n\n    order = pyblish.api.ValidatorOrder + 0.03\n    label = \"Validate Render Output Extensions\"\n    families = [\"render\", \"katana.render\"]\n    actions = [SelectInvalidOutputNodes]\n    optional = True\n\n    def process(self, instance):\n        \"\"\"Reject missing or mismatched render output extensions.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        invalid = self._get_invalid_data(instance)\n        if not invalid:\n            return\n\n        details = \"\\n\".join(f\"- {node.getName()}: {reason}\" for node, reason in invalid)\n        raise PublishValidationError(\n            f\"Katana render output extensions are invalid:\\n{details}\",\n            title=self.label,\n        )\n\n    @classmethod\n    def get_invalid(cls, instance):\n        \"\"\"Return output nodes with missing or mismatched extensions.\"\"\"\n        return [node for node, _reason in cls._get_invalid_data(instance)]\n\n    @classmethod\n    def _get_invalid_data(cls, instance):\n        instance_node = compat.get_node(instance.data.get(\"instance_node\"))\n        if instance_node is None:\n            raise PublishValidationError(\"Katana render instance node is unavailable.\")\n\n        invalid = []\n        for definition in render.get_output_definitions(instance_node):\n            if not definition.path:\n                continue\n            output_node = compat.get_node(definition.node_name)\n            if output_node is None:\n                continue\n\n            path_extension = Path(definition.path).suffix.lstrip(\".\")\n            if not path_extension or set(path_extension) == {\"#\"}:\n                invalid.append(\n                    (\n                        output_node,\n                        f\"path {definition.path!r} has no file extension\",\n                    )\n                )\n                continue\n\n            expected_extension = definition.extension.lstrip(\".\")\n            if (\n                expected_extension\n                and path_extension.casefold() != expected_extension.casefold()\n            ):\n                invalid.append(\n                    (\n                        output_node,\n                        f\"path {definition.path!r} uses .{path_extension}; \"\n                        f\"native output requires .{expected_extension}\",\n                    )\n                )\n        return invalid\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_extensions.html#client.ayon_katana.plugins.publish.validate_render_output_extensions.ValidateRenderOutputExtensions.get_invalid","title":"<code>get_invalid(instance)</code>  <code>classmethod</code>","text":"<p>Return output nodes with missing or mismatched extensions.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_output_extensions.py</code> <pre><code>@classmethod\ndef get_invalid(cls, instance):\n    \"\"\"Return output nodes with missing or mismatched extensions.\"\"\"\n    return [node for node, _reason in cls._get_invalid_data(instance)]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_extensions.html#client.ayon_katana.plugins.publish.validate_render_output_extensions.ValidateRenderOutputExtensions.process","title":"<code>process(instance)</code>","text":"<p>Reject missing or mismatched render output extensions.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_output_extensions.py</code> <pre><code>def process(self, instance):\n    \"\"\"Reject missing or mismatched render output extensions.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    invalid = self._get_invalid_data(instance)\n    if not invalid:\n        return\n\n    details = \"\\n\".join(f\"- {node.getName()}: {reason}\" for node, reason in invalid)\n    raise PublishValidationError(\n        f\"Katana render output extensions are invalid:\\n{details}\",\n        title=self.label,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_names.html","title":"validate_render_output_names","text":"<p>Validate render output names.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_names.html#client.ayon_katana.plugins.publish.validate_render_output_names.ValidateRenderOutputNames","title":"<code>ValidateRenderOutputNames</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate output names and AOV identifiers are present and unique.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_output_names.py</code> <pre><code>class ValidateRenderOutputNames(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate output names and AOV identifiers are present and unique.\"\"\"\n\n    order = pyblish.api.ValidatorOrder + 0.01\n    label = \"Validate Render Output Names\"\n    families = [\"render\", \"katana.render\"]\n    actions = [SelectInvalidOutputNodes]\n    optional = True\n\n    def process(self, instance):\n        \"\"\"Reject missing, invalid, or duplicate render output names.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        invalid = self._get_invalid_data(instance)\n        if not invalid:\n            return\n\n        details = \"\\n\".join(f\"- {node.getName()}: {reason}\" for node, reason in invalid)\n        raise PublishValidationError(\n            f\"Katana render output names are invalid:\\n{details}\",\n            title=self.label,\n        )\n\n    @classmethod\n    def get_invalid(cls, instance):\n        \"\"\"Return output nodes with invalid names or AOV identifiers.\"\"\"\n        return cls._unique_nodes(\n            node for node, _reason in cls._get_invalid_data(instance)\n        )\n\n    @classmethod\n    def _get_invalid_data(cls, instance):\n        instance_node = compat.get_node(instance.data.get(\"instance_node\"))\n        if instance_node is None:\n            raise PublishValidationError(\"Katana render instance node is unavailable.\")\n\n        definitions = render.get_output_definitions(instance_node)\n        definitions_by_name = defaultdict(list)\n        definitions_by_aov = defaultdict(list)\n        invalid = []\n        for definition in definitions:\n            output_node = compat.get_node(definition.node_name)\n            if output_node is None:\n                continue\n            if not definition.name:\n                invalid.append((output_node, \"output name is empty\"))\n                continue\n            definitions_by_name[definition.name].append((definition, output_node))\n            definitions_by_aov[definition.aov_identifier].append(\n                (definition, output_node)\n            )\n\n        for name, items in definitions_by_name.items():\n            if len(items) &lt; 2:\n                continue\n            for _definition, output_node in items:\n                invalid.append((output_node, f\"output name {name!r} is duplicated\"))\n\n        for aov_identifier, items in definitions_by_aov.items():\n            if len(items) &lt; 2:\n                continue\n            label = aov_identifier or \"main\"\n            for _definition, output_node in items:\n                invalid.append(\n                    (output_node, f\"multiple outputs resolve to AOV {label!r}\")\n                )\n        return invalid\n\n    @staticmethod\n    def _unique_nodes(nodes):\n        output = []\n        seen = set()\n        for node in nodes:\n            node_name = node.getName()\n            if node_name in seen:\n                continue\n            seen.add(node_name)\n            output.append(node)\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_names.html#client.ayon_katana.plugins.publish.validate_render_output_names.ValidateRenderOutputNames.get_invalid","title":"<code>get_invalid(instance)</code>  <code>classmethod</code>","text":"<p>Return output nodes with invalid names or AOV identifiers.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_output_names.py</code> <pre><code>@classmethod\ndef get_invalid(cls, instance):\n    \"\"\"Return output nodes with invalid names or AOV identifiers.\"\"\"\n    return cls._unique_nodes(\n        node for node, _reason in cls._get_invalid_data(instance)\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_names.html#client.ayon_katana.plugins.publish.validate_render_output_names.ValidateRenderOutputNames.process","title":"<code>process(instance)</code>","text":"<p>Reject missing, invalid, or duplicate render output names.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_output_names.py</code> <pre><code>def process(self, instance):\n    \"\"\"Reject missing, invalid, or duplicate render output names.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    invalid = self._get_invalid_data(instance)\n    if not invalid:\n        return\n\n    details = \"\\n\".join(f\"- {node.getName()}: {reason}\" for node, reason in invalid)\n    raise PublishValidationError(\n        f\"Katana render output names are invalid:\\n{details}\",\n        title=self.label,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_paths.html","title":"validate_render_output_paths","text":"<p>Validate render output paths.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_paths.html#client.ayon_katana.plugins.publish.validate_render_output_paths.ValidateRenderOutputPaths","title":"<code>ValidateRenderOutputPaths</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate output paths are present and unique within one render graph.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_output_paths.py</code> <pre><code>class ValidateRenderOutputPaths(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate output paths are present and unique within one render graph.\"\"\"\n\n    order = pyblish.api.ValidatorOrder + 0.02\n    label = \"Validate Render Output Paths\"\n    families = [\"render\", \"katana.render\"]\n    actions = [SelectInvalidOutputNodes]\n    optional = True\n\n    def process(self, instance):\n        \"\"\"Reject empty or duplicate paths within a render instance.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        invalid = self._get_invalid_data(instance)\n        if not invalid:\n            return\n\n        details = \"\\n\".join(f\"- {node.getName()}: {reason}\" for node, reason in invalid)\n        raise PublishValidationError(\n            f\"Katana render output paths are invalid:\\n{details}\",\n            title=self.label,\n        )\n\n    @classmethod\n    def get_invalid(cls, instance):\n        \"\"\"Return output nodes with empty or colliding paths.\"\"\"\n        output = []\n        seen = set()\n        for node, _reason in cls._get_invalid_data(instance):\n            node_name = node.getName()\n            if node_name in seen:\n                continue\n            seen.add(node_name)\n            output.append(node)\n        return output\n\n    @classmethod\n    def _get_invalid_data(cls, instance):\n        instance_node = compat.get_node(instance.data.get(\"instance_node\"))\n        if instance_node is None:\n            raise PublishValidationError(\"Katana render instance node is unavailable.\")\n\n        definitions_by_path = defaultdict(list)\n        invalid = []\n        for definition in render.get_output_definitions(instance_node):\n            output_node = compat.get_node(definition.node_name)\n            if output_node is None:\n                continue\n            if not definition.path:\n                invalid.append((output_node, \"output path is empty\"))\n                continue\n            normalized_path = os.path.normcase(os.path.normpath(definition.path))\n            definitions_by_path[normalized_path].append((definition, output_node))\n\n        for items in definitions_by_path.values():\n            if len(items) &lt; 2:\n                continue\n            path = items[0][0].path\n            names = \", \".join(\n                definition.name or definition.node_name\n                for definition, _output_node in items\n            )\n            for _definition, output_node in items:\n                invalid.append(\n                    (\n                        output_node,\n                        f\"path {path!r} is shared by outputs {names}\",\n                    )\n                )\n        return invalid\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_paths.html#client.ayon_katana.plugins.publish.validate_render_output_paths.ValidateRenderOutputPaths.get_invalid","title":"<code>get_invalid(instance)</code>  <code>classmethod</code>","text":"<p>Return output nodes with empty or colliding paths.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_output_paths.py</code> <pre><code>@classmethod\ndef get_invalid(cls, instance):\n    \"\"\"Return output nodes with empty or colliding paths.\"\"\"\n    output = []\n    seen = set()\n    for node, _reason in cls._get_invalid_data(instance):\n        node_name = node.getName()\n        if node_name in seen:\n            continue\n        seen.add(node_name)\n        output.append(node)\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_paths.html#client.ayon_katana.plugins.publish.validate_render_output_paths.ValidateRenderOutputPaths.process","title":"<code>process(instance)</code>","text":"<p>Reject empty or duplicate paths within a render instance.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_output_paths.py</code> <pre><code>def process(self, instance):\n    \"\"\"Reject empty or duplicate paths within a render instance.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    invalid = self._get_invalid_data(instance)\n    if not invalid:\n        return\n\n    details = \"\\n\".join(f\"- {node.getName()}: {reason}\" for node, reason in invalid)\n    raise PublishValidationError(\n        f\"Katana render output paths are invalid:\\n{details}\",\n        title=self.label,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_tokens.html","title":"validate_render_output_tokens","text":"<p>Validate render output frame tokens.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_tokens.html#client.ayon_katana.plugins.publish.validate_render_output_tokens.ValidateRenderOutputTokens","title":"<code>ValidateRenderOutputTokens</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate sequence outputs use one consistent hash frame token.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_output_tokens.py</code> <pre><code>class ValidateRenderOutputTokens(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate sequence outputs use one consistent hash frame token.\"\"\"\n\n    order = pyblish.api.ValidatorOrder + 0.04\n    label = \"Validate Render Output Tokens\"\n    families = [\"render\", \"katana.render\"]\n    actions = [SelectInvalidOutputNodes]\n    optional = True\n\n    def process(self, instance):\n        \"\"\"Reject output paths with invalid frame-token syntax.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        invalid = self._get_invalid_data(instance)\n        if not invalid:\n            return\n\n        details = \"\\n\".join(f\"- {node.getName()}: {reason}\" for node, reason in invalid)\n        raise PublishValidationError(\n            f\"Katana render output frame tokens are invalid:\\n{details}\",\n            title=self.label,\n        )\n\n    @classmethod\n    def get_invalid(cls, instance):\n        \"\"\"Return output nodes with invalid frame-token syntax.\"\"\"\n        output = []\n        seen = set()\n        for node, _reason in cls._get_invalid_data(instance):\n            node_name = node.getName()\n            if node_name in seen:\n                continue\n            seen.add(node_name)\n            output.append(node)\n        return output\n\n    @classmethod\n    def _get_invalid_data(cls, instance):\n        instance_node = compat.get_node(instance.data.get(\"instance_node\"))\n        if instance_node is None:\n            raise PublishValidationError(\"Katana render instance node is unavailable.\")\n\n        frame_start = int(instance.data[\"frameStartHandle\"])\n        frame_end = int(instance.data[\"frameEndHandle\"])\n        frame_step = max(int(instance.data[\"byFrameStep\"]), 1)\n        is_sequence = len(range(frame_start, frame_end + 1, frame_step)) &gt; 1\n\n        invalid = []\n        token_items = []\n        for definition in render.get_output_definitions(instance_node):\n            if not definition.path:\n                continue\n            output_node = compat.get_node(definition.node_name)\n            if output_node is None:\n                continue\n\n            unsupported = _UNSUPPORTED_TOKEN_PATTERN.search(definition.path)\n            if unsupported:\n                invalid.append(\n                    (\n                        output_node,\n                        f\"path {definition.path!r} uses unsupported token \"\n                        f\"{unsupported.group(0)!r}; use one # token\",\n                    )\n                )\n                continue\n\n            tokens = _FRAME_TOKEN_PATTERN.findall(definition.path)\n            if len(tokens) &gt; 1:\n                invalid.append(\n                    (\n                        output_node,\n                        f\"path {definition.path!r} contains multiple # tokens; \"\n                        \"exactly one is allowed\",\n                    )\n                )\n                continue\n            if is_sequence and not tokens:\n                invalid.append(\n                    (\n                        output_node,\n                        f\"sequence path {definition.path!r} has no # frame token\",\n                    )\n                )\n                continue\n            if tokens:\n                token_items.append((output_node, definition.path, len(tokens[0])))\n\n        padding_values = {padding for _node, _path, padding in token_items}\n        if len(padding_values) &gt; 1:\n            expected = \", \".join(str(value) for value in sorted(padding_values))\n            for output_node, path, padding in token_items:\n                invalid.append(\n                    (\n                        output_node,\n                        f\"path {path!r} uses {padding} # characters; all outputs \"\n                        f\"must use consistent padding ({expected})\",\n                    )\n                )\n        return invalid\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_tokens.html#client.ayon_katana.plugins.publish.validate_render_output_tokens.ValidateRenderOutputTokens.get_invalid","title":"<code>get_invalid(instance)</code>  <code>classmethod</code>","text":"<p>Return output nodes with invalid frame-token syntax.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_output_tokens.py</code> <pre><code>@classmethod\ndef get_invalid(cls, instance):\n    \"\"\"Return output nodes with invalid frame-token syntax.\"\"\"\n    output = []\n    seen = set()\n    for node, _reason in cls._get_invalid_data(instance):\n        node_name = node.getName()\n        if node_name in seen:\n            continue\n        seen.add(node_name)\n        output.append(node)\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_output_tokens.html#client.ayon_katana.plugins.publish.validate_render_output_tokens.ValidateRenderOutputTokens.process","title":"<code>process(instance)</code>","text":"<p>Reject output paths with invalid frame-token syntax.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_output_tokens.py</code> <pre><code>def process(self, instance):\n    \"\"\"Reject output paths with invalid frame-token syntax.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    invalid = self._get_invalid_data(instance)\n    if not invalid:\n        return\n\n    details = \"\\n\".join(f\"- {node.getName()}: {reason}\" for node, reason in invalid)\n    raise PublishValidationError(\n        f\"Katana render output frame tokens are invalid:\\n{details}\",\n        title=self.label,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_product_paths_unique.html","title":"validate_render_product_paths_unique","text":"<p>Validate that Katana render products have unique output paths.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_product_paths_unique.html#client.ayon_katana.plugins.publish.validate_render_product_paths_unique.ValidateRenderProductPathsUnique","title":"<code>ValidateRenderProductPathsUnique</code>","text":"<p>               Bases: <code>KatanaContextPlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate that render product paths are unique.</p> <p>This catches multiple render instances that would write to the same files before they overwrite each other at render time.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_product_paths_unique.py</code> <pre><code>class ValidateRenderProductPathsUnique(\n    plugin.KatanaContextPlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate that render product paths are unique.\n\n    This catches multiple render instances that would write to the same files\n    before they overwrite each other at render time.\n    \"\"\"\n\n    label = \"Unique Render Product Paths\"\n    order = pyblish.api.ValidatorOrder\n    families = [\"render\", \"katana.render\"]\n    actions = [SelectInvalidInstanceNodes, SelectInvalidOutputNodes]\n    optional = True\n\n    def process(self, context):\n        \"\"\"Reject concrete output paths shared by render instances.\"\"\"\n        if not self.is_active(context.data):\n            return\n\n        collisions = self._get_collisions(context)\n        if not collisions:\n            return\n\n        details = []\n        for collision in collisions:\n            instance_names = \", \".join(\n                self._instance_label(instance) for instance in collision[\"instances\"]\n            )\n            variants = sorted(collision[\"variants\"], key=str.casefold)\n            details.append(\n                f\"- {collision['path']} ({instance_names})\"\n                + (f\"; variants: {', '.join(variants)}\" if len(variants) &gt; 1 else \"\")\n            )\n        raise PublishValidationError(\n            \"Multiple Katana render instances write to the same concrete \"\n            \"output files:\\n\" + \"\\n\".join(details),\n            title=self.label,\n            description=self.get_description(),\n        )\n\n    @classmethod\n    def get_invalid(cls, context) -&gt; list:\n        \"\"\"Return outer Katana instance Groups that own colliding outputs.\"\"\"\n        nodes = []\n        for collision in cls._get_collisions(context):\n            for instance in collision[\"instances\"]:\n                node = _instance_node(instance)\n                if node is not None:\n                    nodes.append(node)\n\n        unique_nodes = []\n        seen = set()\n        for node in nodes:\n            key = _node_key(node)\n            if key in seen:\n                continue\n            seen.add(key)\n            unique_nodes.append(node)\n        return unique_nodes\n\n    @classmethod\n    def get_invalid_output_nodes(cls, context) -&gt; list:\n        \"\"\"Return colliding output nodes when their expected-files AOV is known.\"\"\"\n        colliding_paths_by_instance = defaultdict(set)\n        for collision in cls._get_collisions(context):\n            for instance in collision[\"instances\"]:\n                colliding_paths_by_instance[id(instance)].add(collision[\"key\"])\n\n        output_nodes = []\n        for instance in cls._render_instances(context):\n            colliding_paths = colliding_paths_by_instance.get(id(instance))\n            if not colliding_paths:\n                continue\n            instance_node = _instance_node(instance)\n            if instance_node is None:\n                continue\n\n            colliding_aovs = {\n                aov_identifier\n                for aov_identifier, paths in _expected_files_by_aov(instance).items()\n                if paths &amp; colliding_paths\n            }\n            if not colliding_aovs:\n                continue\n            for output_definition in render.get_output_definitions(instance_node):\n                if output_definition.aov_identifier not in colliding_aovs:\n                    continue\n                output_node = compat.get_node(output_definition.node_name)\n                if output_node is not None:\n                    output_nodes.append(output_node)\n\n        unique_nodes = []\n        seen = set()\n        for node in output_nodes:\n            key = _node_key(node)\n            if key in seen:\n                continue\n            seen.add(key)\n            unique_nodes.append(node)\n        return unique_nodes\n\n    @classmethod\n    def _get_collisions(cls, context) -&gt; list[dict]:\n        \"\"\"Return all concrete output paths owned by more than one instance.\"\"\"\n        instances_by_path = defaultdict(list)\n        variants_by_path = defaultdict(set)\n        for instance in cls._render_instances(context):\n            if instance.data.get(\"integrate\", True) is False:\n                continue\n\n            paths_for_instance = set()\n            for filepath in get_instance_expected_files(instance):\n                normalized_path = _normalize_output_path(filepath)\n                if not normalized_path:\n                    continue\n                paths_for_instance.add(normalized_path)\n                variants_by_path[normalized_path].add(str(filepath))\n            for normalized_path in paths_for_instance:\n                instances_by_path[normalized_path].append(instance)\n\n        collisions = []\n        for normalized_path, instances in instances_by_path.items():\n            if len(instances) &lt; 2:\n                continue\n            variants = variants_by_path[normalized_path]\n            collisions.append(\n                {\n                    \"key\": normalized_path,\n                    \"path\": sorted(variants, key=str.casefold)[0],\n                    \"variants\": variants,\n                    \"instances\": sorted(instances, key=cls._instance_label),\n                }\n            )\n        return sorted(collisions, key=lambda collision: collision[\"key\"])\n\n    @classmethod\n    def _render_instances(cls, context) -&gt; list:\n        \"\"\"Return only instances matching this context validator's families.\"\"\"\n        return list(pyblish.api.instances_by_plugin(list(context), cls))\n\n    def get_description(self):\n        \"\"\"Return the Publisher help text for output path collisions.\"\"\"\n        return inspect.cleandoc(\n            \"\"\"\n            ### Render product paths are not unique\n\n            Multiple render instances would write to the same output files.\n            Ensure every enabled Katana render output resolves to a unique\n            concrete path before submitting the render.\n            \"\"\"\n        )\n\n    @staticmethod\n    def _instance_label(instance) -&gt; str:\n        \"\"\"Return a deterministic human-readable name for one publish instance.\"\"\"\n        for key in (\"productName\", \"name\", \"instance_node\"):\n            value = instance.data.get(key)\n            if value:\n                return str(value)\n        return str(instance.id)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_product_paths_unique.html#client.ayon_katana.plugins.publish.validate_render_product_paths_unique.ValidateRenderProductPathsUnique.get_description","title":"<code>get_description()</code>","text":"<p>Return the Publisher help text for output path collisions.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_product_paths_unique.py</code> <pre><code>def get_description(self):\n    \"\"\"Return the Publisher help text for output path collisions.\"\"\"\n    return inspect.cleandoc(\n        \"\"\"\n        ### Render product paths are not unique\n\n        Multiple render instances would write to the same output files.\n        Ensure every enabled Katana render output resolves to a unique\n        concrete path before submitting the render.\n        \"\"\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_product_paths_unique.html#client.ayon_katana.plugins.publish.validate_render_product_paths_unique.ValidateRenderProductPathsUnique.get_invalid","title":"<code>get_invalid(context)</code>  <code>classmethod</code>","text":"<p>Return outer Katana instance Groups that own colliding outputs.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_product_paths_unique.py</code> <pre><code>@classmethod\ndef get_invalid(cls, context) -&gt; list:\n    \"\"\"Return outer Katana instance Groups that own colliding outputs.\"\"\"\n    nodes = []\n    for collision in cls._get_collisions(context):\n        for instance in collision[\"instances\"]:\n            node = _instance_node(instance)\n            if node is not None:\n                nodes.append(node)\n\n    unique_nodes = []\n    seen = set()\n    for node in nodes:\n        key = _node_key(node)\n        if key in seen:\n            continue\n        seen.add(key)\n        unique_nodes.append(node)\n    return unique_nodes\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_product_paths_unique.html#client.ayon_katana.plugins.publish.validate_render_product_paths_unique.ValidateRenderProductPathsUnique.get_invalid_output_nodes","title":"<code>get_invalid_output_nodes(context)</code>  <code>classmethod</code>","text":"<p>Return colliding output nodes when their expected-files AOV is known.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_product_paths_unique.py</code> <pre><code>@classmethod\ndef get_invalid_output_nodes(cls, context) -&gt; list:\n    \"\"\"Return colliding output nodes when their expected-files AOV is known.\"\"\"\n    colliding_paths_by_instance = defaultdict(set)\n    for collision in cls._get_collisions(context):\n        for instance in collision[\"instances\"]:\n            colliding_paths_by_instance[id(instance)].add(collision[\"key\"])\n\n    output_nodes = []\n    for instance in cls._render_instances(context):\n        colliding_paths = colliding_paths_by_instance.get(id(instance))\n        if not colliding_paths:\n            continue\n        instance_node = _instance_node(instance)\n        if instance_node is None:\n            continue\n\n        colliding_aovs = {\n            aov_identifier\n            for aov_identifier, paths in _expected_files_by_aov(instance).items()\n            if paths &amp; colliding_paths\n        }\n        if not colliding_aovs:\n            continue\n        for output_definition in render.get_output_definitions(instance_node):\n            if output_definition.aov_identifier not in colliding_aovs:\n                continue\n            output_node = compat.get_node(output_definition.node_name)\n            if output_node is not None:\n                output_nodes.append(output_node)\n\n    unique_nodes = []\n    seen = set()\n    for node in output_nodes:\n        key = _node_key(node)\n        if key in seen:\n            continue\n        seen.add(key)\n        unique_nodes.append(node)\n    return unique_nodes\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_product_paths_unique.html#client.ayon_katana.plugins.publish.validate_render_product_paths_unique.ValidateRenderProductPathsUnique.process","title":"<code>process(context)</code>","text":"<p>Reject concrete output paths shared by render instances.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_product_paths_unique.py</code> <pre><code>def process(self, context):\n    \"\"\"Reject concrete output paths shared by render instances.\"\"\"\n    if not self.is_active(context.data):\n        return\n\n    collisions = self._get_collisions(context)\n    if not collisions:\n        return\n\n    details = []\n    for collision in collisions:\n        instance_names = \", \".join(\n            self._instance_label(instance) for instance in collision[\"instances\"]\n        )\n        variants = sorted(collision[\"variants\"], key=str.casefold)\n        details.append(\n            f\"- {collision['path']} ({instance_names})\"\n            + (f\"; variants: {', '.join(variants)}\" if len(variants) &gt; 1 else \"\")\n        )\n    raise PublishValidationError(\n        \"Multiple Katana render instances write to the same concrete \"\n        \"output files:\\n\" + \"\\n\".join(details),\n        title=self.label,\n        description=self.get_description(),\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_product_paths_unique.html#client.ayon_katana.plugins.publish.validate_render_product_paths_unique.get_instance_expected_files","title":"<code>get_instance_expected_files(instance)</code>","text":"<p>Get the expected source render files for the instance.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_product_paths_unique.py</code> <pre><code>def get_instance_expected_files(instance) -&gt; list[str]:\n    \"\"\"Get the expected source render files for the instance.\"\"\"\n    expected_files = instance.data.get(\"expectedFiles\", [])\n    filepaths = []\n    if expected_files:\n        for expected in expected_files:\n            if isinstance(expected, Mapping):\n                for sequence_files in expected.values():\n                    filepaths.extend(_iter_filepaths(sequence_files))\n                continue\n            filepaths.extend(_iter_filepaths(expected))\n    else:\n        staging_dir = instance.data.get(\"stagingDir\")\n        frames = instance.data.get(\"frames\")\n        if frames is None or not staging_dir:\n            return []\n        if isinstance(frames, str):\n            filepaths.append(f\"{staging_dir}/{frames}\")\n        else:\n            filepaths.extend(f\"{staging_dir}/{frame}\" for frame in frames)\n    return filepaths\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_resolution.html","title":"validate_render_resolution","text":"<p>Validate Katana render resolution against the AYON context.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_resolution.html#client.ayon_katana.plugins.publish.validate_render_resolution.ValidateRenderResolution","title":"<code>ValidateRenderResolution</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate the render resolution setting aligned with the context.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_resolution.py</code> <pre><code>class ValidateRenderResolution(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate the render resolution setting aligned with the context.\"\"\"\n\n    order = pyblish.api.ValidatorOrder\n    families = [\"render\", \"katana.render\"]\n    label = \"Validate Render Resolution\"\n    actions = [SelectInvalidInstanceNodes]\n    optional = True\n\n    def process(self, instance):\n        \"\"\"Reject invalid resolution values or AYON context mismatches.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        invalid = self.get_invalid_resolution(instance)\n        if invalid:\n            raise PublishValidationError(\n                \"Katana RenderSettings has an invalid resolution or does not \"\n                \"match the current AYON context.\",\n                description=self.get_description(),\n            )\n\n    @classmethod\n    def get_invalid_resolution(cls, instance):\n        \"\"\"Return invalid resolution fields for the render instance.\"\"\"\n        width = int(instance.data.get(\"resolutionWidth\") or 0)\n        height = int(instance.data.get(\"resolutionHeight\") or 0)\n        pixel_aspect = float(instance.data.get(\"pixelAspect\") or 0)\n        if width &lt; 1 or height &lt; 1 or pixel_aspect &lt;= 0:\n            cls.log.error(\n                \"Katana RenderSettings has invalid resolution data: %sx%s (%s).\",\n                width,\n                height,\n                pixel_aspect,\n            )\n            return [\"resolution\"]\n\n        expected = cls.get_expected_resolution(instance)\n        if expected is None:\n            return []\n\n        actual = (width, height, pixel_aspect)\n        if actual != expected:\n            cls.log.error(\n                \"Katana render resolution %sx%s (%s) does not match context \"\n                \"resolution %sx%s (%s).\",\n                width,\n                height,\n                pixel_aspect,\n                expected[0],\n                expected[1],\n                expected[2],\n            )\n            return [\"resolution\"]\n        return []\n\n    @classmethod\n    def get_expected_resolution(cls, instance):\n        \"\"\"Return the expected resolution from the task or folder entity.\"\"\"\n        entity = instance.data.get(\"taskEntity\")\n        if not entity:\n            entity = instance.data.get(\"folderEntity\")\n        attributes = (entity or {}).get(\"attrib\") or {}\n        required_keys = {\"resolutionWidth\", \"resolutionHeight\", \"pixelAspect\"}\n        if not required_keys.issubset(attributes):\n            return None\n        return (\n            int(attributes[\"resolutionWidth\"]),\n            int(attributes[\"resolutionHeight\"]),\n            float(attributes[\"pixelAspect\"]),\n        )\n\n    @staticmethod\n    def get_description():\n        \"\"\"Return the Publisher help text for resolution mismatches.\"\"\"\n        return inspect.cleandoc(\n            \"\"\"\n            ### Render Resolution does not match context\n\n            The Katana render resolution or pixel aspect ratio does not match\n            the resolution configured on the current AYON task or folder.\n            Check the native RenderSettings resolution before publishing.\n            \"\"\"\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_resolution.html#client.ayon_katana.plugins.publish.validate_render_resolution.ValidateRenderResolution.get_description","title":"<code>get_description()</code>  <code>staticmethod</code>","text":"<p>Return the Publisher help text for resolution mismatches.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_resolution.py</code> <pre><code>@staticmethod\ndef get_description():\n    \"\"\"Return the Publisher help text for resolution mismatches.\"\"\"\n    return inspect.cleandoc(\n        \"\"\"\n        ### Render Resolution does not match context\n\n        The Katana render resolution or pixel aspect ratio does not match\n        the resolution configured on the current AYON task or folder.\n        Check the native RenderSettings resolution before publishing.\n        \"\"\"\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_resolution.html#client.ayon_katana.plugins.publish.validate_render_resolution.ValidateRenderResolution.get_expected_resolution","title":"<code>get_expected_resolution(instance)</code>  <code>classmethod</code>","text":"<p>Return the expected resolution from the task or folder entity.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_resolution.py</code> <pre><code>@classmethod\ndef get_expected_resolution(cls, instance):\n    \"\"\"Return the expected resolution from the task or folder entity.\"\"\"\n    entity = instance.data.get(\"taskEntity\")\n    if not entity:\n        entity = instance.data.get(\"folderEntity\")\n    attributes = (entity or {}).get(\"attrib\") or {}\n    required_keys = {\"resolutionWidth\", \"resolutionHeight\", \"pixelAspect\"}\n    if not required_keys.issubset(attributes):\n        return None\n    return (\n        int(attributes[\"resolutionWidth\"]),\n        int(attributes[\"resolutionHeight\"]),\n        float(attributes[\"pixelAspect\"]),\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_resolution.html#client.ayon_katana.plugins.publish.validate_render_resolution.ValidateRenderResolution.get_invalid_resolution","title":"<code>get_invalid_resolution(instance)</code>  <code>classmethod</code>","text":"<p>Return invalid resolution fields for the render instance.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_resolution.py</code> <pre><code>@classmethod\ndef get_invalid_resolution(cls, instance):\n    \"\"\"Return invalid resolution fields for the render instance.\"\"\"\n    width = int(instance.data.get(\"resolutionWidth\") or 0)\n    height = int(instance.data.get(\"resolutionHeight\") or 0)\n    pixel_aspect = float(instance.data.get(\"pixelAspect\") or 0)\n    if width &lt; 1 or height &lt; 1 or pixel_aspect &lt;= 0:\n        cls.log.error(\n            \"Katana RenderSettings has invalid resolution data: %sx%s (%s).\",\n            width,\n            height,\n            pixel_aspect,\n        )\n        return [\"resolution\"]\n\n    expected = cls.get_expected_resolution(instance)\n    if expected is None:\n        return []\n\n    actual = (width, height, pixel_aspect)\n    if actual != expected:\n        cls.log.error(\n            \"Katana render resolution %sx%s (%s) does not match context \"\n            \"resolution %sx%s (%s).\",\n            width,\n            height,\n            pixel_aspect,\n            expected[0],\n            expected[1],\n            expected[2],\n        )\n        return [\"resolution\"]\n    return []\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_render_resolution.html#client.ayon_katana.plugins.publish.validate_render_resolution.ValidateRenderResolution.process","title":"<code>process(instance)</code>","text":"<p>Reject invalid resolution values or AYON context mismatches.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_render_resolution.py</code> <pre><code>def process(self, instance):\n    \"\"\"Reject invalid resolution values or AYON context mismatches.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    invalid = self.get_invalid_resolution(instance)\n    if invalid:\n        raise PublishValidationError(\n            \"Katana RenderSettings has an invalid resolution or does not \"\n            \"match the current AYON context.\",\n            description=self.get_description(),\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_review.html","title":"validate_review","text":"<p>Validate Katana Viewer Scene Review capture readiness.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_review.html#client.ayon_katana.plugins.publish.validate_review.ValidateReview","title":"<code>ValidateReview</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate that a review range and an unambiguous visible Viewer exist.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_review.py</code> <pre><code>class ValidateReview(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate that a review range and an unambiguous visible Viewer exist.\"\"\"\n\n    label = \"Validate Scene Review\"\n    order = pyblish.api.ValidatorOrder\n    families = [\"katana.review\"]\n    optional = False\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Validate the captured range, FPS, and Viewer selection.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        try:\n            review.frame_numbers(\n                int(instance.data[\"frameStartHandle\"]),\n                int(instance.data[\"frameEndHandle\"]),\n                int(instance.data[\"byFrameStep\"]),\n            )\n        except (KeyError, TypeError, ValueError) as exc:\n            raise PublishValidationError(\n                f\"Scene Review has an invalid frame range: {exc}\",\n                title=\"Scene Review range invalid\",\n            ) from exc\n\n        try:\n            fps = float(instance.data[\"fps\"])\n        except (KeyError, TypeError, ValueError) as exc:\n            raise PublishValidationError(\n                \"Scene Review requires a valid AYON task FPS.\",\n                title=\"Scene Review FPS missing\",\n            ) from exc\n        if not math.isfinite(fps) or fps &lt;= 0:\n            raise PublishValidationError(\n                \"Scene Review requires a finite positive AYON task FPS.\",\n                title=\"Scene Review FPS invalid\",\n            )\n\n        viewer_widget, reason = thumbnail.select_viewer_widget()\n        if viewer_widget is None:\n            raise PublishValidationError(\n                f\"Scene Review cannot select a Katana Viewer: {reason}.\",\n                title=\"Scene Review Viewer unavailable\",\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_review.html#client.ayon_katana.plugins.publish.validate_review.ValidateReview.process","title":"<code>process(instance)</code>","text":"<p>Validate the captured range, FPS, and Viewer selection.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_review.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Validate the captured range, FPS, and Viewer selection.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    try:\n        review.frame_numbers(\n            int(instance.data[\"frameStartHandle\"]),\n            int(instance.data[\"frameEndHandle\"]),\n            int(instance.data[\"byFrameStep\"]),\n        )\n    except (KeyError, TypeError, ValueError) as exc:\n        raise PublishValidationError(\n            f\"Scene Review has an invalid frame range: {exc}\",\n            title=\"Scene Review range invalid\",\n        ) from exc\n\n    try:\n        fps = float(instance.data[\"fps\"])\n    except (KeyError, TypeError, ValueError) as exc:\n        raise PublishValidationError(\n            \"Scene Review requires a valid AYON task FPS.\",\n            title=\"Scene Review FPS missing\",\n        ) from exc\n    if not math.isfinite(fps) or fps &lt;= 0:\n        raise PublishValidationError(\n            \"Scene Review requires a finite positive AYON task FPS.\",\n            title=\"Scene Review FPS invalid\",\n        )\n\n    viewer_widget, reason = thumbnail.select_viewer_widget()\n    if viewer_widget is None:\n        raise PublishValidationError(\n            f\"Scene Review cannot select a Katana Viewer: {reason}.\",\n            title=\"Scene Review Viewer unavailable\",\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_asset_contribution_default_prim.html","title":"validate_usd_asset_contribution_default_prim","text":"<p>Validate exported Katana layers before USD asset contribution.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_asset_contribution_default_prim.html#client.ayon_katana.plugins.publish.validate_usd_asset_contribution_default_prim.ValidateUsdAssetContributionDefaultPrim","title":"<code>ValidateUsdAssetContributionDefaultPrim</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Require an asset contribution layer to own its expected default prim.</p> <p>This intent must be validated before extraction. Katana's native <code>UsdLayerExport</code> does not expose an equivalent default-prim parameter, so this plug-in validates the staged layer immediately after extraction and before AYON Core composes contribution products.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_usd_asset_contribution_default_prim.py</code> <pre><code>class ValidateUsdAssetContributionDefaultPrim(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Require an asset contribution layer to own its expected default prim.\n\n    This intent must be validated before extraction. Katana's\n    native ``UsdLayerExport`` does not expose an equivalent default-prim\n    parameter, so this plug-in validates the staged layer immediately after\n    extraction and before AYON Core composes contribution products.\n    \"\"\"\n\n    label = \"Validate USD Asset Contribution Default Prim\"\n    order = pyblish.api.ExtractorOrder - 0.48\n    families = [\"katana.usd\"]\n    optional = True\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Reject an asset contribution without the required root prim.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        contribution = self._get_contribution_settings(instance.data)\n        if (\n            not contribution.get(\"contribution_enabled\", False)\n            or contribution.get(\"contribution_target_product_init\") != \"asset\"\n        ):\n            return\n\n        try:\n            layer_path = get_extracted_usd_layer_path(instance.data)\n        except ValueError as exc:\n            raise PublishValidationError(\n                str(exc),\n                title=\"USD contribution representation invalid\",\n            ) from exc\n        from ayon_core.pipeline.usdlib import get_standard_default_prim_name\n        from pxr import Sdf\n\n        layer = Sdf.Layer.FindOrOpen(layer_path.as_posix())\n        if layer is None:\n            raise PublishValidationError(\n                f\"Failed to open the extracted USD layer: {layer_path}\",\n                title=\"USD contribution layer invalid\",\n            )\n\n        expected_name = get_standard_default_prim_name(instance.data[\"folderPath\"])\n        default_prim = str(layer.defaultPrim or \"\").lstrip(\"/\")\n        if default_prim != expected_name:\n            actual = f\"/{default_prim}\" if default_prim else \"not set\"\n            raise PublishValidationError(\n                \"USD Contribution is configured to initialize an asset, but \"\n                f\"the exported layer default prim is {actual}. Author a root \"\n                f\"prim named '/{expected_name}' and make it the layer default \"\n                \"prim, choose 'Initialize as shot' for a shot-style layer, or \"\n                \"disable USD Contribution for this product.\",\n                title=\"USD asset contribution default prim invalid\",\n            )\n\n        expected_path = f\"/{expected_name}\"\n        if layer.GetPrimAtPath(expected_path) is None:\n            raise PublishValidationError(\n                f\"The exported layer declares '{expected_path}' as its default \"\n                \"prim, but that root prim does not exist in the layer.\",\n                title=\"USD asset contribution root prim missing\",\n            )\n\n    @staticmethod\n    def _get_contribution_settings(data: dict) -&gt; dict:\n        \"\"\"Return the shared Core contribution attributes for an instance.\"\"\"\n        return data.get(\"publish_attributes\", {}).get(\n            \"CollectUSDLayerContributions\", {}\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_asset_contribution_default_prim.html#client.ayon_katana.plugins.publish.validate_usd_asset_contribution_default_prim.ValidateUsdAssetContributionDefaultPrim.process","title":"<code>process(instance)</code>","text":"<p>Reject an asset contribution without the required root prim.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_usd_asset_contribution_default_prim.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Reject an asset contribution without the required root prim.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    contribution = self._get_contribution_settings(instance.data)\n    if (\n        not contribution.get(\"contribution_enabled\", False)\n        or contribution.get(\"contribution_target_product_init\") != \"asset\"\n    ):\n        return\n\n    try:\n        layer_path = get_extracted_usd_layer_path(instance.data)\n    except ValueError as exc:\n        raise PublishValidationError(\n            str(exc),\n            title=\"USD contribution representation invalid\",\n        ) from exc\n    from ayon_core.pipeline.usdlib import get_standard_default_prim_name\n    from pxr import Sdf\n\n    layer = Sdf.Layer.FindOrOpen(layer_path.as_posix())\n    if layer is None:\n        raise PublishValidationError(\n            f\"Failed to open the extracted USD layer: {layer_path}\",\n            title=\"USD contribution layer invalid\",\n        )\n\n    expected_name = get_standard_default_prim_name(instance.data[\"folderPath\"])\n    default_prim = str(layer.defaultPrim or \"\").lstrip(\"/\")\n    if default_prim != expected_name:\n        actual = f\"/{default_prim}\" if default_prim else \"not set\"\n        raise PublishValidationError(\n            \"USD Contribution is configured to initialize an asset, but \"\n            f\"the exported layer default prim is {actual}. Author a root \"\n            f\"prim named '/{expected_name}' and make it the layer default \"\n            \"prim, choose 'Initialize as shot' for a shot-style layer, or \"\n            \"disable USD Contribution for this product.\",\n            title=\"USD asset contribution default prim invalid\",\n        )\n\n    expected_path = f\"/{expected_name}\"\n    if layer.GetPrimAtPath(expected_path) is None:\n        raise PublishValidationError(\n            f\"The exported layer declares '{expected_path}' as its default \"\n            \"prim, but that root prim does not exist in the layer.\",\n            title=\"USD asset contribution root prim missing\",\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_camera_content.html","title":"validate_usd_camera_content","text":"<p>Validate semantic Katana USD camera publish content.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_camera_content.html#client.ayon_katana.plugins.publish.validate_usd_camera_content.ValidateUsdCameraContent","title":"<code>ValidateUsdCameraContent</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Require semantic USD camera products to contain a Camera prim.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_usd_camera_content.py</code> <pre><code>class ValidateUsdCameraContent(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Require semantic USD camera products to contain a Camera prim.\"\"\"\n\n    label = \"Validate USD Camera Content\"\n    order = pyblish.api.ExtractorOrder - 0.499\n    families = [\"katana.usd\"]\n    optional = False\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Reject semantic camera products without composed camera content.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        product_base_type = instance.data.get(\"productBaseType\") or instance.data.get(\n            \"productType\"\n        )\n        if product_base_type not in {\"camera\", \"usdCamera\"}:\n            return\n\n        try:\n            stage = open_extracted_usd_stage(instance.data)\n        except (ValueError, RuntimeError) as exc:\n            raise PublishValidationError(\n                f\"Failed to inspect exported USD camera content: {exc}\",\n                title=\"USD camera stage invalid\",\n            ) from exc\n\n        from pxr import UsdGeom\n\n        camera_paths = collect_schema_prim_paths(stage, UsdGeom.Camera)\n        if camera_paths:\n            return\n\n        raise PublishValidationError(\n            \"The exported USD camera product does not contain a composed Camera prim. \"\n            \"Connect native USD camera content to the UsdLayerExport source before \"\n            \"publishing, or publish it as a generic USD product instead.\",\n            title=\"USD camera content missing\",\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_camera_content.html#client.ayon_katana.plugins.publish.validate_usd_camera_content.ValidateUsdCameraContent.process","title":"<code>process(instance)</code>","text":"<p>Reject semantic camera products without composed camera content.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_usd_camera_content.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Reject semantic camera products without composed camera content.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    product_base_type = instance.data.get(\"productBaseType\") or instance.data.get(\n        \"productType\"\n    )\n    if product_base_type not in {\"camera\", \"usdCamera\"}:\n        return\n\n    try:\n        stage = open_extracted_usd_stage(instance.data)\n    except (ValueError, RuntimeError) as exc:\n        raise PublishValidationError(\n            f\"Failed to inspect exported USD camera content: {exc}\",\n            title=\"USD camera stage invalid\",\n        ) from exc\n\n    from pxr import UsdGeom\n\n    camera_paths = collect_schema_prim_paths(stage, UsdGeom.Camera)\n    if camera_paths:\n        return\n\n    raise PublishValidationError(\n        \"The exported USD camera product does not contain a composed Camera prim. \"\n        \"Connect native USD camera content to the UsdLayerExport source before \"\n        \"publishing, or publish it as a generic USD product instead.\",\n        title=\"USD camera content missing\",\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_layer.html","title":"validate_usd_layer","text":"<p>Validate Katana USD layer instances.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_layer.html#client.ayon_katana.plugins.publish.validate_usd_layer.ValidateUsdLayer","title":"<code>ValidateUsdLayer</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate <code>UsdLayerExport</code> nodes and settings.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_usd_layer.py</code> <pre><code>class ValidateUsdLayer(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate ``UsdLayerExport`` nodes and settings.\"\"\"\n\n    label = \"Validate USD Layer\"\n    order = pyblish.api.ValidatorOrder\n    families = [\"katana.usd\"]\n    optional = False\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Validate the export node, source, and settings.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        node_name = instance.data[\"instance_node\"]\n        export_node = compat.get_node(node_name)\n        if export_node is None:\n            raise PublishValidationError(\n                f\"Katana USD export node does not exist: {node_name!r}\",\n                title=\"USD export node missing\",\n            )\n        if export_node.getType() != \"UsdLayerExport\":\n            raise PublishValidationError(\n                \"USD publish instance is not a UsdLayerExport node.\",\n                title=\"USD export node invalid\",\n            )\n\n        input_port = export_node.getInputPort(\"in\")\n        connected_ports = (\n            list(input_port.getConnectedPorts()) if input_port is not None else []\n        )\n        if len(connected_ports) != 1:\n            raise PublishValidationError(\n                \"UsdLayerExport requires exactly one native USD source on its \"\n                \"'in' port.\",\n                title=\"USD source missing\",\n            )\n        source_node = connected_ports[0].getNode()\n        if not is_native_usd_node(source_node):\n            raise PublishValidationError(\n                f\"UsdLayerExport source {source_node.getName()!r} is not a \"\n                f\"native USD node (type: {source_node.getType()!r}). Connect \"\n                \"a node with Katana's 'nativeusd' flavor.\",\n                title=\"USD source is not native\",\n            )\n\n        settings = read_usd_layer_export_settings(export_node)\n        if settings[\"file_format\"] not in USD_FILE_FORMATS:\n            raise PublishValidationError(\n                f\"Unsupported USD file format: {settings['file_format']!r}.\",\n                title=\"USD format invalid\",\n            )\n        if settings[\"time_samples\"] not in USD_TIME_SAMPLE_MODES:\n            raise PublishValidationError(\n                f\"Unsupported USD time sampling: {settings['time_samples']!r}.\",\n                title=\"USD time sampling invalid\",\n            )\n        if settings[\"export_method\"] not in USD_EXPORT_METHODS:\n            raise PublishValidationError(\n                f\"Unsupported USD export method: {settings['export_method']!r}.\",\n                title=\"USD export method invalid\",\n            )\n        if settings[\"samples_per_frame\"] &lt;= 0:\n            raise PublishValidationError(\n                \"USD samples per frame must be greater than zero.\",\n                title=\"USD time sampling invalid\",\n            )\n        if (\n            settings[\"time_samples\"] == \"Frame Range\"\n            and settings[\"frame_end\"] &lt; settings[\"frame_start\"]\n        ):\n            raise PublishValidationError(\n                \"USD frame range end is before its start.\",\n                title=\"USD frame range invalid\",\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_layer.html#client.ayon_katana.plugins.publish.validate_usd_layer.ValidateUsdLayer.process","title":"<code>process(instance)</code>","text":"<p>Validate the export node, source, and settings.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_usd_layer.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Validate the export node, source, and settings.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    node_name = instance.data[\"instance_node\"]\n    export_node = compat.get_node(node_name)\n    if export_node is None:\n        raise PublishValidationError(\n            f\"Katana USD export node does not exist: {node_name!r}\",\n            title=\"USD export node missing\",\n        )\n    if export_node.getType() != \"UsdLayerExport\":\n        raise PublishValidationError(\n            \"USD publish instance is not a UsdLayerExport node.\",\n            title=\"USD export node invalid\",\n        )\n\n    input_port = export_node.getInputPort(\"in\")\n    connected_ports = (\n        list(input_port.getConnectedPorts()) if input_port is not None else []\n    )\n    if len(connected_ports) != 1:\n        raise PublishValidationError(\n            \"UsdLayerExport requires exactly one native USD source on its \"\n            \"'in' port.\",\n            title=\"USD source missing\",\n        )\n    source_node = connected_ports[0].getNode()\n    if not is_native_usd_node(source_node):\n        raise PublishValidationError(\n            f\"UsdLayerExport source {source_node.getName()!r} is not a \"\n            f\"native USD node (type: {source_node.getType()!r}). Connect \"\n            \"a node with Katana's 'nativeusd' flavor.\",\n            title=\"USD source is not native\",\n        )\n\n    settings = read_usd_layer_export_settings(export_node)\n    if settings[\"file_format\"] not in USD_FILE_FORMATS:\n        raise PublishValidationError(\n            f\"Unsupported USD file format: {settings['file_format']!r}.\",\n            title=\"USD format invalid\",\n        )\n    if settings[\"time_samples\"] not in USD_TIME_SAMPLE_MODES:\n        raise PublishValidationError(\n            f\"Unsupported USD time sampling: {settings['time_samples']!r}.\",\n            title=\"USD time sampling invalid\",\n        )\n    if settings[\"export_method\"] not in USD_EXPORT_METHODS:\n        raise PublishValidationError(\n            f\"Unsupported USD export method: {settings['export_method']!r}.\",\n            title=\"USD export method invalid\",\n        )\n    if settings[\"samples_per_frame\"] &lt;= 0:\n        raise PublishValidationError(\n            \"USD samples per frame must be greater than zero.\",\n            title=\"USD time sampling invalid\",\n        )\n    if (\n        settings[\"time_samples\"] == \"Frame Range\"\n        and settings[\"frame_end\"] &lt; settings[\"frame_start\"]\n    ):\n        raise PublishValidationError(\n            \"USD frame range end is before its start.\",\n            title=\"USD frame range invalid\",\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_look_assignments.html","title":"validate_usd_look_assignments","text":"<p>Validate composed geometry material assignments for Katana USD looks.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_look_assignments.html#client.ayon_katana.plugins.publish.validate_usd_look_assignments.ValidateUsdLookAssignments","title":"<code>ValidateUsdLookAssignments</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate all geometry prims have a material binding.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_usd_look_assignments.py</code> <pre><code>class ValidateUsdLookAssignments(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate all geometry prims have a material binding.\"\"\"\n\n    label = \"Validate All Geometry Has Material Assignment\"\n    order = pyblish.api.ExtractorOrder - 0.498\n    families = [\"katana.usd\"]\n    optional = True\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Validate material bindings on composed geometry.\"\"\"\n        if not self.is_active(instance.data) or not is_look_instance(instance.data):\n            return\n        try:\n            stage = get_composed_source_stage(instance.data)\n            invalid_paths = collect_unbound_geometry_paths(stage)\n        except (ValueError, RuntimeError) as exc:\n            raise PublishValidationError(\n                f\"Failed to inspect the composed USD look stage: {exc}\",\n                title=\"USD look stage inspection failed\",\n            ) from exc\n\n        if invalid_paths:\n            formatted = \"\\n\".join(f\"- {path}\" for path in invalid_paths)\n            raise PublishValidationError(\n                \"Found geometry without material bindings.\\n\"\n                f\"Unbound geometry:\\n{formatted}\\n\"\n                \"Bind a material directly or through a material-binding subset \"\n                \"using full, preview, or allPurpose.\",\n                title=\"No assigned materials\",\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_look_assignments.html#client.ayon_katana.plugins.publish.validate_usd_look_assignments.ValidateUsdLookAssignments.process","title":"<code>process(instance)</code>","text":"<p>Validate material bindings on composed geometry.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_usd_look_assignments.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Validate material bindings on composed geometry.\"\"\"\n    if not self.is_active(instance.data) or not is_look_instance(instance.data):\n        return\n    try:\n        stage = get_composed_source_stage(instance.data)\n        invalid_paths = collect_unbound_geometry_paths(stage)\n    except (ValueError, RuntimeError) as exc:\n        raise PublishValidationError(\n            f\"Failed to inspect the composed USD look stage: {exc}\",\n            title=\"USD look stage inspection failed\",\n        ) from exc\n\n    if invalid_paths:\n        formatted = \"\\n\".join(f\"- {path}\" for path in invalid_paths)\n        raise PublishValidationError(\n            \"Found geometry without material bindings.\\n\"\n            f\"Unbound geometry:\\n{formatted}\\n\"\n            \"Bind a material directly or through a material-binding subset \"\n            \"using full, preview, or allPurpose.\",\n            title=\"No assigned materials\",\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_look_disallowed_types.html","title":"validate_usd_look_disallowed_types","text":"<p>Reject authored geometry and composition arcs in Katana USD looks.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_look_disallowed_types.html#client.ayon_katana.plugins.publish.validate_usd_look_disallowed_types.ValidateUsdLookDisallowedTypes","title":"<code>ValidateUsdLookDisallowedTypes</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code></p> <p>Validate the look does not define disallowed content.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_usd_look_disallowed_types.py</code> <pre><code>class ValidateUsdLookDisallowedTypes(plugin.KatanaInstancePlugin):\n    \"\"\"Validate the look does not define disallowed content.\"\"\"\n\n    label = \"Validate Look No Disallowed Types\"\n    order = pyblish.api.ExtractorOrder - 0.498\n    families = [\"katana.usd\"]\n    optional = False\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Validate authored look content.\"\"\"\n        if not is_look_instance(instance.data):\n            return\n        try:\n            layer = open_extracted_look_layer(instance.data)\n            invalid_items = collect_disallowed_authored_look_items(layer)\n        except (ValueError, RuntimeError) as exc:\n            raise PublishValidationError(\n                f\"Failed to inspect the extracted USD look layer: {exc}\",\n                title=\"USD look layer inspection failed\",\n            ) from exc\n\n        if invalid_items:\n            formatted = \"\\n\".join(\n                f\"- {path}: {'; '.join(reasons)}\"\n                for path, reasons in invalid_items.items()\n            )\n            raise PublishValidationError(\n                \"Invalid look members found.\\n\"\n                f\"Invalid authored prims:\\n{formatted}\\n\"\n                \"Author only materials, bindings, and render-geometry settings in \"\n                \"the look layer. Keep geometry, cameras, render settings, references, \"\n                \"and payloads in upstream composition layers.\",\n                title=\"Look Invalid Members\",\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_look_disallowed_types.html#client.ayon_katana.plugins.publish.validate_usd_look_disallowed_types.ValidateUsdLookDisallowedTypes.process","title":"<code>process(instance)</code>","text":"<p>Validate authored look content.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_usd_look_disallowed_types.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Validate authored look content.\"\"\"\n    if not is_look_instance(instance.data):\n        return\n    try:\n        layer = open_extracted_look_layer(instance.data)\n        invalid_items = collect_disallowed_authored_look_items(layer)\n    except (ValueError, RuntimeError) as exc:\n        raise PublishValidationError(\n            f\"Failed to inspect the extracted USD look layer: {exc}\",\n            title=\"USD look layer inspection failed\",\n        ) from exc\n\n    if invalid_items:\n        formatted = \"\\n\".join(\n            f\"- {path}: {'; '.join(reasons)}\"\n            for path, reasons in invalid_items.items()\n        )\n        raise PublishValidationError(\n            \"Invalid look members found.\\n\"\n            f\"Invalid authored prims:\\n{formatted}\\n\"\n            \"Author only materials, bindings, and render-geometry settings in \"\n            \"the look layer. Keep geometry, cameras, render settings, references, \"\n            \"and payloads in upstream composition layers.\",\n            title=\"Look Invalid Members\",\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_look_material_definitions.html","title":"validate_usd_look_material_definitions","text":"<p>Validate authored Material prims are definitions in Katana USD looks.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_look_material_definitions.html#client.ayon_katana.plugins.publish.validate_usd_look_material_definitions.ValidateUsdLookMaterialDefinitions","title":"<code>ValidateUsdLookMaterialDefinitions</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate Material prims are defined instead of overs or classes.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_usd_look_material_definitions.py</code> <pre><code>class ValidateUsdLookMaterialDefinitions(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate Material prims are defined instead of overs or classes.\"\"\"\n\n    label = \"Validate Look Shaders Are Defined\"\n    order = pyblish.api.ExtractorOrder - 0.498\n    families = [\"katana.usd\"]\n    optional = True\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Validate authored Material prim definitions.\"\"\"\n        if not self.is_active(instance.data) or not is_look_instance(instance.data):\n            return\n        try:\n            stage = get_composed_source_stage(instance.data)\n            layer = open_extracted_look_layer(instance.data)\n            invalid_materials = collect_invalid_material_definitions(layer, stage)\n        except (ValueError, RuntimeError) as exc:\n            raise PublishValidationError(\n                f\"Failed to inspect USD look material definitions: {exc}\",\n                title=\"USD look material inspection failed\",\n            ) from exc\n\n        if invalid_materials:\n            formatted = \"\\n\".join(\n                f\"- {path}: authored as {specifier}\"\n                for path, specifier in invalid_materials.items()\n            )\n            raise PublishValidationError(\n                \"Found materials without authored definitions.\\n\"\n                f\"Invalid materials:\\n{formatted}\\n\"\n                \"Define new Material prims in the look layer. If an upstream look \"\n                \"already defines them, mute or exclude that upstream look while \"\n                \"authoring the replacement.\",\n                title=\"Materials not defined\",\n            )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_usd_look_material_definitions.html#client.ayon_katana.plugins.publish.validate_usd_look_material_definitions.ValidateUsdLookMaterialDefinitions.process","title":"<code>process(instance)</code>","text":"<p>Validate authored Material prim definitions.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_usd_look_material_definitions.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Validate authored Material prim definitions.\"\"\"\n    if not self.is_active(instance.data) or not is_look_instance(instance.data):\n        return\n    try:\n        stage = get_composed_source_stage(instance.data)\n        layer = open_extracted_look_layer(instance.data)\n        invalid_materials = collect_invalid_material_definitions(layer, stage)\n    except (ValueError, RuntimeError) as exc:\n        raise PublishValidationError(\n            f\"Failed to inspect USD look material definitions: {exc}\",\n            title=\"USD look material inspection failed\",\n        ) from exc\n\n    if invalid_materials:\n        formatted = \"\\n\".join(\n            f\"- {path}: authored as {specifier}\"\n            for path, specifier in invalid_materials.items()\n        )\n        raise PublishValidationError(\n            \"Found materials without authored definitions.\\n\"\n            f\"Invalid materials:\\n{formatted}\\n\"\n            \"Define new Material prims in the look layer. If an upstream look \"\n            \"already defines them, mute or exclude that upstream look while \"\n            \"authoring the replacement.\",\n            title=\"Materials not defined\",\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_context.html","title":"validate_workfile_context","text":"<p>Validate the AYON context embedded in a Katana project.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_context.html#client.ayon_katana.plugins.publish.validate_workfile_context.ValidateWorkfileContext","title":"<code>ValidateWorkfileContext</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Require embedded project, folder, and task to match publish context.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_workfile_context.py</code> <pre><code>class ValidateWorkfileContext(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Require embedded project, folder, and task to match publish context.\"\"\"\n\n    order = pyblish.api.ValidatorOrder\n    label = \"Validate Katana Workfile Context\"\n    families = [\"workfile\"]\n    optional = True\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Reject workfiles whose embedded context differs from publishing.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        embedded = registered_host().get_context_data()\n        publish_context = instance.context.data\n\n        expected = {\n            \"project\": publish_context[\"projectName\"],\n            \"folder\": instance.data.get(\"folderPath\"),\n            \"task\": instance.data.get(\"task\"),\n        }\n        actual = {\n            \"project\": embedded.get(\"project_name\"),\n            \"folder\": embedded.get(\"folder_path\"),\n            \"task\": embedded.get(\"task_name\"),\n        }\n\n        mismatches = {\n            key: (expected[key], actual[key])\n            for key in expected\n            if expected[key] != actual[key]\n        }\n        if not mismatches:\n            return\n\n        details = \"; \".join(\n            f\"{key}: expected {expected_value!r}, embedded {actual_value!r}\"\n            for key, (expected_value, actual_value) in mismatches.items()\n        )\n        raise PublishValidationError(\n            f\"Katana workfile context does not match the publish context: {details}\",\n            title=\"Katana workfile context mismatch\",\n        )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_context.html#client.ayon_katana.plugins.publish.validate_workfile_context.ValidateWorkfileContext.process","title":"<code>process(instance)</code>","text":"<p>Reject workfiles whose embedded context differs from publishing.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_workfile_context.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Reject workfiles whose embedded context differs from publishing.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    embedded = registered_host().get_context_data()\n    publish_context = instance.context.data\n\n    expected = {\n        \"project\": publish_context[\"projectName\"],\n        \"folder\": instance.data.get(\"folderPath\"),\n        \"task\": instance.data.get(\"task\"),\n    }\n    actual = {\n        \"project\": embedded.get(\"project_name\"),\n        \"folder\": embedded.get(\"folder_path\"),\n        \"task\": embedded.get(\"task_name\"),\n    }\n\n    mismatches = {\n        key: (expected[key], actual[key])\n        for key in expected\n        if expected[key] != actual[key]\n    }\n    if not mismatches:\n        return\n\n    details = \"; \".join(\n        f\"{key}: expected {expected_value!r}, embedded {actual_value!r}\"\n        for key, (expected_value, actual_value) in mismatches.items()\n    )\n    raise PublishValidationError(\n        f\"Katana workfile context does not match the publish context: {details}\",\n        title=\"Katana workfile context mismatch\",\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_paths.html","title":"validate_workfile_paths","text":"<p>Validate external file references stored in the Katana workfile.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_paths.html#client.ayon_katana.plugins.publish.validate_workfile_paths.SelectInvalidWorkfileReferences","title":"<code>SelectInvalidWorkfileReferences</code>","text":"<p>               Bases: <code>Action</code></p> <p>Select Katana nodes with invalid registered file references.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_workfile_paths.py</code> <pre><code>class SelectInvalidWorkfileReferences(pyblish.api.Action):\n    \"\"\"Select Katana nodes with invalid registered file references.\"\"\"\n\n    label = \"Select Invalid\"\n    icon = \"search\"\n    on = \"failed\"\n\n    def process(self, context, plugin) -&gt; None:\n        \"\"\"Select nodes with invalid references.\"\"\"\n        references = []\n        for instance in get_errored_instances_from_context(context, plugin=plugin):\n            references.extend(plugin.get_invalid(instance))\n        nodes = dependencies.unique_reference_nodes(references)\n        if nodes:\n            compat.set_selected_nodes(nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_paths.html#client.ayon_katana.plugins.publish.validate_workfile_paths.SelectInvalidWorkfileReferences.process","title":"<code>process(context, plugin)</code>","text":"<p>Select nodes with invalid references.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_workfile_paths.py</code> <pre><code>def process(self, context, plugin) -&gt; None:\n    \"\"\"Select nodes with invalid references.\"\"\"\n    references = []\n    for instance in get_errored_instances_from_context(context, plugin=plugin):\n        references.extend(plugin.get_invalid(instance))\n    nodes = dependencies.unique_reference_nodes(references)\n    if nodes:\n        compat.set_selected_nodes(nodes)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_paths.html#client.ayon_katana.plugins.publish.validate_workfile_paths.ValidateWorkfilePaths","title":"<code>ValidateWorkfilePaths</code>","text":"<p>               Bases: <code>KatanaInstancePlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Validate external paths used by published workfiles.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_workfile_paths.py</code> <pre><code>class ValidateWorkfilePaths(\n    plugin.KatanaInstancePlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Validate external paths used by published workfiles.\"\"\"\n\n    order = pyblish.api.ValidatorOrder\n    families = [\"workfile\"]\n    label = \"Validate Workfile Paths\"\n    optional = True\n    actions = [SelectInvalidWorkfileReferences, RepairAction]\n\n    def process(self, instance) -&gt; None:\n        \"\"\"Validate external references and relative paths.\"\"\"\n        if not self.is_active(instance.data):\n            return\n\n        issues = self.get_invalid_with_reasons(instance)\n        if not issues:\n            return\n\n        details = \"\\n\".join(\n            f\"- {reference.label}: {reason}\" for reference, reason in issues\n        )\n        raise PublishValidationError(\n            f\"Workfile contains invalid external file references:\\n{details}\",\n            title=self.label,\n        )\n\n    @classmethod\n    def get_invalid(cls, instance) -&gt; list[dependencies.WorkfileReference]:\n        \"\"\"Return invalid references.\"\"\"\n        return [\n            reference for reference, _reason in cls.get_invalid_with_reasons(instance)\n        ]\n\n    @classmethod\n    def get_invalid_with_reasons(\n        cls,\n        instance,\n    ) -&gt; list[tuple[dependencies.WorkfileReference, str]]:\n        \"\"\"Return invalid references and their validation messages.\"\"\"\n        frame_start, frame_end, frame_step = cls._required_frame_range(instance)\n        invalid = []\n        for reference in dependencies.collect_workfile_references():\n            reason = cls._reference_error(\n                reference,\n                frame_start=frame_start,\n                frame_end=frame_end,\n                frame_step=frame_step,\n            )\n            if reason:\n                invalid.append((reference, reason))\n        return invalid\n\n    @classmethod\n    def repair(cls, instance) -&gt; None:\n        \"\"\"Make valid relative paths absolute.\"\"\"\n        frame_start, frame_end, frame_step = cls._required_frame_range(instance)\n        repairable = []\n        for reference, reason in cls.get_invalid_with_reasons(instance):\n            if reason != \"path is relative to the current workfile\":\n                continue\n            if not reference.is_relative_literal or not reference.resolved_value:\n                continue\n            repairable.append(reference)\n\n        if not repairable:\n            return\n\n        from Katana import Utils\n\n        undo_stack = Utils.UndoStack\n        undo_stack.OpenGroup(\"Repair AYON workfile paths\")\n        try:\n            for reference in repairable:\n                cls._repair_reference(\n                    reference,\n                    frame_start=frame_start,\n                    frame_end=frame_end,\n                    frame_step=frame_step,\n                )\n        finally:\n            undo_stack.CloseGroup()\n\n    @classmethod\n    def _repair_reference(\n        cls,\n        reference: dependencies.WorkfileReference,\n        *,\n        frame_start: int | None,\n        frame_end: int | None,\n        frame_step: int,\n    ) -&gt; None:\n        parameter = reference.parameter\n        if parameter.isExpression():\n            return\n\n        current_value = parameter.getValue(0.0)\n        current_value = \"\" if current_value is None else str(current_value)\n        if current_value != reference.authored_value:\n            return\n        if not reference.is_relative_literal or not reference.resolved_value:\n            return\n        if frame_start is not None and frame_end is not None:\n            frame_values = {\n                str(parameter.getValue(float(frame)) or \"\")\n                for frame in range(frame_start, frame_end + 1, frame_step)\n            }\n            if frame_values != {current_value}:\n                return\n\n        required_paths = dependencies.required_reference_paths(\n            reference,\n            frame_start=frame_start,\n            frame_end=frame_end,\n            frame_step=frame_step,\n        )\n        if not required_paths or any(\n            not os.path.isfile(path) for path in required_paths\n        ):\n            return\n\n        parameter.setValue(reference.resolved_value.replace(\"\\\\\", \"/\"), 0.0)\n\n    @staticmethod\n    def _required_frame_range(instance) -&gt; tuple[int | None, int | None, int]:\n        data = instance.data\n        context_data = instance.context.data\n        frame_start = data.get(\"frameStart\", context_data.get(\"frameStart\"))\n        frame_end = data.get(\"frameEnd\", context_data.get(\"frameEnd\"))\n        if frame_start is None or frame_end is None:\n            return None, None, 1\n\n        handle_start = int(\n            data.get(\"handleStart\", context_data.get(\"handleStart\", 0)) or 0\n        )\n        handle_end = int(data.get(\"handleEnd\", context_data.get(\"handleEnd\", 0)) or 0)\n        frame_step = int(data.get(\"frameStep\", context_data.get(\"frameStep\", 1)) or 1)\n        return (\n            int(frame_start) - handle_start,\n            int(frame_end) + handle_end,\n            frame_step,\n        )\n\n    @staticmethod\n    def _reference_error(\n        reference: dependencies.WorkfileReference,\n        *,\n        frame_start: int | None,\n        frame_end: int | None,\n        frame_step: int,\n    ) -&gt; str | None:\n        has_frame_range = frame_start is not None and frame_end is not None\n        if reference.is_entity_uri:\n            return \"entity URI cannot be filesystem-validated and is not repairable\"\n        if not has_frame_range:\n            if not reference.authored_value:\n                return \"path is empty\"\n            if not reference.resolved_value:\n                return \"path could not be resolved\"\n\n        try:\n            required_paths = dependencies.required_reference_paths(\n                reference,\n                frame_start=frame_start,\n                frame_end=frame_end,\n                frame_step=frame_step,\n            )\n        except ValueError as exc:\n            return str(exc)\n        if (\n            not has_frame_range\n            and reference.resolved_value\n            and dependencies.has_sequence_token(reference.resolved_value)\n            and not required_paths\n        ):\n            return \"sequence cannot be validated without a scene frame range\"\n        missing = [path for path in required_paths if not os.path.isfile(path)]\n        if missing:\n            preview = \", \".join(missing[:3])\n            if len(missing) &gt; 3:\n                preview += f\" (+{len(missing) - 3} more)\"\n            return f\"required file does not exist: {preview}\"\n        if reference.is_relative_literal:\n            return \"path is relative to the current workfile\"\n        return None\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_paths.html#client.ayon_katana.plugins.publish.validate_workfile_paths.ValidateWorkfilePaths.get_invalid","title":"<code>get_invalid(instance)</code>  <code>classmethod</code>","text":"<p>Return invalid references.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_workfile_paths.py</code> <pre><code>@classmethod\ndef get_invalid(cls, instance) -&gt; list[dependencies.WorkfileReference]:\n    \"\"\"Return invalid references.\"\"\"\n    return [\n        reference for reference, _reason in cls.get_invalid_with_reasons(instance)\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_paths.html#client.ayon_katana.plugins.publish.validate_workfile_paths.ValidateWorkfilePaths.get_invalid_with_reasons","title":"<code>get_invalid_with_reasons(instance)</code>  <code>classmethod</code>","text":"<p>Return invalid references and their validation messages.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_workfile_paths.py</code> <pre><code>@classmethod\ndef get_invalid_with_reasons(\n    cls,\n    instance,\n) -&gt; list[tuple[dependencies.WorkfileReference, str]]:\n    \"\"\"Return invalid references and their validation messages.\"\"\"\n    frame_start, frame_end, frame_step = cls._required_frame_range(instance)\n    invalid = []\n    for reference in dependencies.collect_workfile_references():\n        reason = cls._reference_error(\n            reference,\n            frame_start=frame_start,\n            frame_end=frame_end,\n            frame_step=frame_step,\n        )\n        if reason:\n            invalid.append((reference, reason))\n    return invalid\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_paths.html#client.ayon_katana.plugins.publish.validate_workfile_paths.ValidateWorkfilePaths.process","title":"<code>process(instance)</code>","text":"<p>Validate external references and relative paths.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_workfile_paths.py</code> <pre><code>def process(self, instance) -&gt; None:\n    \"\"\"Validate external references and relative paths.\"\"\"\n    if not self.is_active(instance.data):\n        return\n\n    issues = self.get_invalid_with_reasons(instance)\n    if not issues:\n        return\n\n    details = \"\\n\".join(\n        f\"- {reference.label}: {reason}\" for reference, reason in issues\n    )\n    raise PublishValidationError(\n        f\"Workfile contains invalid external file references:\\n{details}\",\n        title=self.label,\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_paths.html#client.ayon_katana.plugins.publish.validate_workfile_paths.ValidateWorkfilePaths.repair","title":"<code>repair(instance)</code>  <code>classmethod</code>","text":"<p>Make valid relative paths absolute.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_workfile_paths.py</code> <pre><code>@classmethod\ndef repair(cls, instance) -&gt; None:\n    \"\"\"Make valid relative paths absolute.\"\"\"\n    frame_start, frame_end, frame_step = cls._required_frame_range(instance)\n    repairable = []\n    for reference, reason in cls.get_invalid_with_reasons(instance):\n        if reason != \"path is relative to the current workfile\":\n            continue\n        if not reference.is_relative_literal or not reference.resolved_value:\n            continue\n        repairable.append(reference)\n\n    if not repairable:\n        return\n\n    from Katana import Utils\n\n    undo_stack = Utils.UndoStack\n    undo_stack.OpenGroup(\"Repair AYON workfile paths\")\n    try:\n        for reference in repairable:\n            cls._repair_reference(\n                reference,\n                frame_start=frame_start,\n                frame_end=frame_end,\n                frame_step=frame_step,\n            )\n    finally:\n        undo_stack.CloseGroup()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_saved.html","title":"validate_workfile_saved","text":"<p>Validate that the Katana workfile has been saved.</p>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_saved.html#client.ayon_katana.plugins.publish.validate_workfile_saved.ValidateWorkfileSaved","title":"<code>ValidateWorkfileSaved</code>","text":"<p>               Bases: <code>KatanaContextPlugin</code>, <code>OptionalPyblishPluginMixin</code></p> <p>Require an existing Katana project before publishing.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_workfile_saved.py</code> <pre><code>class ValidateWorkfileSaved(\n    plugin.KatanaContextPlugin,\n    OptionalPyblishPluginMixin,\n):\n    \"\"\"Require an existing Katana project before publishing.\"\"\"\n\n    order = pyblish.api.ValidatorOrder - 0.1\n    label = \"Validate Katana Workfile Saved\"\n    optional = True\n\n    def process(self, context) -&gt; None:\n        \"\"\"Reject unsaved or unsupported Katana project paths.\"\"\"\n        if not self.is_active(context.data):\n            return\n\n        current_file = context.data.get(\"currentFile\")\n        try:\n            context.data[\"currentFile\"] = workio.validate_workfile_path(\n                current_file,\n                require_exists=True,\n            )\n        except ValueError as exc:\n            raise PublishValidationError(\n                f\"Katana workfile is not publishable: {exc}\",\n                title=\"Katana workfile not saved\",\n            ) from exc\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/publish/validate_workfile_saved.html#client.ayon_katana.plugins.publish.validate_workfile_saved.ValidateWorkfileSaved.process","title":"<code>process(context)</code>","text":"<p>Reject unsaved or unsupported Katana project paths.</p> Source code in <code>client/ayon_katana/plugins/publish/validate_workfile_saved.py</code> <pre><code>def process(self, context) -&gt; None:\n    \"\"\"Reject unsaved or unsupported Katana project paths.\"\"\"\n    if not self.is_active(context.data):\n        return\n\n    current_file = context.data.get(\"currentFile\")\n    try:\n        context.data[\"currentFile\"] = workio.validate_workfile_path(\n            current_file,\n            require_exists=True,\n        )\n    except ValueError as exc:\n        raise PublishValidationError(\n            f\"Katana workfile is not publishable: {exc}\",\n            title=\"Katana workfile not saved\",\n        ) from exc\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/index.html","title":"workfile_build","text":"<p>Katana Workfile Builder plugins.</p>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/create_placeholder.html","title":"create_placeholder","text":"<p>Create Katana Workfile Builder placeholders for publish instances.</p>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/create_placeholder.html#client.ayon_katana.plugins.workfile_build.create_placeholder.KatanaPlaceholderCreatePlugin","title":"<code>KatanaPlaceholderCreatePlugin</code>","text":"<p>               Bases: <code>KatanaPlaceholderPlugin</code>, <code>PlaceholderCreateMixin</code></p> <p>Replace Workfile Builder create placeholders with publish instances.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/create_placeholder.py</code> <pre><code>class KatanaPlaceholderCreatePlugin(\n    KatanaPlaceholderPlugin,\n    PlaceholderCreateMixin,\n):\n    \"\"\"Replace Workfile Builder create placeholders with publish instances.\"\"\"\n\n    identifier = \"ayon.create.placeholder\"\n    label = \"Katana Create\"\n\n    def get_placeholder_node_name(self, placeholder_data):\n        \"\"\"Return a stable node name derived from the configured creator.\"\"\"\n        creator_name = placeholder_data[\"creator\"]\n        creator = self.builder.get_creators_by_name().get(creator_name)\n        if creator is None:\n            raise RuntimeError(f\"Unknown AYON creator: {creator_name!r}.\")\n        product_type = creator.product_base_type\n        return lib.sanitize_node_name(\n            f\"AYON_PLACEHOLDER_CREATE_{product_type}\",\n            \"AYON_PLACEHOLDER_CREATE\",\n        )\n\n    def create_placeholder_node(self, node_name):\n        \"\"\"Create a pass-through Group for a creator placeholder.\"\"\"\n        placeholder_node = NodegraphAPI.CreateNode(\"Group\", NodegraphAPI.GetRootNode())\n        placeholder_node.setName(node_name)\n        placeholder_node.addInputPort(\"in\")\n        placeholder_node.addOutputPort(\"out\")\n        placeholder_node.getSendPort(\"in\").connect(\n            placeholder_node.getReturnPort(\"out\")\n        )\n        return placeholder_node\n\n    def populate_placeholder(self, placeholder):\n        \"\"\"Populate a creator placeholder when it is still empty.\"\"\"\n        placeholder_node = compat.get_node(placeholder.scene_identifier)\n        if placeholder_node is None:\n            raise RuntimeError(\n                f\"Katana placeholder no longer exists: {placeholder.scene_identifier}\"\n            )\n        if self._get_created_instance_nodes(placeholder_node):\n            self.log.info(\n                \"Creator placeholder %s is already populated.\",\n                placeholder.scene_identifier,\n            )\n            return\n        self.populate_create_placeholder(placeholder)\n\n    def repopulate_placeholder(self, placeholder):\n        \"\"\"Populate a creator placeholder again when needed.\"\"\"\n        self.populate_placeholder(placeholder)\n\n    def get_placeholder_options(self, options=None):\n        \"\"\"Return the Workfile Builder creator options.\"\"\"\n        return self.get_create_plugin_options(options)\n\n    def collect_placeholders(self):\n        \"\"\"Return creator placeholders stored in the current project.\"\"\"\n        output = []\n        create_placeholders = self.collect_scene_placeholders()\n        for node in create_placeholders:\n            placeholder_data = self._read(node)\n            if placeholder_data.get(\"plugin_identifier\") != self.identifier:\n                continue\n            output.append(\n                CreatePlaceholderItem(\n                    node.getName(),\n                    placeholder_data,\n                    self,\n                )\n            )\n        return output\n\n    def create_succeed(self, placeholder, creator_instance):\n        \"\"\"Connect a successfully created instance inside its placeholder.\"\"\"\n        placeholder_node = compat.get_node(placeholder.scene_identifier)\n        instance_node = creator_instance.transient_data.get(\"node\")\n        if placeholder_node is None or instance_node is None:\n            return\n\n        compat.set_parent(instance_node, placeholder_node)\n        send_port = placeholder_node.getSendPort(\"in\")\n        instance_input = instance_node.getInputPort(\"in\")\n        instance_output = instance_node.getOutputPort(\"out\")\n        return_port = placeholder_node.getReturnPort(\"out\")\n\n        for connected_port in list(send_port.getConnectedPorts()):\n            send_port.disconnect(connected_port)\n        if instance_input is not None:\n            send_port.connect(instance_input)\n        if instance_output is not None:\n            instance_output.connect(return_port)\n        else:\n            send_port.connect(return_port)\n\n        NodegraphAPI.SetNodePosition(instance_node, (0.0, 0.0))\n        placeholder.data[\"created_product_name\"] = creator_instance[\"productName\"]\n        self._imprint(placeholder_node, placeholder.data)\n\n    def delete_placeholder(self, placeholder):\n        \"\"\"Consume a populated placeholder or delete an empty one.\"\"\"\n        placeholder_node = compat.get_node(placeholder.scene_identifier)\n        if placeholder_node is None:\n            return\n        if self._get_created_instance_nodes(placeholder_node):\n            self.clear_placeholder(placeholder_node)\n            return\n        placeholder_node.delete()\n\n    @staticmethod\n    def _get_created_instance_nodes(placeholder_node):\n        output = []\n        for node in instances.iter_nodes(placeholder_node):\n            if instances.read(node) is not None:\n                output.append(node)\n        return output\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/create_placeholder.html#client.ayon_katana.plugins.workfile_build.create_placeholder.KatanaPlaceholderCreatePlugin.collect_placeholders","title":"<code>collect_placeholders()</code>","text":"<p>Return creator placeholders stored in the current project.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/create_placeholder.py</code> <pre><code>def collect_placeholders(self):\n    \"\"\"Return creator placeholders stored in the current project.\"\"\"\n    output = []\n    create_placeholders = self.collect_scene_placeholders()\n    for node in create_placeholders:\n        placeholder_data = self._read(node)\n        if placeholder_data.get(\"plugin_identifier\") != self.identifier:\n            continue\n        output.append(\n            CreatePlaceholderItem(\n                node.getName(),\n                placeholder_data,\n                self,\n            )\n        )\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/create_placeholder.html#client.ayon_katana.plugins.workfile_build.create_placeholder.KatanaPlaceholderCreatePlugin.create_placeholder_node","title":"<code>create_placeholder_node(node_name)</code>","text":"<p>Create a pass-through Group for a creator placeholder.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/create_placeholder.py</code> <pre><code>def create_placeholder_node(self, node_name):\n    \"\"\"Create a pass-through Group for a creator placeholder.\"\"\"\n    placeholder_node = NodegraphAPI.CreateNode(\"Group\", NodegraphAPI.GetRootNode())\n    placeholder_node.setName(node_name)\n    placeholder_node.addInputPort(\"in\")\n    placeholder_node.addOutputPort(\"out\")\n    placeholder_node.getSendPort(\"in\").connect(\n        placeholder_node.getReturnPort(\"out\")\n    )\n    return placeholder_node\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/create_placeholder.html#client.ayon_katana.plugins.workfile_build.create_placeholder.KatanaPlaceholderCreatePlugin.create_succeed","title":"<code>create_succeed(placeholder, creator_instance)</code>","text":"<p>Connect a successfully created instance inside its placeholder.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/create_placeholder.py</code> <pre><code>def create_succeed(self, placeholder, creator_instance):\n    \"\"\"Connect a successfully created instance inside its placeholder.\"\"\"\n    placeholder_node = compat.get_node(placeholder.scene_identifier)\n    instance_node = creator_instance.transient_data.get(\"node\")\n    if placeholder_node is None or instance_node is None:\n        return\n\n    compat.set_parent(instance_node, placeholder_node)\n    send_port = placeholder_node.getSendPort(\"in\")\n    instance_input = instance_node.getInputPort(\"in\")\n    instance_output = instance_node.getOutputPort(\"out\")\n    return_port = placeholder_node.getReturnPort(\"out\")\n\n    for connected_port in list(send_port.getConnectedPorts()):\n        send_port.disconnect(connected_port)\n    if instance_input is not None:\n        send_port.connect(instance_input)\n    if instance_output is not None:\n        instance_output.connect(return_port)\n    else:\n        send_port.connect(return_port)\n\n    NodegraphAPI.SetNodePosition(instance_node, (0.0, 0.0))\n    placeholder.data[\"created_product_name\"] = creator_instance[\"productName\"]\n    self._imprint(placeholder_node, placeholder.data)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/create_placeholder.html#client.ayon_katana.plugins.workfile_build.create_placeholder.KatanaPlaceholderCreatePlugin.delete_placeholder","title":"<code>delete_placeholder(placeholder)</code>","text":"<p>Consume a populated placeholder or delete an empty one.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/create_placeholder.py</code> <pre><code>def delete_placeholder(self, placeholder):\n    \"\"\"Consume a populated placeholder or delete an empty one.\"\"\"\n    placeholder_node = compat.get_node(placeholder.scene_identifier)\n    if placeholder_node is None:\n        return\n    if self._get_created_instance_nodes(placeholder_node):\n        self.clear_placeholder(placeholder_node)\n        return\n    placeholder_node.delete()\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/create_placeholder.html#client.ayon_katana.plugins.workfile_build.create_placeholder.KatanaPlaceholderCreatePlugin.get_placeholder_node_name","title":"<code>get_placeholder_node_name(placeholder_data)</code>","text":"<p>Return a stable node name derived from the configured creator.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/create_placeholder.py</code> <pre><code>def get_placeholder_node_name(self, placeholder_data):\n    \"\"\"Return a stable node name derived from the configured creator.\"\"\"\n    creator_name = placeholder_data[\"creator\"]\n    creator = self.builder.get_creators_by_name().get(creator_name)\n    if creator is None:\n        raise RuntimeError(f\"Unknown AYON creator: {creator_name!r}.\")\n    product_type = creator.product_base_type\n    return lib.sanitize_node_name(\n        f\"AYON_PLACEHOLDER_CREATE_{product_type}\",\n        \"AYON_PLACEHOLDER_CREATE\",\n    )\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/create_placeholder.html#client.ayon_katana.plugins.workfile_build.create_placeholder.KatanaPlaceholderCreatePlugin.get_placeholder_options","title":"<code>get_placeholder_options(options=None)</code>","text":"<p>Return the Workfile Builder creator options.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/create_placeholder.py</code> <pre><code>def get_placeholder_options(self, options=None):\n    \"\"\"Return the Workfile Builder creator options.\"\"\"\n    return self.get_create_plugin_options(options)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/create_placeholder.html#client.ayon_katana.plugins.workfile_build.create_placeholder.KatanaPlaceholderCreatePlugin.populate_placeholder","title":"<code>populate_placeholder(placeholder)</code>","text":"<p>Populate a creator placeholder when it is still empty.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/create_placeholder.py</code> <pre><code>def populate_placeholder(self, placeholder):\n    \"\"\"Populate a creator placeholder when it is still empty.\"\"\"\n    placeholder_node = compat.get_node(placeholder.scene_identifier)\n    if placeholder_node is None:\n        raise RuntimeError(\n            f\"Katana placeholder no longer exists: {placeholder.scene_identifier}\"\n        )\n    if self._get_created_instance_nodes(placeholder_node):\n        self.log.info(\n            \"Creator placeholder %s is already populated.\",\n            placeholder.scene_identifier,\n        )\n        return\n    self.populate_create_placeholder(placeholder)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/create_placeholder.html#client.ayon_katana.plugins.workfile_build.create_placeholder.KatanaPlaceholderCreatePlugin.repopulate_placeholder","title":"<code>repopulate_placeholder(placeholder)</code>","text":"<p>Populate a creator placeholder again when needed.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/create_placeholder.py</code> <pre><code>def repopulate_placeholder(self, placeholder):\n    \"\"\"Populate a creator placeholder again when needed.\"\"\"\n    self.populate_placeholder(placeholder)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/load_placeholder.html","title":"load_placeholder","text":"<p>Load products into Katana Workfile Builder placeholders.</p>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/load_placeholder.html#client.ayon_katana.plugins.workfile_build.load_placeholder.KatanaPlaceholderLoadPlugin","title":"<code>KatanaPlaceholderLoadPlugin</code>","text":"<p>               Bases: <code>KatanaPlaceholderPlugin</code>, <code>PlaceholderLoadMixin</code></p> <p>Replace Workfile Builder load placeholders with AYON products.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/load_placeholder.py</code> <pre><code>class KatanaPlaceholderLoadPlugin(KatanaPlaceholderPlugin, PlaceholderLoadMixin):\n    \"\"\"Replace Workfile Builder load placeholders with AYON products.\"\"\"\n\n    identifier = \"ayon.load.placeholder\"\n    label = \"Katana Load\"\n\n    def populate_placeholder(self, placeholder):\n        \"\"\"Populate a load placeholder.\"\"\"\n        self.populate_load_placeholder(placeholder)\n\n    def repopulate_placeholder(self, placeholder):\n        \"\"\"Populate a placeholder while preserving existing representations.\"\"\"\n        placeholder_node = self._get_placeholder_node(placeholder)\n        loaded_ids = self.get_loaded_representation_ids(placeholder_node)\n        self.populate_load_placeholder(placeholder, loaded_ids)\n\n    def get_placeholder_options(self, options=None):\n        \"\"\"Return the Workfile Builder load options.\"\"\"\n        return self.get_load_plugin_options(options)\n\n    def collect_placeholders(self):\n        \"\"\"Return load placeholders stored in the current Katana project.\"\"\"\n        output = []\n        load_placeholders = self.collect_scene_placeholders()\n        for node in load_placeholders:\n            placeholder_data = self._read(node)\n            if not placeholder_data:\n                continue\n            if placeholder_data.get(\"plugin_identifier\") != self.identifier:\n                continue\n            output.append(LoadPlaceholderItem(node.getName(), placeholder_data, self))\n        return output\n\n    def load_succeed(self, placeholder, container):\n        \"\"\"Connect a successfully loaded container to its placeholder.\"\"\"\n        placeholder_node = self._get_placeholder_node(placeholder)\n        container_node = (\n            container.get(\"node\") if isinstance(container, dict) else container\n        )\n        if container_node is None:\n            raise RuntimeError(\"The Katana loader did not return a container node.\")\n        self.connect_loaded_container(placeholder_node, container_node)\n\n    @staticmethod\n    def _get_placeholder_node(placeholder):\n        \"\"\"Resolve a placeholder's Katana node.\"\"\"\n        placeholder_node = compat.get_node(placeholder.scene_identifier)\n        if placeholder_node is None:\n            raise RuntimeError(\n                f\"Katana placeholder no longer exists: {placeholder.scene_identifier}\"\n            )\n        return placeholder_node\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/load_placeholder.html#client.ayon_katana.plugins.workfile_build.load_placeholder.KatanaPlaceholderLoadPlugin.collect_placeholders","title":"<code>collect_placeholders()</code>","text":"<p>Return load placeholders stored in the current Katana project.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/load_placeholder.py</code> <pre><code>def collect_placeholders(self):\n    \"\"\"Return load placeholders stored in the current Katana project.\"\"\"\n    output = []\n    load_placeholders = self.collect_scene_placeholders()\n    for node in load_placeholders:\n        placeholder_data = self._read(node)\n        if not placeholder_data:\n            continue\n        if placeholder_data.get(\"plugin_identifier\") != self.identifier:\n            continue\n        output.append(LoadPlaceholderItem(node.getName(), placeholder_data, self))\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/load_placeholder.html#client.ayon_katana.plugins.workfile_build.load_placeholder.KatanaPlaceholderLoadPlugin.get_placeholder_options","title":"<code>get_placeholder_options(options=None)</code>","text":"<p>Return the Workfile Builder load options.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/load_placeholder.py</code> <pre><code>def get_placeholder_options(self, options=None):\n    \"\"\"Return the Workfile Builder load options.\"\"\"\n    return self.get_load_plugin_options(options)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/load_placeholder.html#client.ayon_katana.plugins.workfile_build.load_placeholder.KatanaPlaceholderLoadPlugin.load_succeed","title":"<code>load_succeed(placeholder, container)</code>","text":"<p>Connect a successfully loaded container to its placeholder.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/load_placeholder.py</code> <pre><code>def load_succeed(self, placeholder, container):\n    \"\"\"Connect a successfully loaded container to its placeholder.\"\"\"\n    placeholder_node = self._get_placeholder_node(placeholder)\n    container_node = (\n        container.get(\"node\") if isinstance(container, dict) else container\n    )\n    if container_node is None:\n        raise RuntimeError(\"The Katana loader did not return a container node.\")\n    self.connect_loaded_container(placeholder_node, container_node)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/load_placeholder.html#client.ayon_katana.plugins.workfile_build.load_placeholder.KatanaPlaceholderLoadPlugin.populate_placeholder","title":"<code>populate_placeholder(placeholder)</code>","text":"<p>Populate a load placeholder.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/load_placeholder.py</code> <pre><code>def populate_placeholder(self, placeholder):\n    \"\"\"Populate a load placeholder.\"\"\"\n    self.populate_load_placeholder(placeholder)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/load_placeholder.html#client.ayon_katana.plugins.workfile_build.load_placeholder.KatanaPlaceholderLoadPlugin.repopulate_placeholder","title":"<code>repopulate_placeholder(placeholder)</code>","text":"<p>Populate a placeholder while preserving existing representations.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/load_placeholder.py</code> <pre><code>def repopulate_placeholder(self, placeholder):\n    \"\"\"Populate a placeholder while preserving existing representations.\"\"\"\n    placeholder_node = self._get_placeholder_node(placeholder)\n    loaded_ids = self.get_loaded_representation_ids(placeholder_node)\n    self.populate_load_placeholder(placeholder, loaded_ids)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/script_placeholder.html","title":"script_placeholder","text":"<p>Administrator-authored Python placeholder for Katana Workfile Builder.</p>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/script_placeholder.html#client.ayon_katana.plugins.workfile_build.script_placeholder.KatanaPlaceholderScriptPlugin","title":"<code>KatanaPlaceholderScriptPlugin</code>","text":"<p>               Bases: <code>KatanaPlaceholderPlugin</code></p> <p>Execute trusted Python during selected Workfile Builder phases.</p> <p>Scripts are configured by AYON administrators and execute with the same permissions as Katana. They are intentionally not a security sandbox and must never contain untrusted project or artist-provided code.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/script_placeholder.py</code> <pre><code>class KatanaPlaceholderScriptPlugin(KatanaPlaceholderPlugin):\n    \"\"\"Execute trusted Python during selected Workfile Builder phases.\n\n    Scripts are configured by AYON administrators and execute with the same\n    permissions as Katana. They are intentionally not a security sandbox and\n    must never contain untrusted project or artist-provided code.\n    \"\"\"\n\n    identifier = \"katana.runscript\"\n    label = \"Run Python Script (Unsandboxed)\"\n\n    def get_placeholder_options(self, options=None):\n        \"\"\"Return the script phase and ordering controls shown by Core.\"\"\"\n        options = options or {}\n        return [\n            NumberDef(\n                \"order\",\n                label=\"Order\",\n                default=options.get(\"order\") or 0,\n                decimals=0,\n                minimum=0,\n                maximum=999,\n                tooltip=(\"Execution order from 0 to 999. Lower values run first.\"),\n            ),\n            TextDef(\n                \"prepare_script\",\n                label=\"Run at\\nprepare\",\n                tooltip=(\"Trusted, unsandboxed Python executed before population.\"),\n                multiline=True,\n                default=options.get(\"prepare_script\", \"\"),\n            ),\n            TextDef(\n                \"populate_script\",\n                label=\"Run at\\npopulate\",\n                tooltip=(\"Trusted, unsandboxed Python executed at placeholder order.\"),\n                multiline=True,\n                default=options.get(\"populate_script\", EXAMPLE_SCRIPT),\n            ),\n            TextDef(\n                \"depth_processed_script\",\n                label=\"Run after\\ndepth\\niteration\",\n                tooltip=(\n                    \"Trusted, unsandboxed Python executed after every build depth.\"\n                ),\n                multiline=True,\n                default=options.get(\"depth_processed_script\", \"\"),\n            ),\n            TextDef(\n                \"finished_script\",\n                label=\"Run after\\nbuild\",\n                tooltip=(\n                    \"Trusted, unsandboxed Python executed after the build, even \"\n                    \"when another placeholder reported an error.\"\n                ),\n                multiline=True,\n                default=options.get(\"finished_script\", \"\"),\n            ),\n        ]\n\n    def collect_placeholders(self):\n        \"\"\"Collect only Group nodes carrying this plugin identifier.\"\"\"\n        output = []\n        for node in self.collect_scene_placeholders():\n            data = self._read(node)\n            if not data or data.get(\"plugin_identifier\") != self.identifier:\n                continue\n            output.append(PlaceholderItem(node.getName(), data, self))\n        return output\n\n    def prepare_placeholders(self, placeholders):\n        \"\"\"Execute each configured prepare script in placeholder order.\"\"\"\n        super().prepare_placeholders(placeholders)\n        for placeholder in placeholders:\n            script = placeholder.data.get(\"prepare_script\")\n            if script:\n                self.run_script(placeholder, script)\n\n    def populate_placeholder(self, placeholder):\n        \"\"\"Execute populate code and register deferred phase callbacks.\"\"\"\n        populate_script = placeholder.data.get(\"populate_script\")\n        depth_script = placeholder.data.get(\"depth_processed_script\")\n        finished_script = placeholder.data.get(\"finished_script\")\n        order = int(placeholder.order)\n\n        if populate_script:\n            self.run_script(placeholder, populate_script)\n\n        if depth_script:\n            callback = weakref_partial(self.run_script, placeholder, depth_script)\n            self.builder.add_on_depth_processed_callback(callback, order=order)\n        if finished_script:\n            callback = weakref_partial(\n                self.run_script,\n                placeholder,\n                finished_script,\n            )\n            self.builder.add_on_finished_callback(callback, order=order)\n\n        keep_placeholder = placeholder.data.get(\"keep_placeholder\", True)\n        if keep_placeholder:\n            return\n        if depth_script or finished_script:\n            callback = weakref_partial(self._delete_after_finish, placeholder)\n            self.builder.add_on_finished_callback(callback, order=order + 1)\n        else:\n            self.delete_placeholder(placeholder)\n\n    def _delete_after_finish(self, placeholder, event=None) -&gt; None:\n        \"\"\"Delete after build completion while accepting Core's event callback.\"\"\"\n        self.delete_placeholder(placeholder)\n\n    def run_script(self, placeholder, script, event=None):\n        \"\"\"Execute trusted administrator Python with placeholder context.\n\n        Args:\n            placeholder: Workfile Builder placeholder item.\n            script: Trusted administrator-authored Python source.\n            event: Optional Core depth or finished event.\n\n        Returns:\n            The execution namespace, useful for diagnostics and tests.\n\n        Warning:\n            This method is deliberately unsandboxed. The source can import\n            modules, access the network, and modify files as the Katana user.\n        \"\"\"\n        self.log.debug(\"Running unsandboxed Workfile Builder script: %s\", event)\n        namespace = {\n            \"NodegraphAPI\": NodegraphAPI,\n            \"event\": event,\n            \"placeholder\": placeholder,\n            \"plugin\": self,\n        }\n        code = compile(script, \"&lt;AYON Katana Workfile Builder script&gt;\", \"exec\")\n        exec(code, namespace, namespace)\n        return namespace\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/script_placeholder.html#client.ayon_katana.plugins.workfile_build.script_placeholder.KatanaPlaceholderScriptPlugin.collect_placeholders","title":"<code>collect_placeholders()</code>","text":"<p>Collect only Group nodes carrying this plugin identifier.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/script_placeholder.py</code> <pre><code>def collect_placeholders(self):\n    \"\"\"Collect only Group nodes carrying this plugin identifier.\"\"\"\n    output = []\n    for node in self.collect_scene_placeholders():\n        data = self._read(node)\n        if not data or data.get(\"plugin_identifier\") != self.identifier:\n            continue\n        output.append(PlaceholderItem(node.getName(), data, self))\n    return output\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/script_placeholder.html#client.ayon_katana.plugins.workfile_build.script_placeholder.KatanaPlaceholderScriptPlugin.get_placeholder_options","title":"<code>get_placeholder_options(options=None)</code>","text":"<p>Return the script phase and ordering controls shown by Core.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/script_placeholder.py</code> <pre><code>def get_placeholder_options(self, options=None):\n    \"\"\"Return the script phase and ordering controls shown by Core.\"\"\"\n    options = options or {}\n    return [\n        NumberDef(\n            \"order\",\n            label=\"Order\",\n            default=options.get(\"order\") or 0,\n            decimals=0,\n            minimum=0,\n            maximum=999,\n            tooltip=(\"Execution order from 0 to 999. Lower values run first.\"),\n        ),\n        TextDef(\n            \"prepare_script\",\n            label=\"Run at\\nprepare\",\n            tooltip=(\"Trusted, unsandboxed Python executed before population.\"),\n            multiline=True,\n            default=options.get(\"prepare_script\", \"\"),\n        ),\n        TextDef(\n            \"populate_script\",\n            label=\"Run at\\npopulate\",\n            tooltip=(\"Trusted, unsandboxed Python executed at placeholder order.\"),\n            multiline=True,\n            default=options.get(\"populate_script\", EXAMPLE_SCRIPT),\n        ),\n        TextDef(\n            \"depth_processed_script\",\n            label=\"Run after\\ndepth\\niteration\",\n            tooltip=(\n                \"Trusted, unsandboxed Python executed after every build depth.\"\n            ),\n            multiline=True,\n            default=options.get(\"depth_processed_script\", \"\"),\n        ),\n        TextDef(\n            \"finished_script\",\n            label=\"Run after\\nbuild\",\n            tooltip=(\n                \"Trusted, unsandboxed Python executed after the build, even \"\n                \"when another placeholder reported an error.\"\n            ),\n            multiline=True,\n            default=options.get(\"finished_script\", \"\"),\n        ),\n    ]\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/script_placeholder.html#client.ayon_katana.plugins.workfile_build.script_placeholder.KatanaPlaceholderScriptPlugin.populate_placeholder","title":"<code>populate_placeholder(placeholder)</code>","text":"<p>Execute populate code and register deferred phase callbacks.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/script_placeholder.py</code> <pre><code>def populate_placeholder(self, placeholder):\n    \"\"\"Execute populate code and register deferred phase callbacks.\"\"\"\n    populate_script = placeholder.data.get(\"populate_script\")\n    depth_script = placeholder.data.get(\"depth_processed_script\")\n    finished_script = placeholder.data.get(\"finished_script\")\n    order = int(placeholder.order)\n\n    if populate_script:\n        self.run_script(placeholder, populate_script)\n\n    if depth_script:\n        callback = weakref_partial(self.run_script, placeholder, depth_script)\n        self.builder.add_on_depth_processed_callback(callback, order=order)\n    if finished_script:\n        callback = weakref_partial(\n            self.run_script,\n            placeholder,\n            finished_script,\n        )\n        self.builder.add_on_finished_callback(callback, order=order)\n\n    keep_placeholder = placeholder.data.get(\"keep_placeholder\", True)\n    if keep_placeholder:\n        return\n    if depth_script or finished_script:\n        callback = weakref_partial(self._delete_after_finish, placeholder)\n        self.builder.add_on_finished_callback(callback, order=order + 1)\n    else:\n        self.delete_placeholder(placeholder)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/script_placeholder.html#client.ayon_katana.plugins.workfile_build.script_placeholder.KatanaPlaceholderScriptPlugin.prepare_placeholders","title":"<code>prepare_placeholders(placeholders)</code>","text":"<p>Execute each configured prepare script in placeholder order.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/script_placeholder.py</code> <pre><code>def prepare_placeholders(self, placeholders):\n    \"\"\"Execute each configured prepare script in placeholder order.\"\"\"\n    super().prepare_placeholders(placeholders)\n    for placeholder in placeholders:\n        script = placeholder.data.get(\"prepare_script\")\n        if script:\n            self.run_script(placeholder, script)\n</code></pre>"},{"location":"autoapi/client/ayon_katana/plugins/workfile_build/script_placeholder.html#client.ayon_katana.plugins.workfile_build.script_placeholder.KatanaPlaceholderScriptPlugin.run_script","title":"<code>run_script(placeholder, script, event=None)</code>","text":"<p>Execute trusted administrator Python with placeholder context.</p> <p>Parameters:</p> Name Type Description Default <code>placeholder</code> <p>Workfile Builder placeholder item.</p> required <code>script</code> <p>Trusted administrator-authored Python source.</p> required <code>event</code> <p>Optional Core depth or finished event.</p> <code>None</code> <p>Returns:</p> Type Description <p>The execution namespace, useful for diagnostics and tests.</p> Warning <p>This method is deliberately unsandboxed. The source can import modules, access the network, and modify files as the Katana user.</p> Source code in <code>client/ayon_katana/plugins/workfile_build/script_placeholder.py</code> <pre><code>def run_script(self, placeholder, script, event=None):\n    \"\"\"Execute trusted administrator Python with placeholder context.\n\n    Args:\n        placeholder: Workfile Builder placeholder item.\n        script: Trusted administrator-authored Python source.\n        event: Optional Core depth or finished event.\n\n    Returns:\n        The execution namespace, useful for diagnostics and tests.\n\n    Warning:\n        This method is deliberately unsandboxed. The source can import\n        modules, access the network, and modify files as the Katana user.\n    \"\"\"\n    self.log.debug(\"Running unsandboxed Workfile Builder script: %s\", event)\n    namespace = {\n        \"NodegraphAPI\": NodegraphAPI,\n        \"event\": event,\n        \"placeholder\": placeholder,\n        \"plugin\": self,\n    }\n    code = compile(script, \"&lt;AYON Katana Workfile Builder script&gt;\", \"exec\")\n    exec(code, namespace, namespace)\n    return namespace\n</code></pre>"},{"location":"autoapi/server/index.html","title":"server","text":"<p>AYON Server entry point for the Katana addon.</p>"},{"location":"autoapi/server/index.html#server.KatanaAddon","title":"<code>KatanaAddon</code>","text":"<p>               Bases: <code>BaseServerAddon</code></p> <p>Expose Katana settings and client code to AYON Server.</p> Source code in <code>server/addon.py</code> <pre><code>class KatanaAddon(BaseServerAddon):\n    \"\"\"Expose Katana settings and client code to AYON Server.\"\"\"\n\n    settings_model: Type[KatanaSettings] = KatanaSettings\n\n    async def get_default_settings(self):\n        \"\"\"Return validated default Katana settings.\"\"\"\n        settings_model = self.get_settings_model()\n        return settings_model(**DEFAULT_VALUES)\n</code></pre>"},{"location":"autoapi/server/index.html#server.KatanaAddon.get_default_settings","title":"<code>get_default_settings()</code>  <code>async</code>","text":"<p>Return validated default Katana settings.</p> Source code in <code>server/addon.py</code> <pre><code>async def get_default_settings(self):\n    \"\"\"Return validated default Katana settings.\"\"\"\n    settings_model = self.get_settings_model()\n    return settings_model(**DEFAULT_VALUES)\n</code></pre>"},{"location":"autoapi/server/addon.html","title":"addon","text":"<p>AYON Server addon definition for Katana.</p>"},{"location":"autoapi/server/addon.html#server.addon.KatanaAddon","title":"<code>KatanaAddon</code>","text":"<p>               Bases: <code>BaseServerAddon</code></p> <p>Expose Katana settings and client code to AYON Server.</p> Source code in <code>server/addon.py</code> <pre><code>class KatanaAddon(BaseServerAddon):\n    \"\"\"Expose Katana settings and client code to AYON Server.\"\"\"\n\n    settings_model: Type[KatanaSettings] = KatanaSettings\n\n    async def get_default_settings(self):\n        \"\"\"Return validated default Katana settings.\"\"\"\n        settings_model = self.get_settings_model()\n        return settings_model(**DEFAULT_VALUES)\n</code></pre>"},{"location":"autoapi/server/addon.html#server.addon.KatanaAddon.get_default_settings","title":"<code>get_default_settings()</code>  <code>async</code>","text":"<p>Return validated default Katana settings.</p> Source code in <code>server/addon.py</code> <pre><code>async def get_default_settings(self):\n    \"\"\"Return validated default Katana settings.\"\"\"\n    settings_model = self.get_settings_model()\n    return settings_model(**DEFAULT_VALUES)\n</code></pre>"},{"location":"autoapi/server/settings/index.html","title":"settings","text":"<p>Public AYON Server settings contract for the Katana addon.</p>"},{"location":"autoapi/server/settings/index.html#server.settings.KatanaSettings","title":"<code>KatanaSettings</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Combine all Katana addon setting groups.</p> Source code in <code>server/settings/main.py</code> <pre><code>class KatanaSettings(BaseSettingsModel):\n    \"\"\"Combine all Katana addon setting groups.\"\"\"\n\n    imageio: KatanaImageIOModel = SettingsField(\n        default_factory=KatanaImageIOModel,\n        title=\"Color Management\",\n    )\n    create: CreatePluginsModel = SettingsField(\n        default_factory=CreatePluginsModel,\n        title=\"Creator Plugins\",\n    )\n    load: LoadPluginsModel = SettingsField(\n        default_factory=LoadPluginsModel,\n        title=\"Loader Plugins\",\n    )\n    publish: PublishPluginsModel = SettingsField(\n        default_factory=PublishPluginsModel,\n        title=\"Publish Plugins\",\n    )\n    templated_workfile_build: TemplatedWorkfileBuildModel = SettingsField(\n        default_factory=TemplatedWorkfileBuildModel,\n        title=\"Templated Workfile Build\",\n    )\n</code></pre>"},{"location":"autoapi/server/settings/common.html","title":"common","text":"<p>Shared AYON Server setting models for Katana plugins.</p>"},{"location":"autoapi/server/settings/common.html#server.settings.common.EnabledPluginModel","title":"<code>EnabledPluginModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Store the enabled state shared by discoverable plugins.</p> Source code in <code>server/settings/common.py</code> <pre><code>class EnabledPluginModel(BaseSettingsModel):\n    \"\"\"Store the enabled state shared by discoverable plugins.\"\"\"\n\n    enabled: bool = SettingsField(True, title=\"Enabled\")\n</code></pre>"},{"location":"autoapi/server/settings/common.html#server.settings.common.InstanceContextValidatorModel","title":"<code>InstanceContextValidatorModel</code>","text":"<p>               Bases: <code>OptionalPluginModel</code></p> <p>Configure the optional persisted-instance context validator.</p> Source code in <code>server/settings/common.py</code> <pre><code>class InstanceContextValidatorModel(OptionalPluginModel):\n    \"\"\"Configure the optional persisted-instance context validator.\"\"\"\n\n    optional: bool = SettingsField(True, title=\"Optional\")\n</code></pre>"},{"location":"autoapi/server/settings/common.html#server.settings.common.OptionalPluginModel","title":"<code>OptionalPluginModel</code>","text":"<p>               Bases: <code>EnabledPluginModel</code></p> <p>Store the enabled, optional, and active states of a Pyblish plugin.</p> Source code in <code>server/settings/common.py</code> <pre><code>class OptionalPluginModel(EnabledPluginModel):\n    \"\"\"Store the enabled, optional, and active states of a Pyblish plugin.\"\"\"\n\n    optional: bool = SettingsField(False, title=\"Optional\")\n    active: bool = SettingsField(True, title=\"Active\")\n</code></pre>"},{"location":"autoapi/server/settings/create.html","title":"create","text":"<p>Creator settings for the Katana addon.</p>"},{"location":"autoapi/server/settings/create.html#server.settings.create.CreateImageModel","title":"<code>CreateImageModel</code>","text":"<p>               Bases: <code>EnabledPluginModel</code></p> <p>Configure image output instances.</p> Source code in <code>server/settings/create.py</code> <pre><code>class CreateImageModel(EnabledPluginModel):\n    \"\"\"Configure image output instances.\"\"\"\n\n    default_render_target: str = SettingsField(\n        \"local\",\n        title=\"Default render target\",\n        enum_resolver=render_targets_enum,\n    )\n    default_extension: str = SettingsField(\"exr\", title=\"Default extension\")\n    default_colorspace: str = SettingsField(\n        \"\",\n        title=\"Default output colorspace\",\n        description=\"Required on instances marked for review.\",\n    )\n    default_frame_padding: int = SettingsField(\n        4,\n        title=\"Frame padding\",\n        ge=1,\n        le=12,\n    )\n    default_review: bool = SettingsField(False, title=\"Review by default\")\n</code></pre>"},{"location":"autoapi/server/settings/create.html#server.settings.create.CreateNodegraphModel","title":"<code>CreateNodegraphModel</code>","text":"<p>               Bases: <code>EnabledPluginModel</code></p> <p>Configure selected Katana Group node graph instances.</p> Source code in <code>server/settings/create.py</code> <pre><code>class CreateNodegraphModel(EnabledPluginModel):\n    \"\"\"Configure selected Katana Group node graph instances.\"\"\"\n</code></pre>"},{"location":"autoapi/server/settings/create.html#server.settings.create.CreatePluginsModel","title":"<code>CreatePluginsModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Group creator plugin settings.</p> Source code in <code>server/settings/create.py</code> <pre><code>class CreatePluginsModel(BaseSettingsModel):\n    \"\"\"Group creator plugin settings.\"\"\"\n\n    CreateWorkfile: CreateWorkfileModel = SettingsField(\n        default_factory=CreateWorkfileModel,\n        title=\"Create Workfile\",\n    )\n    CreateRender: CreateRenderModel = SettingsField(\n        default_factory=CreateRenderModel,\n        title=\"Create Render\",\n    )\n    CreateNodegraph: CreateNodegraphModel = SettingsField(\n        default_factory=CreateNodegraphModel,\n        title=\"Create Node Graph\",\n    )\n    CreateUsdLayer: CreateUsdLayerModel = SettingsField(\n        default_factory=CreateUsdLayerModel,\n        title=\"Create USD Layer\",\n    )\n    CreateUsdLook: CreateUsdLayerModel = SettingsField(\n        default_factory=CreateUsdLayerModel,\n        title=\"Create USD Look\",\n    )\n    CreateUsdCamera: CreateUsdLayerModel = SettingsField(\n        default_factory=CreateUsdLayerModel,\n        title=\"Create USD Camera\",\n    )\n    CreateUsdLayout: CreateUsdLayerModel = SettingsField(\n        default_factory=CreateUsdLayerModel,\n        title=\"Create USD Layout\",\n    )\n    CreateUsdAssembly: CreateUsdLayerModel = SettingsField(\n        default_factory=CreateUsdLayerModel,\n        title=\"Create USD Assembly\",\n    )\n    CreateRenderSetup: CreateNodegraphModel = SettingsField(\n        default_factory=CreateNodegraphModel,\n        title=\"Create Render Setup\",\n    )\n    CreateImage: CreateImageModel = SettingsField(\n        default_factory=CreateImageModel,\n        title=\"Create Image\",\n    )\n    CreateReview: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Create Scene Review\",\n    )\n</code></pre>"},{"location":"autoapi/server/settings/create.html#server.settings.create.CreateRenderModel","title":"<code>CreateRenderModel</code>","text":"<p>               Bases: <code>EnabledPluginModel</code></p> <p>Configure Katana render instances.</p> Source code in <code>server/settings/create.py</code> <pre><code>class CreateRenderModel(EnabledPluginModel):\n    \"\"\"Configure Katana render instances.\"\"\"\n\n    default_render_target: str = SettingsField(\n        \"farm\",\n        title=\"Default render target\",\n        enum_resolver=render_targets_enum,\n    )\n    default_renderer: str = SettingsField(\n        \"\",\n        title=\"Default renderer\",\n        description=(\n            \"Registered Katana renderer ID. When empty, DEFAULT_RENDERER must \"\n            \"name a registered renderer or render creation fails.\"\n        ),\n    )\n    default_extension: str = SettingsField(\"exr\", title=\"Default extension\")\n    default_channel: str = SettingsField(\"rgba\", title=\"Default channel / AOV\")\n    default_frame_padding: int = SettingsField(\n        4,\n        title=\"Frame padding\",\n        ge=1,\n        le=12,\n    )\n</code></pre>"},{"location":"autoapi/server/settings/create.html#server.settings.create.CreateUsdLayerModel","title":"<code>CreateUsdLayerModel</code>","text":"<p>               Bases: <code>EnabledPluginModel</code></p> <p>Configure USD layer export instances.</p> Source code in <code>server/settings/create.py</code> <pre><code>class CreateUsdLayerModel(EnabledPluginModel):\n    \"\"\"Configure USD layer export instances.\"\"\"\n\n    default_usd_format: str = SettingsField(\n        \"usd\",\n        title=\"Default USD format\",\n        enum_resolver=usd_formats_enum,\n    )\n    default_time_samples: str = SettingsField(\n        \"Current Frame\",\n        title=\"Default time samples\",\n        enum_resolver=usd_time_samples_enum,\n    )\n    default_export_method: str = SettingsField(\n        \"Keep Composition Arcs\",\n        title=\"Default export method\",\n        enum_resolver=usd_export_methods_enum,\n    )\n    default_samples_per_frame: float = SettingsField(\n        1.0,\n        title=\"Default samples per frame\",\n        gt=0,\n    )\n</code></pre>"},{"location":"autoapi/server/settings/create.html#server.settings.create.CreateWorkfileModel","title":"<code>CreateWorkfileModel</code>","text":"<p>               Bases: <code>EnabledPluginModel</code></p> <p>Configure the automatic workfile creator.</p> Source code in <code>server/settings/create.py</code> <pre><code>class CreateWorkfileModel(EnabledPluginModel):\n    \"\"\"Configure the automatic workfile creator.\"\"\"\n\n    is_mandatory: bool = SettingsField(\n        False,\n        title=\"Mandatory workfile\",\n        description=\"Prevent artists from disabling the workfile instance.\",\n    )\n</code></pre>"},{"location":"autoapi/server/settings/create.html#server.settings.create.render_targets_enum","title":"<code>render_targets_enum()</code>","text":"<p>Return supported Katana render targets.</p> Source code in <code>server/settings/create.py</code> <pre><code>def render_targets_enum():\n    \"\"\"Return supported Katana render targets.\"\"\"\n    return [\n        {\"value\": \"farm\", \"label\": \"Farm\"},\n        {\"value\": \"local\", \"label\": \"Local\"},\n        {\"value\": \"local_no_render\", \"label\": \"Use existing frames\"},\n    ]\n</code></pre>"},{"location":"autoapi/server/settings/create.html#server.settings.create.usd_export_methods_enum","title":"<code>usd_export_methods_enum()</code>","text":"<p>Return supported USD layer composition methods.</p> Source code in <code>server/settings/create.py</code> <pre><code>def usd_export_methods_enum():\n    \"\"\"Return supported USD layer composition methods.\"\"\"\n    return [\n        {\"value\": \"Keep Composition Arcs\", \"label\": \"Keep Composition Arcs\"},\n        {\n            \"value\": \"Flatten Sublayers, Keep References\",\n            \"label\": \"Flatten Sublayers, Keep References\",\n        },\n        {\"value\": \"Flatten All\", \"label\": \"Flatten All\"},\n    ]\n</code></pre>"},{"location":"autoapi/server/settings/create.html#server.settings.create.usd_formats_enum","title":"<code>usd_formats_enum()</code>","text":"<p>Return supported USD layer formats.</p> Source code in <code>server/settings/create.py</code> <pre><code>def usd_formats_enum():\n    \"\"\"Return supported USD layer formats.\"\"\"\n    return [\n        {\"value\": \"usd\", \"label\": \"USD\"},\n        {\"value\": \"usda\", \"label\": \"USD ASCII\"},\n        {\"value\": \"usdc\", \"label\": \"USD Crate\"},\n    ]\n</code></pre>"},{"location":"autoapi/server/settings/create.html#server.settings.create.usd_time_samples_enum","title":"<code>usd_time_samples_enum()</code>","text":"<p>Return supported USD time-sampling modes.</p> Source code in <code>server/settings/create.py</code> <pre><code>def usd_time_samples_enum():\n    \"\"\"Return supported USD time-sampling modes.\"\"\"\n    return [\n        {\"value\": \"Current Frame\", \"label\": \"Current Frame\"},\n        {\"value\": \"Frame Range\", \"label\": \"Frame Range\"},\n        {\"value\": \"Project Settings\", \"label\": \"Project Settings\"},\n        {\n            \"value\": \"Defined Layer Metadata\",\n            \"label\": \"Defined Layer Metadata\",\n        },\n    ]\n</code></pre>"},{"location":"autoapi/server/settings/imageio.html","title":"imageio","text":"<p>ImageIO settings exposed to AYON Core for the Katana host.</p>"},{"location":"autoapi/server/settings/imageio.html#server.settings.imageio.ImageIOFileRuleModel","title":"<code>ImageIOFileRuleModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>One host-specific colorspace file rule.</p> Source code in <code>server/settings/imageio.py</code> <pre><code>class ImageIOFileRuleModel(BaseSettingsModel):\n    \"\"\"One host-specific colorspace file rule.\"\"\"\n\n    name: str = SettingsField(\"\", title=\"Rule name\")\n    pattern: str = SettingsField(\"\", title=\"Regex pattern\")\n    colorspace: str = SettingsField(\"\", title=\"Colorspace name\")\n    ext: str = SettingsField(\"\", title=\"File extension\")\n</code></pre>"},{"location":"autoapi/server/settings/imageio.html#server.settings.imageio.ImageIOFileRulesModel","title":"<code>ImageIOFileRulesModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Optional host-specific file rules consumed by AYON Core.</p> Source code in <code>server/settings/imageio.py</code> <pre><code>class ImageIOFileRulesModel(BaseSettingsModel):\n    \"\"\"Optional host-specific file rules consumed by AYON Core.\"\"\"\n\n    activate_host_rules: bool = SettingsField(False, title=\"Use host file rules\")\n    rules: list[ImageIOFileRuleModel] = SettingsField(\n        default_factory=list,\n        title=\"Rules\",\n    )\n\n    @validator(\"rules\")\n    def validate_unique_names(cls, value):\n        \"\"\"Require unique rule names.\"\"\"\n        ensure_unique_names(value)\n        return value\n</code></pre>"},{"location":"autoapi/server/settings/imageio.html#server.settings.imageio.ImageIOFileRulesModel.validate_unique_names","title":"<code>validate_unique_names(value)</code>","text":"<p>Require unique rule names.</p> Source code in <code>server/settings/imageio.py</code> <pre><code>@validator(\"rules\")\ndef validate_unique_names(cls, value):\n    \"\"\"Require unique rule names.\"\"\"\n    ensure_unique_names(value)\n    return value\n</code></pre>"},{"location":"autoapi/server/settings/imageio.html#server.settings.imageio.KatanaImageIOModel","title":"<code>KatanaImageIOModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Katana host colorspace settings compatible with AYON Core lookups.</p> Source code in <code>server/settings/imageio.py</code> <pre><code>class KatanaImageIOModel(BaseSettingsModel):\n    \"\"\"Katana host colorspace settings compatible with AYON Core lookups.\"\"\"\n\n    activate_host_color_management: bool = SettingsField(\n        True,\n        title=\"Enable Color Management\",\n    )\n    file_rules: ImageIOFileRulesModel = SettingsField(\n        default_factory=ImageIOFileRulesModel,\n        title=\"File Rules\",\n    )\n    workfile: WorkfileImageIOModel = SettingsField(\n        default_factory=WorkfileImageIOModel,\n        title=\"Workfile\",\n    )\n</code></pre>"},{"location":"autoapi/server/settings/imageio.html#server.settings.imageio.WorkfileImageIOModel","title":"<code>WorkfileImageIOModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Optional OCIO display/view overrides applied before Katana launches.</p> Source code in <code>server/settings/imageio.py</code> <pre><code>class WorkfileImageIOModel(BaseSettingsModel):\n    \"\"\"Optional OCIO display/view overrides applied before Katana launches.\"\"\"\n\n    enabled: bool = SettingsField(False, title=\"Enabled\")\n    default_display: str = SettingsField(\"\", title=\"Default active displays\")\n    default_view: str = SettingsField(\"\", title=\"Default active views\")\n    review_color_space: str = SettingsField(\"\", title=\"Review colorspace\")\n</code></pre>"},{"location":"autoapi/server/settings/load.html","title":"load","text":"<p>Loader settings for the Katana addon.</p>"},{"location":"autoapi/server/settings/load.html#server.settings.load.LoadPluginsModel","title":"<code>LoadPluginsModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Group loader plugin settings.</p> Source code in <code>server/settings/load.py</code> <pre><code>class LoadPluginsModel(BaseSettingsModel):\n    \"\"\"Group loader plugin settings.\"\"\"\n\n    UsdSublayerLoader: UsdPathLoaderModel = SettingsField(\n        default_factory=UsdPathLoaderModel,\n        title=\"Sublayer USD into Native Graph\",\n    )\n    UsdLoader: UsdPathLoaderModel = SettingsField(\n        default_factory=UsdPathLoaderModel,\n        title=\"Load USD into Scene Graph with UsdIn\",\n    )\n    AbcLoader: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Load Alembic with Alembic_In\",\n    )\n    KatanaImportLoader: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Import Katana Graph\",\n    )\n    ImageLoader: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Load Image with ImageRead\",\n    )\n    ClearUsdResolverCache: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Clear AYON USD Resolver Cache\",\n    )\n</code></pre>"},{"location":"autoapi/server/settings/load.html#server.settings.load.UsdPathLoaderModel","title":"<code>UsdPathLoaderModel</code>","text":"<p>               Bases: <code>EnabledPluginModel</code></p> <p>Configure a loader whose native node resolves a USD asset path.</p> Source code in <code>server/settings/load.py</code> <pre><code>class UsdPathLoaderModel(EnabledPluginModel):\n    \"\"\"Configure a loader whose native node resolves a USD asset path.\"\"\"\n\n    use_ayon_entity_uri: bool = SettingsField(\n        False,\n        title=\"Use AYON Entity URI\",\n        description=(\n            \"Write an AYON entity URI into the USD source node instead of a \"\n            \"resolved path. Enable only when the optional AYON USD Resolver \"\n            \"is installed.\"\n        ),\n    )\n</code></pre>"},{"location":"autoapi/server/settings/main.html","title":"main","text":"<p>Top-level AYON Server settings model for Katana.</p>"},{"location":"autoapi/server/settings/main.html#server.settings.main.KatanaSettings","title":"<code>KatanaSettings</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Combine all Katana addon setting groups.</p> Source code in <code>server/settings/main.py</code> <pre><code>class KatanaSettings(BaseSettingsModel):\n    \"\"\"Combine all Katana addon setting groups.\"\"\"\n\n    imageio: KatanaImageIOModel = SettingsField(\n        default_factory=KatanaImageIOModel,\n        title=\"Color Management\",\n    )\n    create: CreatePluginsModel = SettingsField(\n        default_factory=CreatePluginsModel,\n        title=\"Creator Plugins\",\n    )\n    load: LoadPluginsModel = SettingsField(\n        default_factory=LoadPluginsModel,\n        title=\"Loader Plugins\",\n    )\n    publish: PublishPluginsModel = SettingsField(\n        default_factory=PublishPluginsModel,\n        title=\"Publish Plugins\",\n    )\n    templated_workfile_build: TemplatedWorkfileBuildModel = SettingsField(\n        default_factory=TemplatedWorkfileBuildModel,\n        title=\"Templated Workfile Build\",\n    )\n</code></pre>"},{"location":"autoapi/server/settings/publish.html","title":"publish","text":"<p>Publisher settings for the Katana addon.</p>"},{"location":"autoapi/server/settings/publish.html#server.settings.publish.PublishPluginsModel","title":"<code>PublishPluginsModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Group publisher plugin settings.</p> Source code in <code>server/settings/publish.py</code> <pre><code>class PublishPluginsModel(BaseSettingsModel):\n    \"\"\"Group publisher plugin settings.\"\"\"\n\n    CollectKatanaCurrentFile: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect Current File\",\n        section=\"Collectors\",\n    )\n    CollectWorkfile: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect Workfile Data\",\n    )\n    CollectWorkfileDependencies: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect Workfile Dependencies\",\n    )\n    CollectRender: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect Render Products\",\n    )\n    CollectRenderNode: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect Render Node\",\n    )\n    CollectRenderFrameRange: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect Frame Range\",\n    )\n    CollectAssetHandles: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect Task Handles\",\n    )\n    CollectRenderSettings: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect Render Settings\",\n    )\n    CollectLocalRenderInstances: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect Local Output Instances\",\n    )\n    CollectNodegraph: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect Node Graph\",\n    )\n    CollectNodegraphDependencies: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect Node Graph Dependencies\",\n    )\n    CollectUsdLayer: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect USD Layer\",\n    )\n    CollectImage: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect ImageWrite\",\n    )\n    CollectReview: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Collect Scene Review\",\n    )\n    ValidateWorkfileSaved: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Workfile Saved\",\n        section=\"Validators\",\n    )\n    ValidateWorkfileContext: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Workfile Context\",\n    )\n    ValidateWorkfilePaths: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Workfile Paths\",\n    )\n    ValidateInstanceInContextKatana: InstanceContextValidatorModel = SettingsField(\n        default_factory=InstanceContextValidatorModel,\n        title=\"Instance in Same Context\",\n    )\n    ValidateRender: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Render\",\n    )\n    ValidateRenderProductPathsUnique: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Unique Render Product Paths\",\n    )\n    ValidateRenderCamera: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Render Camera\",\n    )\n    ValidateRenderResolution: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Render Resolution\",\n    )\n    ValidateRenderOutputNames: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Render Output Names\",\n    )\n    ValidateRenderOutputPaths: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Render Output Paths\",\n    )\n    ValidateRenderOutputExtensions: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Render Output Extensions\",\n    )\n    ValidateRenderOutputTokens: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Render Output Tokens\",\n    )\n    ValidateRenderColorspace: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Render Colorspace\",\n    )\n    ValidateNodegraph: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Node Graph\",\n    )\n    ValidateUsdLayer: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate USD Layer\",\n    )\n    ValidateUsdCameraContent: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate USD Camera Content\",\n    )\n    ValidateUsdAssetContributionDefaultPrim: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate USD Asset Contribution Default Prim\",\n    )\n    ValidateUsdLookAssignments: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate All Geometry Has Material Assignment\",\n    )\n    ValidateUsdLookDisallowedTypes: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Validate Look No Disallowed Types\",\n    )\n    ValidateUsdLookMaterialDefinitions: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Look Shaders Are Defined\",\n    )\n    ValidateImage: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate ImageWrite\",\n    )\n    ValidateReview: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Validate Scene Review\",\n    )\n    FinalizeUsdPublish: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Finalize USD\",\n    )\n    IncrementCurrentFile: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Increment current file\",\n    )\n    ExtractWorkfileThumbnail: OptionalPluginModel = SettingsField(\n        default_factory=OptionalPluginModel,\n        title=\"Extract Workfile Thumbnail\",\n    )\n    ExtractReviewCapture: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Extract Scene Review Capture\",\n    )\n    ExtractWorkfile: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Extract Workfile\",\n        section=\"Extractors\",\n    )\n    ExtractLocalRender: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Extract Local Output\",\n    )\n    ExtractNodegraph: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Extract Node Graph\",\n    )\n    ExtractUsdLayer: EnabledPluginModel = SettingsField(\n        default_factory=EnabledPluginModel,\n        title=\"Extract USD Layer\",\n    )\n</code></pre>"},{"location":"autoapi/server/settings/templated_workfile_build.html","title":"templated_workfile_build","text":"<p>Workfile Builder settings for the Katana addon.</p>"},{"location":"autoapi/server/settings/templated_workfile_build.html#server.settings.templated_workfile_build.TemplatedWorkfileBuildModel","title":"<code>TemplatedWorkfileBuildModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Store Workfile Builder profiles.</p> Source code in <code>server/settings/templated_workfile_build.py</code> <pre><code>class TemplatedWorkfileBuildModel(BaseSettingsModel):\n    \"\"\"Store Workfile Builder profiles.\"\"\"\n\n    profiles: list[TemplatedWorkfileProfileModel] = SettingsField(\n        default_factory=list,\n        title=\"Profiles\",\n    )\n</code></pre>"},{"location":"autoapi/server/settings/templated_workfile_build.html#server.settings.templated_workfile_build.TemplatedWorkfileProfileModel","title":"<code>TemplatedWorkfileProfileModel</code>","text":"<p>               Bases: <code>BaseSettingsModel</code></p> <p>Select a Katana workfile template for an AYON context.</p> Source code in <code>server/settings/templated_workfile_build.py</code> <pre><code>class TemplatedWorkfileProfileModel(BaseSettingsModel):\n    \"\"\"Select a Katana workfile template for an AYON context.\"\"\"\n\n    task_types: list[str] = SettingsField(\n        default_factory=list,\n        title=\"Task types\",\n        enum_resolver=task_types_enum,\n    )\n    task_names: list[str] = SettingsField(\n        default_factory=list,\n        title=\"Task names\",\n    )\n    folder_types: list[str] = SettingsField(\n        default_factory=list,\n        title=\"Folder types\",\n        enum_resolver=folder_types_enum,\n    )\n    folder_paths: list[str] = SettingsField(\n        default_factory=list,\n        title=\"Folder paths\",\n    )\n    path: str = SettingsField(\n        \"\",\n        title=\"Template path\",\n        description=\"Path to a Katana template project.\",\n    )\n    keep_placeholder: bool = SettingsField(False, title=\"Keep placeholders\")\n    execute_on_new_file: bool = SettingsField(\n        False,\n        title=\"Apply to New Scene\",\n    )\n    execute_on_app_launch: bool = SettingsField(\n        True,\n        title=\"Apply on Katana Launch\",\n    )\n    create_first_version: bool = SettingsField(\n        True,\n        title=\"Save first workfile version\",\n    )\n</code></pre>"}]}